defmodule Kiln.Sandboxes do
  @moduledoc """
  Execution layer — ephemeral Docker sandbox per stage
  (ARCHITECTURE.md §4, §10). Phase 4 ships the `Sandboxes.Driver`
  behaviour + `Docker` implementation + `Supervisor`.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
