defmodule Kiln.Agents do
  @moduledoc """
  Execution layer — external LLM agent orchestration
  (ARCHITECTURE.md §4). Phase 3 ships the `Kiln.Agents.Adapter`
  behaviour + Anthropix adapter + session supervisor.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
