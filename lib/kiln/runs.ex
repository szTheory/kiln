defmodule Kiln.Runs do
  @moduledoc """
  Execution layer — run state machine (Ecto `state` field +
  `Kiln.Runs.Transitions` command module per ARCHITECTURE.md §4).
  Phase 2 ships the `Run` schema, `Transitions` module, `RunDirector`
  GenServer, and `RunSupervisor` DynamicSupervisor.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
