defmodule Kiln.Workflows do
  @moduledoc """
  Workflow layer — YAML loader, JSV schema validation, topological
  graph compile (ARCHITECTURE.md §4). Phase 2 ships
  `Kiln.Workflows.load!/1` and the DAG compiler.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
