defmodule Kiln.Stages do
  @moduledoc """
  Execution layer — individual workflow stages (planning, coding,
  testing, verifying). Phase 2 ships the `Stage` schema and
  `Stages.start/2` + `Stages.complete/2`.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
