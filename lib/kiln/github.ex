defmodule Kiln.GitHub do
  @moduledoc """
  Execution layer — GitHub integration (PR create, check observe, git
  push). Phase 6 ships the integration + the four `external_operations`
  `op_kind` workers (git_push, git_commit, gh_pr_create, gh_check_observe).

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
