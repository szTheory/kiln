defmodule Kiln.Intents do
  @moduledoc """
  Intent layer — queued operator requests ("start run", "resume run").
  Phase 2 ships the real `Kiln.Intents.Intent` schema + `enqueue/1`.

  P1 placeholder — see `Kiln.Specs` for the naming-contract rationale.
  """
end
