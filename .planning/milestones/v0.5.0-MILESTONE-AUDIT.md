---
milestone: v0.5.0
audited: "2026-04-23T23:59:00Z"
status: gaps_found
scores:
  requirements: 6/7
  phases: 1/3
  integration: 2/3
  flows: 3/4
gaps:
  requirements:
    - id: UAT-04
      status: unsatisfied
      phase: "27-local-first-run-proof"
      claimed_by_plans:
        - "27-01-PLAN.md"
      completed_by_plans:
        - "27-01-SUMMARY.md"
      verification_status: "contradicted"
      evidence: >-
        `27-01-SUMMARY.md` claims `requirements-completed: [UAT-04]` and
        `27-VERIFICATION.md` cites `mix kiln.first_run.prove`, but the current
        integration audit re-ran the command and it failed in
        `integration.first_run` with `permission denied for table oban_jobs`,
        `/health` never became available, and the proof exited with
        `** (Mix) integration.first_run failed (exit 7)`.
  integration:
    - from: "27-local-first-run-proof -> integration.first_run"
      to: "runtime startup / Oban / database permissions"
      requirements: ["UAT-04"]
      issue: >-
        The repository-owned proof command is wired, but the delegated
        integration layer does not currently reach a healthy app boot because
        the runtime DB role cannot access `oban_jobs`.
  flows:
    - name: "setup-ready local first-run proof"
      requirements: ["UAT-04"]
      breaks_at: "`mix kiln.first_run.prove` -> `mix integration.first_run`"
      issue: >-
        The topology proof fails before `/health` responds, so the repository
        does not currently have a working end-to-end first-run proof command.
tech_debt:
  - phase: 26-first-live-template-run
    items:
      - >-
        `26-VERIFICATION.md` remains `status: partial` because it recorded a
        repo-level `check_no_signature_block` failure during Phase 26 closure;
        this is now historical artifact debt rather than a currently
        reproducing precommit failure.
  - phase: 27-local-first-run-proof
    items:
      - >-
        `ROADMAP.md` still shows Phase 27 unchecked and `REQUIREMENTS.md` still
        marks `UAT-04` pending, while Phase 27 summary/verification artifacts
        claim completion.
      - >-
        `27-VERIFICATION.md` has no YAML frontmatter status, which weakens the
        standard three-source audit path.
nyquist:
  compliant_phases:
    - 25-local-live-readiness-ssot
  partial_phases:
    - 27-local-first-run-proof
  missing_phases:
    - 26-first-live-template-run
  overall: partial
integration_checker_note: >-
  Static UI and route integration for phases 25-26 is sound: `/settings`
  remains the readiness/remediation SSOT, blocked starts return to the first
  missing settings anchor, `hello-kiln` is the single first-run recommendation,
  and successful starts land on a proof-first `/runs/:id` surface. The blocker
  is the repository-level proof command, whose delegated `integration.first_run`
  step currently fails at app boot because of `oban_jobs` database permissions.
---

# Milestone audit — v0.5.0 (Local first success)

**Audited:** 2026-04-23  
**Status:** `gaps_found`

## Executive summary

Phase 25 meets the three-source bar for `SETUP-01`, `SETUP-02`, and `DOCS-09`.
Phase 26 provides credible targeted proof for `LIVE-01`, `LIVE-02`, and
`LIVE-03`, but its verification artifact remains `status: partial` because an
existing repo-level precommit failure was carried forward. Phase 27 does not
meet milestone close criteria: `27-01-SUMMARY.md` and `27-VERIFICATION.md`
claim `UAT-04` complete, but the owning proof command currently fails in the
delegated integration layer.

Result: milestone v0.5.0 is not ready to close. The blocking requirement is
`UAT-04`.

## Phase verification inventory

| Phase | VERIFICATION.md | Frontmatter status | Notes |
|-------|-----------------|--------------------|-------|
| 25 | Yes | passed | Readiness SSOT, remediation routing, and docs closure verified |
| 26 | Yes | partial | Requirement proof passed; repo-level precommit blocker deferred |
| 27 | Yes | missing | Verification doc exists but lacks frontmatter; current proof command fails |

## Requirements (three-source matrix)

| REQ-ID | Phase | VERIFICATION | SUMMARY | REQUIREMENTS | Final |
|--------|-------|--------------|---------|--------------|-------|
| SETUP-01 | 25 | passed | listed | checked | Complete |
| SETUP-02 | 25 | passed | listed | checked | Complete |
| DOCS-09 | 25 | passed | listed | checked | Complete |
| LIVE-01 | 26 | partial | listed | checked | Complete with verification debt |
| LIVE-02 | 26 | partial | listed | checked | Complete with verification debt |
| LIVE-03 | 26 | partial | listed | checked | Complete with verification debt |
| UAT-04 | 27 | contradicted | listed | unchecked | Unsatisfied |

## Cross-phase integration

- **Phase 25 -> Phase 26:** working. `/templates` start uses backend preflight,
  first-blocker routing, and `/settings` remediation return context.
- **Phase 26 UI journey:** working. `hello-kiln` is the single recommended
  starter and successful launch lands on a proof-first `/runs/:id` surface.
- **Phase 27 repository proof:** broken. `mix kiln.first_run.prove` exists and
  its delegation order is tested, but the delegated integration layer currently
  fails at app boot because of `oban_jobs` permission errors.

## End-to-end flows

- **Verified:** `/settings` shows readiness state and remediation controls.
- **Verified:** blocked template launch returns to `/settings?...#settings-item-*`
  and preserves return context.
- **Verified:** setup-ready `/settings` -> `hello-kiln` -> start -> `/runs/:id`
  has focused LiveView proof.
- **Broken:** repository-level first-run proof command `mix kiln.first_run.prove`.

## Artifact drift

- `ROADMAP.md` still marks Phase 27 incomplete.
- `REQUIREMENTS.md` still marks `UAT-04` pending.
- `27-01-SUMMARY.md` claims `UAT-04` complete.
- `27-VERIFICATION.md` claims `mix kiln.first_run.prove` passed, but current
  audit execution contradicts that claim.

## Nyquist discovery

Phase 25 has `nyquist_compliant: true`. Phase 27 has a validation artifact but
it is explicitly non-compliant. Phase 26 has no `*-VALIDATION.md` artifact.
Nyquist posture is therefore `partial`, but this is not the milestone-close
blocker. The blocker remains `UAT-04`.

## Close recommendation

Do not run `$gsd-complete-milestone v0.5.0` yet.

Next action: fix the `integration.first_run` runtime/app-start contract so
`mix kiln.first_run.prove` boots cleanly, reaches `/health`, and passes end to
end. After that rerun the proof, then update Phase 27 verification plus
`ROADMAP.md` and `REQUIREMENTS.md` into one consistent state.
