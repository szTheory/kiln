---
phase: 16-read-only-run-replay
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/run_replay_live.ex
autonomous: true
requirements:
  - REPL-01
milestone: v0.3.0
must_haves:
  truths:
    - "`live \"/runs/:run_id/replay\", RunReplayLive, :show` appears **strictly above** `live \"/runs/:run_id\", RunDetailLive, :show` in `router.ex`"
    - "Invalid path `run_id` → same outcome as `RunDetailLive` mount: `put_flash(:error, \"Invalid run id\")` + `push_navigate(to: ~p\"/\")`"
    - "Root replay wrapper exposes **`id=\\\"run-replay\\\"`** and **`data-run-id`** on the primary inner layout container (inside `<Layouts.app>`)"
  artifacts:
    - path: "lib/kiln_web/live/run_replay_live.ex"
      provides: "RunReplayLive module"
      contains: "RunReplayLive"
    - path: "lib/kiln_web/router.ex"
      provides: "replay route"
      contains: "RunReplayLive"
  key_links:
    - from: "router.ex"
      to: "run_replay_live.ex"
      via: "live macro"
      pattern: "phoenix routes"
---

<objective>
Ship **REPL-01 routing + LiveView shell + first audit spine window:** register **`GET /runs/:run_id/replay`**, **`RunReplayLive`** with **`Ecto.UUID.cast/1`** gate on path `run_id`, **`Runs.get/1`** nil → flash **"Run not found"** + `~p"/"` (match **`RunDetailLive`**), load **`Kiln.Audit.replay([run_id: run.id], limit: 500)`** into a **`stream(:events, …, reset: true)`**, assign **`truncated?: length(events)==500`**, parse optional query **`at`** (audit event id) into **`selected_event_id`** when UUID valid, render **empty** / **truncation** copy per **16-UI-SPEC.md** — **no** keyset pagination, PubSub, or range scrubber yet (plans **02–03**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-read-only-run-replay/16-CONTEXT.md
@.planning/phases/16-read-only-run-replay/16-RESEARCH.md
@.planning/phases/16-read-only-run-replay/16-UI-SPEC.md
@.planning/phases/16-read-only-run-replay/16-PATTERNS.md
@lib/kiln_web/router.ex
@lib/kiln_web/live/run_detail_live.ex
@lib/kiln/audit.ex
@lib/kiln_web/live/audit_live.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ LiveView | Path and query strings are operator-controlled |
| Replay ↔ DB | **SELECT-only** via `Audit.replay/1` and `Runs.get/1` |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-16-01 | T | mitigate | No `String.to_atom/1` on params |
| T-16-02 | I | mitigate | Invalid UUID → flash + redirect (path) |
| T-16-03 | E | mitigate | HEEx only; event payloads rendered as **text** / pre-encoded — **no** `raw/1` on audit payload |

## Notes

Solo operator — same **`LiveScope`** / hooks as sibling LiveViews.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Router: register `RunReplayLive` before dynamic `/runs/:run_id`</name>
  <read_first>
    - lib/kiln_web/router.ex
    - .planning/phases/16-read-only-run-replay/16-CONTEXT.md (D-01, D-02)
  </read_first>
  <acceptance_criteria>
    - `grep -n 'runs/:run_id/replay' lib/kiln_web/router.ex | head -1` prints a line number **strictly less than** the line from `grep -n 'live "/runs/:run_id"' lib/kiln_web/router.ex | grep RunDetailLive | head -1`
    - `grep -qF 'RunReplayLive' lib/kiln_web/router.ex` exits 0
    - `grep -qF 'live "/runs/:run_id/replay"' lib/kiln_web/router.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex</files>
  <action>
Inside **`live_session :default`**, add **`live "/runs/:run_id/replay", RunReplayLive, :show`** on the line **immediately above** **`live "/runs/:run_id", RunDetailLive, :show`**. Follow the same aliasing style as neighboring `RunCompareLive` / `RunDetailLive` declarations.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Replay route registered in safe order.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — `RunReplayLive`: mount gate, `handle_params`, first spine window</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - lib/kiln_web/live/run_board_live.ex (Layouts.app assigns)
    - lib/kiln_web/live/audit_live.ex
    - lib/kiln/audit.ex
    - .planning/phases/16-read-only-run-replay/16-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `test -f lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'def handle_params' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'run-replay' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'data-run-id' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'stream(:events' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'Audit.replay' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'Layouts.app' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'factory_summary' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'operator_runtime_mode' lib/kiln_web/live/run_replay_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_replay_live.ex</files>
  <action>
Create **`lib/kiln_web/live/run_replay_live.ex`**:

1. **`mount/3`**: match **`%{"run_id" => run_id}"`**. On `:error` from **`Ecto.UUID.cast(run_id)`** → **`put_flash(:error, "Invalid run id")` + `push_navigate(to: ~p"/")`**. On **`{:ok, uuid}`** → **`Runs.get(uuid)`**; if **`nil`**, flash **"Run not found"** + **`~p"/"`**; else **`{:ok, socket |> assign(:run, run) |> assign(:page_title, "Run replay") |> stream(:events, [], reset: true)}`** (streams filled in **`handle_params`**).

2. **`handle_params/3`**: read **`params["at"]`**. If present and **`Ecto.UUID.cast`** is **`:error`**, assign **`selected_event_id: nil`** and **`put_flash(:error, "Invalid event id")`** (stay on page). If **`{:ok, at}`**, assign **`selected_event_id: at`**. Call **`events = Kiln.Audit.replay(run_id: run.id, limit: 500)`** (keyword **must** include **`run_id:`** filter). **`stream(socket, :events, events, reset: true)`**, assign **`events_empty?: events == []`**, **`truncated?: length(events) == 500`**.

3. **`render/1`**: **`<Layouts.app flash={@flash} current_scope={@current_scope} factory_summary={@factory_summary} operator_runtime_mode={@operator_runtime_mode} operator_snapshots={@operator_snapshots}>`** matching **`RunDetailLive`**. Outer **`div id="run-replay" data-run-id={@run.id}`**. List uses **`phx-update="stream"`** with **`id="replay-events"`**; each row **`id={"replay-event-#{event.id}"}`** from stream id tuple pattern used elsewhere in app. Empty + truncation copy per **UI-SPEC** (plain text strings).

4. **No** `handle_event` for scrub yet; **no** `Phoenix.PubSub.subscribe` in this plan.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>RunReplayLive serves first 500 audit rows with stable shell ids.</done>
</task>

</tasks>

<verification>
- `mix compile --warnings-as-errors` exits 0
</verification>

<success_criteria>
- [ ] `/runs/:run_id/replay` route ordered before `/runs/:run_id`
- [ ] Invalid path UUID → flash + home redirect
- [ ] `#run-replay` + `data-run-id` present
- [ ] Audit events stream from DB read-only
</success_criteria>
