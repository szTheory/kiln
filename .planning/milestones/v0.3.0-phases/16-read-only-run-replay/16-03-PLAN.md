---
phase: 16-read-only-run-replay
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - lib/kiln_web/live/run_replay_live.ex
  - lib/kiln_web/live/run_detail_live.ex
  - test/kiln_web/live/run_replay_live_test.exs
autonomous: true
requirements:
  - REPL-01
milestone: v0.3.0
must_haves:
  truths:
    - "**`RunReplayLive`** uses **`Kiln.Audit.replay_page/1`** (from plan **02**) for the spine window — **not** raw **`replay/1`** with silent 500 cap as the **only** source of truth"
    - "**`handle_params`** drives **`push_patch`** when scrubbing **`at`** so URL is shareable (CONTEXT D-03)"
    - "**Terminal** run states **`merged`**, **`failed`**, **`escalated`** → **no** live `Phoenix.PubSub.subscribe` for audit tail; **in-flight** others → subscribe to **`audit:run:#{run_id}`** **and** **`run:#{run_id}`** **and** **`work_units:run:#{run_id}`** with **coalesced** refresh (debounce **≥100ms** assign flag)"
  artifacts:
    - path: "test/kiln_web/live/run_replay_live_test.exs"
      provides: "LiveView regression tests"
      contains: "RunReplayLive"
    - path: "lib/kiln_web/live/run_detail_live.ex"
      provides: "deep link into replay"
      contains: "replay"
  key_links:
    - from: "run_replay_live.ex"
      to: "audit.ex"
      via: "replay_page"
      pattern: "read model"
---

<objective>
Complete **REPL-01 operator scrub:** refactor **`RunReplayLive`** to **`replay_page`**, add **Prev / Next / First / Last** (or minimal **Prev/Next** + **Jump to latest**) via **`push_patch`** updating **`at`**, add **`input type="range"`** with **`id="replay-scrubber"`** mapping to **event index** (commit on **`phx-change`** only — **no** per-tick server storms), implement **terminal vs in-flight** subscription rules + **“N new events — jump to latest”** buffer per CONTEXT D-18–D-20, add **`RunDetailLive`** navigation control **“Timeline”** linking to **`~p"/runs/#{@run.id}/replay"`**, add **LazyHTML** LiveView tests for **happy path**, **invalid path UUID**, and **empty list**; add **read-only audit** deep link helper to **`~p"/audit"`** with **`run_id`** query prefilled.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-read-only-run-replay/16-CONTEXT.md
@.planning/phases/16-read-only-run-replay/16-UI-SPEC.md
@lib/kiln_web/live/run_replay_live.ex
@lib/kiln_web/live/run_board_live.ex
@lib/kiln/work_units/pubsub.ex
@lib/kiln/runs/run.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| PubSub | Only **subscribes**; never **broadcasts** fabricated audit rows |
| Cross-page links | **`~p` sigils** only — no string concat of untrusted HTML |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-16-20 | T | mitigate | **`handle_event`** handlers validate UUIDs with **`Ecto.UUID.cast/1`** before patch |
| T-16-21 | DoS | mitigate | Slider + PubSub paths **debounce** server work |

## Notes

**Read-only MVP:** grep gate in acceptance proves **no** `Audit.append`, `Repo.insert`, `Repo.update`, **`Transitions.`** in **`run_replay_live.ex`** after edits.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Refactor `RunReplayLive` to `replay_page` + scrub + subscriptions</name>
  <read_first>
    - lib/kiln_web/live/run_replay_live.ex
    - lib/kiln/audit.ex
    - lib/kiln/runs/run.ex
    - lib/kiln_web/live/run_board_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'replay_page' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'replay-scrubber' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'Phoenix.PubSub.subscribe' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -q 'audit:run:' lib/kiln_web/live/run_replay_live.ex` exits 0
    - `grep -E 'Audit\\.append|Repo\\.(insert|update)|Transitions\\.' lib/kiln_web/live/run_replay_live.ex` exits **1** (no mutation helpers in this module)
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_replay_live.ex</files>
  <action>
1. Replace **`Audit.replay(run_id:, limit: 500)`** usage with **`Audit.replay_page(run_id: run.id, limit: 200, after: cursor)`** (pick **`limit`** 100–500 consistent with UI perf; document chosen integer in moduledoc).

2. **Navigation:** implement **`handle_event("scrub", …)`** or **`"jump", …)`** that **`push_patch`**es **`~p"/runs/#{run.id}/replay?#{[at: id]}"`** (use proper **`URI.encode_query`** / **`Phoenix.VerifiedRoutes`** pattern consistent with codebase).

3. **Range:** **`input id="replay-scrubber" type="range"`** bound to local assigns; **`phx-change="scrub_range"`** updates **`at`** to the **id** of the event at that index.

4. **Subscriptions in `mount` or `update` after run loaded:** If **`run.state` in `[:merged, :failed, :escalated]`** → **skip** subscribe. Else subscribe **`audit:run:#{run.id}`**, **`run:#{run.id}`**, **`work_units:run:#{run.id}`**. **`handle_info`** coalesces **`{:audit_event, _}`**, **`{:run_state, _}`**, **`{:work_unit, _}`** into a **single** debounced **`Process.send_after(self(), :flush_tail, 120)`** tick that refetches **only when** socket assign **`live_edge?`** is true; when false, increment **`pending_tail_count`** assign and show **“Jump to latest”** strip (CONTEXT D-20).

5. **Deep link:** render **`<.link navigate={~p"/audit?#{URI.encode_query(%{"run_id" => run.id})}"}>`** (flat **`run_id`** query key matches **`AuditLive.handle_params/3`**) with link text **“Open in Audit”**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Replay LiveView is feature-complete for MVP scrub.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — `RunDetailLive` “Timeline” link</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - .planning/phases/16-read-only-run-replay/16-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -qF '/replay' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -qi 'timeline' lib/kiln_web/live/run_detail_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex</files>
  <action>
Add a **secondary** text link **“Timeline”** (sentence case) pointing to **`~p"/runs/#{@run.id}/replay"`** in the **header chrome** area near existing navigation — **do not** remove existing controls. Use **`navigate={...}`** on **`<.link>`**.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Operators can reach replay from run detail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 — LiveView tests</name>
  <read_first>
    - test/kiln_web/live/run_compare_live_test.exs
    - test/support/conn_case.ex
    - lib/kiln_web/live/run_replay_live.ex
  </read_first>
  <acceptance_criteria>
    - `test -f test/kiln_web/live/run_replay_live_test.exs` exits 0
    - `mix test test/kiln_web/live/run_replay_live_test.exs` exits 0
    - Test file contains **`#run-replay`** as a **substring**
    - Test file contains **`LazyHTML`** as a **substring**
  </acceptance_criteria>
  <files>test/kiln_web/live/run_replay_live_test.exs</files>
  <action>
Create **`test/kiln_web/live/run_replay_live_test.exs`** with:

1. **`invalid uuid path`** → follow redirect / flash like **`RunDetailLive`** tests (mirror pattern from existing tests).

2. **`happy path`** with **minimal fixture run** (reuse factory helpers if available in **`Kiln.DataCase`** / support modules) — assert **`has_element?(view, "#run-replay")`** and **`has_element?(view, "[data-run-id]")`**.

3. **Empty audit** run → assert copy containing **`No audit events`** substring from UI.

Use **`Phoenix.LiveViewTest`** **`live/2`** or **`live_isolated`** consistent with repo conventions.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_replay_live_test.exs</automated>
  </verify>
  <done>Automated coverage for replay shell.</done>
</task>

</tasks>

<verification>
- `mix test test/kiln_web/live/run_replay_live_test.exs` exits 0
- `mix test test/kiln/audit_replay_test.exs` exits 0
- `mix compile --warnings-as-errors` exits 0
</verification>

<success_criteria>
- [ ] URL-driven scrub via **`at`**
- [ ] Read-only guarantee preserved in **`RunReplayLive`**
- [ ] Tests green for replay + audit paging
</success_criteria>
