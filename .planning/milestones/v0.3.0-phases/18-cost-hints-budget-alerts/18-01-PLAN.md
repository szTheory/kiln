---
phase: 18-cost-hints-budget-alerts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/audit/event_kind.ex
  - lib/kiln/budget_alerts.ex
  - priv/audit_schemas/v1/budget_threshold_crossed.json
  - priv/repo/migrations/*_extend_audit_event_kinds_p18_budget_threshold_crossed.exs
  - test/kiln/budget_alerts_test.exs
  - test/kiln/audit/event_kind_test.exs
autonomous: true
requirements:
  - COST-02
milestone: v0.3.0
must_haves:
  truths:
    - "`Kiln.BudgetAlerts` evaluates soft thresholds using the same cap + spend sum semantics as `Kiln.Agents.BudgetGuard`"
    - "Each upward crossing of a configured threshold emits at most one `budget_threshold_crossed` audit per band per run until spend drops and re-crosses"
    - "`Kiln.Audit.EventKind` and DB CHECK constraint include `:budget_threshold_crossed`"
  artifacts:
    - path: "lib/kiln/budget_alerts.ex"
      provides: "threshold evaluation API"
      contains: "def evaluate"
    - path: "priv/audit_schemas/v1/budget_threshold_crossed.json"
      provides: "JSV payload for new audit kind"
      contains: "pct"
  key_links:
    - from: "lib/kiln/budget_alerts.ex"
      to: "lib/kiln/agents/budget_guard.ex"
      via: "shared cap/spend query pattern"
      pattern: "Decimal compare"
---

<objective>
Introduce **`Kiln.BudgetAlerts`** with **pure** threshold evaluation (configurable default **50** and **80** percent of frozen `max_tokens_usd`), **append-only** audit support via new **`budget_threshold_crossed`** `EventKind`, matching **JSON schema** under `priv/audit_schemas/v1/`, and an **Ecto migration** that extends `audit_events_event_kind_check` using the same **`EventKind.values_as_strings/0`** pattern as `20260421224335_extend_audit_event_kinds_p8_follow_up_drafted.exs`. Ship **ExUnit** proving ladder math, idempotent re-evaluation when already audited for a band, and update **`test/kiln/audit/event_kind_test.exs`** (or equivalent) if it asserts kind counts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-cost-hints-budget-alerts/18-CONTEXT.md
@.planning/phases/18-cost-hints-budget-alerts/18-RESEARCH.md
@.planning/phases/18-cost-hints-budget-alerts/18-VALIDATION.md
@lib/kiln/agents/budget_guard.ex
@lib/kiln/audit/event_kind.ex
@lib/kiln/audit.ex
@priv/repo/migrations/20260421224335_extend_audit_event_kinds_p8_follow_up_drafted.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventKind + audit schema + migration</name>
  <read_first>
    - lib/kiln/audit/event_kind.ex
    - priv/repo/migrations/20260421224335_extend_audit_event_kinds_p8_follow_up_drafted.exs
    - priv/audit_schemas/v1/notification_fired.json
  </read_first>
  <acceptance_criteria>
    - `grep -q ':budget_threshold_crossed' lib/kiln/audit/event_kind.ex` exits 0
    - `test -f priv/audit_schemas/v1/budget_threshold_crossed.json` exits 0
    - `grep -q 'budget_threshold_crossed' priv/audit_schemas/v1/budget_threshold_crossed.json` exits 0
    - `mix ecto.migrate` exits 0 in dev/test after new migration is added
  </acceptance_criteria>
  <files>lib/kiln/audit/event_kind.ex, priv/audit_schemas/v1/budget_threshold_crossed.json, priv/repo/migrations/*_extend_audit_event_kinds_p18_budget_threshold_crossed.exs</files>
  <action>
Append **`:budget_threshold_crossed`** to `@kinds` in **`Kiln.Audit.EventKind`** (append-only; update moduledoc count). Add **`priv/audit_schemas/v1/budget_threshold_crossed.json`** with string keys for **`pct`**, **`cap_usd`**, **`spent_usd`**, **`threshold_name`** (and optional **`band`**) matching **D-1818**. Run **`mix ecto.gen.migration extend_audit_event_kinds_p18_budget_threshold_crossed`** and implement **`up`/`down`** by copying the **drop + CHECK** pattern from the Phase 8 migration file cited in `read_first`, using **`Kiln.Audit.EventKind.values_as_strings/0`** in `up` (never hand-maintain a divergent string list in `up` only — **single source** is `EventKind`). Ensure **`down`** restores the **previous** CHECK list exactly as the template migration’s `down` does.
  </action>
  <verify>
    <automated>mix test test/kiln/audit/event_kind_test.exs test/kiln/audit/event_kind_p3_test.exs --max-failures 1</automated>
  </verify>
  <done>Taxonomy + schema + migration land.</done>
</task>

<task type="auto">
  <name>Task 2: Kiln.BudgetAlerts core module</name>
  <read_first>
    - lib/kiln/agents/budget_guard.ex
    - lib/kiln/runs/run.ex
    - config/config.exs
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.BudgetAlerts" lib/kiln/budget_alerts.ex` matches
    - `grep -n "Application.compile_env" lib/kiln/budget_alerts.ex` OR `grep -n "Application.get_env :kiln, Kiln.BudgetAlerts" lib/kiln/budget_alerts.ex` matches
    - `mix test test/kiln/budget_alerts_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/budget_alerts.ex, config/config.exs (or runtime.exs), test/kiln/budget_alerts_test.exs</files>
  <action>
Add **`Kiln.BudgetAlerts`** with:
    1. **`threshold_percentages/0`** reading an application env key (e.g. **`:kiln, Kiln.BudgetAlerts, :soft_thresholds_pct`** defaulting to **`[50, 80]`** — use **`Application.compile_env`** if values must be static in releases, otherwise **`get_env` with defaults** per project convention).
    2. **`evaluate_crossings(run_id, opts)`** (exact name planner’s choice) that loads **`Kiln.Runs.get!/1`**, reads **`caps_snapshot["max_tokens_usd"]`** as **`Decimal`**, recomputes **spent** using the **same query shape** as **`BudgetGuard.sum_stage_spend/1`** (prefer **calling a shared private** or **extracting `Kiln.Agents.BudgetGuard` public `sum_stage_spend/1`** if already public — if private, **duplicate the `from` query verbatim** in `BudgetAlerts` and cite **D-138** in a one-line comment to avoid drift).
    3. For each configured pct, detect **upward crossing**: prior spend strictly below **`pct/100 * cap`** and current spend **≥** that boundary — consult **existing** `budget_threshold_crossed` audit rows for `run_id` (via **`Kiln.Audit` replay or targeted query**) to avoid double-fire on retries.
    4. Return a **list** of crossing maps **without** side effects (pure evaluation) in this task boundary; **next plan** wires `Audit.append` + notifications.

Add **`test/kiln/budget_alerts_test.exs`** with Sandbox if Repo used for audit replay; otherwise pure **`Decimal`** fixtures for cap/spent and fake audit list passed as **`opts`**.
  </action>
  <verify>
    <automated>mix test test/kiln/budget_alerts_test.exs --max-failures 1</automated>
  </verify>
  <done>Evaluator module + unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit payload ↔ operator | Threshold events are operator-visible; must not embed secrets |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | `caps_snapshot` tamper | mitigate | Trust DB row; same trust as `BudgetGuard` |
| T-18-02 | E | Migration CHECK drift | mitigate | Generate CHECK list from `EventKind.values_as_strings/0` only |
</threat_model>

<verification>
mix test test/kiln/budget_alerts_test.exs test/kiln/audit/event_kind_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
`budget_threshold_crossed` kind is legal in DB + Ecto; `Kiln.BudgetAlerts` computes crossings with `Decimal`; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cost-hints-budget-alerts/18-01-SUMMARY.md`
</output>

## PLANNING COMPLETE
