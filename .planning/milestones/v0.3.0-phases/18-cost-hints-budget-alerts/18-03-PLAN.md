---
phase: 18-cost-hints-budget-alerts
plan: "03"
type: execute
wave: 2
depends_on:
  - "02"
files_modified:
  - lib/kiln_web/live/run_detail_live.ex
  - lib/kiln_web/live/cost_live.ex
  - test/kiln_web/live/run_detail_live_test.exs
autonomous: true
requirements:
  - COST-01
  - COST-02
milestone: v0.3.0
must_haves:
  truths:
    - "Run detail shows stage-scoped advisory panel with both disclaimer chips whenever $ or tier text appears"
    - "Run detail subscribes to `run:<uuid>` and refreshes assigns on `{:run_state, _}` and budget alert tuple from Plan 02"
    - "Soft threshold banner uses calm copy + `aria-live=\"polite\"`"
  artifacts:
    - path: "lib/kiln_web/live/run_detail_live.ex"
      provides: "COST-01 panel + COST-02 banner"
      contains: "Advisory — does not change run caps"
  key_links:
    - from: "lib/kiln_web/live/run_detail_live.ex"
      to: ".planning/phases/18-cost-hints-budget-alerts/18-UI-SPEC.md"
      via: "copy table"
      pattern: "HEEx panel"
---

<objective>
Implement **COST-01** retrospective hint **panel** on **`KilnWeb.RunDetailLive`**: after each **completed** stage for the inspected `stage_run_id`, show **`cost_usd`**, **`requested_model`**, **`actual_model_used`**, and **cap headroom** (`Decimal.sub(cap, spent_sum)`) using **`@run.caps_snapshot`** and summed **`stage_runs`**. Always render the two disclaimer chips **`Advisory — does not change run caps`** and **`Spend follows routed model`** when any monetary or tier hint is shown (**D-1805**). Subscribe in **`mount/3`** or **`connected?/1`** to **`"run:#{run.id}"`** and **`handle_info`** for **`{:run_state, _}`** plus the **budget alert** message tuple defined in **18-02-PLAN** — **assign** refresh by **`Runs.get!/1` + `Stages.list_for_run/1`**. Add **inline banner** for the latest soft threshold (assign **`@budget_alert`** map or `nil`). Optionally add **one** sentence to **`CostLive`** footer referencing disclaimers (**D-1804**) without duplicating the panel. Extend **`test/kiln_web/live/run_detail_live_test.exs`** to assert chip strings and subscription refresh path (use **`Phoenix.LiveViewTest`** + **`render_hook`** or **`send`** to process `handle_info`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-cost-hints-budget-alerts/18-CONTEXT.md
@.planning/phases/18-cost-hints-budget-alerts/18-UI-SPEC.md
@lib/kiln_web/live/run_detail_live.ex
@lib/kiln_web/live/cost_live.ex
@test/kiln_web/live/run_detail_live_test.exs
@lib/kiln/runs/run.ex
@lib/kiln/stages/stage_run.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunDetailLive panel + PubSub + banner</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - lib/kiln_web/live/run_replay_live.ex
    - prompts/kiln-brand-book.md
  </read_first>
  <acceptance_criteria>
    - `grep -q 'Advisory — does not change run caps' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -q 'Spend follows routed model' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -n 'Phoenix.PubSub.subscribe' lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n 'handle_info' lib/kiln_web/live/run_detail_live.ex` matches
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
Add assigns computed in **`mount`** or **`handle_params`** for **selected stage** (reuse existing stage selection param): **headroom** `Decimal` and **models**. Render a **bordered** panel (`border border-base-300` or Iron/Char classes per existing run detail styling) **below** the stage header or in the existing detail column per layout fit. Implement **`connected?/1`** subscription to **`"run:#{run.id}"`** mirroring **`RunReplayLive`** pattern. **`handle_info`** clauses: refresh run + stages on **`{:run_state, _}`** and on budget alert tuple — **clear** banner dismiss flag if new crossing arrives.

Banner: `role="status"` **or** `aria-live="polite"` wrapper; **dismiss** button sets assign only (no server delete of audit).

Use **`Decimal`** string formatting helpers consistent with **`CostLive`** / **`RunCompareLive`** (`to_string` / `Decimal.round`).
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures 1</automated>
  </verify>
  <done>COST-01 + COST-02 UI on run detail.</done>
</task>

<task type="auto">
  <name>Task 2: CostLive optional disclaimer line</name>
  <read_first>
    - lib/kiln_web/live/cost_live.ex
    - .planning/phases/18-cost-hints-budget-alerts/18-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - If copy added: `grep -q 'Advisory' lib/kiln_web/live/cost_live.ex` OR file unchanged with task note "deferred" — **planner must pick one**; default **add** one line in template footer
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/cost_live.ex</files>
  <action>
Add a **single** muted footer sentence on **`CostLive`** stating rollups are **indicative** and **do not change caps**, linking textually (not URL required) to run detail for per-run truth — match **D-1804**. If footer already crowded, use **`<p class="text-sm opacity-70">`** one sentence only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Cost page aligned with advisory contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView assigns ↔ HTML | No raw `Phoenix.HTML.raw` for operator-provided strings |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | X | XSS in model names | mitigate | HEEx default escaping; no `raw/1` on models |
</threat_model>

<verification>
mix test test/kiln_web/live/run_detail_live_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
Run detail shows advisory panel + soft banner path; tests cover visible copy and LiveView wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cost-hints-budget-alerts/18-03-SUMMARY.md`
</output>

## PLANNING COMPLETE
