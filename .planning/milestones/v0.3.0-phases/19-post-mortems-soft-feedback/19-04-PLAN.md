---
phase: 19-post-mortems-soft-feedback
plan: "04"
type: execute
wave: 2
depends_on:
  - "19-01-PLAN.md"
files_modified:
  - priv/repo/migrations/*_runs_operator_nudge_cursor.exs
  - lib/kiln/runs/run.ex
  - lib/kiln/operator_nudges.ex
  - test/kiln/operator_nudges_test.exs
autonomous: true
requirements:
  - FEEDBACK-01
milestone: v0.3.0
must_haves:
  truths:
    - "Accepting a nudge writes `:operator_feedback_received` audit in the same transaction as cursor advance placeholder (if separate table skipped, still transactional audit-only path)"
    - "Server rejects bodies over configured grapheme cap and empty-after-trim"
    - "Telemetry `[:kiln, :operator, :nudge, :received]` emits without raw body in metadata"
  artifacts:
    - path: "lib/kiln/operator_nudges.ex"
      provides: "validate + persist operator nudge"
      contains: "submit"
  key_links:
    - from: "lib/kiln/audit.ex"
      to: "lib/kiln/operator_nudges.ex"
      via: "Audit.append"
      pattern: "transactional append"
---

<objective>
Implement the **FEEDBACK-01 write path**: **`Kiln.OperatorNudges`** validates a **bounded UTF-8 body**, enforces **cooldown + hourly cap** using **`:ets`** or **DB-backed counts** (planner chooses; must be **testable without wall-clock sleep** via injectable **`utc_now`** fun), persists **`operator_feedback_received`** via **`Kiln.Audit.append/1`**, and relies on **`runs.operator_nudge_last_audit_id`** (**nullable `binary_id`**) added in Task 1 for **consumption cursor** state (D-1924) — **submit does not advance** this column.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-mortems-soft-feedback/19-CONTEXT.md
@.planning/phases/19-post-mortems-soft-feedback/19-RESEARCH.md
@lib/kiln/audit.ex
@lib/kiln/runs/run.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: runs.operator_nudge_last_audit_id column (UUID)</name>
  <read_first>
    - priv/repo/migrations/20260419000002_create_runs.exs
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - Migration file exists under `priv/repo/migrations/` containing `operator_nudge_last_audit_id`
    - `grep -q 'operator_nudge_last_audit_id' lib/kiln/runs/run.ex` exits 0
    - `MIX_ENV=test mix ecto.migrate --quiet` exits 0
  </acceptance_criteria>
  <files>priv/repo/migrations/*_runs_operator_nudge*.exs, lib/kiln/runs/run.ex</files>
  <action>
Generate migration **`add_operator_nudge_last_audit_id_to_runs`** adding nullable **`operator_nudge_last_audit_id`** as **`binary_id`** references **`audit_events(id)`** `on_delete: :nilify` **OR** a plain nullable **`binary_id`** without FK if circular migration ordering is painful (document choice). Update **`Kiln.Runs.Run`** schema field as **`Ecto.UUID`**. This column stores **last consumed** audit row id (UUID v7) for **double-delivery prevention** (D-1924); **writers of nudges do not advance it** — only consumption plan advances it.
  </action>
  <verify>
    <automated>MIX_ENV=test mix ecto.migrate --quiet && mix compile --warnings-as-errors</automated>
  </verify>
  <done>Cursor column exists.</done>
</task>

<task type="auto">
  <name>Task 2: OperatorNudges.submit/3 + tests</name>
  <read_first>
    - lib/kiln/audit.ex
    - priv/audit_schemas/v1/operator_feedback_received.json
  </read_first>
  <acceptance_criteria>
    - `grep -q 'defmodule Kiln.OperatorNudges' lib/kiln/operator_nudges.ex` OR `grep -q 'defmodule Kiln.OperatorFeedback' lib/kiln/operator_feedback.ex` exits 0
    - `grep -q 'operator_feedback_received' lib/kiln/operator_nudges.ex` OR `grep -q 'operator_feedback_received' lib/kiln/operator_feedback.ex` exits 0
    - `mix test test/kiln/operator_nudges_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/operator_nudges.ex, test/kiln/operator_nudges_test.exs</files>
  <action>
Implement **`Kiln.OperatorNudges.submit(run_id, body, opts)`** returning **`{:ok, audit_id}`** where **`audit_id`** is **`Ecto.UUID.t()`** or **`{:error, atom}`**:

- **Normalize** Unicode whitespace; strip C0 controls; reject empty.
- **Grapheme cap** default **180** (configurable via **`Application.compile_env`** or **`get_env`** with default).
- **Cooldown** default **20s** between accepts per **`run_id`** using an **`ETS`** table **`Kiln.OperatorNudgeRateLimit`** owned by a **supervised** `Agent` OR a **GenServer** started in **`Kiln.Application`** children list — **must** be started in **test** too (add child in **`lib/kiln/application.ex`** if new process registry table is used).
- Inside **`Repo.transact`**: insert audit via **`Audit.append(%{event_kind: :operator_feedback_received, run_id: ..., payload: %{...}})`** using **`body_preview`** per schema (truncate with **`String.slice/2`** on graphemes using **`String.graphemes/1`** join pattern).

**Telemetry:** `:telemetry.execute([:kiln, :operator, :nudge, :received], %{count: 1}, %{run_id: run_id})` — **no** **`body`** key.

Tests cover: happy path, oversize rejection, cooldown rejection.
  </action>
  <verify>
    <automated>mix test test/kiln/operator_nudges_test.exs --max-failures 1</automated>
  </verify>
  <done>Write path + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator text → persistence | Untrusted content; never log raw body |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-30 | I | prompt-injection via nudge | mitigate | Consumption plan restricts to planner-only advisory framing |
| T-19-31 | D | spam / DoS | mitigate | Cooldown + hourly cap + short body |
</threat_model>

<verification>
mix test test/kiln/operator_nudges_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
Nudge accept persists audit; rate limits enforced; telemetry fires without raw text.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-mortems-soft-feedback/19-04-SUMMARY.md`
</output>

## PLANNING COMPLETE
