---
phase: 15-run-comparison
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - lib/kiln_web/live/run_compare_live.ex
  - lib/kiln_web/live/run_board_live.ex
  - lib/kiln_web/live/run_detail_live.ex
  - test/kiln_web/live/run_compare_live_test.exs
autonomous: true
requirements:
  - PARA-02
milestone: v0.3.0
must_haves:
  truths:
    - "**`data-baseline-id`** and **`data-candidate-id`** attributes render with **UUID string** values on happy path"
    - "**`data-stage-key`** present on **each** union stage row element in DOM"
    - "**Swap** control issues **`push_patch`** to URL with **`baseline`** and **`candidate`** **swapped** (CONTEXT D-08)"
    - "Deep links to diff use **`~p\"/runs/#{run_id}\"`** with **`stage=`** query for the correct **`workflow_stage_id`** when a stage exists"
  artifacts:
    - path: "test/kiln_web/live/run_compare_live_test.exs"
      provides: "LiveView happy path"
      contains: "run-compare"
    - path: "lib/kiln_web/live/run_board_live.ex"
      provides: "compare entry UI"
      contains: "compare"
  key_links:
    - from: "run_board_live.ex"
      to: "run_compare_live.ex"
      via: "push_navigate"
      pattern: "entry"
---

<objective>
Complete **PARA-02 operator UX:** full compare **layout** (identity band + union spine + cost summary strip per UI-SPEC), **Swap** control, **entry** flows from **run board** (two-slot selection) and **run detail** (“Compare with…” → navigate), **deep links** back to **`RunDetailLive`**, and **LiveView tests** with stable selectors (CONTEXT D-21).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-run-comparison/15-CONTEXT.md
@.planning/phases/15-run-comparison/15-UI-SPEC.md
@.planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
@lib/kiln_web/live/run_compare_live.ex
@lib/kiln_web/live/run_board_live.ex
@lib/kiln_web/live/run_detail_live.ex
@lib/kiln/runs/compare.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Picker / slots | Only navigates to internal **`/runs/compare`** URLs |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-15-06 | T | mitigate | **`phx-click`** targets use run ids from server assigns only |
| T-15-07 | I | mitigate | Modal/drawer lists runs already visible on board/detail — **no** new unauthenticated export |

## Notes

Solo operator — reuse existing **`allow?`** / scope stubs if present on other lives.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Compare template: identity band + union table + cost strip</name>
  <read_first>
    - lib/kiln_web/live/run_compare_live.ex
    - .planning/phases/15-run-comparison/15-UI-SPEC.md
    - lib/kiln_web/live/cost_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'data-baseline-id' lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -q 'data-candidate-id' lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -q 'data-stage-key' lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -qE 'font-mono|tabular-nums' lib/kiln_web/live/run_compare_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_compare_live.ex</files>
  <action>
Extend **`render/1`** in **`RunCompareLive`**:

1. **Identity band:** show **short id** (first **8** hex chars, same convention as board cards) + **workflow id** + **state** for baseline and candidate when run struct present; when **`nil`**, render **UI-SPEC** missing copy in that column.
2. **Cost summary strip:** sum **`cost_usd`** / **`tokens_used`** from **latest stage runs** per side when available — display with **mono** or **`tabular-nums`** utility.
3. **Union spine:** **`for stage_id <- @snapshot.union_stage_ids`** render a row with **`data-stage-key={stage_id}`** and two sub-cells pulling from **`@snapshot.rows`** map/list.
4. **Gap semantics:** when one side’s stage is **`nil`**, show muted text **"Present only in baseline"** / **"Present only in candidate"** per CONTEXT D-20.
5. **Duplicate run warning:** when **`baseline_id == candidate_id`**, show warning strip (CONTEXT D-07).

Use **Tailwind** classes consistent with **`RunBoardLive`** (`border-ash`, `bg-char`, `text-bone`, etc.).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Compare UI structure complete.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Swap + artifact deep links</name>
  <read_first>
    - lib/kiln_web/live/run_compare_live.ex
    - lib/kiln_web/live/run_detail_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'swap' lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -q 'handle_event(\"swap' lib/kiln_web/live/run_compare_live.ex` exits 0 **OR** `grep -q "swap_sides" lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -q 'push_patch' lib/kiln_web/live/run_compare_live.ex` exits 0
    - `grep -q '~p"/runs/' lib/kiln_web/live/run_compare_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_compare_live.ex</files>
  <action>
1. Add **`phx-click`** button **Swap** (id **`run-compare-swap`**). **`handle_event`** builds new query **`baseline=candidate_val&candidate=baseline_val`** and returns **`{:noreply, push_patch(socket, to: ~p"/runs/compare?#{query}")}`** (use **`URI.encode_query/1`** on a map).
2. For each **artifact row**, render **`link patch={...}` or `navigate`** to **`~p"/runs/#{run_id}?#{URI.encode_query(%{stage: workflow_stage_id, pane: \"diff\"})}"`** — use the **artifact’s** associated **`workflow_stage_id`** from snapshot row metadata (extend snapshot maps in plan **02** if needed so link has stage id).

**Do not** add Ember styling to Swap (secondary control).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Swap + deep links implemented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — Board entry: two-slot compare strip</name>
  <read_first>
    - lib/kiln_web/live/run_board_live.ex
    - lib/kiln_web/live/run_compare_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'compare' lib/kiln_web/live/run_board_live.ex` exits 0
    - `grep -q 'push_navigate' lib/kiln_web/live/run_board_live.ex` exits 0
    - `grep -q 'runs/compare' lib/kiln_web/live/run_board_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_board_live.ex</files>
  <action>
Add **assigns** **`compare_baseline_id`**, **`compare_candidate_id`** (both default **`nil`**) in **`mount`**.

Add **UI strip** under page title with two slots **Choose baseline** / **Choose candidate** (CONTEXT D-11). **v1 approach (pick one and document in moduledoc):** either (a) **`phx-click="pick_compare_slot"`** on each **`board_run_card`** with **`phx-value-id`** and **`phx-value-slot`** (`"baseline"` or `"candidate"`), or (b) two small controls per card labeled **Baseline** / **Candidate**. Prefer the smallest diff that preserves kanban **`phx-update="stream"`** semantics (use **`phx-click`** on elements inside streamed cards — supported in LiveView).

**When both ids set:** **`push_navigate(socket, to: ~p\"/runs/compare?#{URI.encode_query(%{baseline: b, candidate: c})}\")`** and **reset** slots.

Use **`id=\"compare-strip\"`** on the strip container for tests.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Board navigates to compare URL.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 — Detail entry: “Compare with…”</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'Compare with' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -q 'runs/compare' lib/kiln_web/live/run_detail_live.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex</files>
  <action>
Add a **secondary** button or link near the run header: **"Compare with…"** (`id="run-detail-compare-open"`).

Implement **`handle_event(\"open_compare_picker\", …)`** or **JS-less** **`live_patch`** to a **modal-like** section in the same LiveView that lists **recent runs** (`Kiln.Runs.list_for_board/0` filtered to **exclude** current `run.id`, take **first 10`**) — picking one fires **`push_navigate`** to **`/runs/compare?baseline=#{socket.assigns.run.id}&candidate=#{picked}`** with **current** run as **baseline** (CONTEXT D-10).

If picker is too large for this task, **minimal** implementation: **`handle_event(\"compare_with\", %{\"other_id\" => oid})`** plus hidden test-only assign is **NOT** acceptable — ship **visible** picker or **link to board with flash** is **NOT** acceptable; ship **modal + list** as specified.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Detail compare entry exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5 — `run_compare_live_test.exs`</name>
  <read_first>
    - test/kiln_web/live/run_board_live_test.exs
    - test/kiln_web/live/run_detail_live_test.exs
    - test/support/conn_case.ex
  </read_first>
  <acceptance_criteria>
    - `test -f test/kiln_web/live/run_compare_live_test.exs` exits 0
    - `mix test test/kiln_web/live/run_compare_live_test.exs --max-failures 1` exits 0
    - `grep -q '#run-compare' test/kiln_web/live/run_compare_live_test.exs` exits 0
    - `grep -q 'data-baseline-id' test/kiln_web/live/run_compare_live_test.exs` exits 0
  </acceptance_criteria>
  <files>test/kiln_web/live/run_compare_live_test.exs</files>
  <action>
Create **`test/kiln_web/live/run_compare_live_test.exs`** using patterns from sibling LiveView tests:

1. Insert **two runs** (via factory or **`Runs.create/1`**) with valid attrs.
2. **`live(conn, ~p\"/runs/compare?baseline=...&candidate=...\")`**
3. Assert **`has_element?(view, \"#run-compare\")`**
4. Assert **`has_element?(view, \"[data-baseline-id='...']\")`** with exact baseline uuid string
5. **Invalid UUID test:** `live(conn, ~p\"/runs/compare?baseline=not-a-uuid&candidate=#{Ecto.UUID.generate()}\")` then assert **redirect** to **`/`** OR flash (match project’s **`follow_redirect`** pattern).

Use **`LazyHTML`** / **`has_element?`** only — **no** raw full HTML string equality.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_compare_live_test.exs --max-failures 1</automated>
  </verify>
  <done>LiveView tests green.</done>
</task>

</tasks>

<verification>
- `mix test test/kiln_web/live/run_compare_live_test.exs test/kiln/runs/run_compare_test.exs --max-failures 1` exits 0
- `mix compile --warnings-as-errors` exits 0
</verification>

<success_criteria>
- [ ] Stable DOM ids / data attributes per CONTEXT D-21
- [ ] Swap + entry flows + tests
- [ ] Compiler + targeted tests green
</success_criteria>
