---
phase: 14-fair-parallel-runs
plan: "03"
type: execute
wave: 2
depends_on:
  - phase: 14-fair-parallel-runs
    plan: "01"
  - phase: 14-fair-parallel-runs
    plan: "02"
files_modified:
  - lib/kiln/stages/next_stage_dispatcher.ex
  - test/kiln/stages/next_stage_dispatcher_test.exs
  - test/kiln/runs/run_parallel_fairness_test.exs
  - README.md
autonomous: true
requirements:
  - PARA-01
milestone: v0.3.0
must_haves:
  truths:
    - "**`NextStageDispatcher`** merges top-level **`%{\"run_id\" => run_id}`** into Oban **`meta`** alongside **`Telemetry.pack_meta()`** so operators can grep jobs/logs without unpacking **`kiln_ctx`** (**D-12** naming story complements args)"
    - "**README** gains **≤ ~35 lines** subsection **Fair scheduling** documenting RR grain, tie-break, **`[:kiln, :run, :scheduling, :queued, :stop]`** event name, and the three wait signals (**run_queued vs oban_queue vs db_pool_queue**) per **D-16**"
    - "**`run_parallel_fairness_test.exs`** (`async: false`) proves **N** runs (N=**3**–**5**) each emit **queued dwell stop** under contention harness — **no `Process.sleep`**; sync via **telemetry handoff**, **`Oban.drain_queue`**, and/or **`:sys.get_state/1`** (**D-14**)"
  artifacts:
    - path: "README.md"
      provides: "operator-facing fairness doc"
      contains: "Fair scheduling"
    - path: "test/kiln/runs/run_parallel_fairness_test.exs"
      provides: "PARA-01 integration signal"
      contains: ":scheduling, :queued, :stop"
  key_links:
    - from: "next_stage_dispatcher.ex"
      to: "telemetry.ex"
      via: "Oban.insert meta"
      pattern: "run_id in meta"
---

<objective>
Close PARA-01 **operator + CI loop**: enrich **Oban meta** for `:stages` jobs, add **integration** test for **multi-run** dwell telemetry under contention, and document **fairness grain + metrics** in **README** — building on plans **01–02**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-fair-parallel-runs/14-CONTEXT.md
@.planning/phases/14-fair-parallel-runs/14-RESEARCH.md
@.planning/phases/14-fair-parallel-runs/14-PATTERNS.md
@lib/kiln/stages/next_stage_dispatcher.ex
@lib/kiln/runs/run_director.ex
@lib/kiln/runs/transitions.ex
@README.md
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Oban meta → log pipelines | `run_id` is non-secret UUID — acceptable |

## STRIDE

| Threat ID | Category | Mitigation |
|-----------|----------|------------|
| T-14-20 | I | Do **not** put secrets in new meta keys — **`run_id` only** |
| T-14-21 | E | README must not claim cross-node fairness (deferred) |

## Notes

Integration test must respect **`KilnWeb.Telemetry`** policy — attach handler in test process only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — `run_id` in Oban job `meta` for `StageWorker` inserts</name>
  <read_first>
    - lib/kiln/stages/next_stage_dispatcher.ex
    - lib/kiln/telemetry.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q '\"run_id\"' lib/kiln/stages/next_stage_dispatcher.ex` exits 0 OR `grep -q \"'run_id'\" lib/kiln/stages/next_stage_dispatcher.ex` exits 0 (string map key **`\"run_id\"`** preferred for JSON meta)
    - `grep -q 'pack_meta' lib/kiln/stages/next_stage_dispatcher.ex` exits **0** (still calls **`Telemetry.pack_meta/0`**) AND `grep -q 'run_id' lib/kiln/stages/next_stage_dispatcher.ex` exits **0**
  </acceptance_criteria>
  <files>lib/kiln/stages/next_stage_dispatcher.ex</files>
  <action>
In **`enqueue_next!/2`**, where **`StageWorker.new(meta: Telemetry.pack_meta())`** is called, change to:

```elixir
meta = Map.merge(Telemetry.pack_meta(), %{"run_id" => run_id})
...
|> StageWorker.new(meta: meta)
```

**`Map.merge/2`** left **`Telemetry.pack_meta()`** so **`kiln_ctx`** stays intact; right map’s **`\"run_id\"`** wins on collision (must not exist in **`pack_meta/0`** today).

**Do not** add **`run_id`** to Prometheus metrics.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Stage jobs carry top-level run_id meta.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Extend `next_stage_dispatcher_test.exs` for meta</name>
  <read_first>
    - test/kiln/stages/next_stage_dispatcher_test.exs
  </read_first>
  <acceptance_criteria>
    - `mix test test/kiln/stages/next_stage_dispatcher_test.exs --max-failures 1` exits 0
    - New or updated assertion checks enqueued job **`meta[\"run_id\"]`** equals run UUID string (`grep -q 'run_id' test/kiln/stages/next_stage_dispatcher_test.exs` exits 0)
  </acceptance_criteria>
  <files>test/kiln/stages/next_stage_dispatcher_test.exs</files>
  <action>
Use **`Oban.Testing.assert_enqueued/1`** (or project pattern) to capture job and assert **`meta`** includes **`\"run_id\"`**.

If no Oban sandbox in that test today, follow existing file setup.
  </action>
  <verify>
    <automated>mix test test/kiln/stages/next_stage_dispatcher_test.exs --max-failures 1</automated>
  </verify>
  <done>Meta contract tested.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — Integration `run_parallel_fairness_test.exs`</name>
  <read_first>
    - test/support/rehydration_case.ex
    - test/support/oban_case.ex
    - .planning/phases/14-fair-parallel-runs/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `@moduletag :integration` OR `@tag :integration` documented if test is slow — optional
    - **`use ExUnit.Case, async: false`** appears in file (`grep -q 'async: false' test/kiln/runs/run_parallel_fairness_test.exs` exits 0)
    - `mix test test/kiln/runs/run_parallel_fairness_test.exs --max-failures 1` exits 0
    - Test counts **≥ N** `[:kiln, :run, :scheduling, :queued, :stop]` events matching **N** runs created (`grep -q ':scheduling, :queued, :stop' test/kiln/runs/run_parallel_fairness_test.exs` exits 0)
  </acceptance_criteria>
  <files>test/kiln/runs/run_parallel_fairness_test.exs</files>
  <action>
Create **`test/kiln/runs/run_parallel_fairness_test.exs`**:

1. **`async: false`**.
2. Use **`DataCase`** + **`ObanCase`** / **`SQL.Sandbox`** patterns as needed.
3. Create **N=3** runs (valid workflow_id / checksum from factory — copy attrs from **`run_director_p3_test.exs`** or **`transitions_test`** minimal happy path).
4. Attach **`:telemetry.attach/4`** handler counting **`[:kiln, :run, :scheduling, :queued, :stop]`**.
5. For each run in RR-friendly sequence, call **`RunDirector.start_run(run.id)`** when prerequisites satisfied (operator readiness stub if needed — follow **`run_director_readiness_test.exs`** env **`KILN_SKIP_OPERATOR_READINESS`** if present).
6. Assert **handler count == N** and each **`duration`** ≥ 0.
7. **No `Process.sleep/1`** — if synchronization needed, **`Oban.drain_queue(:stages)`** or **`:sys.get_state(Kiln.Runs.RunDirector)`**.

If **`start_run`** blocked by readiness in test env, set documented **`System.put_env`** in `setup` block exactly as existing tests do.

**If** harness proves **too heavy** for one file, split helper module under **`test/support/`** — keep **one** `mix test ...` entry point.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/run_parallel_fairness_test.exs --max-failures 1</automated>
  </verify>
  <done>Multi-run telemetry integration exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 — README **Fair scheduling** subsection (D-16)</name>
  <read_first>
    - README.md
    - .planning/phases/14-fair-parallel-runs/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -q 'Fair scheduling' README.md` exits 0
    - `grep -q '[:kiln, :run, :scheduling, :queued, :stop]' README.md` exits 0 **OR** fenced code block contains the atom list without brackets typo — acceptable: **backtick string** `[:kiln, :run, :scheduling, :queued, :stop]` in prose
    - `grep -qi 'run_queued' README.md` exits 0 (or **`run queued`** phrase distinguishing signals)
  </acceptance_criteria>
  <files>README.md</files>
  <action>
Add **`## Fair scheduling`** (or **`### Fair scheduling`** under an existing Ops / Architecture heading) covering:

- **Grain:** per-run **RR** among active factory work at **`RunDirector`** scan + stable tie-break (**`inserted_at`**, **`run_id`**).
- **Telemetry:** literal event **`[:kiln, :run, :scheduling, :queued, :stop]`** + **`duration` ms** meaning (**wall** time since **`inserted_at`** in v1).
- **Three waits:** **run queued dwell** vs **Oban `queue_time`** vs **Ecto pool queue** — one short bullet each (**D-12**).
- **Out of scope line:** no weighted fair-share / cross-node chart (points to Phase **15+** / deferred CONTEXT items).

Keep prose **operator-tone** per **`CLAUDE.md`** brand contract (calm, precise).
  </action>
  <verify>
    <automated>grep -q 'Fair scheduling' README.md</automated>
  </verify>
  <done>README documents fairness + metrics.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5 — Repository gate</name>
  <read_first>
    - mix.exs
  </read_first>
  <acceptance_criteria>
    - `mix check` exits 0
  </acceptance_criteria>
  <files>README.md, lib/kiln/stages/next_stage_dispatcher.ex, test/kiln/runs/run_parallel_fairness_test.exs</files>
  <action>
Run **`mix check`** after Tasks 1–4.

Fix **Credo / Dialyzer / Sobelow** issues only if introduced by this plan’s files.
  </action>
  <verify>
    <automated>mix check</automated>
  </verify>
  <done>Full meta-runner green.</done>
</task>

</tasks>

<verification>
1. `mix test test/kiln/stages/next_stage_dispatcher_test.exs test/kiln/runs/run_parallel_fairness_test.exs --max-failures 1` exits **0**.
2. `grep -q 'Fair scheduling' README.md` exits **0**.
3. `mix check` exits **0**.
</verification>

<success_criteria>
- **PARA-01** observable in **CI** via integration test + README operator clarity.
- **Oban** jobs on `:stages` expose **`run_id`** at meta top-level for observability.
- **No** new high-cardinality Prometheus labels.
</success_criteria>

## PLANNING COMPLETE

Plan **03** (wave **2**) — Oban meta + integration test + README.
