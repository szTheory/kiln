---
phase: 14-fair-parallel-runs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/runs/scheduling_telemetry.ex
  - lib/kiln/runs/transitions.ex
  - test/kiln/runs/run_scheduling_telemetry_test.exs
autonomous: true
requirements:
  - PARA-01
milestone: v0.3.0
must_haves:
  truths:
    - "Telemetry event **`[:kiln, :run, :scheduling, :queued, :stop]`** fires **exactly once** per successful transition **out of** `:queued` (first departure only — matrix has no return edge to `:queued` in v1) (**D-11**)"
    - "Measurement **`duration`** is **integer milliseconds** (wall-clock from `run.inserted_at` to transition completion instant), documented in module doc (**D-11**)"
    - "Metadata includes **`run_id`** (string UUID) and **`next_state`** (string); **no** API keys, model payloads, or **`run_id`** as a **Prometheus** / `Telemetry.Metrics` label in `KilnWeb.Telemetry` (**D-11**)"
    - "ORCH-06 / ORCH-07 precedence preserved: if `transition/3` returns **error** or escalates **without** leaving `:queued`**, **no** dwell-stop event (**D-07**)"
  artifacts:
    - path: "lib/kiln/runs/scheduling_telemetry.ex"
      provides: "named event + emit helper"
      contains: ":run, :scheduling, :queued, :stop"
    - path: "lib/kiln/runs/transitions.ex"
      provides: "hook after successful queued exit"
      contains: "SchedulingTelemetry"
  key_links:
    - from: "transitions.ex"
      to: "scheduling_telemetry.ex"
      via: "function call after {:ok, _} commit path"
      pattern: "telemetry execute"
---

<objective>
Implement **PARA-01 observability pillar (D-11):** emit a **single** `:telemetry` event when a run **successfully** leaves `:queued`, with **grep-verifiable** event name, **millisecond** dwell, and **safe** metadata — without adding metric label cardinality to `KilnWeb.Telemetry`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-fair-parallel-runs/14-CONTEXT.md
@.planning/phases/14-fair-parallel-runs/14-RESEARCH.md
@.planning/phases/14-fair-parallel-runs/14-PATTERNS.md
@lib/kiln/runs/transitions.ex
@lib/kiln/runs/run.ex
@lib/kiln/application.ex
@lib/kiln_web/telemetry.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Run state → telemetry consumers | Events cross test / ops / future metrics pipelines |
| Transitions → Logger | Existing metadata patterns must not regress |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|---------------|------------|
| T-14-01 | I | mitigate | Metadata whitelist: **only** `run_id`, `next_state`, optional `correlation_id` |
| T-14-02 | E | mitigate | **No** `run_id` in `Telemetry.Metrics` reporter tags in this plan |
| T-14-03 | I | mitigate | Emit **after** successful DB commit path only (same process as today) |

## Notes

v1 uses **`inserted_at`** as queued-start proxy (no `queued_at` column) — document limitation in `@moduledoc` (runs always created in `:queued`).
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Add `Kiln.Runs.SchedulingTelemetry` helper module</name>
  <read_first>
    - lib/kiln/runs/run.ex
    - lib/kiln/telemetry.ex
    - .planning/phases/14-fair-parallel-runs/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f lib/kiln/runs/scheduling_telemetry.ex` exits 0
    - `grep -qF '[:kiln, :run, :scheduling, :queued, :stop]' lib/kiln/runs/scheduling_telemetry.ex` exits 0
    - `grep -q 'emit_queued_dwell_stop' lib/kiln/runs/scheduling_telemetry.ex` exits 0
    - `grep -q 'millisecond' lib/kiln/runs/scheduling_telemetry.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln/runs/scheduling_telemetry.ex</files>
  <action>
Create **`lib/kiln/runs/scheduling_telemetry.ex`** with:

- **Public function** `emit_queued_dwell_stop/2` — args: **`%Kiln.Runs.Run{}` as `run`**, **`to_state :: atom()`**.
- **Guard:** if `run.state != :queued`, **return `:ok` without executing** (idempotent for callers).
- **Compute** `duration_ms = max(0, DateTime.diff(DateTime.utc_now(), run.inserted_at, :millisecond))`.
- **Invoke** `:telemetry.execute([:kiln, :run, :scheduling, :queued, :stop], %{duration: duration_ms}, %{run_id: to_string(run.id), next_state: Atom.to_string(to_state), correlation_id: run.correlation_id || "none"})` — adjust **`correlation_id`** only if column nil is possible; use **`"none"`** string when absent so metadata stays JSON-friendly.
- **`@moduledoc`** must state: **wall-clock ms from `inserted_at`**, not monotonic; **no Prometheus labels** on `run_id`.

No dependencies on Phoenix.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>SchedulingTelemetry module compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Invoke emit from `Transitions` on successful `queued → *` transition</name>
  <read_first>
    - lib/kiln/runs/transitions.ex
    - lib/kiln/runs/scheduling_telemetry.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'SchedulingTelemetry.emit_queued_dwell_stop' lib/kiln/runs/transitions.ex` exits 0
    - `grep -q 'emit_queued_dwell_stop' lib/kiln/runs/transitions.ex` exits 0
    - Call site is **`defp transition_ok/4`** (or adjacent private helper it delegates to), **after** `append_audit/4` succeeds and **before** the `with` pipeline returns, using the **pre-update** `%Run{}` where **`run.state == :queued`**
  </acceptance_criteria>
  <files>lib/kiln/runs/transitions.ex</files>
  <action>
In **`Kiln.Runs.Transitions`**, extend **`defp transition_ok(run, to, meta, new_window)`** (the successful state+audit path):

- After **`append_audit(updated, run.state, to, meta)`** succeeds (same **`with` chain** as today), invoke:

  **`_ = Kiln.Runs.SchedulingTelemetry.emit_queued_dwell_stop(run, to)`**

  using the **original** `run` argument (**still `:queued`** when that edge is taken) — **not** `updated`.

- **Rationale:** `emit` runs **inside** `Repo.transact` **after** row update + audit append succeeded; duration uses **`run.inserted_at`** vs “now” and matches **committed** state. Cost is one `:telemetry.execute` (microseconds) — does **not** hold locks across I/O.

Add **`alias Kiln.Runs.SchedulingTelemetry`** near other aliases in `transitions.ex`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Transitions invokes SchedulingTelemetry on queued exit only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 — ExUnit: `run_scheduling_telemetry_test.exs`</name>
  <read_first>
    - test/test_helper.exs
    - test/support/data_case.ex
    - test/kiln/runs/transitions_test.exs
  </read_first>
  <acceptance_criteria>
    - `test -f test/kiln/runs/run_scheduling_telemetry_test.exs` exits 0
    - `mix test test/kiln/runs/run_scheduling_telemetry_test.exs --max-failures 1` exits 0
    - Test attaches **handler** for `[:kiln, :run, :scheduling, :queued, :stop]`, creates run in `:queued`, calls **`Transitions.transition(run.id, :planning)`**, asserts **one** event with **`is_integer(measurements.duration)`** and **`metadata.run_id`** equals **`to_string(run.id)`**
  </acceptance_criteria>
  <files>test/kiln/runs/run_scheduling_telemetry_test.exs</files>
  <action>
Create **`test/kiln/runs/run_scheduling_telemetry_test.exs`** using **`Kiln.DataCase`** (or project equivalent), **async: false** if sharing DB sandbox with Oban not needed — default **`async: true`** OK if isolated.

Steps in test:

1. `attach_handler` with `:telemetry.attach/4` unique handler id.
2. Insert run via **`Kiln.Runs.create/1`** with valid attrs from factory or minimal map (see existing transition tests).
3. `Transitions.transition(run.id, :planning)` — expect **`{:ok, _}`**.
4. Assert received measurement map has **`:duration`** key ≥ 0.
5. **Detach** handler in `on_exit`.

If **`Transitions.transition`** requires extra setup (workflow checksum, etc.), **copy minimal valid attrs** from **`test/kiln/runs/transitions_test.exs`** or factories.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/run_scheduling_telemetry_test.exs --max-failures 1</automated>
  </verify>
  <done>Telemetry contract covered by ExUnit.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4 — Confirm `KilnWeb.Telemetry` does not label metrics by `run_id`</name>
  <read_first>
    - lib/kiln_web/telemetry.ex
    - .planning/phases/14-fair-parallel-runs/14-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -q 'run_id' lib/kiln_web/telemetry.ex` exits **1** OR if present, **only** in comments — **no** `tags: .*run_id` in **`Telemetry.Metrics`** reporters added by this phase
    - Document in **`scheduling_telemetry.ex`** moduledoc: **do not** add `run_id` to summary metrics in `KilnWeb.Telemetry` (executor verifies grep)
  </acceptance_criteria>
  <files>lib/kiln_web/telemetry.ex, lib/kiln/runs/scheduling_telemetry.ex</files>
  <action>
**Read-only** audit of **`lib/kiln_web/telemetry.ex`**. If **`run_id`** already appears as a metric tag from prior work, **do not** worsen — add **WARNING** comment in **`scheduling_telemetry.ex`** moduledoc referencing existing debt.

**Do not** add new `Telemetry.Metrics` summaries for per-run dwell in this plan (optional Phase 15+) — PARA-01 v1 is **event-first** per CONTEXT **D-13**.
  </action>
  <verify>
    <automated>grep -E 'tags:.*run_id' lib/kiln_web/telemetry.ex ; test $? -eq 1</automated>
  </verify>
  <done>No new cardinality labels introduced.</done>
</task>

</tasks>

<verification>
1. `mix test test/kiln/runs/run_scheduling_telemetry_test.exs --max-failures 1` exits **0**.
2. `grep -qF '[:kiln, :run, :scheduling, :queued, :stop]' lib/kiln/runs/scheduling_telemetry.ex` exits **0**.
3. `grep -q 'SchedulingTelemetry.emit_queued_dwell_stop' lib/kiln/runs/transitions.ex` exits **0**.
4. `mix compile --warnings-as-errors` exits **0**.
</verification>

<success_criteria>
- **PARA-01** primary signal (**run queued dwell stop**) exists and is **test-covered**.
- **D-07** respected: no emit on failed transitions from `:queued`.
- **D-11** naming + metadata contract documented and grep-verified.
</success_criteria>

## PLANNING COMPLETE

Plan **01** (wave **1**) — telemetry for queued dwell.
