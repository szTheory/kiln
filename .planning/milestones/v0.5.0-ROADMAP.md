# Milestone v0.5.0: Local first success

**Status:** ✅ SHIPPED 2026-04-24
**Phases:** 25-28
**Total Plans:** 8

## Overview

v0.5.0 closed the gap between "Kiln looks promising" and "Kiln completed one real local run for me." The milestone made `/settings` the canonical live-readiness and remediation surface, promoted `hello-kiln` into the single recommended first local live run, routed blocked launches back to the exact missing readiness step, and finished with a rerun-backed repository proof command that now succeeds under the intended runtime role.

## Phases

### Phase 25: Local live readiness SSOT

**Goal**: Make live readiness operational rather than informational so the operator can tell, from one surface, whether Kiln is ready for a real local run and what to fix next.
**Depends on**: Phase 24
**Plans**: 3 plans

Plans:

- [x] `25-01-PLAN.md` — pessimistic-by-default operator readiness state and singleton persistence repair
- [x] `25-02-PLAN.md` — align readiness-aware operator surfaces on `/settings` remediation
- [x] `25-03-PLAN.md` — close README and planning SSOT around the readiness contract

**Details:**
Phase 25 made `/settings` the single authoritative readiness checklist, removed the false-ready singleton fallback, added the readiness backfill migration, and aligned onboarding, providers, templates, and run board recovery flows on one remediation target. README, `ROADMAP.md`, `REQUIREMENTS.md`, and `STATE.md` were updated to describe the same shipped readiness contract.

### Phase 26: First live template run

**Goal**: Turn the existing template/run surfaces into one trustworthy first live run path instead of making the operator guess where to start.
**Depends on**: Phase 25
**Plans**: 3 plans

Plans:

- [x] `26-01-PLAN.md` — present `hello-kiln` as the single recommended first live run
- [x] `26-02-PLAN.md` — route `Start run` through backend-authoritative preflight and settings recovery
- [x] `26-03-PLAN.md` — make `/runs/:id` the proof-first arrival surface and close milestone SSOT for LIVE-01..03

**Details:**
Phase 26 reframed `/templates` around a single first-run hero, demoted other templates to honest follow-on roles, moved launch authority into the backend, and preserved exact return context from blocked starts back through `/settings#settings-item-*`. It also strengthened `/runs/:id` into the first proof surface and recorded exact verification commands, although some Phase 26 artifacts remained historical debt after the later Phase 28 rerun.

### Phase 27: Local first-run proof

**Goal**: Leave the milestone with one explicit, repeatable proof that the local operator journey works end to end.
**Depends on**: Phases 25-26
**Plans**: 1 plan

Plans:

- [x] `27-01-PLAN.md` — add the dedicated `mix kiln.first_run.prove` wrapper and exact verification citation

**Details:**
Phase 27 introduced the owning proof command and the focused LiveView continuity seam from setup-ready `/settings` through `hello-kiln` into the run-detail shell. The phase later became historical context only: the milestone audit showed the delegated runtime boot still failing on Oban privileges, so final `UAT-04` closure moved to Phase 28.

### Phase 28: First-run proof runtime closure

**Goal**: Close the real repository-level first-run proof gap by fixing the integration boot contract, rerunning the owning proof command end to end, and reconciling the contradicted milestone artifacts.
**Depends on**: Phase 27
**Plans**: 1 plan

Plans:

- [x] `28-01-PLAN.md` — repair the Oban/runtime proof seam, rerun the owning command under `kiln_app`, and reconcile milestone SSOT

**Details:**
Phase 28 added forward-only Oban privilege repairs for existing repositories, updated the delegated integration layer to boot Phoenix as `kiln_app`, reran `mix kiln.first_run.prove`, and reconciled Phase 27, `ROADMAP.md`, and `REQUIREMENTS.md` around the final passing proof. This phase now owns `UAT-04` closure.

---

## Milestone Summary

**Decimal Phases:**

- None in milestone scope. Parking phases `999.3` and `999.4` remained outside v0.5.0 execution.

**Key Decisions:**

- `/settings` is the canonical live-readiness and remediation surface for local-first operation.
- `hello-kiln` is the single recommended first live run; other templates remain visible but explicitly secondary.
- `/runs/:id` is the first proof surface after a successful launch; the broader run board is a follow-on view.
- `mix kiln.first_run.prove` remains the top-level proof owner; runtime seams are repaired underneath it rather than replaced.

**Issues Resolved:**

- Removed the false-ready operator readiness fallback and backfilled safe defaults for existing databases.
- Added backend-authoritative launch preflight and deterministic routing to the first missing readiness step.
- Fixed the Oban/runtime privilege seam that previously prevented the delegated first-run proof from reaching `/health`.

**Issues Deferred:**

- Phase 26 still carries historical artifact debt (`26-VERIFICATION.md` partial status and `deferred-items.md` note) even though the repository-level proof later passed.
- Nyquist posture remains partial because Phase 26 has no `26-VALIDATION.md` and Phase 27 remains explicitly historical/non-compliant.
- One planning todo for orphan Phase 03 worktree residue was acknowledged at close and moved to `STATE.md`.

**Technical Debt Incurred:**

- `STATE.md` required normalization after placeholder execution text leaked out of phase tooling.
- Historical phase artifacts now need a later cleanup pass so audit and progress routing stay trustworthy without reading milestone context.

---

_For current project status, see `.planning/ROADMAP.md` and `.planning/MILESTONES.md`._
