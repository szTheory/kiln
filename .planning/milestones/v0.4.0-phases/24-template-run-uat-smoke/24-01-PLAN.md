---
phase: 24-template-run-uat-smoke
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/kiln_web/live/templates_live_test.exs
  - .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - UAT-03
milestone: v0.4.0
must_haves:
  truths:
    - "`Phoenix.LiveViewTest` remains the owning proof for the `/templates` happy path, and the test file explicitly satisfies onboarding readiness before mounting gated routes (**D-2405**, onboarding constraint)."
    - "The regression uses explicit DOM ids for operator actions and success-state boundaries, with `#templates-success-panel` and `#templates-start-run` as the primary in-flow proof points (**D-2401** to **D-2404**)."
    - "The start-run assertion follows navigation into the destination LiveView and proves the existing `#run-detail` shell instead of stopping at a redirect tuple (**D-2408** to **D-2411**)."
    - "Phase 24 verification cites the exact focused command `mix test test/kiln_web/live/templates_live_test.exs` and states honestly that it proves only the narrow template-to-run journey (**D-2412** to **D-2417**)."
  artifacts:
    - path: "test/kiln_web/live/templates_live_test.exs"
      provides: "Readiness-aware LiveView happy-path regression for `/templates` -> `/runs/:id`"
    - path: ".planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md"
      provides: "Focused evidence artifact with the exact file-level `mix test` command"
    - path: ".planning/REQUIREMENTS.md"
      provides: "UAT-03 completion traceability"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 completion state and plan inventory"
  key_links:
    - from: "test/kiln_web/live/templates_live_test.exs"
      to: "lib/kiln/operator_readiness.ex"
      via: "Readiness setup using persisted probe flags before gated route mounts"
      pattern: "OperatorReadiness\\.mark_step"
    - from: "test/kiln_web/live/templates_live_test.exs"
      to: "lib/kiln_web/live/templates_live.ex"
      via: "Id-first form submits and success-panel assertions"
      pattern: "#template-use-form-hello-kiln|#templates-success-panel|#templates-start-run-form"
    - from: "test/kiln_web/live/templates_live_test.exs"
      to: "lib/kiln_web/live/run_detail_live.ex"
      via: "Live navigation follow-up proof"
      pattern: "#run-detail"
    - from: ".planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md"
      to: "test/kiln_web/live/templates_live_test.exs"
      via: "Exact command citation"
      pattern: "mix test test/kiln_web/live/templates_live_test\\.exs"
---

<objective>
Close Phase 24 by adding one honest, deterministic regression for the operator’s first-success journey: choose a template, apply it, start a run, follow navigation, and prove the run detail shell.

Purpose: satisfy UAT-03 without widening scope beyond the existing LiveView seam.
Output: a readiness-aware `TemplatesLiveTest`, a passed `24-VERIFICATION.md`, and milestone SSOT updated only after the focused command passes.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-template-run-uat-smoke/24-CONTEXT.md
@.planning/phases/24-template-run-uat-smoke/24-UI-SPEC.md
@.planning/phases/24-template-run-uat-smoke/24-RESEARCH.md
@.planning/phases/24-template-run-uat-smoke/24-PATTERNS.md
@.planning/phases/17-template-library-onboarding-specs/17-CONTEXT.md
@.planning/phases/22-merge-authority-operator-docs/22-CONTEXT.md
@.planning/phases/22-merge-authority-operator-docs/22-VERIFICATION.md
@lib/kiln/operator_readiness.ex
@lib/kiln_web/plugs/onboarding_gate.ex
@lib/kiln_web/live/templates_live.ex
@lib/kiln_web/live/run_detail_live.ex
@test/kiln_web/live/templates_live_test.exs
@test/kiln_web/live/run_detail_live_test.exs
@test/kiln/specs/template_instantiate_test.exs

<interfaces>
From `lib/kiln/operator_readiness.ex`:

```elixir
@spec ready?() :: boolean()
def ready? do
  System.get_env("KILN_SKIP_OPERATOR_READINESS") == "1" or row_ready?()
end

@spec mark_step(:anthropic | :github | :docker, boolean()) ::
        {:ok, ProbeRow.t()} | {:error, Ecto.Changeset.t()}
def mark_step(step, value) when is_boolean(value) and step in [:anthropic, :github, :docker]

@spec current_state() :: %{
        anthropic: boolean(),
        github: boolean(),
        docker: boolean(),
        verified: non_neg_integer(),
        total: non_neg_integer()
      }
def current_state
```

From `lib/kiln_web/plugs/onboarding_gate.ex`:

```elixir
def call(conn, _opts) do
  if Kiln.OperatorReadiness.ready?() or allowed_path?(conn.request_path) do
    conn
  else
    conn
    |> put_flash(:error, "Complete setup before starting a run")
    |> redirect(to: "/onboarding")
    |> halt()
  end
end
```

From `lib/kiln_web/live/templates_live.ex`:

```elixir
<form id={"template-use-form-#{t.id}"} phx-submit="use_template">
<div id="templates-success-panel">
<form id="templates-start-run-form" phx-submit="start_run" class="mt-3">
<button type="submit" id="templates-start-run" class="btn btn-sm btn-primary">
```

From `lib/kiln_web/live/run_detail_live.ex`:

```elixir
<div id="run-detail">
```

From `test/kiln/specs/template_instantiate_test.exs`:

```elixir
assert {:ok, %{spec: spec}} = Specs.instantiate_template_promoted("hello-kiln")
assert {:ok, run} = Runs.create_for_promoted_template(spec, "hello-kiln")
assert run.state == :queued
```
</interfaces>
</context>

<source_audit>
## Coverage Audit

| Source type | Item | Covered by |
|-------------|------|------------|
| GOAL | Automated regression cover for the template -> run path | Task 1, Task 2 |
| REQ | `UAT-03` LiveView happy path with stable ids + verification command citation | Task 1, Task 2 |
| RESEARCH | OnboardingGate redirects `/templates` until readiness is true | Task 1 |
| RESEARCH | Use `follow_redirect/3` and stop at `#run-detail`, not deep run internals | Task 1 |
| CONTEXT | D-2401 to D-2404 id-first selector contract | Task 1 |
| CONTEXT | D-2405 to D-2407 LiveView ownership only | Task 1 |
| CONTEXT | D-2408 to D-2411 follow navigation and assert `#run-detail` | Task 1 |
| CONTEXT | D-2412 to D-2417 exact focused command and honest verification claims | Task 2 |
| CONTEXT | Deferred browser-E2E expansion and deep run-internal assertions | Explicitly excluded by Task 1 action |
| UI-SPEC | `#templates-success-panel`, `#templates-start-run`, `#run-detail` remain the selector contract | Task 1 |
| PATTERNS | Readiness setup belongs in test setup, not product code | Task 1 |
| PRIOR PHASE 17 | `/templates` stays the canonical catalog with `Use template` then `Start run` | Task 1 |
| PRIOR PHASE 22 | Verification must map exact commands to exact claims | Task 2 |
| CURRENT FAILURE | `mix test test/kiln_web/live/templates_live_test.exs` currently fails 4/4 at `/onboarding` | Task 1 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make `TemplatesLiveTest` readiness-aware and extend the happy-path proof to `#run-detail`</name>
  <files>test/kiln_web/live/templates_live_test.exs</files>
  <behavior>
    - Test 1: the `/templates` catalog mounts behind `OnboardingGate` because the test setup marks readiness true before `live/2`.
    - Test 2: submitting `#template-use-form-hello-kiln` shows `#templates-success-panel` and `#templates-start-run` using ids as the primary contract.
    - Test 3: submitting `#templates-start-run-form` follows navigation and asserts `#run-detail` on the destination LiveView instead of stopping at a redirect tuple.
    - Test 4: the file remains narrow; it does not re-prove run checksum, queued-state internals, or browser-only behavior already owned elsewhere.
  </behavior>
  <action>
Implement the Phase 24 regression in `test/kiln_web/live/templates_live_test.exs` and keep ownership inside `Phoenix.LiveViewTest` per D-2405 to D-2407.

1. Change the test module to `async: false` because onboarding readiness is a singleton persisted row shared across tests.
2. Add readiness setup in this file itself. Capture the current readiness booleans with `Kiln.OperatorReadiness.current_state/0`, mark `:anthropic`, `:github`, and `:docker` true before mounting `/templates`, and restore all three flags in `on_exit`. Do not bypass the gate with `KILN_SKIP_OPERATOR_READINESS`; prove the real route contract instead.
3. Keep the selector contract id-first per D-2401 to D-2404:
   - use `form("#template-use-form-hello-kiln") |> render_submit()`
   - assert `has_element?(view, "#templates-success-panel")`
   - assert `has_element?(view, "#templates-start-run")`
   - avoid making free-text the primary seam
4. Strengthen the start-run test per D-2408 to D-2411:
   - submit `#templates-start-run-form`
   - keep the destination route assertion
   - then call `follow_redirect/3` on the submit result and assert `has_element?(run_view, "#run-detail")`, following the shallow destination-shell proof style already used in `test/kiln_web/live/run_detail_live_test.exs`
5. Preserve narrow scope:
   - do not touch `lib/kiln_web/live/templates_live.ex` unless the executor discovers a genuinely missing state-boundary id
   - do not add browser E2E coverage
   - do not query the repo for run internals already covered by `test/kiln/specs/template_instantiate_test.exs`
6. Use `has_element?/2` and `element/2`-style assertions rather than raw HTML checks except for an incidental sanity check if a selector needs debugging.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/templates_live_test.exs</automated>
  </verify>
  <done>The LiveView file mounts `/templates` without onboarding redirects in test, proves the success panel by id, and follows the start-run navigation through to `#run-detail`.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 24 verification and flip milestone SSOT only after the focused test passes</name>
  <files>.planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Implement the verification artifact and the requirement/roadmap closure in one honest sequence per D-2412 to D-2417.

Part A — write `.planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md`:
1. Frontmatter:
   - `status: draft` while authoring
   - `phase: 24-template-run-uat-smoke`
   - `verified: 2026-04-23`
   - `requirements:` containing `UAT-03`
2. Title: `# Phase 24 verification — Template -> run UAT smoke`
3. `## Automated` table must name:
   - `mix test test/kiln_web/live/templates_live_test.exs`
   - the result of that command
   - the concrete proof it provides: readiness-aware `/templates` mount, id-first template promotion, start-run navigation followed to `#run-detail`
4. Include a fenced command block with the exact single-file command:

```bash
mix test test/kiln_web/live/templates_live_test.exs
```

5. State explicitly that this is targeted evidence for the template-to-run operator journey and does not replace the broader merge-authority suite from Phase 22.
6. Run the exact command above. Only if it exits 0, change the verification frontmatter to `status: passed`.
7. After the focused command passes and after the SSOT updates are in place, run `bash script/precommit.sh` as the final repo gate required by project instructions. Keep the Phase 24 claim narrow: the verification artifact should still cite the single-file `mix test` command as the phase proof, while precommit is the required closeout gate. Use `.planning/phases/22-merge-authority-operator-docs/22-VERIFICATION.md` as the wording analog for exact-command honesty.

Part B — after `24-VERIFICATION.md` is `status: passed`, update milestone SSOT:
1. In `.planning/REQUIREMENTS.md`, mark `UAT-03` complete in the same style as the other v0.4.0 rows and point the citation to `24-VERIFICATION.md`.
2. Update the traceability row to `| UAT-03 | Phase 24 | Complete |`.
3. Update the coverage summary counts so they remain numerically correct.
4. In `.planning/ROADMAP.md`, mark Phase 24 complete in the top v0.4.0 phase checklist with `completed 2026-04-23`.
5. In the Phase 24 detail section, preserve the goal text, keep `**Plans:** 1 plan`, and check the `24-01-PLAN.md` entry only after the phase work is verified.
6. Do not claim broader CI coverage than the focused file-level command proves.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/templates_live_test.exs && test -f .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q '^status: passed$' .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q '^verified: 2026-04-23$' .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q '^mix test test/kiln_web/live/templates_live_test.exs$' .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q 'targeted evidence for the template -> run journey' .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q 'does not replace the broader merge-authority suite from Phase 22' .planning/phases/24-template-run-uat-smoke/24-VERIFICATION.md && grep -q '\[x\] \*\*UAT-03\*\*: .*Complete 2026-04-23.*(`24-VERIFICATION.md`)' .planning/REQUIREMENTS.md && grep -q '| UAT-03 | Phase 24 | Complete |' .planning/REQUIREMENTS.md && grep -q 'Coverage: v0.4.0 requirements: \*\*3\*\* — mapped: \*\*3\*\* — unmapped: \*\*0\*\* — complete: \*\*3\*\* — pending: \*\*0\*\*\\.' .planning/REQUIREMENTS.md && grep -q '\[x\] \*\*Phase 24: Template → run UAT smoke\*\* — UAT-03 — LiveView (or integration) test + VERIFICATION command citation\. — completed 2026-04-23\.' .planning/ROADMAP.md && grep -q '\- \[x\] `24-01-PLAN.md` — Make `TemplatesLiveTest` readiness-aware, follow navigation to `#run-detail`, then record focused verification evidence\.' .planning/ROADMAP.md && bash script/precommit.sh</automated>
  </verify>
  <done>`24-VERIFICATION.md` uses the exact focused command with narrow-evidence wording modeled on Phase 22 honesty, both milestone SSOT files show Phase 24 / UAT-03 complete on 2026-04-23 with the expected traceability and coverage counts only after that command passes, and the final repo gate runs via `bash script/precommit.sh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser pipeline -> gated LiveView tests | `OnboardingGate` can redirect the whole flow before the regression reaches `/templates` |
| Template form submit -> run navigation proof | A redirect tuple alone can overstate confidence if the destination LiveView never mounts |
| Verification prose -> merge confidence | The phase verification file could misrepresent narrow file-level evidence as broader CI authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `test/kiln_web/live/templates_live_test.exs` | mitigate | Force readiness true in test setup using `OperatorReadiness.mark_step/2`, restore prior values on exit, and run the real `/templates` route rather than bypassing the gate |
| T-24-02 | R | start-run success proof | mitigate | Require `follow_redirect/3` plus `has_element?(run_view, "#run-detail")` so the proof cannot stop at a redirect tuple |
| T-24-03 | I | selector contract | mitigate | Keep ids as the primary seam and avoid brittle text-only assertions for operator actions and state transitions |
| T-24-04 | R | `24-VERIFICATION.md` | mitigate | Cite the exact file-level `mix test` command and state that it is targeted evidence, not a replacement for broader CI merge authority |
</threat_model>

<verification>
1. `mix test test/kiln_web/live/templates_live_test.exs`
2. After the focused command passes, confirm `24-VERIFICATION.md`, `REQUIREMENTS.md`, and `ROADMAP.md` contain the exact Task 2 evidence lines.
</verification>

<success_criteria>
- [ ] `TemplatesLiveTest` sets onboarding readiness explicitly and no longer fails by redirecting `/templates` to `/onboarding`
- [ ] The happy-path regression proves `#templates-success-panel`, then follows navigation and proves `#run-detail`
- [ ] `24-VERIFICATION.md` cites `mix test test/kiln_web/live/templates_live_test.exs` verbatim and describes it as narrow journey evidence only
- [ ] `UAT-03` and Phase 24 flip complete in milestone SSOT only after the focused test passes
- [ ] `bash script/precommit.sh` runs after the focused proof and SSOT updates as the final repo gate
</success_criteria>

<output>
After completion, create `.planning/phases/24-template-run-uat-smoke/24-01-SUMMARY.md` per the execute-plan workflow; it is not part of this phase's required deliverable scope.
</output>
