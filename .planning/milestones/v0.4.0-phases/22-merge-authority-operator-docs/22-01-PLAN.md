---
phase: 22-merge-authority-operator-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - README.md
autonomous: true
requirements:
  - DOCS-08
milestone: v0.4.0
must_haves:
  truths:
    - "`.planning/PROJECT.md` contains **`## Merge authority`** with a **3–5 row** semantic tier table aligned to **`.github/workflows/ci.yml`** jobs `mix check`, `integration smoke (first_run.sh)`, `tag vs mix.exs version`, and the **boot checks** step inside `check` (**D-2202**)"
    - "Same section includes **Recommended before push (optional, not merge authority)** bullets naming **`just planning-gates`**, **`just shift-left`**, **`script/precommit.sh` / `mix precommit`**, **`DOCS=1 mix docs.verify`** where those exist in-repo (**D-2203**)"
    - "**`README.md`** has a **compact** (target 3–6 lines) merge callout: green **GitHub Actions** on PRs to **`main`**; link **`.planning/PROJECT.md#merge-authority`**; **no** `| **Tier A` pipe-row table in README (**D-2201**)"
    - "**Phase 12 honesty:** `README.md` or `.planning/PROJECT.md` references **`.planning/phases/12-local-docker-dx/12-01-SUMMARY.md`** for documented local **PARTIAL** vs CI (**D-2204**, DOCS-08)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical merge-authority + Local vs CI"
    - path: "README.md"
      provides: "Skim-level merge pointer + Phase 12 callout"
  key_links:
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "#merge-authority anchor"
---

<objective>
Ship **DOCS-08**: one **canonical** merge-authority story in **`.planning/PROJECT.md`** and a **non-duplicative** **README** summary that points operators and contributors at the same truth, including **honest Phase 12 local PARTIAL** language (**22-CONTEXT.md** D-2201–D-2205).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-merge-authority-operator-docs/22-CONTEXT.md
@.planning/phases/22-merge-authority-operator-docs/22-RESEARCH.md
@.github/workflows/ci.yml
@README.md
@.planning/PROJECT.md
@.planning/phases/12-local-docker-dx/12-01-SUMMARY.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader → CI | Docs describe **GitHub Actions** as merge oracle; must not imply optional scripts are branch-protected gates |
| Contributor → repo | Mis-stating tiers could cause merges without intended checks or block healthy merges |

## STRIDE threat register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | I | README / PROJECT prose | mitigate | Explicit “optional local” list **separate** from merge table; cite `12-01-SUMMARY.md` for PARTIAL |
| T-22-02 | E | Wrong job names | mitigate | Table footnote lists **exact** `name:` strings from `.github/workflows/ci.yml` |

## ASVS L1 notes

Documentation only; no new attack surface. Risk is **social engineering via bad guidance** — mitigated with grep-verifiable strings and CI file cross-reference.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — PROJECT.md merge authority SSOT</name>
  <read_first>
    - .planning/phases/22-merge-authority-operator-docs/22-CONTEXT.md
    - .planning/phases/22-merge-authority-operator-docs/22-RESEARCH.md
    - .github/workflows/ci.yml
    - .planning/PROJECT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n '^## Merge authority' .planning/PROJECT.md` exits 0 (exact H2 heading for stable anchor `#merge-authority`)
    - `grep -q 'mix check' .planning/PROJECT.md` exits 0 AND `grep -q 'integration smoke (first_run.sh)' .planning/PROJECT.md` exits 0 AND `grep -q 'tag vs mix.exs version' .planning/PROJECT.md` exits 0 — strings match **job `name:`** in `.github/workflows/ci.yml` lines 30, 111, 17
    - `grep -q 'KILN_DB_ROLE=kiln_owner mix ecto.migrate && mix kiln.boot_checks' .planning/PROJECT.md` exits 0 OR the line appears split across wrapping with both **`KILN_DB_ROLE=kiln_owner`** and **`mix kiln.boot_checks`** on the same bullet or table cell
    - `grep -q 'just planning-gates' .planning/PROJECT.md` exits 0 AND `grep -q 'just shift-left' .planning/PROJECT.md` exits 0 AND `grep -q 'mix precommit' .planning/PROJECT.md` exits 0 AND `grep -q 'docs.verify' .planning/PROJECT.md` exits 0 — optional-local bullets (**D-2203**)
    - `grep -q '12-local-docker-dx/12-01-SUMMARY.md' .planning/PROJECT.md` exits 0
    - `grep -q '## Local vs CI' .planning/PROJECT.md` exits 0 OR `grep -qi 'Local vs CI' .planning/PROJECT.md` exits 0
  </acceptance_criteria>
  <files>.planning/PROJECT.md</files>
  <action>
Insert a new **`## Merge authority`** section in **`.planning/PROJECT.md`** (implementer picks placement; recommended: after **Current State** / v0.4.0 intro block, before **Out of Scope** or at end of milestone narrative — must not bury inside **Validated** checkbox list).

Content requirements:

1. **Opening paragraph:** States that **green GitHub Actions checks on pull requests targeting `main`** are merge authority (align **D-2201**); branch protection is outside this doc’s control but CI job names below map to what `.github/workflows/ci.yml` runs today.

2. **Tiered table (3–5 rows, semantic tiers — D-2202):**  
   - **Tier A — PR merge gate:** Job named **`mix check`** — include sub-points or footnote that this job runs **`mix compile --warnings-as-errors`**, **`mix check`**, on **Postgres 16** service as defined in the workflow.  
   - **Tier B — Durability / DB invariants:** Same `check` job, step **`Kiln boot checks (CI parity — D-34)`** — command **`KILN_DB_ROLE=kiln_owner mix ecto.migrate && mix kiln.boot_checks`**.  
   - **Tier C — Compose integration smoke:** Job **`integration smoke (first_run.sh)`** — **`bash test/integration/first_run.sh`** after `check`.  
   - **Tier D — Tag-only:** Job **`tag vs mix.exs version`** — runs on **`v*`** tags only; not required for every PR.

3. **Workflow reference footnote or row:** Literal path **`.github/workflows/ci.yml`** so contributors can reconcile UI with YAML.

4. **Subsection “Recommended before push (optional, not merge authority)” (D-2203):** One line each (or tight bullets) for **`just planning-gates`**, **`just shift-left`**, **`script/precommit.sh`** / **`mix precommit`**, **`DOCS=1 mix docs.verify`** — each states it is **not** a merge gate unless added to CI + branch protection.

5. **Subsection “Local vs CI” (D-2204):** Calm tone: CI guarantees listed above; local **`mix check`** may report **PARTIAL** or fail when Postgres, Docker, Dialyzer PLT, or env differ from CI — cite **`.planning/phases/12-local-docker-dx/12-01-SUMMARY.md`** (verification log / Self-Check PARTIAL) as factual evidence.

6. **Cohesion sentence:** Optional **Phase 21** devcontainer path remains **below** canonical host quick start; merge authority stays **Actions** (**D-2205**).
  </action>
  <verify>grep checks in acceptance_criteria</verify>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — README merge callout + Phase 12 pointer</name>
  <read_first>
    - .planning/PROJECT.md
    - README.md
    - .planning/phases/22-merge-authority-operator-docs/22-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -qF '.planning/PROJECT.md#merge-authority' README.md` exits 0
    - `grep -F '| **Tier A' README.md` exits **1** (README must not contain the PROJECT tier-table row prefix — avoids duplicate authoritative table; use `-F` so `|` is not a shell pipe)
    - `grep -q '12-local-docker-dx/12-01-SUMMARY.md' README.md` exits 0
    - `grep -qiE 'GitHub Actions|Actions' README.md` exits 0 in the merge callout region — use `grep -n 'merge' README.md` manually if needed; automated: `grep -q 'github.com/szTheory/kiln/actions' README.md` exits 0 OR `grep -qi 'github actions' README.md` exits 0
    - Merge callout block is **≤ 12 lines** of consecutive non-empty lines: `awk '/merge authority|Merge authority|MERGE AUTHORITY/,/^$/ {print}' README.md | wc -l` ≤ 12 OR place callout in a single short paragraph under **Quick start** / **Operator checklist** as per **D-2204** (if awk fragile, enforce **≤ 8** new lines added in the PR diff for this block — executor documents line count in SUMMARY)
  </acceptance_criteria>
  <files>README.md</files>
  <action>
Add a **compact** README block (target **3–6 lines**, max **8** if linking two paths):

1. State clearly: **merge** requires **green CI on the PR** to **`main`** (badge already links Actions — reuse that idea in prose).

2. Link full policy: **`.planning/PROJECT.md#merge-authority`** (exact fragment **`#merge-authority`**).

3. **One** Phase 12 honesty sentence + link **`.planning/phases/12-local-docker-dx/12-01-SUMMARY.md`** for local **PARTIAL** / env mismatch narrative (**D-2204**).

4. **Do not** duplicate the tier markdown table from PROJECT (**D-2201**).

Placement: near **Quick start** or **Operator checklist** per **22-CONTEXT.md** (not above-the-fold wall of text).
  </action>
  <verify>grep checks in acceptance_criteria</verify>
</task>

</tasks>

<verification>
1. Run all `<acceptance_criteria>` grep commands from Task 1 and Task 2; each exits with the code specified.
2. Spot-read `.github/workflows/ci.yml` if any job `name:` was renamed during execution — if drift, update PROJECT table to match YAML **before** claiming complete.
3. `mix format` only if Elixir/heex files were touched (not expected for this plan).
</verification>

<success_criteria>
- [ ] `## Merge authority` exists in `.planning/PROJECT.md` with tier table + optional locals + Local vs CI + `12-01-SUMMARY.md` citation
- [ ] README contains `#merge-authority` link and **no** Tier A pipe-table row
- [ ] DOCS-08 traceability satisfied (same policy README ↔ PROJECT)
</success_criteria>
