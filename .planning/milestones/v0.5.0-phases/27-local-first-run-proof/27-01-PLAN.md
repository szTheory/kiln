---
phase: 27-local-first-run-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/kiln.first_run.prove.ex
  - test/mix/tasks/kiln.first_run.prove_test.exs
  - test/kiln_web/live/settings_live_test.exs
  - test/kiln_web/live/templates_live_test.exs
  - test/kiln_web/live/run_detail_live_test.exs
  - .planning/phases/27-local-first-run-proof/27-VERIFICATION.md
autonomous: true
requirements:
  - UAT-04
must_haves:
  truths:
    - "One explicit `mix` command proves the setup-ready local first-run path end to end."
    - "That proof runs the real local-topology smoke before the focused operator-path UI proof."
    - "The operator-visible story remains setup-ready `/settings` -> recommended `hello-kiln` template -> `Start run` -> `/runs/:id`."
    - "Phase verification cites the single top-level proof command and transparently lists its delegated subcommands."
  artifacts:
    - path: lib/mix/tasks/kiln.first_run.prove.ex
      provides: "Dedicated proof-owning Mix command for Phase 27"
    - path: test/mix/tasks/kiln.first_run.prove_test.exs
      provides: "Delegation-order proof that only the two locked layers run"
    - path: test/kiln_web/live/settings_live_test.exs
      provides: "Readiness-SSOT setup-ready return-path proof coverage"
    - path: test/kiln_web/live/templates_live_test.exs
      provides: "Operator-path proof coverage that lands on run detail"
    - path: .planning/phases/27-local-first-run-proof/27-VERIFICATION.md
      provides: "Exact verification command citation and transparent subcommand listing"
  key_links:
    - from: lib/mix/tasks/kiln.first_run.prove.ex
      to: mix integration.first_run
      via: "first delegated proof layer per D-2703"
      pattern: "Mix\\.Task\\.run\\(\"integration\\.first_run\""
    - from: lib/mix/tasks/kiln.first_run.prove.ex
      to: mix test
      via: "second delegated proof layer with the focused LiveView files per D-2703"
      pattern: "Mix\\.Task\\.run\\(\"test\""
    - from: test/kiln_web/live/settings_live_test.exs
      to: test/kiln_web/live/templates_live_test.exs
      via: "setup-ready return-context story anchored on `/settings` and resumed on `hello-kiln`"
      pattern: "settings-return-to-template|templates-start-run-form|run-detail"
    - from: .planning/phases/27-local-first-run-proof/27-VERIFICATION.md
      to: lib/mix/tasks/kiln.first_run.prove.ex
      via: "verification cites only the top-level command while enumerating delegated layers"
      pattern: "mix kiln\\.first_run\\.prove|mix integration\\.first_run"
---

<objective>
Add one honest, repeatable proof command for the local first-run path and leave Phase 27 with a verification artifact that cites it exactly.

Purpose: satisfy `UAT-04` without broadening the milestone into shift-left or browser E2E, while honoring the locked two-layer proof architecture from `27-CONTEXT.md`.
Output: a dedicated proof-owning Mix task, supporting tests that pin delegation and the setup-ready route story, and `27-VERIFICATION.md` citing the exact command.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-local-first-run-proof/27-CONTEXT.md
@.planning/phases/27-local-first-run-proof/27-RESEARCH.md
@.planning/phases/24-template-run-uat-smoke/24-CONTEXT.md
@.planning/phases/26-first-live-template-run/26-CONTEXT.md
@.planning/phases/26-first-live-template-run/26-01-PLAN.md
@mix.exs
@lib/mix/tasks/shift_left/verify.ex
@test/integration/first_run.sh
@script/shift_left_verify.sh
@test/kiln_web/live/settings_live_test.exs
@test/kiln_web/live/templates_live_test.exs
@test/kiln_web/live/run_detail_live_test.exs

<interfaces>
From mix.exs:
```elixir
"integration.first_run": &integration_first_run/1
```

From lib/mix/tasks/shift_left/verify.ex:
```elixir
def run(_) do
  script = Path.expand("script/shift_left_verify.sh", File.cwd!())
  case System.cmd("bash", [script], cd: File.cwd!(), stderr_to_stdout: true) do
    {out, 0} -> ...
    {out, code} -> Mix.raise("shift_left.verify failed (exit #{code})")
  end
end
```

From test/kiln_web/live/settings_live_test.exs:
```elixir
{:ok, view, _html} =
  live(conn, "/settings?return_to=%2Ftemplates%2Fhello-kiln&template_id=hello-kiln#settings-item-anthropic")

assert has_element?(view, "#settings-return-context")
assert has_element?(view, "#settings-return-to-template[href=\"/templates/hello-kiln\"]")
```

From test/kiln_web/live/templates_live_test.exs:
```elixir
result =
  view
  |> form("#templates-start-run-form")
  |> render_submit()

{:ok, run_view, _html} = follow_redirect(result, conn)
assert has_element?(run_view, "#run-detail")
```

From test/kiln_web/live/run_detail_live_test.exs:
```elixir
assert has_element?(view, "#run-detail")
assert has_element?(view, "#run-detail-overview")
assert has_element?(view, "#run-detail-current-state")
assert has_element?(view, "#run-detail-next-action")
```
</interfaces>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | One explicit, repeatable proof that the local operator journey works end to end | Task 1, Task 2, Task 3 |
| REQ | `UAT-04` automated proof path plus exact command citation | Task 1, Task 3 |
| CONTEXT | D-2701/D-2702 dedicated thin Mix command | Task 1 |
| CONTEXT | D-2703 exact two-layer ordered delegation | Task 1, Task 2 |
| CONTEXT | D-2704/D-2705 do not broaden to `shift_left.verify` or shell-only ownership | Task 1, Task 3 |
| CONTEXT | D-2706/D-2707 real local topology via existing `integration.first_run` SSOT | Task 1, Task 3 |
| CONTEXT | D-2708/D-2710 no LiveView-only proof and no broader external/browser dependency | Task 1, Task 3 |
| CONTEXT | D-2711/D-2713 setup-ready `/settings` -> `/templates` -> `/runs/:id` story must be explicit | Task 2 |
| CONTEXT | D-2714 keep scope to one coherent happy path | Task 2 |
| CONTEXT | D-2715/D-2718 stable ids/routes for UI proof, shell only at integration boundary | Task 2 |
| CONTEXT | D-2719 exact top-level command citation with transparent subcommand list | Task 3 |
| CONTEXT | D-2720/D-2722 least surprise, no hidden suites, ergonomic but honest command | Task 1, Task 3 |
| RESEARCH | Existing proof layers already exist; missing piece is ownership plus one small story seam if needed | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the proof-owning Mix command and pin its delegation contract</name>
  <files>lib/mix/tasks/kiln.first_run.prove.ex, test/mix/tasks/kiln.first_run.prove_test.exs</files>
  <read_first>.planning/phases/27-local-first-run-proof/27-CONTEXT.md, .planning/phases/27-local-first-run-proof/27-RESEARCH.md, mix.exs, lib/mix/tasks/shift_left/verify.ex, test/integration/first_run.sh</read_first>
  <action>Create a dedicated Mix task named `mix kiln.first_run.prove` per D-2701. Keep it wrapper-only per D-2702: do not shell out to new scripts, do not duplicate bootstrap logic from `first_run.sh`, and do not route through `shift_left.verify`. The task must delegate exactly two layers in locked order per D-2703: first `Mix.Task.run("integration.first_run")`, then `Mix.Task.run("test", ["test/kiln_web/live/templates_live_test.exs", "test/kiln_web/live/run_detail_live_test.exs"])`. Add a focused task test that proves only those two calls run and that the ordering is preserved. If the test needs stubbing, use a minimal reversible Mix-shell/task interception approach local to the test rather than changing production architecture.</action>
  <verify>
    <automated>mix test test/mix/tasks/kiln.first_run.prove_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix kiln.first_run.prove` exists as a dedicated Mix task rather than an alias or shell-only instruction.
    - The task delegates exactly two layers in order: `integration.first_run` first, then the focused `mix test` file list, per D-2703.
    - The task adds no third suite, no `mix check`, no Playwright, and no duplicated bootstrap logic.
    - The task-level test fails if delegation order or delegated command list drifts.
  </acceptance_criteria>
  <done>The repository has one memorable proof-owning command whose narrow scope is enforced by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the setup-ready `/settings` -> `hello-kiln` -> `/runs/:id` story explicit in LiveView proof</name>
  <files>test/kiln_web/live/settings_live_test.exs, test/kiln_web/live/templates_live_test.exs, test/kiln_web/live/run_detail_live_test.exs</files>
  <read_first>.planning/phases/27-local-first-run-proof/27-CONTEXT.md, .planning/phases/26-first-live-template-run/26-CONTEXT.md, test/kiln_web/live/settings_live_test.exs, test/kiln_web/live/templates_live_test.exs, test/kiln_web/live/run_detail_live_test.exs</read_first>
  <action>Add one small Phase-27-specific proof seam that makes the setup-ready story from D-2711 explicit without broadening coverage. Keep `/settings` as the readiness SSOT and stable return-context surface, but the required stitched ownership seam MUST live inside the files executed by `mix kiln.first_run.prove`: `test/kiln_web/live/templates_live_test.exs` and/or `test/kiln_web/live/run_detail_live_test.exs`. Express the operator story by asserting a ready return path from `/settings` into `/templates/hello-kiln`, then through `Start run`, then into `/runs/:id` with `#run-detail` proof-of-life. `SettingsLiveTest` may retain or gain supporting coverage for the return-context surface, but it must not carry the owning Phase 27 seam by itself. Preserve the existing Phase 24/26 seams and keep ids/routes as the contract per D-2715. Do not add browser E2E, screenshot assertions, blocked-path ownership, or deep run-domain assertions.</action>
  <verify>
    <automated>mix test test/kiln_web/live/settings_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/run_detail_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The focused UI proof now makes the setup-ready `/settings` -> `/templates/hello-kiln` -> `Start run` -> `/runs/:id` story explicit inside `templates_live_test.exs` and/or `run_detail_live_test.exs`, which are the files executed by `mix kiln.first_run.prove`.
    - Proof remains based on stable route/DOM-id seams, with only minimal text assertions when disambiguation is necessary.
    - `SettingsLiveTest` remains supporting coverage only and is not the sole carrier of the owning Phase 27 proof seam.
    - Existing blocked-path coverage remains supporting behavior, not the main ownership seam for Phase 27.
    - No new browser/E2E harness or broader UI matrix is introduced.
  </acceptance_criteria>
  <done>The operator-visible happy path now reads as one coherent proof story rather than disconnected readiness and start-run tests.</done>
</task>

<task type="auto">
  <name>Task 3: Record the proof honestly in the phase verification artifact</name>
  <files>.planning/phases/27-local-first-run-proof/27-VERIFICATION.md</files>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/27-local-first-run-proof/27-CONTEXT.md, .planning/phases/24-template-run-uat-smoke/24-CONTEXT.md</read_first>
  <action>Write `27-VERIFICATION.md` so it cites only `mix kiln.first_run.prove` as the Phase 27 proof command per D-2719. Under that top-level citation, transparently list the two delegated subcommands and what each layer proves: real local topology via `mix integration.first_run`, then focused operator-path proof via the targeted LiveView files. Keep the claim narrow: this is the setup-ready local first-run proof for `UAT-04`, not the broader merge-authority suite and not `shift_left.verify`. Record any workstation-specific caveats factually if execution surfaces them, but do not change the ownership command.</action>
  <verify>
    <automated>rg -n "mix kiln\\.first_run\\.prove|mix integration\\.first_run|templates_live_test\\.exs|run_detail_live_test\\.exs" .planning/phases/27-local-first-run-proof/27-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `27-VERIFICATION.md` cites `mix kiln.first_run.prove` verbatim as the owning proof command.
    - The artifact transparently lists the two delegated subcommands and what they prove.
    - The artifact does not cite `mix shift_left.verify`, `just shift-left`, or `test/integration/first_run.sh` as the owning Phase 27 command.
    - The verification claim is explicitly scoped to the setup-ready local first-run proof for `UAT-04`.
  </acceptance_criteria>
  <done>Phase 27 ends with one honest verification artifact whose command citation matches the implemented proof surface exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> Mix task | A human-invoked proof command crosses into repository automation and must not hide broader work than it claims |
| Mix task -> shell/bootstrap layer | The proof task invokes real local-machine setup logic with Docker, `.env`, and host Phoenix boot |
| LiveView test harness -> routed UI surfaces | Focused UI proof must validate operator-visible state boundaries without relying on brittle copy or hidden internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | T | `mix kiln.first_run.prove` scope | mitigate | Lock the delegated commands and order in `test/mix/tasks/kiln.first_run.prove_test.exs` so the command cannot silently grow beyond D-2703 |
| T-27-02 | R | `27-VERIFICATION.md` proof claim | mitigate | Cite only the top-level Mix command and enumerate subcommands underneath so the artifact stays auditable and non-misleading |
| T-27-03 | I | LiveView proof seam | accept | UI tests assert only stable ids/routes and avoid secrets or deep domain-state inspection; no sensitive values are introduced in this phase |
| T-27-04 | D | local integration smoke dependency | accept | Reuse the existing `integration.first_run` SSOT rather than cloning bootstrap logic; if Docker/port/env issues fail, the proof stops loudly with the existing operator-facing diagnostics |
</threat_model>

<verification>
- `mix test test/mix/tasks/kiln.first_run.prove_test.exs`
- `mix test test/kiln_web/live/settings_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/run_detail_live_test.exs`
- `mix kiln.first_run.prove`
- `rg -n "mix kiln\\.first_run\\.prove|mix integration\\.first_run|templates_live_test\\.exs|run_detail_live_test\\.exs" .planning/phases/27-local-first-run-proof/27-VERIFICATION.md`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- The repository exposes one dedicated `mix kiln.first_run.prove` command for the local first-run proof.
- That command delegates exactly the two locked proof layers in the required order and no broader suite.
- The focused Phoenix proof makes the setup-ready `/settings` -> `hello-kiln` -> `/runs/:id` story explicit.
- `27-VERIFICATION.md` cites only the top-level command and transparently lists its delegated subcommands.
</success_criteria>

<output>
After completion, create `.planning/phases/27-local-first-run-proof/27-01-SUMMARY.md`
</output>
