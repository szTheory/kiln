---
phase: 26-first-live-template-run
plan: "02"
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - lib/kiln/operator_setup.ex
  - lib/kiln/runs.ex
  - lib/kiln/runs/run_director.ex
  - lib/kiln_web/live/templates_live.ex
  - lib/kiln_web/live/settings_live.ex
  - test/kiln/runs/run_director_readiness_test.exs
  - test/kiln/specs/template_instantiate_test.exs
  - test/kiln_web/live/templates_live_test.exs
  - test/kiln_web/live/settings_live_test.exs
autonomous: true
requirements:
  - LIVE-02
  - LIVE-03
must_haves:
  truths:
    - "Starting a live run exercises a real backend preflight instead of relying only on disabled-button UI logic."
    - "A blocked start routes the operator to the first missing `/settings#settings-item-*` anchor in checklist order."
    - "Blocked recovery preserves enough template context for the operator to return to the same first-run path after fixing readiness."
  artifacts:
    - path: lib/kiln/operator_setup.ex
      provides: "Deterministic helper(s) for first blocker selection and settings-target routing"
    - path: lib/kiln/runs.ex
      provides: "Backend template-start seam that can return success or a typed blocked outcome"
    - path: lib/kiln/runs/run_director.ex
      provides: "Reused backend readiness gate, still authoritative for live start"
    - path: lib/kiln_web/live/templates_live.ex
      provides: "UI start-run choreography that consumes the backend preflight result and routes recovery"
    - path: lib/kiln_web/live/settings_live.ex
      provides: "Return-context affordance back to the selected template path"
    - path: test/kiln/runs/run_director_readiness_test.exs
      provides: "Backend proof that unreadiness still blocks live start deterministically"
    - path: test/kiln/specs/template_instantiate_test.exs
      provides: "Proof for the new template-start seam and queued/run-start behavior"
    - path: test/kiln_web/live/templates_live_test.exs
      provides: "Blocked recovery and successful live-start coverage from the operator UI"
  key_links:
    - from: lib/kiln/operator_setup.ex
      to: lib/kiln_web/live/templates_live.ex
      via: "First blocker `href` and settings return target"
      pattern: "settings-item-|blocker"
    - from: lib/kiln/runs.ex
      to: lib/kiln/runs/run_director.ex
      via: "Queued-run creation flowing through the authoritative start gate"
      pattern: "create_for_promoted_template|start_run"
    - from: lib/kiln_web/live/templates_live.ex
      to: lib/kiln_web/live/settings_live.ex
      via: "Blocked start routing with template return context"
      pattern: "return_to|template_id|settings-item-"
    - from: test/kiln_web/live/templates_live_test.exs
      to: lib/kiln_web/live/templates_live.ex
      via: "Live preflight and recovery assertions by id and navigation destination"
      pattern: "templates-start-run-form|templates-live-hero|settings-item-"
---

<objective>
Turn `Start run` into a real backend-gated preflight with deterministic blocked recovery.

Purpose: satisfy `LIVE-02` and the launch half of `LIVE-03` by routing the operator-facing template flow through the existing authoritative readiness gate and sending blocked starts back to the exact settings step that must be fixed.
Output: backend template-start API, blocker helper(s), settings return-context support, and focused domain/LiveView proof.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-first-live-template-run/26-CONTEXT.md
@.planning/phases/26-first-live-template-run/26-RESEARCH.md
@.planning/phases/25-local-live-readiness-ssot/25-CONTEXT.md
@lib/kiln/operator_setup.ex
@lib/kiln/runs.ex
@lib/kiln/runs/run_director.ex
@lib/kiln_web/live/templates_live.ex
@lib/kiln_web/live/settings_live.ex
@test/kiln/runs/run_director_readiness_test.exs
@test/kiln/specs/template_instantiate_test.exs
@test/kiln_web/live/templates_live_test.exs
@test/kiln_web/live/settings_live_test.exs
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| ROADMAP | Starting a live run performs a readiness preflight and routes the operator back to the missing prerequisite | Task 1, Task 2, Task 3 |
| REQUIREMENTS | `LIVE-02` | Task 1, Task 2, Task 3 |
| CONTEXT | D-2610 to D-2615 (backend-authoritative gate, first missing settings anchor, `/settings` remediation SSOT, preserved return context) | Task 1, Task 2, Task 3 |
| RESEARCH | Current template flow bypasses `RunDirector.start_run/1` | Task 1 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a backend template-start seam that returns typed preflight results</name>
  <files>lib/kiln/operator_setup.ex, lib/kiln/runs.ex, lib/kiln/runs/run_director.ex, test/kiln/runs/run_director_readiness_test.exs, test/kiln/specs/template_instantiate_test.exs</files>
  <read_first>.planning/phases/26-first-live-template-run/26-CONTEXT.md, lib/kiln/operator_setup.ex, lib/kiln/runs.ex, lib/kiln/runs/run_director.ex</read_first>
  <action>Create a domain-level API for the template start path instead of letting `TemplatesLive` stitch together queued-run creation and navigation itself. The new seam must be able to return either a successful started run or a typed blocked outcome that includes the first missing readiness target. Use `OperatorSetup` to expose deterministic first-blocker selection from the existing checklist order and reuse `RunDirector.start_run/1` as the final readiness gate rather than duplicating readiness rules. Design the return shape so the UI can distinguish unreadiness from generic changeset/workflow failures. Avoid a design that treats queued-run insertion as launch success when readiness is still missing.</action>
  <verify>mix test test/kiln/runs/run_director_readiness_test.exs test/kiln/specs/template_instantiate_test.exs</verify>
  <acceptance_criteria>
    - A domain function exists for “start live run from promoted template” and returns a typed blocked result for missing readiness.
    - The first blocker comes from the existing checklist order and carries a `/settings#settings-item-*` anchor.
    - Backend tests cover both unreadiness and successful start behavior through the new seam.
  </acceptance_criteria>
  <done>The backend now owns template preflight semantics, and unreadiness is a typed, deterministic outcome rather than a UI-only guess.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the backend preflight result from `TemplatesLive` and preserve return context</name>
  <files>lib/kiln_web/live/templates_live.ex, lib/kiln_web/live/settings_live.ex, test/kiln_web/live/templates_live_test.exs, test/kiln_web/live/settings_live_test.exs</files>
  <read_first>lib/kiln_web/live/templates_live.ex, lib/kiln_web/live/settings_live.ex, .planning/phases/25-local-live-readiness-ssot/25-CONTEXT.md</read_first>
  <action>Update the `Start run` event flow so `TemplatesLive` calls the new backend start seam and handles three cases distinctly: success, blocked unreadiness, and ordinary failure. On blocked unreadiness, navigate directly to the first missing settings anchor and include template return context in a durable, route-based form so the operator can fix readiness and come back to the same first-run path without re-orienting. Keep `/settings` as the remediation SSOT; do not recreate a full checklist on `/templates`. Strengthen the settings UI only as needed to make the return affordance obvious and testable.</action>
  <verify>mix test test/kiln_web/live/templates_live_test.exs test/kiln_web/live/settings_live_test.exs</verify>
  <acceptance_criteria>
    - `TemplatesLive` no longer treats queued-run creation alone as a successful live launch.
    - A blocked start navigates to the first missing settings anchor and preserves the chosen template as return context.
    - `/settings` exposes a stable affordance or copy seam that points back to the selected template path after remediation.
  </acceptance_criteria>
  <done>The operator can attempt a real start, get routed to the exact blocker when unreadiness remains, and return to the same template path afterward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retire disabled-only unreadiness as the owning behavior and tighten UI proof</name>
  <files>lib/kiln_web/live/templates_live.ex, test/kiln_web/live/templates_live_test.exs</files>
  <read_first>lib/kiln_web/live/templates_live.ex, test/kiln_web/live/templates_live_test.exs</read_first>
  <action>Adjust the start-run controls and disconnected-state copy so the operator still gets calm guidance, but the owning contract is the backend preflight path rather than the disabled button. Keep explanatory UI for unreadiness, but do not let that be the only behavior that exists in live mode. Update tests to prove both blocked recovery and successful launch through the exact `Start run` seam the user clicks.</action>
  <verify>mix test test/kiln_web/live/templates_live_test.exs</verify>
  <acceptance_criteria>
    - Live-mode guidance remains present without making disabled CTA logic the only unreadiness contract.
    - UI tests prove both blocked and successful start flows from the same `#templates-start-run-form`.
    - No new duplicate readiness checklist surface is created on `/templates`.
  </acceptance_criteria>
  <done>The operator-visible launch flow is honest, calm, and anchored on the real backend preflight path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/kiln/runs/run_director_readiness_test.exs test/kiln/specs/template_instantiate_test.exs`
- [ ] `mix test test/kiln_web/live/templates_live_test.exs test/kiln_web/live/settings_live_test.exs`
- [ ] Blocked starts route to the first missing `/settings#settings-item-*` anchor with template return context intact
</verification>

<success_criteria>
- `Start run` now uses a real backend preflight.
- Blocked recovery lands on the exact missing settings item and preserves the selected template path.
- Tests cover both the domain contract and the operator-visible recovery flow.
</success_criteria>

<output>
After completion, create `.planning/phases/26-first-live-template-run/26-02-SUMMARY.md`
</output>
