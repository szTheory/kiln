---
phase: 28-first-run-proof-runtime-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260424000000_grant_oban_runtime_privileges.exs
  - test/kiln/repo/migrations/oban_runtime_privileges_test.exs
  - test/integration/first_run.sh
  - .planning/phases/28-first-run-proof-runtime-closure/28-VERIFICATION.md
  - .planning/phases/27-local-first-run-proof/27-VERIFICATION.md
  - .planning/phases/27-local-first-run-proof/27-01-SUMMARY.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - UAT-04
must_haves:
  truths:
    - "`mix kiln.first_run.prove` reaches the delegated `/health` confirmation instead of dying inside `mix integration.first_run`."
    - "The first-run proof explicitly exercises the restricted runtime role rather than passing only because role switching is inactive."
    - "Phase 28 owns the final `UAT-04` closure artifact, and the historical Phase 27 claim is reconciled rather than left contradictory."
  artifacts:
    - path: priv/repo/migrations/20260424000000_grant_oban_runtime_privileges.exs
      provides: "Forward-only Oban privilege repair for already-migrated repositories"
    - path: test/kiln/repo/migrations/oban_runtime_privileges_test.exs
      provides: "Role-scoped regression proving `kiln_app` can access the Oban relations needed during app boot"
    - path: test/integration/first_run.sh
      provides: "Explicit proof-path runtime-role activation for the delegated integration layer"
    - path: .planning/phases/28-first-run-proof-runtime-closure/28-VERIFICATION.md
      provides: "Phase 28 closure artifact with the exact proof command and rerun-backed result"
    - path: .planning/phases/27-local-first-run-proof/27-VERIFICATION.md
      provides: "Historical Phase 27 artifact corrected to reflect superseded closure"
    - path: .planning/phases/27-local-first-run-proof/27-01-SUMMARY.md
      provides: "Phase 27 summary reconciled with Phase 28 runtime-proof closure"
    - path: .planning/ROADMAP.md
      provides: "Milestone-level completion state for Phase 28 and final proof status"
    - path: .planning/REQUIREMENTS.md
      provides: "Requirement SSOT marking `UAT-04` complete only after the rerun passes"
  key_links:
    - from: priv/repo/migrations/20260424000000_grant_oban_runtime_privileges.exs
      to: priv/repo/migrations/20260418000005_install_oban.exs
      via: "post-install ownership/grant repair for Oban relations created before the runtime-role policy was applied"
      pattern: "oban_jobs|GRANT|ALTER TABLE"
    - from: test/kiln/repo/migrations/oban_runtime_privileges_test.exs
      to: priv/repo/migrations/20260424000000_grant_oban_runtime_privileges.exs
      via: "regression under `SET ROLE kiln_app` on the exact privilege seam that broke the audit run"
      pattern: "SET ROLE kiln_app|oban_jobs"
    - from: test/integration/first_run.sh
      to: config/runtime.exs
      via: "proof path explicitly boots the app under the intended runtime role"
      pattern: "KILN_DB_ROLE=kiln_app|parameters: \\[role: role\\]"
    - from: .planning/phases/28-first-run-proof-runtime-closure/28-VERIFICATION.md
      to: lib/mix/tasks/kiln.first_run.prove.ex
      via: "Phase 28 cites the exact top-level proof command it reran"
      pattern: "mix kiln\\.first_run\\.prove"
    - from: .planning/ROADMAP.md
      to: .planning/REQUIREMENTS.md
      via: "Phase 28 and `UAT-04` closure remain consistent after the rerun"
      pattern: "UAT-04|Phase 28"
---

<objective>
Close the repository-level first-run proof gap by fixing the Oban/runtime DB privilege contract, making the delegated integration proof actually boot under the restricted runtime role, rerunning the owning proof command end to end, and reconciling the contradicted planning artifacts.

Purpose: satisfy `UAT-04` on current repository evidence without broadening into new browser harnesses, unrelated feature work, or generic documentation cleanup.
Output: a durable Oban privilege repair, a role-scoped regression plus proof-path activation that would have caught the original failure, and a Phase 28 verification artifact that closes the requirement while bringing Phase 27 and milestone SSOT back into agreement.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v0.5.0-MILESTONE-AUDIT.md
@.planning/phases/28-first-run-proof-runtime-closure/28-RESEARCH.md
@.planning/phases/27-local-first-run-proof/27-01-PLAN.md
@.planning/phases/27-local-first-run-proof/27-01-SUMMARY.md
@.planning/phases/27-local-first-run-proof/27-VALIDATION.md
@.planning/phases/27-local-first-run-proof/27-VERIFICATION.md
@lib/mix/tasks/kiln.first_run.prove.ex
@mix.exs
@test/integration/first_run.sh
@config/runtime.exs
@config/dev.exs
@config/config.exs
@lib/kiln/application.ex
@lib/kiln_web/plugs/health.ex
@priv/repo/migrations/20260418000002_create_roles.exs
@priv/repo/migrations/20260418000005_install_oban.exs

<notes>
- The v0.5.0 milestone audit recorded `mix kiln.first_run.prove` failing in the delegated integration layer before `/health`, with `permission denied for table oban_jobs` as the break. Phase 28 exists specifically to close that contradiction.
- Current local reruns can pass because `KILN_DB_ROLE` is unset by default, which means the proof may not be exercising `kiln_app` at all. Phase 28 must close that ambiguity rather than treating the current pass as sufficient.
- `UAT-04` now belongs to Phase 28, so the final command citation should live in `28-VERIFICATION.md`. Phase 27 artifacts should become historical context, not the sole closure claim.
</notes>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Fix the repository-level first-run proof so it completes honestly from repo root | Task 1, Task 2 |
| REQ | `UAT-04` explicit automated proof path with exact command citation in the owning phase verification artifact | Task 2 |
| ROADMAP | `mix kiln.first_run.prove` completes and reaches `/health` inside the delegated integration layer | Task 1, Task 2 |
| ROADMAP | Runtime/app-start contract no longer fails on `oban_jobs` permissions during the proof path | Task 1 |
| ROADMAP | `27-VERIFICATION.md`, `ROADMAP.md`, and `REQUIREMENTS.md` agree after the rerun | Task 2 |
| AUDIT | Proof failed before `/health` because runtime DB role could not access `oban_jobs` | Task 1 |
| AUDIT | `UAT-04` is contradicted, not merely undocumented | Task 2 |
| RESEARCH | Current green runs may bypass `kiln_app` because `KILN_DB_ROLE` is unset by default | Task 1 |
| RESEARCH | Oban tables exist without repo-owned runtime grants for `kiln_app` | Task 1 |
| CONSTRAINT | Keep scope to runtime-proof closure; no new browser harnesses or generic docs cleanup | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the Oban privilege seam and make the delegated proof exercise the runtime role</name>
  <files>priv/repo/migrations/20260424000000_grant_oban_runtime_privileges.exs, test/kiln/repo/migrations/oban_runtime_privileges_test.exs, test/integration/first_run.sh</files>
  <read_first>priv/repo/migrations/20260418000002_create_roles.exs, priv/repo/migrations/20260418000005_install_oban.exs, priv/repo/migrations/20260418000006_create_external_operations.exs, priv/repo/migrations/20260419000002_create_runs.exs, config/runtime.exs, test/integration/first_run.sh, .planning/phases/28-first-run-proof-runtime-closure/28-RESEARCH.md</read_first>
  <action>Create a new forward-only migration that repairs the actual Oban privilege drift for already-migrated repositories instead of editing the historical install migration. Follow the repo’s existing table-ownership posture: keep `kiln_owner` as owner, then grant the minimum runtime privileges needed for Oban startup and normal queue operation to `kiln_app` on the Oban tables and any required sequences. Add a focused regression under `SET ROLE kiln_app` that proves the startup-critical Oban access path no longer raises `permission denied for table oban_jobs`. In the same task, update `test/integration/first_run.sh` so the app boot portion of the delegated proof explicitly runs under the intended runtime role rather than silently inheriting an unset `KILN_DB_ROLE`; keep setup/migrations on `kiln_owner`, but make the `mix phx.server` proof path exercise the repaired `kiln_app` contract. Do not solve the failure by booting as `kiln_owner`, weakening the two-role model, or bypassing Oban startup.</action>
  <verify>
    <automated>mix test test/kiln/repo/migrations/oban_runtime_privileges_test.exs && mix integration.first_run</automated>
  </verify>
  <acceptance_criteria>
    - Existing repositories receive the fix through a new migration rather than a rewritten historical migration.
    - The delegated integration proof explicitly boots under the restricted runtime role.
    - The restricted runtime role regains the minimum Oban access required for app boot and queue operation.
    - The regression fails if `kiln_app` loses `oban_jobs` access again.
    - The fix stays scoped to the first-run runtime-proof seam and does not broaden into generic DB-role cleanup.
  </acceptance_criteria>
  <done>The repository has a durable Oban privilege repair and the first-run proof now actually exercises the runtime-role path that failed in the audit.</done>
</task>

<task type="auto">
  <name>Task 2: Rerun the owning proof command and reconcile the contradicted artifacts into Phase 28 closure</name>
  <files>.planning/phases/28-first-run-proof-runtime-closure/28-VERIFICATION.md, .planning/phases/27-local-first-run-proof/27-VERIFICATION.md, .planning/phases/27-local-first-run-proof/27-01-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/milestones/v0.5.0-MILESTONE-AUDIT.md, .planning/phases/27-local-first-run-proof/27-VERIFICATION.md, .planning/phases/27-local-first-run-proof/27-01-SUMMARY.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, lib/mix/tasks/kiln.first_run.prove.ex, .planning/phases/28-first-run-proof-runtime-closure/28-RESEARCH.md</read_first>
  <action>After the runtime fix lands, rerun `mix kiln.first_run.prove` from repo root and use that fresh run as the sole closure authority for `UAT-04`. Create `28-VERIFICATION.md` with explicit status/frontmatter and the exact top-level proof command citation, recording that the delegated integration layer now reaches `/health` under the intended runtime role before the focused LiveView layer runs. Narrowly correct `27-VERIFICATION.md` and `27-01-SUMMARY.md` so they no longer present a contradicted standalone completion claim; `27-VERIFICATION.md` must gain YAML frontmatter with an explicit non-owning status such as `superseded` or `historical`, and both Phase 27 artifacts should state that the proof command landed in Phase 27 while requirement closure completed in Phase 28 after the runtime fix and rerun. Then update `ROADMAP.md` and `REQUIREMENTS.md` so Phase 28 and `UAT-04` reflect the post-rerun truth. Keep the SSOT work tight: no milestone-history rewrite, no new proof command, and no broad planning cleanup outside the contradiction named by the audit.</action>
  <verify>
    <automated>mix kiln.first_run.prove && bash script/precommit.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mix kiln.first_run.prove` completes successfully from repo root and the delegated integration layer reaches `/health`.
    - `28-VERIFICATION.md` cites `mix kiln.first_run.prove` verbatim and records the rerun-backed final status.
    - `27-VERIFICATION.md` gains explicit YAML frontmatter with a non-owning status and no longer reads as an unscoped completion claim.
    - `27-01-SUMMARY.md` no longer inaccurately claims standalone `UAT-04` closure without Phase 28 context.
    - `ROADMAP.md` and `REQUIREMENTS.md` agree on the final post-rerun `UAT-04` status.
    - Final repo verification includes `bash script/precommit.sh` per project instructions.
  </acceptance_criteria>
  <done>The proof command is green on current evidence, Phase 28 carries the final verification artifact, and the audit contradiction is removed from the planning SSOT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| delegated integration proof -> Postgres runtime role | The proof only closes the audit gap if app boot actually runs as the restricted runtime role. |
| migration ownership -> existing local repositories | The fix must repair already-migrated databases without destructive resets or historical migration edits. |
| verification artifacts -> milestone SSOT | Closing `UAT-04` requires fresh proof plus aligned artifacts, not inherited summary prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | D | Oban startup / app boot | mitigate | Add a forward-only migration granting `kiln_app` the Oban table and sequence privileges it actually needs so boot no longer dies before `/health`. |
| T-28-02 | T | Runtime-role contract | mitigate | Update the delegated proof path so it explicitly boots under `kiln_app`; do not “fix” the failure by running the app as owner. |
| T-28-03 | R | Phase verification / requirement closure | mitigate | Use a fresh `mix kiln.first_run.prove` rerun as closure authority, then update `28-VERIFICATION.md`, `27-VERIFICATION.md`, `27-01-SUMMARY.md`, `ROADMAP.md`, and `REQUIREMENTS.md` from that result. |
| T-28-04 | I | Scope control | accept | No secrets are introduced; residual risk is only status drift, addressed by narrow SSOT reconciliation rather than broader docs cleanup. |

</threat_model>

<verification>
- `mix test test/kiln/repo/migrations/oban_runtime_privileges_test.exs`
- `mix integration.first_run`
- `mix kiln.first_run.prove`
- `rg -n "status:|superseded|historical|mix kiln\\.first_run\\.prove|/health|UAT-04|Phase 28" .planning/phases/28-first-run-proof-runtime-closure/28-VERIFICATION.md .planning/phases/27-local-first-run-proof/27-VERIFICATION.md .planning/phases/27-local-first-run-proof/27-01-SUMMARY.md .planning/ROADMAP.md .planning/REQUIREMENTS.md`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- The repository-level first-run proof no longer fails on `oban_jobs` permissions during runtime boot.
- The delegated integration layer explicitly exercises the restricted runtime role and still reaches `/health`.
- `mix kiln.first_run.prove` succeeds end to end from the repository root.
- `28-VERIFICATION.md`, `27-VERIFICATION.md`, `27-01-SUMMARY.md`, `ROADMAP.md`, and `REQUIREMENTS.md` all reflect the same final truth about `UAT-04`.
- The closure stays scoped to runtime-proof repair and SSOT reconciliation, with no new harnesses or unrelated feature work.
</success_criteria>
