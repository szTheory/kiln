---
phase: 999.1-docs-landing-site
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - site/src/pages/index.astro
  - site/src/styles/custom.css
  - site/astro.config.mjs
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "Landing lives at `/` (built path respects `base` so URLs resolve under `/kiln/`)"
    - "Primary CTA links into `/kiln/docs/` (or relative `/docs/` that resolves with base)"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Marketing landing"
      contains: "Kiln"
    - path: "site/src/styles/custom.css"
      provides: "Brand CSS variables"
      contains: "#121212"
---

<objective>
Ship the **operator landing page** (DOCS-01): brand-locked layout per **`prompts/kiln-brand-book.md`** (Coal `#121212`, Char `#1B1D21`, Bone `#F5EFE6`, Ember `#E07A3F`, Inter + IBM Plex Mono), restrained copy, and a **primary CTA** into the Starlight docs happy path (`/docs/`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
@prompts/kiln-brand-book.md
@site/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand CSS variables</name>
  <read_first>
    - prompts/kiln-brand-book.md
    - site/astro.config.mjs
  </read_first>
  <acceptance_criteria>
    - `test -f site/src/styles/custom.css` exits 0
    - `grep -E '#121212|#1B1D21|#E07A3F' site/src/styles/custom.css` matches at least two lines
    - `grep -qi "inter\|plex" site/src/styles/custom.css` OR landing imports fonts with those names in `index.astro`
  </acceptance_criteria>
  <files>site/src/styles/custom.css</files>
  <action>
Create **`site/src/styles/custom.css`** defining CSS variables: **`--color-coal: #121212`**, **`--color-char: #1B1D21`**, **`--color-ember: #E07A3F`**, **`--color-bone: #F5EFE6`** (plus Iron/Ash/Smoke from brand book). Wire file into Starlight via **`starlight({ customCss: ['./src/styles/custom.css'] })`** in **`site/astro.config.mjs`** (merge with existing `starlight()` options from Plan 01).
  </action>
  <verify>
    <automated>grep -q custom.css site/astro.config.mjs</automated>
  </verify>
  <done>Theme file linked.</done>
</task>

<task type="auto">
  <name>Task 2: Landing page — index.astro</name>
  <read_first>
    - site/src/styles/custom.css
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f site/src/pages/index.astro` exits 0
    - `grep -Ei "first spec|/docs|get started|Start" site/src/pages/index.astro` matches at least one line
    - `grep -qi "Kiln" site/src/pages/index.astro` matches
  </acceptance_criteria>
  <files>site/src/pages/index.astro</files>
  <action>
Add **`site/src/pages/index.astro`** (root landing, not inside `content/docs`). Use **semantic HTML** (`<main>`, `<h1>`). Copy must follow brand voice: **short sentences, concrete nouns, no hype**. Include **one primary link** to docs using Astro **`import.meta.env.BASE`**-safe URL, e.g. **`<a href={import.meta.env.BASE + 'docs/'}>…</a>`** so it works with **`base: '/kiln'`**. Optional: note where embed for “60–90s demo” will go (placeholder section is fine for v1 scaffold).
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>Landing builds.</done>
</task>

<task type="auto">
  <name>Task 3: Microcopy lint (grep-level)</name>
  <read_first>
    - site/src/pages/index.astro
    - prompts/kiln-brand-book.md
  </read_first>
  <acceptance_criteria>
    - `grep -Ei "AI magic|revolutionary|game.?changer" site/src/pages/index.astro` exits non-zero (no matches) when those patterns are absent
    - `grep -Ei "border|rounded" site/src/pages/index.astro` OR `grep -q "border" site/src/styles/custom.css` matches
  </acceptance_criteria>
  <files>site/src/pages/index.astro</files>
  <action>
Review headings and CTA labels against **microcopy list** in brand book (`Start run`, etc. adapted for marketing: e.g. **“Read the docs”**, **“Run your first spec”**). Remove forbidden tone words listed in CONTEXT.
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>Copy passes brand constraints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML | Landing is static; no user-supplied HTML in v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-02 | I | font CDN | mitigate | Prefer **`@fontsource/inter`** + **`@fontsource/ibm-plex-mono`** npm packages over third-party CDNs; self-host in `site/` |
</threat_model>

<verification>
cd site && pnpm exec astro build && test -d site/dist
</verification>

<success_criteria>
Built site serves a distinct `/` landing and CTA resolves into docs tree under `base`.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-docs-landing-site/999.1-02-SUMMARY.md`
</output>
