---
phase: 999.1-docs-landing-site
plan: "05"
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - mix.exs
  - CONTRIBUTING.md
  - README.md
autonomous: true
requirements:
  - DOCS-06
must_haves:
  truths:
    - "`DOCS=1 mix docs.verify` exists and exits 0 when tools are installed, warns (exit 0) or documents skip when tools missing — must NOT break default `mix check`"
    - "CONTRIBUTING.md documents editing docs without running Phoenix"
  artifacts:
    - path: "mix.exs"
      provides: "docs.verify alias"
      contains: "docs.verify"
---

<objective>
Add **optional local parity** (CONTEXT D-9991-23): Mix alias **`docs.verify`** (gated by **`DOCS=1`**) that shells to **`pnpm`** / **`htmltest`** / **`lychee`** / **`typos`** when binaries exist, **warns** when missing, and **documents** the path-filtered CI as source of truth. Update **CONTRIBUTING.md** and **README.md** with “edit the docs site” instructions and published Pages URL.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
@mix.exs
@CONTRIBUTING.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: mix alias docs.verify</name>
  <read_first>
    - mix.exs
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "docs.verify" mix.exs` matches
    - `grep -n "DOCS" mix.exs` matches
    - `mix help` or `mix run -e` pattern documents usage (executor runs `grep -A3 'docs.verify' mix.exs`)
  </acceptance_criteria>
  <files>mix.exs</files>
  <action>
In **`mix.exs`** **`aliases`**, add **`docs.verify`** as a **`fn args -> ... end`** task runner (or delegate to **`Mix.Task`** module if cleaner). **Behavior:** If **`System.get_env("DOCS") != "1"`**, print **`"[kiln] skipping docs.verify (set DOCS=1 to run)"`** to stderr and **exit 0**. If **`DOCS=1`**, sequentially: **`System.cmd("pnpm", ["install"], cd: "site")`** (non-zero → **`Mix.raise`**), **`System.cmd("pnpm", ["exec", "astro", "build"], cd: "site")`**, then if **`System.find_executable("htmltest")`**, run **`htmltest`** on **`site/dist`** with config **`site/.htmltest.yml`**; if missing, **`Mix.raise("htmltest not on PATH")`**. Same pattern for **`lychee`** with **`site/lychee.toml`** and **`typos`** at repo root — **all three required when `DOCS=1`** after successful build, or document partial skip only if CONTEXT is updated (prefer **strict** when `DOCS=1`).
  </action>
  <verify>
    <automated>mix docs.verify 2>&1 | head -5</automated>
  </verify>
  <done>Alias registered.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md — docs workflow</name>
  <read_first>
    - CONTRIBUTING.md
    - site/README.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "site/" CONTRIBUTING.md` matches OR path `docs`/`site` documented explicitly
    - `grep -n "DOCS=1" CONTRIBUTING.md` matches
    - `grep -n "pnpm" CONTRIBUTING.md` matches
  </acceptance_criteria>
  <files>CONTRIBUTING.md</files>
  <action>
Add section **“Documentation site (Astro)”**: prerequisites (**Node 22 + pnpm**), commands mirroring **`site/README.md`**, note that **CI** is authoritative for **`main`**, and **`mix docs.verify`** optional parity with **`DOCS=1`**.
  </action>
  <verify>
    <automated>grep -q DOCS=1 CONTRIBUTING.md</automated>
  </verify>
  <done>Contributor docs updated.</done>
</task>

<task type="auto">
  <name>Task 3: README — published docs URL</name>
  <read_first>
    - README.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "github.io/kiln" README.md` matches OR full `https://szTheory.github.io/kiln/` string present
  </acceptance_criteria>
  <files>README.md</files>
  <action>
Add single line or short subsection **“Documentation”** linking **`https://szTheory.github.io/kiln/`** (or **`/kiln/docs/`** path as appropriate once live). Mention site is built from **`site/`**.
  </action>
  <verify>
    <automated>grep -q szTheory.github.io README.md</automated>
  </verify>
  <done>README points to Pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine ↔ Mix | `docs.verify` only runs shell commands; no network beyond pnpm registry when invoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-06 | E | command injection | mitigate | Do not interpolate untrusted env into shell; use **`System.cmd/3`** with argv list or explicit `cd site` only |
</threat_model>

<verification>
mix compile --warnings-as-errors && mix docs.verify
</verification>

<success_criteria>
Contributors can discover optional docs verification; README links to GitHub Pages.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-docs-landing-site/999.1-05-SUMMARY.md`
</output>
