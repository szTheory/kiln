---
phase: 999.1-docs-landing-site
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - site/astro.config.mjs
  - site/src/content/docs/onboarding.md
  - site/src/content/docs/workflows.md
  - site/src/content/docs/architecture.md
  - site/src/content/docs/configuration.md
  - site/README.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-05
must_haves:
  truths:
    - "Starlight sidebar lists onboarding, workflows, architecture, configuration entries"
    - "architecture.md contains a fenced mermaid code block and build-time or scripted validation per CONTEXT"
  artifacts:
    - path: "site/src/content/docs/architecture.md"
      provides: "Mermaid sample"
      contains: "mermaid"
---

<objective>
Create the **documentation spine** (DOCS-02–05): stub pages with correct IA and **happy-path-first** structure, **Starlight sidebar** wired in **`astro.config.mjs`**, and **Mermaid** authoring set up so **invalid diagram syntax cannot silently ship** (CONTEXT D-9991-30–32 — build-time or CI validation path must exist and be documented).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
@.planning/REQUIREMENTS.md
@site/astro.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar IA — four guide stubs</name>
  <read_first>
    - site/astro.config.mjs
    - .planning/REQUIREMENTS.md
  </read_first>
  <acceptance_criteria>
    - `test -f site/src/content/docs/onboarding.md` exits 0
    - `test -f site/src/content/docs/workflows.md` exits 0
    - `test -f site/src/content/docs/architecture.md` exits 0
    - `test -f site/src/content/docs/configuration.md` exits 0
    - `grep -n "onboarding" site/astro.config.mjs` OR sidebar autogenerate covers `src/content/docs` with slug order documented in `site/README.md`
  </acceptance_criteria>
  <files>site/src/content/docs/onboarding.md</files>
  <action>
Add four markdown files under **`site/src/content/docs/`** with Starlight frontmatter **`title`** and **`description`**: **`onboarding.md`** (zero-to-first-run outline per DOCS-02), **`workflows.md`** (YAML/spec outline per DOCS-03), **`architecture.md`** (four-layer overview per DOCS-04), **`configuration.md`** (env/config index per DOCS-05). Update **`starlight({ sidebar: [...] })`** with explicit **`{ label, link }`** entries in logical order: Overview (`/docs/`), Onboarding, Workflows, Architecture, Configuration.
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>IA stubs present.</done>
</task>

<task type="auto">
  <name>Task 2: Mermaid — fail-closed pipeline</name>
  <read_first>
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
    - site/src/content/docs/architecture.md
  </read_first>
  <acceptance_criteria>
    - `grep -q mermaid site/src/content/docs/architecture.md` exits 0
    - `grep -Ei "mermaid|mmdc|rehype" site/README.md` documents the validation approach
    - `cd site && pnpm exec astro build` exits 0 after Mermaid integration
  </acceptance_criteria>
  <files>site/src/content/docs/architecture.md</files>
  <action>
In **`architecture.md`**, add a **minimal valid** diagram, e.g. `flowchart LR` with two nodes. Implement ONE of: (A) Starlight **`markdown`** / **`expressiveCode`** / official integration that **parses at build**, or (B) add **`site/scripts/check-mermaid.sh`** invoked from **`package.json`** script **`"verify:mermaid"`** using **`npx @mermaid-js/mermaid-cli@^10.9.0`** to batch-render fenced blocks. Document chosen option in **`site/README.md`**. CONTEXT forbids client-only silent failure — **build or `verify:mermaid` must fail** on broken syntax (demonstrate with intentional typo in a branch is optional; not required here).
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>Mermaid path documented and green.</done>
</task>

<task type="auto">
  <name>Task 3: Docs index cross-links landing</name>
  <read_first>
    - site/src/content/docs/index.md
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -Ei "onboarding|workflows|architecture|configuration" site/src/content/docs/index.md` matches at least 3 lines
  </acceptance_criteria>
  <files>site/src/content/docs/index.md</files>
  <action>
Expand **`index.md`** into a short hub: **what to read next** with Markdown links to **`./onboarding/`**, **`./workflows/`**, etc. (Starlight-relative paths). Reinforce **happy-path**: onboarding first.
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>Hub page complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static Markdown | No executable user content in docs v1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-03 | T | mermaid CLI supply chain | mitigate | Pin **`@mermaid-js/mermaid-cli`** version in script or package.json devDependencies |
</threat_model>

<verification>
cd site && pnpm exec astro build
</verification>

<success_criteria>
All DOCS-02–05 entry pages exist, sidebar navigable, Mermaid governance satisfied per CONTEXT.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-docs-landing-site/999.1-03-SUMMARY.md`
</output>
