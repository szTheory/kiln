# Phase 999.1 — Pattern Map

Analogs in this repo for the docs / CI work:

| Planned area | Analog | Excerpt / pattern |
|--------------|--------|-------------------|
| GitHub Actions structure | `.github/workflows/ci.yml` | Matrix OS, `actions/checkout`, `erlef/setup-beam` — mirror naming and comment style for new path-filtered workflow |
| Path-filtered jobs | (add new) | Use `on.push.paths` / `pull_request.paths` for `site/**`, `pnpm-lock.yaml`, `.github/workflows/docs.yml` |
| Meta-runner philosophy | `mix.exs` aliases | `mix check` stays Elixir-primary; docs verification is separate alias + workflow per CONTEXT |
| Brand source of truth | `prompts/kiln-brand-book.md` | Hex colors + font stacks — map into `site/src/styles/custom.css` or Starlight theme vars |
| Planning artifact shape | `.planning/phases/09-dogfood-release-v0-1-0/09-01-PLAN.md` | Frontmatter + `<tasks>` XML + `<threat_model>` block |

**Note:** No existing Astro tree — first plan creates greenfield `site/`.

## PATTERN MAPPING COMPLETE
