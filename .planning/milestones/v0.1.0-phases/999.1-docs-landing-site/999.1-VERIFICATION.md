---
phase: 999.1
status: passed
verified: 2026-04-22
---

# Phase 999.1 — Verification

## Automated checks run

| Check | Result |
|-------|--------|
| `cd site && pnpm install && pnpm exec astro build` | PASS |
| `cd site && pnpm run verify:mermaid` | PASS |
| `cd site && PATH=<htmltest> ./scripts/htmltest-ci.sh` | PASS |
| `mix compile --warnings-as-errors` | PASS |
| `mix docs.verify` (without `DOCS=1`) | PASS (skip message) |

## Must-haves

- DOCS-07: Astro + Starlight under `site/` with lockfile and green build — **met**.
- DOCS-01: Landing at `/` with brand styling and CTA into docs — **met** (`/kiln/docs/` with `base`).
- DOCS-02–05: Spine pages, sidebar, Mermaid fail-closed path — **met**.
- DOCS-06: Path-filtered CI, optional `DOCS=1 mix docs.verify`, no Node in default `mix check` — **met**.

## Notes

- Full `mix check` in this environment may require `DATABASE_URL` and Postgres for boot-related jobs; that is unchanged by this phase.
- GitHub Pages deploy requires repository Pages settings + `github-pages` environment approval on first use.

## Human verification

None required for static docs scaffolding.
