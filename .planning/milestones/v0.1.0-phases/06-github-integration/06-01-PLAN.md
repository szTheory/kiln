---
phase: 06-github-integration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/git.ex
  - lib/kiln/git/cmd.ex
  - test/kiln/git_test.exs
autonomous: true
requirements:
  - GIT-01
must_haves:
  truths:
    - "`Kiln.Git` exposes testable wrappers over `git` with injectable command runner"
    - "`git ls-remote` precondition + compare-and-swap metadata builders exist for push workers"
    - "stderr/exit classification maps to typed atoms documented in 06-CONTEXT D-G17"
  artifacts:
    - path: "lib/kiln/git.ex"
      provides: "git operations shell boundary"
      contains: "ls_remote"
  key_links:
    - from: "lib/kiln/sandboxes/docker_driver.ex"
      to: "lib/kiln/git.ex"
      via: "System.cmd pattern + telemetry"
      pattern: "fetch_or_record_intent"
---

<objective>
Ship **`Kiln.Git`** — a thin, testable boundary around **`System.cmd("git", argv, opts)`** for **ls-remote**, **commit** (message + trailers per D-G13–D-G15), and **push** argv assembly. No Oban in this plan; workers consume this module in Plan 03.

Purpose: isolate git I/O, enable hermetic unit tests via injected runner, and centralize CAS precondition helpers for GIT-01.
Output: `lib/kiln/git.ex` (+ optional `lib/kiln/git/cmd.ex` behaviour), `test/kiln/git_test.exs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-github-integration/06-CONTEXT.md
@.planning/phases/06-github-integration/06-RESEARCH.md
@lib/kiln/sandboxes/docker_driver.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Git command runner behaviour + default impl</name>
  <read_first>
    - lib/kiln/sandboxes/docker_driver.ex
    - lib/kiln/logging/secret_redactor.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.Git" lib/kiln/git.ex` returns a match
    - `grep -n "@behaviour" lib/kiln/git/cmd.ex 2>/dev/null || grep -n "cmd_runner" lib/kiln/git.ex` matches at least one line (injectable runner documented)
    - `mix test test/kiln/git_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/git.ex, lib/kiln/git/cmd.ex, test/kiln/git_test.exs</files>
  <action>
Introduce **`Kiln.Git.Cmd`** behaviour: `@callback run_git(list(String.t()), keyword()) :: {:ok, String.t()} | {:error, map()}` where map includes `:exit_status`, `:stderr`, `:stdout`.

Default implementation delegates to `System.cmd("git", argv, stderr_to_stdout: false)` and normalizes `{output, exit}`.

**Do not** log raw stderr at `:info` if it may contain remote URLs with embedded credentials — use `Kiln.Logging.SecretRedactor` or truncate per SEC-01.
  </action>
  <verify>
    <automated>mix test test/kiln/git_test.exs --max-failures=1</automated>
  </verify>
  <done>Injectable git runner exists with hermetic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ls_remote + CAS payload builder</name>
  <read_first>
    - .planning/phases/06-github-integration/06-CONTEXT.md
    - lib/kiln/git.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "ls_remote" lib/kiln/git.ex` matches at least one `def` or `defp`
    - `grep -n "expected_remote" lib/kiln/git.ex` matches (CAS field name documented)
    - `mix test test/kiln/git_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/git.ex, test/kiln/git_test.exs</files>
  <action>
Implement **`Kiln.Git.ls_remote_tip/3`** (`remote`, `ref`, runner) parsing first hash from `git ls-remote` output.

Implement **`Kiln.Git.push_intent_payload/2`** returning **exact** map keys: `"expected_remote_sha"`, `"local_commit_sha"`, `"refspec"` (string values) for persistence into `external_operations.intent_payload` (D-G16).

Unit tests: stub runner returns deterministic `ls-remote` lines; assert payload map equality.
  </action>
  <verify>
    <automated>mix test test/kiln/git_test.exs --max-failures=1</automated>
  </verify>
  <done>CAS precondition helpers ready for PushWorker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify push failures → typed atoms</name>
  <read_first>
    - .planning/phases/06-github-integration/06-CONTEXT.md
    - priv/playbooks/v1/gh_permissions_insufficient.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "classify_push" lib/kiln/git.ex` OR `grep -n "non_fast_forward" lib/kiln/git.ex` matches
    - `mix test test/kiln/git_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/git.ex, test/kiln/git_test.exs</files>
  <action>
Pure function **`Kiln.Git.classify_push_failure/2`** taking exit code + stderr substring, returns **one of**: `:git_non_fast_forward`, `:git_remote_advanced`, `:git_push_rejected`, `:ok` (when unknown, map to `:git_push_rejected` with reason string in a structured tuple if preferred — document choice in `@moduledoc`).

Include at least **three** ExUnit cases with representative stderr fragments (no secrets in source strings).
  </action>
  <verify>
    <automated>mix test test/kiln/git_test.exs --max-failures=1</automated>
  </verify>
  <done>Typed classification available for `fail_op` + Transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace → git | git mutates only configured workspace cwd |
| logs → operator | stderr may contain URLs — must be redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | I | stderr logging | mitigate | SecretRedactor / truncation |
| T-06-02 | T | forged ls-remote output | mitigate | tests + caller validates sha format |
| T-06-03 | E | command injection | mitigate | argv as list only; never shell string concat |
</threat_model>

<verification>
mix test test/kiln/git_test.exs
</verification>

<success_criteria>
Hermetic tests cover runner injection, CAS payload, and failure classification without network.
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-integration/06-01-SUMMARY.md`
</output>
