---
phase: 05-spec-verification-bounded-loop
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_create_specs.exs
  - priv/repo/migrations/*_create_spec_revisions.exs
  - priv/repo/migrations/*_create_holdout_scenarios.exs
  - priv/repo/migrations/*_runs_phase5_budget_stuck.exs
  - lib/kiln/specs/spec.ex
  - lib/kiln/specs/spec_revision.ex
  - lib/kiln/specs/holdout_scenario.ex
  - lib/kiln/specs.ex
  - lib/kiln/repo.ex
  - test/kiln/specs/spec_revision_test.exs
autonomous: true
requirements:
  - SPEC-01
  - SPEC-04
  - ORCH-06
  - OBS-04
must_haves:
  truths:
    - "`specs` and `spec_revisions` persist markdown + `scenario_manifest_sha256` per revision"
    - "`holdout_scenarios` exists with FK-safe linkage to specs or revisions as decided in migration"
    - "`runs` carries durable fields for cap counters and stuck sliding-window (jsonb or typed columns) updated only via Transitions"
  artifacts:
    - path: "lib/kiln/specs/spec_revision.ex"
      provides: "revision row with body + manifest hash"
      contains: "scenario_manifest_sha256"
  key_links:
    - from: "lib/kiln/runs/run.ex"
      to: "priv/repo/migrations/*_runs_phase5_budget_stuck.exs"
      via: "new columns referenced from changeset"
      pattern: "field\\(:stuck|caps|governed"
---

<objective>
Establish the **Postgres durability floor** for Phase 5: versioned specs (`specs`, `spec_revisions` with `scenario_manifest_sha256`), `holdout_scenarios` storage, and **run-level** columns for bounded-autonomy counters + stuck-detector window data. Replace the `Kiln.Specs` placeholder module with a real `Kiln.Specs` context API limited to CRUD needed by later plans (no LiveView in this plan).

Purpose: later parser, compiler, and verifier plans commit against stable tables and `Run` fields.
Output: migrations (use `mix ecto.gen.migration`), Ecto schemas, ownership/grants mirroring `runs` / `audit_events` posture, and schema tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
@.planning/phases/05-spec-verification-bounded-loop/05-RESEARCH.md
@priv/repo/migrations/20260419000002_create_runs.exs
@lib/kiln/runs/run.ex
@lib/kiln/application.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `specs` + `spec_revisions` migrations and schemas</name>
  <read_first>
    - priv/repo/migrations/20260419000002_create_runs.exs
    - lib/kiln/runs/run.ex
    - lib/kiln/specs.ex
  </read_first>
  <acceptance_criteria>
    - `grep -R "scenario_manifest_sha256" lib/kiln/specs/` returns at least one match in `spec_revision.ex` or embedded schema
    - Migration file contains `create table(:spec_revisions` and a `text` or `:string` column for markdown body
    - `mix test test/kiln/specs/spec_revision_test.exs` exits 0
  </acceptance_criteria>
  <files>priv/repo/migrations/*_create_specs.exs, priv/repo/migrations/*_create_spec_revisions.exs, lib/kiln/specs/spec.ex, lib/kiln/specs/spec_revision.ex, lib/kiln/specs.ex, test/kiln/specs/spec_revision_test.exs</files>
  <action>
Run `mix ecto.gen.migration create_specs` and `mix ecto.gen.migration create_spec_revisions` (or one migration with both tables if preferred — document in SUMMARY). Tables: `specs` (id uuid v7, title, timestamps), `spec_revisions` (id, spec_id FK restrict, body text, `scenario_manifest_sha256` string 64 hex nullable until first compile, inserted_at). Transfer ownership to `kiln_owner`; grant `kiln_app` INSERT/SELECT/UPDATE on mutable tables per existing migration style.

Implement `Kiln.Specs.Spec`, `Kiln.Specs.SpecRevision`, and expand `lib/kiln/specs.ex` with `create_spec/1`, `create_revision/2`, `get_revision!/1` — no parser yet.

Add `test/kiln/specs/spec_revision_test.exs` proving insert + changeset validations.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/spec_revision_test.exs --max-failures=1</automated>
  </verify>
  <done>Spec revision storage exists with manifest hash column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `holdout_scenarios` table + schema module</name>
  <read_first>
    - .planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
    - priv/repo/migrations/20260418000004_audit_events_immutability.exs
  </read_first>
  <acceptance_criteria>
    - `grep "holdout_scenarios" priv/repo/migrations/*.exs` matches at least one file
    - `lib/kiln/specs/holdout_scenario.ex` contains `schema "holdout_scenarios"`
    - `mix test test/kiln/specs/holdout_scenario_test.exs` exits 0
  </acceptance_criteria>
  <files>priv/repo/migrations/*_create_holdout_scenarios.exs, lib/kiln/specs/holdout_scenario.ex, test/kiln/specs/holdout_scenario_test.exs</files>
  <action>
Create `holdout_scenarios` with references to `spec_id` (or revision — pick one and document), `label`, `body` or `artifact_sha` FK to CAS table if bodies are large. Follow D-S02a. **Do not** grant SELECT to `kiln_app` in this migration (plan 04 adds narrow role — here default deny after owner transfer). Mirror grant style from other secured tables.

Schema module `Kiln.Specs.HoldoutScenario` for verifier code path only (no public list API for app role in this plan).
  </action>
  <verify>
    <automated>mix test test/kiln/specs/holdout_scenario_test.exs --max-failures=1</automated>
  </verify>
  <done>Holdout table exists; grants deny app role read (verified in plan 04 tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `runs` columns for caps + stuck window</name>
  <read_first>
    - lib/kiln/runs/run.ex
    - lib/kiln/runs/transitions.ex
  </read_first>
  <acceptance_criteria>
    - New migration alters `runs` and includes comment referencing ORCH-06 / OBS-04
    - `lib/kiln/runs/run.ex` defines new fields (e.g. `governed_attempt_count`, `stuck_signal_window` jsonb — exact names in migration + schema)
    - `mix test test/kiln/runs/run_test.exs` or new `test/kiln/runs/phase5_run_fields_test.exs` exits 0
  </acceptance_criteria>
  <files>priv/repo/migrations/*_runs_phase5_budget_stuck.exs, lib/kiln/runs/run.ex, test/kiln/runs/phase5_run_fields_test.exs</files>
  <action>
Add migration: columns for **governed attempt** counter, optional **wall clock anchor** if not derivable from `inserted_at`, and **jsonb** deque/array for last K `(stage_id_or_kind, failure_class)` pairs per D-S04/D-S05. Extend `Run` schema and changesets used by `Transitions` only where needed (defaults 0 / empty list).

Document field semantics in `@moduledoc` on `Run`.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/phase5_run_fields_test.exs --max-failures=1</automated>
  </verify>
  <done>Run row carries durable cap + stuck-detector inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> `spec_revisions` | versioned markdown persists with audit later |
| `kiln_app` -> `holdout_scenarios` | must not read holdout rows (enforced in plan 01 migration grants + plan 04 tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | `spec_revisions` | mitigate | manifests bind verify to exact bytes |
| T-05-04 | E | holdout grants | mitigate | REVOKE SELECT from `kiln_app`; verifier role in plan 04 |
| T-05-05 | T | run counters | mitigate | only `Transitions` / Repo tx update paths write counters |
</threat_model>

<verification>
`mix test test/kiln/specs/ test/kiln/runs/phase5_run_fields_test.exs`
</verification>

<success_criteria>
Migrations apply cleanly on empty DB; schemas and tests prove spec revision + holdout storage + run extension fields exist.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-01-SUMMARY.md`
</output>
