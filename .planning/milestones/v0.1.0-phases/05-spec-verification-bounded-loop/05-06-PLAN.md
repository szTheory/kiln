---
phase: 05-spec-verification-bounded-loop
plan: "06"
type: execute
wave: 4
depends_on: ["01", "02", "03", "04", "05"]
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/spec_editor_live.ex
  - lib/kiln_web/live/spec_editor_live.html.heex
  - assets/js/app.js
  - lib/mix/tasks/check_no_manual_qa_gates.ex
  - .github/workflows/ci.yml
  - test/kiln_web/live/spec_editor_live_test.exs
autonomous: true
requirements:
  - SPEC-01
  - UAT-01
  - UAT-02
must_haves:
  truths:
    - "`/specs/:id/edit` renders under `<Layouts.app flash={@flash} ...>` per Phoenix 1.8"
    - "`mix check_no_manual_qa_gates` fails on forbidden substrings in **code paths** (not docs) per D-26 expansion"
    - "CI / `mix test` runs `@moduletag :kiln_scenario` suite including holdout-tagged tests"
  artifacts:
    - path: "lib/kiln_web/live/spec_editor_live.ex"
      provides: "SPEC-01 editor"
      contains: "mount"
  key_links:
    - from: "lib/kiln_web/router.ex"
      to: "lib/kiln_web/live/spec_editor_live.ex"
      via: "live"
      pattern: "SpecEditorLive"
---

<objective>
Complete **SPEC-01** operator surface: full-page **LiveView** spec editor at `/specs/:id/edit` matching **`05-UI-SPEC.md`** (autosave states, revision timeline placeholder acceptable if revision UI is phased — **minimum**: load body, edit textarea, save creates new revision row, parse errors inline). Wire **UAT-01**: ensure `mix test` invoked from `.check.exs` includes generated + holdout scenario tests (add `test/kiln/scenarios/**/*.exs` glob or `mix test --include kiln_scenario` pattern — document exact flag in SUMMARY). **UAT-02**: extend **`Mix.Tasks.CheckNoManualQaGates`** beyond stub to grep **LiveView** and **Runs** code for banned “manual review” / “ASK-HUMAN” style tokens **scoped by allowlist file** `priv/qa_gate_allowlist.txt` (empty initially) to avoid doc false positives per RESEARCH.md pitfall #7.

Purpose: zero-human-QA enforcement + operator authoring path live.
Output: LiveView + tests + mix task + CI doc touch if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spec-verification-bounded-loop/05-UI-SPEC.md
@lib/kiln_web/router.ex
@lib/kiln_web/components/layouts.ex
@AGENTS.md
@prompts/kiln-brand-book.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router + `SpecEditorLive` skeleton</name>
  <read_first>
    - lib/kiln_web/router.ex
    - lib/kiln_web/components/layouts.ex
    - AGENTS.md
  </read_first>
  <acceptance_criteria>
    - `grep "SpecEditorLive" lib/kiln_web/router.ex` matches
    - Template begins with `<Layouts.app` (grep first line of heex file)
    - `mix test test/kiln_web/live/spec_editor_live_test.exs` exits 0 with `has_element?(view, "#spec-editor-form")` or equivalent
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/spec_editor_live.ex, lib/kiln_web/live/spec_editor_live.html.heex, test/kiln_web/live/spec_editor_live_test.exs</files>
  <action>
Add `scope "/", KilnWeb` `live "/specs/:id/edit", SpecEditorLive, :edit` (adjust to project’s `live_session` / auth pattern — **solo v1**: reuse existing `:browser` pipe).

`mount/3`: load `%SpecRevision{}` latest for spec id or create empty revision via `Kiln.Specs`.

Assign `@save_state` atom `:saved | :saving | :unsaved | :error`, `@last_saved_at`, `@parse_error`.

`handle_event("save", ...)`: upsert revision body via `Kiln.Specs`.

Use brand palette classes (Coal/Char/Bone) from `kiln-brand-book.md`.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/spec_editor_live_test.exs --max-failures=1</automated>
  </verify>
  <done>SPEC-01 minimal editor shipped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Debounced autosave + Cmd/Ctrl+S</name>
  <read_first>
    - lib/kiln_web/live/spec_editor_live.ex
    - assets/js/app.js
  </read_first>
  <acceptance_criteria>
    - `phx-hook` uses colocated hook with name starting with `.` OR `push_event` debounce documented
    - `grep "debounc" lib/kiln_web/live/spec_editor_live.ex` OR hook file documents 2–4s debounce
    - LiveView test covers at least one save path (`render_submit` or `render_blur` if applicable)
  </acceptance_criteria>
  <files>lib/kiln_web/live/spec_editor_live.ex, lib/kiln_web/live/spec_editor_live.html.heex, assets/js/app.js</files>
  <action>
Implement `phx-change` validation calling `Kiln.Specs.ScenarioParser` for inline errors (D-S06d). Add keyboard shortcut via colocated **`Phoenix.LiveView.ColocatedHook`** per AGENTS.md (no raw `<script>`).

Keep heap bounded: do not load unlimited revision history in assign — cap or lazy-load.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/spec_editor_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Autosave UX matches UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `check_no_manual_qa_gates` + CI scenario include</name>
  <read_first>
    - lib/mix/tasks/check_no_manual_qa_gates.ex
    - .check.exs
    - .github/workflows/ci.yml
  </read_first>
  <acceptance_criteria>
    - `mix check_no_manual_qa_gates` exits 0 on current tree after allowlist / scope correct
    - `.check.exs` `{:ex_unit, ...}` line documents inclusion of `:kiln_scenario` tests OR separate tool entry
    - `grep "kiln_scenario" .check.exs` OR `grep "include" .check.exs` shows scenario wiring
  </acceptance_criteria>
  <files>lib/mix/tasks/check_no_manual_qa_gates.ex, priv/qa_gate_allowlist.txt, .check.exs, .github/workflows/ci.yml</files>
  <action>
Implement grep allowlist: only scan `lib/kiln/**/*.ex` and `lib/kiln_web/**/*.ex` (adjust list in task code). Banned tokens list in module attribute: `~w(MANUAL_REVIEW ASK_HUMAN)` plus project-chosen phrases — document in `@moduledoc`.

For UAT-01: if ExUnit tags used, set `mix test --include kiln_scenario` as second `ex_unit` entry OR change single entry to include tag (ensure default CI run executes holdouts). Coordinate with plan 02 generated tests’ `@moduletag :kiln_scenario`.
  </action>
  <verify>
    <automated>mix check_no_manual_qa_gates && mix test --include kiln_scenario test/kiln/specs/ --max-failures=1</automated>
  </verify>
  <done>UAT gates wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> LiveView | standard CSRF / session from Phoenix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | I | spec body | mitigate | escape HTML in display; textarea only for edit |
| T-05-06b | E | save events | mitigate | validate UUID spec id; authorize scope (solo: assert spec exists) |
</threat_model>

<verification>
`mix test test/kiln_web/live/spec_editor_live_test.exs` then `mix check_no_manual_qa_gates` then spot-check `mix test --include kiln_scenario`.
</verification>

<success_criteria>
Editor tests green; manual QA grep gate active; scenario include documented in `.check.exs`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-06-SUMMARY.md`
</output>
