---
phase: 05-spec-verification-bounded-loop
plan: "05"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - lib/kiln/runs/transitions.ex
  - lib/kiln/policies/stuck_detector.ex
  - lib/kiln/policies/stuck_window.ex
  - lib/kiln/external_operations.ex
  - test/kiln/runs/transitions_stuck_test.exs
  - test/kiln/policies/stuck_window_test.exs
autonomous: true
requirements:
  - ORCH-06
  - OBS-04
must_haves:
  truths:
    - "Cap breach transitions to `:escalated` with typed reason in audit payload (same transaction)"
    - "StuckDetector evaluates sliding window using **pure** function on run fields — **no** `Repo` query inside `GenServer.call` from locked transaction"
    - "`{:halt, :stuck, payload}` from detector routes to `:escalated` inside `Transitions.transition/3` without deadlock"
    - "`:telemetry` execute event emitted on stuck alarm (name documented in code)"
  artifacts:
    - path: "lib/kiln/policies/stuck_window.ex"
      provides: "pure window math"
      contains: "push"
  key_links:
    - from: "lib/kiln/runs/transitions.ex"
      to: "lib/kiln/policies/stuck_detector.ex"
      via: "check/1"
      pattern: "StuckDetector"
---

<objective>
Implement **ORCH-06** bounded autonomy checks and **OBS-04** stuck-run detection **inside the existing transaction discipline**: extend `Kiln.Runs.Run` fields from plan 01, increment counters only on idempotent-safe boundaries, enforce precedence **global halt → wall → governed attempts → spend** (global halt may defer to existing `FactoryCircuitBreaker` integration point — document), and replace `StuckDetector` internals with **pure** `StuckWindow` module + jsonb persistence on run row updated only from `Transitions` process holding `FOR UPDATE` lock — **no nested Repo lock** inside GenServer (D-S05 / RESEARCH pitfall #1).

On terminal `failed`/`escalated`, call **`Kiln.ExternalOperations.abandon_op/2`** for stranded intents per D-S05e (hook in `Transitions` or dedicated policy module).

Purpose: close roadmap P1/P2; same-tx audit + PubSub-after-commit preserved.
Output: policy modules, transition wiring, telemetry, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln/runs/transitions.ex
@lib/kiln/policies/stuck_detector.ex
@lib/kiln/external_operations.ex
@lib/kiln/runs/run.ex
@.planning/phases/05-spec-verification-bounded-loop/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure `StuckWindow` + unit tests</name>
  <read_first>
    - .planning/phases/05-spec-verification-bounded-loop/05-RESEARCH.md
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - `lib/kiln/policies/stuck_window.ex` exports `push_event/4` returning `{window, {:ok | :halt, payload}}`
    - Default **N=3** identical `(stage, failure_class)` occurrences in window K (document K constant, e.g. 15) triggers `:halt`
    - `mix test test/kiln/policies/stuck_window_test.exs` exits 0
  </acceptance_criteria>
  <files>lib/kiln/policies/stuck_window.ex, test/kiln/policies/stuck_window_test.exs</files>
  <action>
Model failure_class as small atom set module attribute `Kiln.Policies.FailureClass`. Window stored as list of `{stage_atom, failure_class}` tuples in **insertion order**, trim to K.

Reset policy: implement `decay_on_progress/2` stub returning cleared window on successful stage advance (called from Transitions with prior+next state).
  </action>
  <verify>
    <automated>mix test test/kiln/policies/stuck_window_test.exs --max-failures=1</automated>
  </verify>
  <done>Window math isolated from OTP/Repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `StuckDetector.check/1` delegates to pure path only</name>
  <read_first>
    - lib/kiln/policies/stuck_detector.ex
    - lib/kiln/runs/transitions.ex
  </read_first>
  <acceptance_criteria>
    - `handle_call` for `{:check, ctx}` does **not** call `Kiln.Repo.*`
    - `grep "Repo\." lib/kiln/policies/stuck_detector.ex` returns no matches OR only in commented example (prefer zero)
    - `Transitions.transition/3` handles `{:halt, :stuck, payload}` by attempting `to: :escalated` in same transaction (grep `{:halt` in transitions.ex)
  </acceptance_criteria>
  <files>lib/kiln/policies/stuck_detector.ex, lib/kiln/runs/transitions.ex, test/kiln/runs/transitions_stuck_test.exs</files>
  <action>
Pass `run.stuck_signal_window` into GenServer call; GenServer applies `StuckWindow.push_event/4` on copied list; returns `:ok` + updated window **data** to caller OR return `{:halt, ...}` — **preferred**: perform window update in **Transitions** before calling GenServer only for telemetry side effect OR remove GenServer from hot path entirely (choose minimal diff: keep GenServer but stateless `handle_call` calling pure module only).

**Critical:** Update `run` row’s jsonb window inside `update_state/3` path when not halting.

Wire `with`/`case` in `Transitions` so `{:halt, :stuck, payload}` triggers `assert_allowed(run.state, :escalated)` branch and `update_state` to escalated with meta payload; append audit with `event_kind` including `stuck_detector_alarmed` if not already in `Kiln.Audit.EventKind` — extend enum + migration if required per Phase 1 audit patterns.

Add test `transitions_stuck_test.exs` using sandbox DB transaction or `Ecto.Adapters.SQL.Sandbox` to assert single transaction.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/transitions_stuck_test.exs --max-failures=1</automated>
  </verify>
  <done>OBS-04 visible in transitions tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cap enforcement + abandon external ops</name>
  <read_first>
    - lib/kiln/runs/run.ex
    - lib/kiln/external_operations.ex
    - lib/kiln/runs/transitions.ex
  </read_first>
  <acceptance_criteria>
    - When `governed_attempt_count` exceeds cap from `caps_snapshot` (document key name e.g. `"max_governed_attempts"`), transition to `:escalated` with reason atom in audit
    - `grep "abandon" lib/kiln/runs/transitions.ex` OR dedicated module called from transition on terminal states
    - `mix test test/kiln/runs/transitions_caps_test.exs` exits 0
  </acceptance_criteria>
  <files>lib/kiln/runs/transitions.ex, test/kiln/runs/transitions_caps_test.exs</files>
  <action>
Read caps from `run.caps_snapshot` map with defaults infinity when absent. Implement wall clock using `DateTime.diff(DateTime.utc_now(), run.inserted_at, :second)` vs `caps_snapshot["max_elapsed_seconds"]`.

On transition to `:failed` or `:escalated`, enumerate open `external_operations` for run and mark **abandon** using existing `ExternalOperations` API.

Emit `:telemetry.execute` for `[:kiln, :stuck_detector, :alarmed]` with measurements `%{count: 1}` and metadata `%{run_id: ..., failure_class: ...}`.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/transitions_caps_test.exs --max-failures=1</automated>
  </verify>
  <done>ORCH-06 + abandon wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy -> `runs` row | only while holding `FOR UPDATE` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | D | StuckDetector | mitigate | pure window; no nested locks |
| T-05-05b | E | caps_snapshot | mitigate | frozen at run start; validate keys in run creation |
</threat_model>

<verification>
`mix test test/kiln/policies/stuck_window_test.exs test/kiln/runs/transitions_stuck_test.exs test/kiln/runs/transitions_caps_test.exs`
</verification>

<success_criteria>
Stuck + cap tests green; no new compile warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-05-SUMMARY.md`
</output>
