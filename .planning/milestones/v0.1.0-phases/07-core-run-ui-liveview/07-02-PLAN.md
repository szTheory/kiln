---
phase: 07-core-run-ui-liveview
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/kiln/runs.ex
  - lib/kiln_web/live/run_board_live.ex
  - test/kiln_web/live/run_board_live_test.exs
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - "Kanban columns exist for all nine `Run` states in `Ecto.Enum` order used by product"
    - "`stream(:runs, ...)` backs cards; `phx-update=\"stream\"` on column hosts"
    - "Subscribes to `runs:board` PubSub topic only (same string as `Transitions`)"
  artifacts:
    - path: "lib/kiln_web/live/run_board_live.ex"
      provides: "Realtime board"
      contains: "subscribe"
    - path: "lib/kiln/runs.ex"
      provides: "Board listing query"
      contains: "list_for_board"
  key_links:
    - from: "lib/kiln_web/live/run_board_live.ex"
      to: "lib/kiln/runs/transitions.ex"
      via: "Phoenix.PubSub.broadcast(Kiln.PubSub, \"runs:board\""
      pattern: "Mirror topic string exactly"
---

<objective>
Ship **UI-01 run board**: query all runs (active + terminal) into **LiveView streams**, **kanban columns by state**, **`Phoenix.PubSub.subscribe(Kiln.PubSub, "runs:board")`**, handle `{:run_state, run}` with **`stream_insert`** / **`stream_delete`** (no full `reset: true` on each message per D-725). Add **`Kiln.Runs.list_for_board/0`** returning `[Run.t()]` ordered for stable cards (e.g. `desc: :updated_at` within state).

Output: board populated from DB; LiveView tests cover stream DOM + one PubSub injection.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
@.planning/phases/07-core-run-ui-liveview/07-RESEARCH.md
@lib/kiln/runs/transitions.ex
@lib/kiln/runs/run.ex
@lib/kiln/runs.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: list_for_board/0</name>
  <read_first>
    - lib/kiln/runs.ex
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "def list_for_board" lib/kiln/runs.ex` matches
    - `grep -n "@spec list_for_board" lib/kiln/runs.ex` matches
    - `mix test test/kiln/runs_test.exs --max-failures=1 2>/dev/null` exits 0 OR test file `test/kiln/runs_test.exs` created with at least one test for `list_for_board/0` and `mix test test/kiln/runs_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/runs.ex, test/kiln/runs_test.exs</files>
  <action>
Add **`@spec list_for_board() :: [Run.t()]`** and **`def list_for_board/0`** selecting **all** runs where state is in **`Run.states()`** (queued..escalated), **`order_by: [asc: :state, desc: :updated_at]`** (or equivalent deterministic sort documented in `@doc`). Use single query — N+1 avoided until card needs associations (defer preloads until card fields exist).

Add **ExUnit** in `test/kiln/runs_test.exs` (create file if absent): insert **two** runs with different states via `Runs.create/1` + `Transitions` if needed, assert **both** returned and ordering predicate holds.
  </action>
  <verify>
    <automated>mix test test/kiln/runs_test.exs --max-failures=1</automated>
  </verify>
  <done>Query API ready for mount.</done>
</task>

<task type="auto">
  <name>Task 2: Streams + PubSub on RunBoardLive</name>
  <read_first>
    - lib/kiln_web/live/run_board_live.ex
    - lib/kiln/runs/transitions.ex
    - lib/kiln_web/live/spec_editor_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "subscribe(Kiln.PubSub, \"runs:board\")" lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "stream(:runs" lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "handle_info({:run_state" lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "defp allow?" lib/kiln_web/live/run_board_live.ex` matches
    - `mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_board_live.ex, test/kiln_web/live/run_board_live_test.exs</files>
  <action>
In **`mount/3`**: call **`Runs.list_for_board/0`**, **`Phoenix.PubSub.subscribe(Kiln.PubSub, "runs:board")`**, initialize **`stream(socket, :runs, runs, reset: true)`** (initial load only).

Template: **kanban** — one column container per state with **`data-state="coding"`** (etc.) and **`phx-update="stream"`** on a parent — **note:** if using one global `:runs` stream, each card carries **`data-state`** attribute filtered in CSS flex, OR use **one stream per column** (`:runs_queued`, `:runs_planning`, …) — **pick one documented approach** in moduledoc (streams are not enumerable; per-column streams require `stream/3` into named keys).

On **`handle_info({:run_state, %Run{} = run}, socket)`**: upsert via **`stream_insert(socket, :runs, run)`** (Phoenix 1.1 idempotent by DOM id convention using **`dom_id(run)`** or explicit **`id: "runs-#{run.id}"`** on card root).

Every new **`handle_event`**: guard with **`allow?/1`** (reuse from Plan 01).

Tests: render board with **Repo** sandbox data; **`Phoenix.PubSub.broadcast(Kiln.PubSub, "runs:board", {:run_state, updated_run})`**; **`_ = :sys.get_state(view.pid)`** or **`render(view)`**; assert **updated** card text/state badge via **`has_element?/2`**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Realtime kanban board meets UI-01 core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → operator | Runs may contain workflow_id strings — no secrets |
| PubSub → LV | Only server emits `{:run_state, run}` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | E | forged PubSub message | mitigate | Only server processes broadcast; LV ignores unknown shapes |
| T-07-04 | I | run card data | mitigate | Data from Repo only — no user JSON in queries |
</threat_model>

<verification>
mix test test/kiln_web/live/run_board_live_test.exs test/kiln/runs_test.exs
</verification>

<success_criteria>
Board lists runs, reacts to `runs:board`, streams-first, `allow?` on events.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-02-SUMMARY.md`
</output>
