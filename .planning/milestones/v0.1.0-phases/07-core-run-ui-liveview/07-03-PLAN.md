---
phase: 07-core-run-ui-liveview
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/run_detail_live.ex
  - lib/kiln/artifacts.ex
  - test/kiln_web/live/run_detail_live_test.exs
autonomous: true
requirements:
  - UI-02
must_haves:
  truths:
    - "`GET /runs/:run_id?stage=&pane=` validated in `handle_params/3`"
    - "Stage graph + four panes (diff, logs, events, chatter) use streams or bounded assigns per UI-SPEC"
    - "Diff text loaded from `Kiln.Artifacts` for selected `stage_run` when `*.diff` exists"
  artifacts:
    - path: "lib/kiln_web/live/run_detail_live.ex"
      provides: "Run inspection UI"
      contains: "RunDetailLive"
    - path: "lib/kiln_web/router.ex"
      provides: "Run detail route"
      contains: "/runs/:run_id"
  key_links:
    - from: "lib/kiln_web/live/run_detail_live.ex"
      to: "lib/kiln/stages.ex"
      via: "list_for_run/1"
      pattern: "Stage rows for graph"
---

<objective>
Ship **UI-02 run detail**: **`RunDetailLive`** at **`/runs/:run_id`** with **`handle_params/3`** parsing **`stage`** (workflow_stage_id string or stage_run id — **pick one** and document; recommend **workflow_stage_id** matching `StageRun.workflow_stage_id`) and **`pane`** ∈ **`diff|logs|events|chatter`**. Topological stage graph (read **`CompiledGraph`** via **`Kiln.Workflows`** — add **`fetch_compiled_for_run/1`** or reuse existing if present). **Streams** for logs/events/chatter rows; **diff** pane reads **`Artifacts.get(stage_run_id, name)`** preferring first artifact whose `name` ends with **`.diff`** or **`.patch`** for latest succeeded `stage_run` for that workflow stage.

Output: route, LiveView, LazyHTML tests with stable IDs (`id="run-detail"`, pane tab IDs per UI-SPEC).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
@.planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
@lib/kiln/runs/run.ex
@lib/kiln/stages.ex
@lib/kiln/artifacts.ex
@lib/kiln/workflows/compiled_graph.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route + mount + handle_params validation</name>
  <read_first>
    - lib/kiln_web/router.ex
    - lib/kiln_web/live/spec_editor_live.ex
    - lib/kiln/runs.ex
    - lib/kiln/stages.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "RunDetailLive" lib/kiln_web/router.ex` matches `live "/runs/:run_id"`
    - `grep -n "handle_params" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n 'id="run-detail"' lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "Select a stage" lib/kiln_web/live/run_detail_live.ex` matches (UI-SPEC copy when no valid stage)
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/run_detail_live.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
Add **`live "/runs/:run_id", RunDetailLive, :show`** inside **`live_session :default`**.

**`RunDetailLive`**: `mount` loads **`Run = Kiln.Runs.get!(id)`**, **`stages = Kiln.Stages.list_for_run(id)`**, assigns **`selected_stage_id`**, **`pane`** from params with defaults **`nil`** and **`"diff"`**.

**`handle_params(params, _uri, socket)`**: read **`stage`**, **`pane`**; validate **`pane`** in **`~w(diff logs events chatter)`** else set **`"diff"`**; validate **`stage`** belongs to current run’s **`stages`** list (match **`workflow_stage_id`** on latest attempt per stage key — document algorithm); if unknown, assign flash-less inline assign **`@stage_missing?`** and keep shell (D-708).

Implement **`defp allow?(_socket), do: true`** and call from **`handle_event`** stubs.

Tests: insert minimal run + stage_run fixtures (use factories or direct `Repo.insert`); **`live ~p"/runs/#{run.id}"`**; assert **`#run-detail`**; invalid `?stage=bogus` shows **"Stage not found"** OR UI-SPEC **pick latest** path per CONTEXT D-708 (must match chosen moduledoc).
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1</automated>
  </verify>
  <done>URL-driven shell validated.</done>
</task>

<task type="auto">
  <name>Task 2: Stage graph + diff pane + streams scaffold</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - lib/kiln/artifacts.ex
    - lib/kiln/workflows/compiled_graph.ex
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "stream(:logs" lib/kiln_web/live/run_detail_live.ex` OR `grep -n "stream(:events" lib/kiln_web/live/run_detail_live.ex` matches at least one stream
    - `grep -n "Artifacts" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "push_patch" lib/kiln_web/live/run_detail_live.ex` matches (pane/stage navigation)
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex, lib/kiln/artifacts.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
1. **Graph:** Render one node per distinct **`workflow_stage_id`** from **`stages`**, edges from **`CompiledGraph`** if **`Kiln.Workflows`** exposes **`stages_in_order/1`** for run’s **`workflow_id` + version + checksum`** — if no public API yet, add **`Kiln.Workflows.graph_for_run(%Run{})`** returning ordered stage ids **or** document "linear order by `inserted_at`" as v1 fallback in `@moduledoc`.

2. **Diff:** For selected stage’s latest **`StageRun`**, list artifact names via new **`Kiln.Artifacts.list_names_for_stage_run/1`** **or** query `from a in Artifact, where: a.stage_run_id == ^id, select: a.name` inside **`Kiln.Artifacts`** — **add `list_for_stage_run/1`** returning `[%{name: ..., content_type: ...}]`. Pick first **`.diff`** / **`.patch`**; load body with **`Artifacts.stream!/1`** into assign **`@diff_text`** capped at **512 KiB** with truncation message (D-713).

3. **Streams:** Initialize **`stream(:logs, [])`**, **`stream(:events, [])`**, **`stream(:chatter, [])`**; populate **logs** from **`stage_run.error_summary`** or placeholder "No log lines for this stage yet" per UI-SPEC; **events** from **`Audit.replay(run_id: ...)`** filtered by **`stage_id`** when column populated (binary id) — **if** `audit_events.stage_id` is stage_run id, wire correctly; else document mapping.

4. **Pane tabs:** Buttons use **`push_patch`** to same URL with **`pane=`** query.

Extend tests: diff pane shows **truncation** or **content** substring when artifact fixture inserted.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Run detail core inspection surfaces live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_id URL | Must not expose other runs when UUID malformed — 404 flash + redirect |
| CAS stream | Diff bytes never echoed to Logger at :info |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | I | cross-run leakage | mitigate | `get!` scoped by id; stage list from `list_for_run(run.id)` only |
| T-07-06 | I | diff content | mitigate | escape HEEx default; no `raw` unless explicitly sanitized |
</threat_model>

<verification>
mix test test/kiln_web/live/run_detail_live_test.exs
</verification>

<success_criteria>
Run detail route, params, graph shell, diff from artifacts, streams scaffold, tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-03-SUMMARY.md`
</output>
