---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/inbox_live.ex
  - test/kiln_web/live/inbox_live_test.exs
autonomous: true
requirements:
  - INTAKE-01
  - INTAKE-02
must_haves:
  truths:
    - "`GET /inbox` renders InboxLive with `id=\"inbox\"`"
    - "Empty + non-empty states match `08-UI-SPEC.md` verbatim headings where specified"
  artifacts:
    - path: "lib/kiln_web/live/inbox_live.ex"
      provides: "Inbox UI"
      contains: "InboxLive"
---

<objective>
Ship **`KilnWeb.InboxLive`** at **`/inbox`**: list open drafts via `stream(:drafts, …)`, actions **Promote / Archive / Edit**, freeform **New draft** form, markdown file upload (server parses file contents into draft), GitHub import field calling **`GitHubIssueImporter`**. INTAKE-01(a)(b)(c) + INTAKE-02 UI shell.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
@lib/kiln_web/live/spec_editor_live.ex
@lib/kiln_web/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router + InboxLive mount</name>
  <read_first>
    - lib/kiln_web/router.ex
    - lib/kiln_web/live/run_board_live.ex
    - lib/kiln_web/components/layouts.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n 'live "/inbox"' lib/kiln_web/router.ex` matches
    - `grep -n 'id="inbox"' lib/kiln_web/live/inbox_live.ex` matches
    - `grep -n "No drafts in the inbox" lib/kiln_web/live/inbox_live.ex` matches
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/inbox_live.ex</files>
  <action>
Register **`live "/inbox", InboxLive, :index`** inside existing `live_session :default`. Implement mount: load `Specs.list_open_drafts/0`, **`stream(socket, :drafts, rows, reset: true)`**. Empty state copy **exact** from `08-UI-SPEC.md` § Copywriting Contract (Inbox empty). Navbar: add **Inbox** link next to Workflows using `~p"/inbox"`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Inbox route live.</done>
</task>

<task type="auto">
  <name>Task 2: Events + LiveView tests</name>
  <read_first>
    - lib/kiln_web/live/inbox_live.ex
    - lib/kiln/specs.ex
    - test/kiln_web/live/run_board_live_test.exs
  </read_first>
  <acceptance_criteria>
    - `grep -n "unless allow?" lib/kiln_web/live/inbox_live.ex` matches for first line of each `handle_event` OR single guard helper invoked first
    - `mix test test/kiln_web/live/inbox_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/inbox_live.ex, test/kiln_web/live/inbox_live_test.exs</files>
  <action>
Events: **`promote`**, **`archive`**, **`import_github`**, **`create_freeform`**, **`import_markdown`**. Each starts with **`unless allow?(socket), do: {:noreply, put_flash(...)}`**. Promote calls **`Specs.promote_draft/1`** and removes stream entry on success. Tests: empty `#inbox`; seed one draft in setup assert row renders with **Promote** button `phx-click`.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/inbox_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Inbox interactions tested.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | T | file upload | mitigate | Size cap on uploaded markdown (e.g. 256 KiB); reject binary |
</threat_model>

<verification>
mix test test/kiln_web/live/inbox_live_test.exs
</verification>

<success_criteria>
Inbox route + streams + promote/archive + import entry points + tests green.
</success_criteria>

<output>
After completion, create `08-03-SUMMARY.md` in this phase directory.
</output>
