---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "10"
type: execute
wave: 5
depends_on:
  - "05"
files_modified:
  - lib/kiln_web/components/layouts.ex
  - lib/kiln_web/components/factory_header.ex
  - lib/kiln_web/components/run_progress.ex
  - lib/kiln_web/components/agent_ticker.ex
  - lib/kiln_web/live/run_board_live.ex
  - lib/kiln_web/live/run_detail_live.ex
  - lib/kiln/application.ex
  - test/kiln_web/live/run_board_live_test.exs
  - test/kiln_web/components/factory_header_test.exs
autonomous: true
requirements:
  - UI-07
  - UI-08
  - UI-09
must_haves:
  truths:
    - "**`factory:summary`** PubSub topic used for header counts (single low-rate subscription)"
    - "**`agent_ticker`** stream only mounted on **`/`** (`RunBoardLive`), **`id=\"agent-ticker\"`**"
    - "**`RunProgress`** on board card + run detail header with stages fraction + staleness classes"
---

<objective>
Ship **global factory chrome**: **`FactoryHeader`** (UI-07) in **`Layouts.app`**, **`RunProgress`** component on **`RunBoardLive`** cards + **`RunDetailLive`** header (UI-08), **`AgentTicker`** stream on home only (UI-09). Wire **`Phoenix.PubSub`** publishers: debounced **`factory:summary`** publisher process or telemetry hook; rate-limited **`agent_ticker`** events from existing run/stage transition broadcasts (extend where Phase 7 already publishes — **do not** subscribe header to per-run high-volume topics).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
@lib/kiln_web/live/run_board_live.ex
@lib/kiln_web/components/layouts.ex
@lib/kiln/runs/transitions.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: FactoryHeader + layout</name>
  <read_first>
    - lib/kiln_web/components/layouts.ex
    - .planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n 'id="factory-header"' lib/kiln_web/components/factory_header.ex` matches
    - `grep -n "FactoryHeader" lib/kiln_web/components/layouts.ex` matches
    - `grep -n "factory:summary" lib/kiln_web/components/factory_header.ex` matches
  </acceptance_criteria>
  <files>lib/kiln_web/components/factory_header.ex, lib/kiln_web/components/layouts.ex</files>
  <action>
Implement **`KilnWeb.Components.FactoryHeader`** as function component inserted **inside** `Layouts.app` header row. **`mount`-less**: receives assigns from **`Layouts`** — use **`assign(socket, :factory_summary, …)`** in a **wrapper** approach: actually **`Layouts.app`** cannot subscribe; **use `live_session` on_mount hook** `KilnWeb.LiveScope` extension OR subscribe inside each LiveView — **preferred per D-821**: add **`on_mount {KilnWeb.FactorySummaryHook, :default}`** in `router.ex` `live_session` that assigns summary from cache updated by **`Kiln.FactorySummary`** GenServer subscribed once. If too heavy for this plan, **document** incremental approach: header shows static zeros first commit — **NOT acceptable**; must show **live counts** by end of plan: implement **`Kiln.FactorySummary`** simple **`GenServer`** subscribed to **`"runs:board"`** + timer debounce 300ms publishing derived counts to **`factory:summary`** and LiveViews subscribe in hook.

**Concrete minimal path:** Add **`KilnWeb.FactorySummaryHook`** in `lib/kiln_web/live/factory_summary_hook.ex` implementing **`on_mount/4`** — `Phoenix.LiveView.attach_hook(socket, :factory_summary, :handle_info, …)` pattern OR standard **`connected?(socket)` subscribe**. Subscribe to **`factory:summary`**. **`Kiln.FactorySummaryPublisher`** child in **`Kiln.Application`** listening to existing PubSub **`"runs:board"`** with debounce.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Header shows wired subscription + `id="factory-header"`.</done>
</task>

<task type="auto">
  <name>Task 2: RunProgress component</name>
  <read_first>
    - lib/kiln_web/live/run_board_live.ex
    - lib/kiln_web/live/run_detail_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "RunProgress" lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "RunProgress" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "Not enough history" lib/kiln_web/components/run_progress.ex` matches
  </acceptance_criteria>
  <files>lib/kiln_web/components/run_progress.ex, lib/kiln_web/live/run_board_live.ex, lib/kiln_web/live/run_detail_live.ex</files>
  <action>
Attrs: **`run`**, **`stages_done`**, **`stages_total`**, **`last_activity_at`**. Staleness CSS classes: **green** `<30s`, **amber** `<5min`, else **red** per ROADMAP. Estimate label uses historical percentiles if helper exists; else **"Not enough history"**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Progress visible on board + detail tests extended.</done>
</task>

<task type="auto">
  <name>Task 3: AgentTicker on home only</name>
  <read_first>
    - lib/kiln_web/live/run_board_live.ex
    - .planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n 'id="agent-ticker"' lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "agent_ticker" lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "stream(" lib/kiln_web/live/run_board_live.ex` matches with **`:ticker`** or similar atom
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_board_live.ex, lib/kiln_web/components/agent_ticker.ex</files>
  <action>
Add **`stream(:ticker_lines, …)`** with **`phx-update="stream"`** container **`id="agent-ticker"`**. Subscribe to **`agent_ticker`** topic only in **`RunBoardLive.mount`**. Publisher: extend **`Kiln.Runs.Transitions`** success path broadcast to include **rate-limited** fan-out message `{run_id, stage_id, line}` — token bucket **`Kiln.AgentTickerRateLimiter`** module (ETS counter) dropping >10/sec per run. Cap stream at **75** lines using **`stream_delete`** on tail in **`handle_info`**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Ticker stream + subscription on `/` only.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | DoS | ticker flood | mitigate | Token bucket + stream cap |
| T-08-10b | I | cross-run leak | mitigate | Lines include only run ids operator already sees on board |
</threat_model>

<verification>
mix test test/kiln_web/live/run_board_live_test.exs
</verification>

<success_criteria>
Factory header + run progress + agent ticker meet UI-07..09 with tests.
</success_criteria>

<output>
After completion, create `08-10-SUMMARY.md` in this phase directory.
</output>
