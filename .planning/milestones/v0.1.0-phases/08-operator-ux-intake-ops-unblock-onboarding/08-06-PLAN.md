---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "06"
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/kiln_web/live/cost_live.ex
  - test/kiln_web/live/cost_live_test.exs
autonomous: true
requirements:
  - OPS-04
must_haves:
  truths:
    - "Cost intel lives in **same** `CostLive` — `?tab=intel` or `/costs` + `handle_params` branch per D-802"
    - "At least one advisory string template when data present: **You're spending** … **profile** …"
---

<objective>
**OPS-04:** Extend **`KilnWeb.CostLive`** with **Intel / advisory** segment: pivots per-run / per-workflow / per-agent / per-provider; daily/weekly/monthly toggles via **`push_patch`** query params **`period=`** and **`pivot=`**. Render **≥1** advisory callout when aggregates exist using copy pattern from ROADMAP SC 4 (exact prefix **"You're spending"** in template when spend > 0).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln_web/live/cost_live.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: handle_params + assigns for intel tab</name>
  <read_first>
    - lib/kiln_web/live/cost_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "tab=intel\\|\"intel\"" lib/kiln_web/live/cost_live.ex` matches (tab routing)
    - `grep -n "You're spending" lib/kiln_web/live/cost_live.ex` matches
  </acceptance_criteria>
  <files>lib/kiln_web/live/cost_live.ex</files>
  <action>
Parse **`params["tab"]`** default `summary`. When **`tab=intel`**, load pivot rows from existing cost query functions (extend queries if needed using **`actual_model_used`** field per CONTEXT). Add **period** and **pivot** assigns with defaults `week` and `provider`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Intel branch compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Template + LiveView tests</name>
  <read_first>
    - test/kiln_web/live/cost_live_test.exs
  </read_first>
  <acceptance_criteria>
    - `mix test test/kiln_web/live/cost_live_test.exs --max-failures=1` exits 0
    - Test navigates to **`/costs?tab=intel`** and asserts **`You're spending`** OR empty-state **Not enough history** from UI-SPEC
  </acceptance_criteria>
  <files>lib/kiln_web/live/cost_live.ex, test/kiln_web/live/cost_live_test.exs</files>
  <action>
Add nav pills **Summary | Intel** using `<.link patch={~p"/costs?#{params}"}>`. Advisory: deterministic string when fixture spend > 0.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/cost_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Intel tab tested.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | I | spend data | mitigate | Read-only queries; no user SQL |
</threat_model>

<verification>
mix test test/kiln_web/live/cost_live_test.exs
</verification>

<success_criteria>
Cost intel tab + advisory + tests green.
</success_criteria>

<output>
After completion, create `08-06-SUMMARY.md` in this phase directory.
</output>
