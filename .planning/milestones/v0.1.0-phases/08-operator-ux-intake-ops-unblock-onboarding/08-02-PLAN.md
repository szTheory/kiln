---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/kiln/specs/github_issue_importer.ex
  - lib/kiln/specs.ex
  - test/kiln/specs/github_issue_importer_test.exs
autonomous: true
requirements:
  - INTAKE-01
must_haves:
  truths:
    - "`Kiln.Specs.GitHubIssueImporter` uses Req (no shell `curl`)"
    - "URLs built from parsed owner/repo/number tuples — no raw string interpolation from user paste"
  artifacts:
    - path: "lib/kiln/specs/github_issue_importer.ex"
      provides: "GitHub → draft"
      contains: "GitHubIssueImporter"
  key_links: []
---

<objective>
Implement **`Kiln.Specs.GitHubIssueImporter`** per D-815: fetch GitHub Issue JSON via **`Req`**, map title/body/labels into **`create_draft/1`**, support **ETag** on refresh path. INTAKE-01(c).

Output: pure unit tests with **`Req.Test`** or stubbed adapter (no network in default `mix test`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@lib/kiln/specs.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importer module + parse helpers</name>
  <read_first>
    - lib/kiln/specs.ex
    - mix.exs
    - .planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.Specs.GitHubIssueImporter" lib/kiln/specs/github_issue_importer.ex` matches
    - `grep -n "Req\." lib/kiln/specs/github_issue_importer.ex` matches at least one call
    - `grep -n "System.cmd" lib/kiln/specs/github_issue_importer.ex` returns exit 1 (no shell fetch in default path)
  </acceptance_criteria>
  <files>lib/kiln/specs/github_issue_importer.ex</files>
  <action>
Add **`import_from_slug/1`** accepting `"owner/repo#N"` and **`import_from_url/1`** accepting `https://github.com/owner/repo/issues/N`. Parse into `{owner, repo, number}` with strict regex; reject malformed input with `{:error, :invalid_reference}`. Build API URL `https://api.github.com/repos/{owner}/{repo}/issues/{number}`. Headers: `Accept: application/vnd.github+json`, optional `If-None-Match` from draft row when refreshing.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Importer module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Tests with Req stub + idempotent open row</name>
  <read_first>
    - lib/kiln/specs/github_issue_importer.ex
    - test/test_helper.exs
  </read_first>
  <acceptance_criteria>
    - `mix test test/kiln/specs/github_issue_importer_test.exs --max-failures=1` exits 0
    - Test file contains string `etag` or `If-None-Match` (case-insensitive grep)
  </acceptance_criteria>
  <files>test/kiln/specs/github_issue_importer_test.exs</files>
  <action>
Use **`Req.Test`** (or `Bypass` if already in deps) to return fixture JSON for issue `{"title","body","labels",...}`. Assert **`Kiln.Specs.create_draft`** receives mapped attrs with `source: :github_issue`. Second import with same node_id returns **same draft** (partial unique index) — assert `{:ok, same}` or idempotent error tuple documented in moduledoc.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/github_issue_importer_test.exs --max-failures=1</automated>
  </verify>
  <done>Importer tested without live GitHub.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | I | URL builder | mitigate | Parse slug/url; never `String.to_atom` on user input |
| T-08-03 | E | GitHub API abuse | mitigate | Req timeouts; surface 429 copy to operator |
</threat_model>

<verification>
mix test test/kiln/specs/github_issue_importer_test.exs
</verification>

<success_criteria>
GitHub issue → draft path implemented and unit-tested.
</success_criteria>

<output>
After completion, create `08-02-SUMMARY.md` in this phase directory.
</output>
