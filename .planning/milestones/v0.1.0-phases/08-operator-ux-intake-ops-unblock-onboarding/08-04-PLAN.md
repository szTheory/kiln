---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "04"
type: execute
wave: 4
depends_on:
  - "01"
  - "03"
files_modified:
  - lib/kiln/specs.ex
  - lib/kiln/external_operations.ex
  - lib/kiln_web/live/run_detail_live.ex
  - test/kiln/specs/follow_up_draft_test.exs
  - test/kiln_web/live/run_detail_live_test.exs
autonomous: true
requirements:
  - INTAKE-03
must_haves:
  truths:
    - "Run detail shows **File as follow-up** for eligible runs (define: `state == :merged` in plan code comments if that is the gate)"
    - "Duplicate click returns same draft id via `external_operations` intent key `follow_up_draft:run_id:{correlation_id}`"
---

<objective>
Implement **INTAKE-03**: **`File as follow-up`** on **`RunDetailLive`** creates inbox draft with **`source_run_id`**, artifact refs, **`operator_summary`** per D-816; **`external_operations`** idempotency per D-817 + audit `follow_up_drafted` in same transaction.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln/external_operations.ex
@lib/kiln_web/live/run_detail_live.ex
@lib/kiln/audit.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Specs.file_follow_up_draft/2</name>
  <read_first>
    - lib/kiln/external_operations.ex
    - lib/kiln/specs.ex
    - lib/kiln/artifacts.ex
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "follow_up_draft" lib/kiln/specs.ex` matches
    - `grep -n "ExternalOperations" lib/kiln/specs.ex` matches
    - `mix test test/kiln/specs/follow_up_draft_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/specs.ex, test/kiln/specs/follow_up_draft_test.exs</files>
  <action>
Add **`file_follow_up_from_run/2`** taking `%Run{}` + optional correlation id (default `Ecto.UUID.generate/0`). Build intent key **`"follow_up_draft:" <> run.id <> ":" <> correlation`**. If intent completed, return **`{:ok, existing_draft}`**. Else insert draft with **lazy payload** fields + **`Audit.Event`** in same `Repo.transaction` as intent row. Store **`source_run_id`** on draft (add column in new migration if not in plan 01 — if plan 01 missed it, add follow-up migration in this plan).
  </action>
  <verify>
    <automated>mix test test/kiln/specs/follow_up_draft_test.exs --max-failures=1</automated>
  </verify>
  <done>Domain idempotency proven by tests.</done>
</task>

<task type="auto">
  <name>Task 2: RunDetailLive button + event</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - .planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "File as follow-up" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "phx-click=\"follow_up\"" lib/kiln_web/live/run_detail_live.ex` OR `phx-click="follow_up"` matches
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
Render button only when run matches product rule (use **`run.state == :merged`** unless product says otherwise — document in `@moduledoc`). **`handle_event("follow_up", _, socket)`** calls domain function; flash **"Draft created"**; link to **`~p"/inbox"`** in flash or inline. Extend existing **`run_detail_live_test`** with merged run fixture.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Operator path from run → inbox draft.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | I | artifact refs in draft | mitigate | Store CAS keys only; no secret literals |
</threat_model>

<verification>
mix test test/kiln/specs/follow_up_draft_test.exs test/kiln_web/live/run_detail_live_test.exs
</verification>

<success_criteria>
Follow-up filing idempotent + visible on run detail + tests green.
</success_criteria>

<output>
After completion, create `08-04-SUMMARY.md` in this phase directory.
</output>
