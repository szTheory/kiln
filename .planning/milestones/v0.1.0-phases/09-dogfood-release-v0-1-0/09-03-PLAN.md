---
phase: 09-dogfood-release-v0-1-0
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - test/integration/first_run.sh
  - .env.sample
  - test/kiln_web/live/onboarding_live_test.exs
autonomous: true
requirements:
  - LOCAL-03
must_haves:
  truths:
    - "README happy path lists: prerequisites → env → `docker compose up -d db` → `KILN_DB_ROLE=kiln_owner mix setup` → `mix phx.server` → open `/onboarding` first (D-931)"
    - "`first_run.sh` header documents same prerequisite assumptions as README (D-932)"
  artifacts:
    - path: "README.md"
      provides: "Zero-to-first-run"
      contains: "onboarding"
---

<objective>
Close **LOCAL-03** / ROADMAP **SC4** documentation: align **`README.md`** with **`test/integration/first_run.sh`** and **Phase 8 onboarding** routes (CONTEXT D-931–D-934). No false claims about unattended `gh auth` or vendor keys — explicit **human vs automated** matrix.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
@README.md
@test/integration/first_run.sh
@.env.sample
@config/runtime.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: README structure rewrite</name>
  <read_first>
    - README.md
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
    - .planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "/onboarding" README.md` matches in **Open first** or numbered happy-path section
    - `grep -n "KILN_DB_ROLE=kiln_owner mix setup" README.md` matches
    - `grep -n "docker compose" README.md` matches
  </acceptance_criteria>
  <files>README.md</files>
  <action>
Restructure README into: **Prerequisites** (Elixir/OTP versions from `.tool-versions` or `mix.exs`, Docker, optional asdf); **Quick start** numbered steps matching **D-931**; **Environment** pointer to `.env.sample` + `KILN_DB_ROLE` one-paragraph explanation linking **`config/runtime.exs`**; **Human-required steps** subsection listing API keys / `gh auth login` if applicable (D-934); **CI** subsection pointing at badge from Plan 01.
  </action>
  <verify>
    <automated>test -f README.md && wc -l README.md | awk '$1>=40 {exit 0} {exit 1}'</automated>
  </verify>
  <done>README is skimmable and route-accurate.</done>
</task>

<task type="auto">
  <name>Task 2: first_run.sh ↔ README parity</name>
  <read_first>
    - test/integration/first_run.sh
    - README.md
  </read_first>
  <acceptance_criteria>
    - `head -n 20 test/integration/first_run.sh` contains comment block referencing README alignment
    - `grep -n "asdf" test/integration/first_run.sh` matches if README mentions asdf (or both omit asdf with explicit “Elixir on PATH” wording)
  </acceptance_criteria>
  <files>test/integration/first_run.sh</files>
  <action>
Update header comments (lines 1–20) to state **exact prerequisite contract**: whether script assumes **asdf** or only **`mix` on PATH** — must **match** README wording (D-932). If README says “install Elixir 1.19.x per `.tool-versions`”, script comments must not claim `asdf install` unless script actually runs it.
  </action>
  <verify>
    <automated>bash -n test/integration/first_run.sh</automated>
  </verify>
  <done>Script header is honest.</done>
</task>

<task type="auto">
  <name>Task 3: .env.sample drift check</name>
  <read_first>
    - .env.sample
    - config/runtime.exs
  </read_first>
  <acceptance_criteria>
    - `grep -n "DATABASE_URL" .env.sample` matches
    - `grep -n "SECRET_KEY_BASE" .env.sample` matches OR documents generation via `mix phx.gen.secret`
  </acceptance_criteria>
  <files>.env.sample</files>
  <action>
Ensure **every** `System.get_env` **required for dev boot** appears in **`.env.sample`** with placeholder or comment — cross-check `config/runtime.exs` for `raise` on missing vars. Add **OTEL_** vars as comments only (optional dev) per Plan 02.
  </action>
  <verify>
    <automated>grep -c "=" .env.sample | awk '{exit ($1>=3 ? 0 : 1)}'</automated>
  </verify>
  <done>Sample env covers boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator ↔ README | README must not instruct copying real secrets into git |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | I | .env.sample | mitigate | Placeholders only; link to secret storage pattern |
</threat_model>

<verification>
bash -n test/integration/first_run.sh && mix test test/kiln_web/live/onboarding_live_test.exs --max-failures=1
</verification>

<success_criteria>
A new engineer can follow README to `/onboarding` without contradicting `first_run.sh` comments.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dogfood-release-v0-1-0/09-03-SUMMARY.md`
</output>
