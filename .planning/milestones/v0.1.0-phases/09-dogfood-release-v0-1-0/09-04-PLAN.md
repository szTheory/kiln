---
phase: 09-dogfood-release-v0-1-0
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - dogfood/spec.md
  - lib/kiln/github/dogfood.ex
  - lib/kiln/workers/dogfood_pr_worker.ex
  - lib/kiln_web/live/inbox_live.ex
  - lib/kiln_web/live/spec_editor_live.ex
  - config/runtime.exs
  - test/kiln/github/dogfood_test.exs
autonomous: false
requirements:
  - GIT-04
  - OBS-02
  - LOCAL-03
must_haves:
  truths:
    - "Checked-in `dogfood/spec.md` is canonical template bytes (D-901)"
    - "GitHub PR automation uses `external_operations` + Oban unique for mutating calls (D-903)"
    - "Path allowlist prevents silent workflow edits (D-904)"
  artifacts:
    - path: "dogfood/spec.md"
      provides: "Golden dogfood story"
      contains: "Kiln.Version"
---

<objective>
Prove **ROADMAP SC1** / CONTEXT **D-901–D-905**: operator loads **canonical** `dogfood/spec.md` from the **Kiln UI**, starts run; automation opens / updates **one** dogfood PR with **label idempotency**, respects **path allowlist**, enables **auto-merge** when **`mix check`** green; **GIT-04 + OBS-02 + LOCAL-03** validated in concert. **`autonomous: false`** because GitHub App / token setup needs operator secrets.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
@.planning/phases/09-dogfood-release-v0-1-0/09-UI-SPEC.md
@.planning/phases/06-github-integration/06-CONTEXT.md
@lib/kiln/github
@lib/kiln/application.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canonical dogfood spec artifact</name>
  <read_first>
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f dogfood/spec.md` exits 0
    - `grep -n "Kiln.Version" dogfood/spec.md` matches OR equivalent low-contention module name per D-905
    - `grep -n "mix check" dogfood/spec.md` matches as acceptance bar in spec text
  </acceptance_criteria>
  <files>dogfood/spec.md</files>
  <action>
Create **`dogfood/spec.md`** — markdown spec body describing **smallest shippable change** (example: add **`Kiln.Version`** returning **`Application.spec(:kiln, :vsn)`** or **`Mix.Project.config[:version]`** via compile-time embed — **pick one concrete approach** in spec text), **files touched allowlist** (`lib/kiln/version.ex`, `test/kiln/version_test.exs`, optional `README` line), and **verification** = **`mix check`**. Keep under **~80 lines**.
  </action>
  <verify>
    <automated>wc -l dogfood/spec.md | awk '$1<=120 {exit 0} {exit 1}'</automated>
  </verify>
  <done>Golden spec in git.</done>
</task>

<task type="auto">
  <name>Task 2: UI — load dogfood template</name>
  <read_first>
    - lib/kiln_web/live/inbox_live.ex
    - lib/kiln_web/live/spec_editor_live.ex
    - lib/kiln_web/router.ex
    - .planning/phases/09-dogfood-release-v0-1-0/09-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -Rin "dogfood" lib/kiln_web/live/` matches at least one file
    - `grep -Rin "spec.md" lib/kiln_web/live/` matches in `lib/kiln_web/live/` tree
    - `mix test test/kiln_web/live/` includes at least one new or updated test file asserting template load event (path adjusted to actual test file created)
  </acceptance_criteria>
  <files>lib/kiln_web/live/inbox_live.ex, lib/kiln_web/live/spec_editor_live.ex, test/kiln_web/live/dogfood_template_test.exs</files>
  <action>
Add **`phx-click` handler** on **`InboxLive`** or **`SpecEditorLive`** (pick the surface that owns draft body per router) — label **“Load dogfood template”** — that reads **`dogfood/spec.md`** via **`Application.app_dir(:kiln, "priv/dogfood/spec.md")`** after adding **`priv/dogfood/spec.md`** with **`{:embed_text, ...}`** in **`mix.exs`** **OR** reads repo root in dev only via **`Path.join([File.cwd!(), "dogfood/spec.md"])`** guarded by **`Mix.env() == :dev`** — **executor must choose one documented strategy** so **release** and **CI tests** see identical bytes (D-901). **Never** fetch remote URL for canonical bytes.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/dogfood_template_test.exs --max-failures=1</automated>
  </verify>
  <done>Operator can populate draft from template in LV test.</done>
</task>

<task type="manual">
  <name>Task 3: GitHub credentials wiring</name>
  <read_first>
    - lib/kiln/github
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "external_operations" lib/kiln/github/dogfood.ex` matches (or module name chosen by implementer under `lib/kiln/github/`)
    - `grep -n "kiln/dogfood" lib/` matches for branch prefix convention
    - Document in **SUMMARY** which secret names were added to **`runtime.exs` / releases** (values never in repo)
  </acceptance_criteria>
  <files>lib/kiln/github/dogfood.ex, lib/kiln/workers/dogfood_pr_worker.ex, config/runtime.exs</files>
  <action>
Implement **`Kiln.GitHub.Dogfood`** (name adjustable) orchestrating: compute **spec content hash**; **lookup open PR** by label **`kiln-dogfood:<hash>`**; if absent, **create branch `kiln/dogfood/<hash>-<short_id>`**, push commit(s) respecting **path allowlist** (reject if diff touches `.github/workflows/` unless spec metadata allows); **open PR**; **apply label**; call **`POST /repos/.../pulls/{id}/merge` auto-merge** only when **required check** = existing **`mix check`** job green. **Every** GitHub mutation preceded by **`external_operations`** intent row + **Oban unique** key per D-903. **Operator** must supply **GitHub App ID / installation / key** or **fine-grained PAT** via env — document exact var names.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Code compiles; live merge requires operator credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kiln ↔ GitHub | Tokens only via env / `persistent_term` refs — never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | E | GitHub API | mitigate | Rate-limit + idempotent Oban jobs |
| T-09-07 | T | workflow path | mitigate | Allowlist denies `.github/workflows/` by default (D-904) |
| T-09-08 | I | PR body | mitigate | No secrets in PR template; redact env dumps |
</threat_model>

<verification>
mix check && mix test test/kiln/github/dogfood_test.exs --max-failures=1
</verification>

<success_criteria>
With secrets configured, one dogfood run produces a single auto-merged PR that passes **`mix check`**; Jaeger shows correlated trace (ties to Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/09-dogfood-release-v0-1-0/09-04-SUMMARY.md`
</output>
