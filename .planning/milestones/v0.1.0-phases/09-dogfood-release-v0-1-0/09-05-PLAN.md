---
phase: 09-dogfood-release-v0-1-0
plan: "05"
type: execute
wave: 3
depends_on:
  - "04"
files_modified:
  - CHANGELOG.md
  - LICENSE
  - README.md
  - NOTICE
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - GIT-04
  - LOCAL-03
must_haves:
  truths:
    - "`CHANGELOG.md` has `[0.1.0]` with REQ-ID coverage for GIT-04, OBS-02, LOCAL-03 (D-920)"
    - "`LICENSE` is Apache-2.0 full text unless legal override recorded in SUMMARY (D-922)"
    - "Annotated tag `v0.1.0` on merge commit after CHANGELOG final (D-924)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Release notes"
      contains: "0.1.0"
---

<objective>
Finish **release mechanics** after dogfood merge: **Keep a Changelog** entry **0.1.0** (D-920), **Apache-2.0 `LICENSE`** + **`NOTICE`** if required by deps (D-922), README **license pointer** update, **GitHub Release** body from changelog (D-921), **annotated tag `v0.1.0`** pushed so Plan **01** tag CI passes (D-923–D-924). **`autonomous: false`** — human runs `git tag` / `gh release` with review.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
@README.md
@mix.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: CHANGELOG.md initial 0.1.0</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f CHANGELOG.md` exits 0
    - `grep -nF "[0.1.0]" CHANGELOG.md` matches (Keep a Changelog section header)
    - `grep -n "GIT-04" CHANGELOG.md` matches AND `grep -n "OBS-02" CHANGELOG.md` matches AND `grep -n "LOCAL-03" CHANGELOG.md` matches
  </acceptance_criteria>
  <files>CHANGELOG.md</files>
  <action>
Create **`CHANGELOG.md`** per **Keep a Changelog** 1.1.0 with sections **`[Unreleased]`** and **`[0.1.0] - YYYY-MM-DD`**. Under **0.1.0**, include **bullet narrative** of shipped milestone plus explicit **sub-list** of **GIT-04, OBS-02, LOCAL-03** and pointer “maps **55** v1 REQ-IDs across phases 1–9” (adjust count if REQUIREMENTS.md differs — **sync to REQUIREMENTS** table).
  </action>
  <verify>
    <automated>grep -c "GIT-04" CHANGELOG.md | awk '{exit ($1>=1 ? 0 : 1)}'</automated>
  </verify>
  <done>Changelog documents v0.1.0.</done>
</task>

<task type="auto">
  <name>Task 2: LICENSE + README pointer</name>
  <read_first>
    - README.md
    - mix.exs
  </read_first>
  <acceptance_criteria>
    - `test -f LICENSE` exits 0
    - `head -n 5 LICENSE` contains "Apache" case-insensitive
    - `grep -n "LICENSE" README.md` matches
  </acceptance_criteria>
  <files>LICENSE, NOTICE, README.md</files>
  <action>
Add **Apache License 2.0** full **`LICENSE`** text (use **SPDX** standard header block). If **`mix licenses`** or manual audit shows Apache-2.0 components needing attribution, add minimal **`NOTICE`**. Update **README** “License” section: **Apache-2.0**, link to **`LICENSE`** file path in repo.
  </action>
  <verify>
    <automated>wc -l LICENSE | awk '$1>=150 {exit 0} {exit 1}'</automated>
  </verify>
  <done>License is concrete, not TBD.</done>
</task>

<task type="manual">
  <name>Task 3: Tag + GitHub Release</name>
  <read_first>
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
    - .github/workflows/ci.yml
  </read_first>
  <acceptance_criteria>
    - `git tag -l v0.1.0` shows tag after operator runs documented commands (verify locally post-hoc)
    - GitHub **Releases** page shows **v0.1.0** (manual verification URL in SUMMARY)
  </acceptance_criteria>
  <files>n/a</files>
  <action>
**Operator steps (document verbatim in SUMMARY):** (1) Confirm **`mix.exs` `:version` is `0.1.0`**. (2) **`git pull`** clean `main` with merged dogfood. (3) **`git tag -a v0.1.0 -m "v0.1.0"`** then **`git push origin v0.1.0`**. (4) Wait **tag-check** CI green. (5) **`gh release create v0.1.0 --title "v0.1.0" --notes-file CHANGELOG.md`** (or paste **`[0.1.0]`** section only). (6) Update **`.planning/ROADMAP.md`** Phase 9 row to **Complete** with date if process allows.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Release artifacts exist on GitHub.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legal ↔ repo | LICENSE text is canonical; changelog is operator-facing truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | R | tag | mitigate | Tag only from protected `main`; GPG optional follow-up |
</threat_model>

<verification>
mix check
</verification>

<success_criteria>
`v0.1.0` tag on GitHub; Release notes match CHANGELOG; CI tag gate green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dogfood-release-v0-1-0/09-05-SUMMARY.md`
</output>
