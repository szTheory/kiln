---
phase: 09-dogfood-release-v0-1-0
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - script/verify_tag_version.sh
autonomous: true
requirements:
  - GIT-04
must_haves:
  truths:
    - "`mix check` runs on every push and PR to `main` (existing) plus tag push job validates version"
    - "README shows a CI status badge for the default workflow"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Tag-aware CI gate"
      contains: "push.tags"
---

<objective>
Extend **Kiln’s own GitHub Actions** so **GIT-04** is satisfied for **tag releases** (ROADMAP SC2 + CONTEXT D-923): keep existing PR/push `mix check`, add **`on.push.tags`** (`v*`) job that **fails** when the pushed tag (strip leading `v`) ≠ **`Mix.Project.config()[:version]`** in `mix.exs` (currently `0.1.0`). Add **README CI badge** pointing at the same workflow.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
@.planning/phases/09-dogfood-release-v0-1-0/09-RESEARCH.md
@.github/workflows/ci.yml
@mix.exs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tag ↔ mix.exs version verifier script</name>
  <read_first>
    - mix.exs
    - .github/workflows/ci.yml
  </read_first>
  <acceptance_criteria>
    - `test -f script/verify_tag_version.sh` exits 0
    - `grep -n "Mix.Project" script/verify_tag_version.sh` OR `grep -n "mix.exs" script/verify_tag_version.sh` matches
    - `bash script/verify_tag_version.sh 0.1.0 v0.1.0` exits 0 (exact interface as implemented — document in script header)
  </acceptance_criteria>
  <files>script/verify_tag_version.sh</files>
  <action>
Add **`script/verify_tag_version.sh`** (bash, `set -euo pipefail`) that reads **`version:`** from `mix.exs` via **`mix run -e 'IO.puts(Mix.Project.config()[:version])'`** (or robust `grep` of `version:` in `def project`) and compares to **`${GITHUB_REF_NAME#v}`** when `GITHUB_REF_NAME` is like `v0.1.0`, OR accepts **first arg = expected version**, **second arg = tag** for local testing. Exit **1** on mismatch with stderr explaining both values.
  </action>
  <verify>
    <automated>bash -n script/verify_tag_version.sh</automated>
  </verify>
  <done>Script exists and documents local invocation.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] ci.yml — tags + version gate job</name>
  <read_first>
    - .github/workflows/ci.yml
    - script/verify_tag_version.sh
  </read_first>
  <acceptance_criteria>
    - `grep -n "tags:" .github/workflows/ci.yml` matches for `v*` under `on.push` or top-level `on` map
    - `grep -n "verify_tag_version" .github/workflows/ci.yml` matches OR job step runs equivalent `mix` one-liner comparing tag to mix version
    - `grep -n "workflow_dispatch" .github/workflows/ci.yml` is optional; file must still contain existing `check` job for branches
  </acceptance_criteria>
  <files>.github/workflows/ci.yml</files>
  <action>
Under **`on:`**, add **`push.tags: ['v*']`** (array form). Add job **`tag-check`** (name up to implementer) on **`ubuntu-24.04`** that **checks out**, sets up BEAM **same versions as existing job** (`1.19.5-otp-28` / `28.1.2`), runs **`bash script/verify_tag_version.sh`** with env **`GITHUB_REF_NAME`** (document expected env in workflow comment). **Do not** duplicate full `mix check` on every tag unless desired — **minimum** is version consistency per D-923; optional second step `mix deps.get && mix compile --warnings-as-errors` is acceptable cost.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Tag pushes are validated in CI.</done>
</task>

<task type="auto">
  <name>Task 3: README CI badge</name>
  <read_first>
    - README.md
    - .github/workflows/ci.yml
  </read_first>
  <acceptance_criteria>
    - `grep -n "github.com/szTheory/kiln/actions" README.md` matches (badge URL contains repo + workflow)
    - `grep -n "CI" README.md | head -5` shows badge line near top or in documented **Badges** section
  </acceptance_criteria>
  <files>README.md</files>
  <action>
Insert a **Markdown shield or official GitHub workflow badge** for workflow **`ci.yml`** (workflow filename must match `name:`/`file` — use **`CI`** workflow from `.github/workflows/ci.yml`). Link to **`https://github.com/szTheory/kiln/actions/workflows/ci.yml`**.
  </action>
  <verify>
    <automated>grep -q "szTheory/kiln" README.md</automated>
  </verify>
  <done>Badge visible on default README render.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub ↔ workflow | Tag job runs only on `v*`; no untrusted input to `mix` beyond ref name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | tag spoof | mitigate | Compare only `GITHUB_REF_NAME` from Actions; document release operator uses annotated tags |
| T-09-02 | I | workflow YAML | mitigate | No secrets in YAML; use `GITHUB_TOKEN` default read where needed only |
</threat_model>

<verification>
mix compile --warnings-as-errors && mix check
</verification>

<success_criteria>
Tag `v0.1.0` would fail CI if `mix.exs` version drifts; README shows green CI badge for the canonical workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/09-dogfood-release-v0-1-0/09-01-SUMMARY.md`
</output>
