---
phase: operator-demo-vs-live-mode-and-provider-readiness-ux
plan: "02"
type: execute
wave: 2
depends_on:
  - "999.2-01-PLAN.md"
files_modified:
  - lib/kiln_web/live/operator_chrome_hook.ex
  - lib/kiln_web/router.ex
  - test/kiln_web/live/operator_chrome_hook_test.exs
autonomous: true
requirements: []
milestone: v0.3.0
must_haves:
  truths:
    - "`OperatorChromeHook` assigns `:operator_runtime_mode` and `:operator_snapshots` on every `live_session :default` view"
    - "`router.ex` lists `{KilnWeb.OperatorChromeHook, :default}` after `FactorySummaryHook`"
  artifacts:
    - path: "lib/kiln_web/live/operator_chrome_hook.ex"
      provides: "on_mount hook for operator chrome assigns"
      contains: "def on_mount"
    - path: "lib/kiln_web/router.ex"
      provides: "hook registration"
      contains: "OperatorChromeHook"
  key_links:
    - from: "router.ex"
      to: "operator_chrome_hook.ex"
      via: "on_mount tuple"
      pattern: "LiveView session hooks"
---

<objective>
Add **`KilnWeb.OperatorChromeHook`** to the default **`live_session`**: keep **`@operator_runtime_mode`** in sync with **`Kiln.OperatorRuntime.mode/0`** and refresh **`@operator_snapshots`** from **`Kiln.ModelRegistry.provider_health_snapshots/0`** on a **5_000 ms** timer (same order of magnitude as **`ProviderHealthLive`**).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-RESEARCH.md
@.planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-PATTERNS.md
@lib/kiln_web/live/factory_summary_hook.ex
@lib/kiln_web/live/provider_health_live.ex
@lib/kiln/model_registry.ex
@lib/kiln_web/router.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Hook ↔ ModelRegistry | Read-only snapshots; no persistence |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-999.2-02 | I | mitigate | Snapshots are **aggregates** only — same contract as **`ProviderHealthLive`** |
| T-999.2-03 | DoS | accept | 5s timer per LiveView process — acceptable for solo v1 operator shell |

## Notes

Do not add new **`String.to_atom/1`** on user input in this hook.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Implement `KilnWeb.OperatorChromeHook`</name>
  <read_first>
    - lib/kiln_web/live/factory_summary_hook.ex
    - lib/kiln_web/live/provider_health_live.ex
    - lib/kiln/operator_runtime.ex
    - lib/kiln/model_registry.ex
  </read_first>
  <acceptance_criteria>
    - `test -f lib/kiln_web/live/operator_chrome_hook.ex` exits 0
    - `grep -q 'def on_mount(:default' lib/kiln_web/live/operator_chrome_hook.ex` exits 0
    - `grep -q ':operator_snapshots' lib/kiln_web/live/operator_chrome_hook.ex` exits 0
    - `grep -q ':operator_runtime_mode' lib/kiln_web/live/operator_chrome_hook.ex` exits 0
    - `grep -q 'provider_health_snapshots' lib/kiln_web/live/operator_chrome_hook.ex` exits 0
    - `grep -q 'OperatorRuntime.mode' lib/kiln_web/live/operator_chrome_hook.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/operator_chrome_hook.ex</files>
  <action>
Create **`lib/kiln_web/live/operator_chrome_hook.ex`** with **`@moduledoc`** referencing phase **999.2** / **OPS-01** composition.

Implement **`on_mount(:default, _params, _session, socket)`** returning **`{:cont, socket}`** after:

1. **`assign(socket, :operator_runtime_mode, Kiln.OperatorRuntime.mode())`**.
2. **`assign(socket, :operator_snapshots, Kiln.ModelRegistry.provider_health_snapshots())`**.
3. When **`connected?(socket)`**, **`Process.send_after(self(), :operator_chrome_tick, 5_000)`** (constant **`@poll_ms 5_000`** module attribute).

Use **`attach_hook(socket, :operator_chrome, :handle_info, fn msg, sock -> … end)`** where:

- On **`:operator_chrome_tick`**, refresh **both** assigns (call **`OperatorRuntime.mode/0`** again + **`provider_health_snapshots/0`**), then schedule the next tick with **`Process.send_after(self(), :operator_chrome_tick, @poll_ms)`**.
- Other messages: **`{:cont, sock}`**.

**Private `handle_info` is not available on hooks** — implement entirely inside **`attach_hook`** callback (same pattern style as **`FactorySummaryHook`** but add tick message branch).

Initial tick: only when **`connected?`**.

Module alias: **`alias Kiln.OperatorRuntime`** and **`alias Kiln.ModelRegistry`** at top for clarity.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Hook module compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Register hook in `router.ex`</name>
  <read_first>
    - lib/kiln_web/router.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n 'OperatorChromeHook' lib/kiln_web/router.ex | head -1` prints a line number **strictly greater** than the line from `grep -n 'FactorySummaryHook' lib/kiln_web/router.ex | head -1`
    - `grep -q '{KilnWeb.OperatorChromeHook, :default}' lib/kiln_web/router.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex</files>
  <action>
In **`live_session :default, on_mount: […]`**, append **`{KilnWeb.OperatorChromeHook, :default}`** **after** **`{KilnWeb.FactorySummaryHook, :default}`** (ordering matters for predictable mount side effects).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Hook registered for all operator LiveViews.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 — LiveView smoke: hook handles tick without crash</name>
  <read_first>
    - test/kiln_web/live/provider_health_live_test.exs
    - lib/kiln_web/live/operator_chrome_hook.ex
  </read_first>
  <acceptance_criteria>
    - `test -f test/kiln_web/live/operator_chrome_hook_test.exs` exits 0
    - `mix test test/kiln_web/live/operator_chrome_hook_test.exs` exits 0
  </acceptance_criteria>
  <files>test/kiln_web/live/operator_chrome_hook_test.exs</files>
  <action>
Add **`test/kiln_web/live/operator_chrome_hook_test.exs`** using the same **`KilnWeb.ConnCase`** / import style as **`test/kiln_web/live/provider_health_live_test.exs`**.

1. **`{:ok, view, _html} = live(conn, ~p"/")`** ( **`RunBoardLive`** is in **`live_session :default`** with hooks).
2. Assert **`render(view)`** includes **`Kiln`** (existing shell marker).
3. **`send(view.pid, :operator_chrome_tick)`** then **`_ = render(view)`** — must not raise (hook **`attach_hook`** handles **`handle_info`**).
4. Optionally assert **`length(Kiln.ModelRegistry.provider_health_snapshots()) == 4`**.

Use **`async: false`** if the test mutates shared runtime state; otherwise follow sibling tests.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/operator_chrome_hook_test.exs</automated>
  </verify>
  <done>Hook smoke-tested.</done>
</task>

</tasks>
