---
phase: 999.2-operator-demo-vs-live-mode-and-provider-readiness-ux
plan: "03"
type: execute
wave: 3
depends_on:
  - "999.2-02-PLAN.md"
files_modified:
  - lib/kiln_web.ex
  - lib/kiln_web/components/layouts.ex
  - lib/kiln_web/components/operator_chrome.ex
  - lib/kiln_web/live/run_board_live.ex
  - lib/kiln_web/live/run_compare_live.ex
  - lib/kiln_web/live/run_detail_live.ex
  - lib/kiln_web/live/provider_health_live.ex
  - lib/kiln_web/live/cost_live.ex
  - lib/kiln_web/live/audit_live.ex
  - lib/kiln_web/live/inbox_live.ex
  - lib/kiln_web/live/onboarding_live.ex
  - lib/kiln_web/live/workflow_live.ex
  - lib/kiln_web/live/spec_editor_live.html.heex
  - test/kiln_web/live/operator_chrome_live_test.exs
autonomous: true
requirements: []
milestone: v0.3.0
must_haves:
  truths:
    - "`Layouts.app` renders **`id=\"operator-mode-chip\"`**, **`id=\"operator-provider-readiness\"`**, **`id=\"operator-config-presence\"`** when assigns are present (stable ids from UI-SPEC)"
    - "Every operator **`Layouts.app` invocation passes `operator_runtime_mode` and `operator_snapshots` from the LiveView assigns populated by `OperatorChromeHook`**"
  artifacts:
    - path: "lib/kiln_web/components/operator_chrome.ex"
      provides: "Mode chip + readiness + config presence function components"
      contains: "operator_mode_chip"
    - path: "lib/kiln_web/components/layouts.ex"
      provides: "Shell integration"
      contains: "operator-mode-chip"
  key_links:
    - from: "layouts.ex"
      to: "operator_chrome.ex"
      via: "function components"
      pattern: "Phase 07 header composition"
---

<objective>
Render **999.2 UI-SPEC chrome** in **`Layouts.app`**: mode chip (demo/live/unknown), **optional** provider readiness strip (live degradation + link **`~p"/providers"`**), and **config presence** row (**names only**). Wire **`@operator_runtime_mode`** and **`@operator_snapshots`** through every operator **`Layouts.app`** call site. Add LiveView tests for stable ids + canonical copy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-UI-SPEC.md
@.planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-PATTERNS.md
@lib/kiln_web/components/layouts.ex
@lib/kiln_web/live/provider_health_live.ex
</context>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ HEEx | Only snapshot **fields** already approved for **`ProviderHealthLive`** |

## STRIDE (ASVS L1)

| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-999.2-10 | I | mitigate | **Never** render API keys, env secret values, or `Inspect` of secret structs — **provider id + configured boolean only** |
| T-999.2-11 | T | mitigate | Links use **`~p"/providers"`** (verified route) — no string-built URLs from user input |

## Notes

Copy must match **999.2-UI-SPEC.md** tables verbatim (allow punctuation only if Elixir string escaping requires it).
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 — Add `KilnWeb.Components.OperatorChrome`</name>
  <read_first>
    - .planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-UI-SPEC.md
    - lib/kiln_web/components/factory_header.ex
    - lib/kiln_web/live/provider_health_live.ex
  </read_first>
  <acceptance_criteria>
    - `test -f lib/kiln_web/components/operator_chrome.ex` exits 0
    - `grep -q 'def operator_mode_chip' lib/kiln_web/components/operator_chrome.ex` exits 0
    - `grep -q 'operator-mode-chip' lib/kiln_web/components/operator_chrome.ex` exits 0
    - `grep -q 'operator-provider-readiness' lib/kiln_web/components/operator_chrome.ex` exits 0
    - `grep -q 'operator-config-presence' lib/kiln_web/components/operator_chrome.ex` exits 0
    - `grep -q 'Open provider health' lib/kiln_web/components/operator_chrome.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/components/operator_chrome.ex</files>
  <action>
Create **`lib/kiln_web/components/operator_chrome.ex`** with **`use KilnWeb, :html`** (match sibling components).

Implement function components:

1. **`operator_mode_chip/1`** — attrs: **`mode`** (`:demo | :live | :unknown`). Render **`id="operator-mode-chip"`**, **`aria-label`** per UI-SPEC pattern (mode name + short consequence). Titles/supporting lines **exact strings** from UI-SPEC table ("Live", "Runtime credentials apply; external APIs may incur cost.", "Demo", "Outcomes use fixtures and stubs; no paid provider calls."). **Unknown** uses UI-SPEC **Runtime mode unavailable** block (heading + body + link **`Open provider health`** → **`<.link navigate={~p"/providers"}>`**).

2. **`operator_config_presence/1`** — attrs: **`snapshots`** (list). Wrapper **`id="operator-config-presence"`**, section label **`Providers`**. For each snapshot, display **`{display_name}: configured`** or **`{display_name}: not configured`** where **`display_name`** is a **capitalized human label** (`:anthropic` → **`Anthropic`**, `:openai` → **`OpenAI`**, `:google` → **`Google`**, `:ollama` → **`Ollama`**) derived from **`to_string(snap.id)`** + **`String.capitalize/1`** rules you document in a **private** `display_name/1` helper — **do not** show secret material.

3. **`operator_provider_readiness/1`** — attrs: **`mode`**, **`snapshots`**. Wrapper **`id="operator-provider-readiness"`**, **`role="status"`**.  
   - If **`mode == :demo`**: if any snapshot has **`recent_error_rate >= 0.5`** or **`!key_configured?`**, show **Demo provider offline** title/body from UI-SPEC; else render **empty** (`<% %>` nothing).  
   - If **`mode == :live`**: if any **`!key_configured?`**, show **Credential not configured** copy; else if any **`recent_error_rate >= 0.5`**, show **Provider not reachable** copy; else render **empty** (UI-SPEC prefers omit when healthy).  
   - If **`mode == :unknown`**: render nothing here (mode chip already covers error path).

   Each non-empty readiness body includes **exactly one** primary CTA link labeled **`Open provider health`** to **`~p"/providers"`** with Tailwind classes that reserve **Ember** only for this link per UI-SPEC.

4. Optional compositor **`operator_chrome/1`** taking **`mode`** + **`snapshots`** that lays out chip + readiness + config in a **single flex column** inside the header region **above** **`FactoryHeader`** row (match IA: left of / above factory summary band — implement as a new **`div`** in **`layouts.ex`** between the navbar row and the **`FactoryHeader`** **`div`**).

**Icons:** use **`<.icon name="hero-…">`** only (pick one appropriate hero icon for degraded state if needed; optional).

**No** `Phoenix.HTML.raw/1` on external strings.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Operator chrome components compile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 — Integrate into `Layouts.app` + import</name>
  <read_first>
    - lib/kiln_web/components/layouts.ex
    - lib/kiln_web.ex
    - lib/kiln_web/components/operator_chrome.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'import KilnWeb.Components.OperatorChrome' lib/kiln_web.ex` exits 0
    - `grep -q 'attr :operator_runtime_mode' lib/kiln_web/components/layouts.ex` exits 0
    - `grep -q 'attr :operator_snapshots' lib/kiln_web/components/layouts.ex` exits 0
    - `grep -q 'operator_mode_chip' lib/kiln_web/components/layouts.ex` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web.ex, lib/kiln_web/components/layouts.ex</files>
  <action>
1. Add **`import KilnWeb.Components.OperatorChrome`** next to other **`KilnWeb.Components.*`** imports inside **`html_helpers/0`** in **`lib/kiln_web.ex`**.

2. In **`Layouts.app/1`** in **`layouts.ex`**, add **`attr :operator_runtime_mode, :atom, default: :unknown`** and **`attr :operator_snapshots, :list, default: []`**.

3. Insert a **`div`** with classes **`border-t border-ash/60 bg-iron/40 px-4 py-2 sm:px-6 lg:px-8`** (tune to match Phase 07 palette) **between** the **`navbar`** **`div`** closure and the **`FactoryHeader`** **`div`**, containing:

   **`<.operator_mode_chip mode={@operator_runtime_mode} />`**  
   readiness + config as siblings per your compositor layout (either call **`operator_chrome/1`** or the three components explicitly).

4. Ensure the **`div`** does **not** break the existing max-width grid: wrap with **`mx-auto max-w-7xl`**.

5. **`Layouts.app`** remains valid for call sites until they pass new assigns — **defaults** prevent compile errors, but plan task **3** updates call sites anyway.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Layout shell exposes chrome.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 — Pass assigns from all operator LiveViews</name>
  <read_first>
    - lib/kiln_web/components/layouts.ex
    - lib/kiln_web/live/run_board_live.ex
    - lib/kiln_web/live/spec_editor_live.html.heex
  </read_first>
  <acceptance_criteria>
    - `grep -R "Layouts.app" lib/kiln_web/live --include='*.ex' --include='*.heex' -l | while read f; do grep -q 'operator_runtime_mode' "$f" || exit 1; done` exits 0
    - `grep -R "Layouts.app" lib/kiln_web/live --include='*.ex' --include='*.heex' -l | while read f; do grep -q 'operator_snapshots' "$f" || exit 1; done` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/*.ex, lib/kiln_web/live/*.html.heex</files>
  <action>
Update **every** **`<Layouts.app …>`** in:

- **`lib/kiln_web/live/run_board_live.ex`**
- **`lib/kiln_web/live/run_compare_live.ex`**
- **`lib/kiln_web/live/run_detail_live.ex`**
- **`lib/kiln_web/live/provider_health_live.ex`**
- **`lib/kiln_web/live/cost_live.ex`**
- **`lib/kiln_web/live/audit_live.ex`**
- **`lib/kiln_web/live/inbox_live.ex`**
- **`lib/kiln_web/live/onboarding_live.ex`**
- **`lib/kiln_web/live/workflow_live.ex`**
- **`lib/kiln_web/live/spec_editor_live.html.heex`**

Append attributes:

**`operator_runtime_mode={@operator_runtime_mode}`**  
**`operator_snapshots={@operator_snapshots}`**

Keep existing **`flash`**, **`current_scope`**, **`factory_summary`** order consistent with **`AGENTS.md`** (**`Layouts.app` first** in templates).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>All operator pages pass chrome assigns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 — LiveView tests: ids + SEC-01 sanity</name>
  <read_first>
    - test/kiln_web/live/provider_health_live_test.exs
    - .planning/phases/999.2-operator-demo-vs-live-mode-and-provider-readiness-ux/999.2-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `test -f test/kiln_web/live/operator_chrome_live_test.exs` exits 0
    - `mix test test/kiln_web/live/operator_chrome_live_test.exs` exits 0
    - `! grep -qE 'sk-ant-|xoxb-|BEGIN RSA' test/kiln_web/live/operator_chrome_live_test.exs` exits 0
  </acceptance_criteria>
  <files>test/kiln_web/live/operator_chrome_live_test.exs</files>
  <action>
Add **`test/kiln_web/live/operator_chrome_live_test.exs`**:

1. With **`Application.put_env(:kiln, :operator_runtime_mode, :demo, persistent: false)`** in **`setup`** + restore on exit, **`live(conn, ~p"/")`**, assert **`render(view)`** contains **`id="operator-mode-chip"`** and the substring **`Demo`**.
2. Switch env to **`:live`** in a separate test (restore on exit), assert **`Live`** substring appears in **`render(view)`**.
3. Assert **`render(view)`** contains **`id="operator-config-presence"`** and **`Providers`** label.
4. Refute obvious secret patterns in HTML: **`refute render(view) =~ "sk-"`** and **`refute render(view) =~ "OPENAI_API_KEY"`** (string literals, not real secrets).

Use patterns consistent with **`provider_health_live_test.exs`** for auth / conn setup.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/operator_chrome_live_test.exs</automated>
  </verify>
  <done>Chrome covered by LiveView tests.</done>
</task>

</tasks>
