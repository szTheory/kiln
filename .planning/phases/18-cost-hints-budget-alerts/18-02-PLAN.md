---
phase: 18-cost-hints-budget-alerts
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/kiln/blockers/reason.ex
  - lib/kiln/blockers.ex
  - lib/kiln/budget_alerts.ex
  - lib/kiln/notifications.ex
  - lib/kiln/stages/stage_worker.ex
  - priv/playbooks/v1/budget_threshold_50.md
  - priv/playbooks/v1/budget_threshold_80.md
  - test/kiln/blockers/reason_test.exs
  - test/kiln/notifications_test.exs
autonomous: true
requirements:
  - COST-02
milestone: v0.3.0
must_haves:
  truths:
    - "Soft threshold desktop notifications use atoms distinct from `:budget_exceeded`"
    - "`raise_block/3` rejects soft-threshold atoms"
    - "Crossing path appends `budget_threshold_crossed` audit before optional desktop dispatch"
  artifacts:
    - path: "priv/playbooks/v1/budget_threshold_50.md"
      provides: "playbook body"
      contains: "The run continues"
    - path: "priv/playbooks/v1/budget_threshold_80.md"
      provides: "playbook body"
      contains: "The run continues"
  key_links:
    - from: "lib/kiln/budget_alerts.ex"
      to: "lib/kiln/audit.ex"
      via: "Audit.append"
      pattern: "append-only"
---

<objective>
Extend **`Kiln.Blockers.Reason`** with **two** soft-alert atoms (planner picks final names, e.g. **`:budget_threshold_50`** and **`:budget_threshold_80`**) plus **`blocking?/1`**; guard **`Kiln.Blockers.raise_block/3`** to **reject** non-blocking reasons. Add **`priv/playbooks/v1/<reason>.md`** with **D-1816** copy (**“The run continues.”**). Wire **`Kiln.BudgetAlerts.maybe_notify_for_run/1`** (or equivalent) that: evaluates crossings, **`Audit.append`** for each new crossing with **`event_kind: :budget_threshold_crossed`**, then **`Kiln.Notifications.desktop/2`** with **severity** `"info"` / `"warning"` — **only after** audit succeeds. Invoke from **`Kiln.Stages.StageWorker`** immediately after **`update_stage_run(... %{state: :succeeded})`** succeeds (and any future agent path that updates spend **must** call the same entrypoint). Broadcast **`Phoenix.PubSub.broadcast(Kiln.PubSub, "run:#{run_id}", {:budget_alert, payload})`** (tuple shape is planner’s choice) after successful crossing batch so **`RunDetailLive`** can refresh. Update **`test/kiln/blockers/reason_test.exs`** for new atom list length and **`raise_block`** guard test.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-cost-hints-budget-alerts/18-CONTEXT.md
@.planning/phases/18-cost-hints-budget-alerts/18-RESEARCH.md
@lib/kiln/blockers/reason.ex
@lib/kiln/blockers.ex
@lib/kiln/notifications.ex
@lib/kiln/stages/stage_worker.ex
@lib/kiln/budget_alerts.ex
@lib/kiln/audit.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reason + playbooks + raise_block guard</name>
  <read_first>
    - lib/kiln/blockers/reason.ex
    - lib/kiln/blockers.ex
    - lib/kiln/blockers/playbook_registry.ex
    - priv/playbooks/v1/budget_exceeded.md
  </read_first>
  <acceptance_criteria>
    - `grep -E ':budget_threshold_50|:budget_threshold_80' lib/kiln/blockers/reason.ex` finds both atoms (or the exact pair chosen in implementation)
    - `grep -n "def blocking?" lib/kiln/blockers/reason.ex` matches
    - `grep -n "blocking?" lib/kiln/blockers.ex` matches
    - `test/kiln/blockers/reason_test.exs` asserts `length(Reason.all()) ==` the new total (document integer in test)
    - `test -f priv/playbooks/v1/budget_threshold_50.md` AND `test -f priv/playbooks/v1/budget_threshold_80.md` exit 0
    - `grep -q 'The run continues' priv/playbooks/v1/budget_threshold_50.md` exits 0
  </acceptance_criteria>
  <files>lib/kiln/blockers/reason.ex, lib/kiln/blockers.ex, priv/playbooks/v1/budget_threshold_50.md, priv/playbooks/v1/budget_threshold_80.md, test/kiln/blockers/reason_test.exs</files>
  <action>
Implement **`Reason.blocking?/1`** returning **`true`** for the original nine halt reasons and **`false`** for the two new advisory atoms. In **`raise_block/3`**, **`unless Reason.blocking?(reason)`**, raise **`ArgumentError`** with message containing **`"non-blocking reason"`**. Extend **`@reasons`**, **`@type`**, and **`defguard is_reason`** to include the new atoms (keep ordering: **append**). Copy playbook YAML frontmatter style from **`budget_exceeded.md`** but body must reflect **soft advisory** per **18-UI-SPEC** copy table.
  </action>
  <verify>
    <automated>mix test test/kiln/blockers/reason_test.exs test/kiln/blockers/playbook_registry_test.exs --max-failures 1</automated>
  </verify>
  <done>Reason taxonomy + playbooks + guard.</done>
</task>

<task type="auto">
  <name>Task 2: Fire path — audit, desktop, PubSub, StageWorker hook</name>
  <read_first>
    - lib/kiln/budget_alerts.ex
    - lib/kiln/notifications.ex
    - lib/kiln/stages/stage_worker.ex
    - lib/kiln/audit.ex
    - test/kiln/notifications_test.exs
  </read_first>
  <acceptance_criteria>
    - `grep -n "BudgetAlerts" lib/kiln/stages/stage_worker.ex` matches
    - `grep -n "PubSub.broadcast" lib/kiln/budget_alerts.ex` OR `grep -n "PubSub.broadcast" lib/kiln/stages/stage_worker.ex` matches for `run:#`
    - `mix test test/kiln/notifications_test.exs test/kiln/stages/stage_worker_test.exs --max-failures 1` exits 0 (skip `stage_worker_test` if file missing — then run `mix test test/kiln/notifications_test.exs` only and add `stage_worker` test in same task)
  </acceptance_criteria>
  <files>lib/kiln/budget_alerts.ex, lib/kiln/stages/stage_worker.ex, test/kiln/notifications_test.exs</files>
  <action>
Extend **`BudgetAlerts`** with **`notify_run_if_needed/1`** (name flexible) that performs **DB-backed** crossing detection from Plan 01, **appends** `budget_threshold_crossed` with **JSV-valid** payload, then calls **`Notifications.desktop(reason, ctx)`** mapping **50% → `:budget_threshold_50`**, **80% → `:budget_threshold_80`** with **`ctx`** including **`:run_id`**, **`:severity`**, and playbook tokens for **spent/cap/pct**. Mirror **`BudgetGuard`** `maybe_notify` **ETS/repo presence** guard if present so minimal test apps do not crash (**D-1821**). After successful notification batch, **`Phoenix.PubSub.broadcast(Kiln.PubSub, "run:#{run_id}", tuple)`**.

From **`StageWorker`**, after **`{:ok, _} <- update_stage_run(stage_run_id, %{state: :succeeded})`**, call **`BudgetAlerts.notify_run_if_needed(run_id)`** (fire-and-forget **`Task.start`** only if justified by profiling — default **sync** inside Oban job is acceptable for v1).

Add/adjust tests: use **`Mox`** or existing notification test helpers to avoid real **`osascript`**; assert **`notification_fired`** or **`Audit.append`** kinds depending on existing patterns in **`notifications_test.exs`**.
  </action>
  <verify>
    <automated>mix test test/kiln/notifications_test.exs --max-failures 1</automated>
  </verify>
  <done>End-to-end soft alert fire path from stage success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop shell args | Only `inspect/1`-wrapped strings per `Notifications` contract |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | E | `raise_block` abuse with soft reason | mitigate | `blocking?/1` guard |
| T-18-04 | I | Playbook token injection | mitigate | Use `Blockers.render/2` token allow-list patterns |
</threat_model>

<verification>
mix test test/kiln/blockers/reason_test.exs test/kiln/notifications_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
Soft thresholds emit audit + optional desktop + PubSub; `:budget_exceeded` semantics unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cost-hints-budget-alerts/18-02-SUMMARY.md`
</output>

## PLANNING COMPLETE
