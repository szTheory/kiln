---
phase: 07-core-run-ui-liveview
plan: "04"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - priv/repo/migrations/*_workflow_definition_snapshots.exs
  - lib/kiln/workflows/snapshot.ex
  - lib/kiln/workflows.ex
  - lib/kiln/workflows/loader.ex
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/workflow_live.ex
  - test/kiln_web/live/workflow_live_test.exs
autonomous: true
requirements:
  - UI-03
must_haves:
  truths:
    - "`workflow_definition_snapshots` rows inserted on successful compile/load only"
    - "`WorkflowLive` shows read-only YAML + side list labeled per D-715 (Snapshots / Loaded definitions)"
    - "No `<.form>` that POSTs YAML mutations — anti-feature enforced"
  artifacts:
    - path: "lib/kiln_web/live/workflow_live.ex"
      provides: "Registry UI"
      contains: "WorkflowLive"
    - path: "lib/kiln/workflows/snapshot.ex"
      provides: "Snapshot schema"
      contains: "WorkflowDefinitionSnapshot"
  key_links:
    - from: "lib/kiln/workflows/loader.ex"
      to: "lib/kiln/workflows.ex"
      via: "insert_snapshot/1"
      pattern: "Post-success hook"
---

<objective>
Ship **UI-03 workflow registry**: **Postgres snapshot table** (D-716) storing **`workflow_id`**, **`version`**, **`compiled_checksum`**, **`inserted_at`**, **`yaml_body`** (text, bounded) **or** **`artifact_id`** nullable FK when size > threshold — **v1: inline text capped at 256 KiB** with overflow flag + checksum only if truncated. Hook **`Kiln.Workflows.Loader`** success path to **`Kiln.Workflows.record_snapshot/1`**. **`WorkflowLive`** at **`/workflows`** (index lists snapshots) and **`/workflows/:workflow_id`** (detail: YAML `<pre phx-no-curly-interpolation>` + snapshot list). Use **`stream_async/4`** when **`byte_size(yaml) > 65536`** for body fetch.

Output: migration, context API, LiveView, tests asserting **no** edit controls.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
@lib/kiln/workflows/loader.ex
@lib/kiln/workflows/compiled_graph.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + schema + record_snapshot/1</name>
  <read_first>
    - lib/kiln/workflows/compiled_graph.ex
    - lib/kiln/workflows/loader.ex
    - priv/repo/migrations
  </read_first>
  <acceptance_criteria>
    - `grep -l "workflow_definition_snapshots" priv/repo/migrations/*.exs` returns a migration file path
    - `grep -n "defmodule Kiln.Workflows.WorkflowDefinitionSnapshot" lib/kiln/workflows/snapshot.ex` matches OR schema module name documented in `workflows.ex`
    - `grep -n "record_snapshot" lib/kiln/workflows.ex` matches
    - `mix ecto.migrate` in dev succeeds (executor runs locally)
  </acceptance_criteria>
  <files>priv/repo/migrations/, lib/kiln/workflows/snapshot.ex, lib/kiln/workflows.ex, lib/kiln/workflows/loader.ex</files>
  <action>
1. **`mix ecto.gen.migration create_workflow_definition_snapshots`** — table columns:

   - **`id`** uuid v7 PK
   - **`workflow_id`** string not null
   - **`version`** integer not null
   - **`compiled_checksum`** string(64) not null
   - **`yaml_body`** text null (store when under cap)
   - **`truncated`** boolean default false
   - **`inserted_at`** utc_datetime_usec not null

   Index **`workflow_id, inserted_at`**.

2. Ecto schema **`Kiln.Workflows.WorkflowDefinitionSnapshot`** (separate file per project convention — no nested modules in same file).

3. **`Kiln.Workflows.record_snapshot/1`** takes map **`%{workflow_id:, version:, compiled_checksum:, yaml:}`** — if `byte_size(yaml) > 262144`, store **`yaml_body: nil`**, **`truncated: true`** (still persist checksum+version).

4. In **`Loader`** (after successful compile), call **`record_snapshot/1`** with raw YAML string available at load site — **thread through** from caller if loader currently returns only compiled struct.

5. **`mix ecto.migrate`** (document in SUMMARY).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Durable snapshots recorded on load.</done>
</task>

<task type="auto">
  <name>Task 2: WorkflowLive read-only UI</name>
  <read_first>
    - lib/kiln_web/router.ex
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
    - lib/kiln_web/live/spec_editor_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "WorkflowLive" lib/kiln_web/router.ex` matches `/workflows` routes
    - `grep -n "No workflows loaded" lib/kiln_web/live/workflow_live.ex` matches empty copy (UI-SPEC)
    - `grep -n "Editing stays out of the browser" lib/kiln_web/live/workflow_live.ex` matches OR `grep -n "Snapshots" lib/kiln_web/live/workflow_live.ex` matches
    - `grep -n "phx-no-curly-interpolation" lib/kiln_web/live/workflow_live.ex` matches on YAML `<pre>` or parent
    - `mix test test/kiln_web/live/workflow_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/workflow_live.ex, test/kiln_web/live/workflow_live_test.exs</files>
  <action>
Routes:

- **`live "/workflows", WorkflowLive, :index`**
- **`live "/workflows/:workflow_id", WorkflowLive, :show`**

**Index:** query latest **10** snapshots per workflow grouped — simple v1: **`Repo.all(from s in Snapshot, order_by: [desc: s.inserted_at], limit: 50)`** with filter form **not** required.

**Show:** list snapshots left rail labeled **"Snapshots"**; main pane read-only YAML from selected snapshot row.

**`handle_event`:** **`allow?/1`** guard; **no** events mutate YAML.

Tests: assert **no** `phx-submit` for yaml; assert empty state strings.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/workflow_live_test.exs --max-failures=1</automated>
  </verify>
  <done>UI-03 satisfied at MVP depth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DB | INSERT snapshots only from server loader — never from LV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | T | forged YAML POST | mitigate | No write routes; LiveView has no insert handler |
</threat_model>

<verification>
mix test test/kiln_web/live/workflow_live_test.exs
</verification>

<success_criteria>
Snapshots persisted; WorkflowLive read-only; migration applied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-04-SUMMARY.md`
</output>
