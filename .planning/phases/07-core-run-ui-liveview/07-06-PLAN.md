---
phase: 07-core-run-ui-liveview
plan: "06"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - lib/kiln/audit.ex
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/audit_live.ex
  - test/kiln/audit_test.exs
  - test/kiln_web/live/audit_live_test.exs
autonomous: true
requirements:
  - UI-05
must_haves:
  truths:
    - "`Audit.replay/1` supports filters: run_id, stage_id, actor_role, event_kind, occurred_after, occurred_before"
    - "`AuditLive` uses `stream(:events, ...)` and **`id=\"audit-filter-form\"`**"
    - "No write path from LiveView to `audit_events`"
  artifacts:
    - path: "lib/kiln_web/live/audit_live.ex"
      provides: "Audit ledger UI"
      contains: "AuditLive"
    - path: "lib/kiln/audit.ex"
      provides: "Extended replay filters"
      contains: "apply_replay_filters"
  key_links:
    - from: "lib/kiln_web/live/audit_live.ex"
      to: "lib/kiln/audit.ex"
      via: "replay/1"
      pattern: "Read-only"
---

<objective>
Ship **UI-05 audit ledger** at **`/audit`**: extend **`Kiln.Audit.replay/1`** **`apply_replay_filters/2`** clauses for **`{:stage_id, uuid}`**, **`{:actor_role, string}`**, **`{:occurred_after, %DateTime{}}`**, **`{:occurred_before, %DateTime{}}`** (inclusive/exclusive documented). **`AuditLive`**: filter form (`<.form for={@form} id="audit-filter-form">` per UI-SPEC) with **`phx-change="validate"`** + **`phx-submit="search"`** driving **`push_patch`** query params **`run_id`, `stage_id`, `actor_role`, `kind`, `from`, `to`**. Results as **`stream(:events, rows, reset: true)`** with expandable payload `<details>`.

Output: filters + stream + tests; **`grep -n "INSERT" lib/kiln_web/live/audit_live.ex`** must return **no** matches (sanity).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
@lib/kiln/audit.ex
@lib/kiln/audit/event.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Audit.replay filters</name>
  <read_first>
    - lib/kiln/audit.ex
    - lib/kiln/audit/event.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "stage_id" lib/kiln/audit.ex` matches inside `apply_replay_filters`
    - `grep -n "occurred_after" lib/kiln/audit.ex` matches
    - `grep -n "actor_role" lib/kiln/audit.ex` matches
    - `mix test test/kiln/audit_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/audit.ex, test/kiln/audit_test.exs</files>
  <action>
Extend **`apply_replay_filters/2`**:

- **`{:stage_id, id}`** → **`where(e.stage_id == ^id)`**
- **`{:actor_role, role}`** → **`where(e.actor_role == ^role)`** (nil-safe: skip if blank)
- **`{:occurred_after, dt}`** → **`where(e.occurred_at >= ^dt)`**
- **`{:occurred_before, dt}`** → **`where(e.occurred_at <= ^dt)`**

Add **`@spec replay([...])`** update.

Tests in **`test/kiln/audit_test.exs`**: insert **two** events with different **`run_id` / `stage_id` / `occurred_at`** via **`Audit.append/1`** in sandbox; assert **`replay`** filters narrow correctly.
  </action>
  <verify>
    <automated>mix test test/kiln/audit_test.exs --max-failures=1</automated>
  </verify>
  <done>Query supports ledger UI filters.</done>
</task>

<task type="auto">
  <name>Task 2: AuditLive</name>
  <read_first>
    - lib/kiln_web/router.ex
    - lib/kiln_web/live/spec_editor_live.ex
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "AuditLive" lib/kiln_web/router.ex` matches `"/audit"`
    - `grep -n 'id="audit-filter-form"' lib/kiln_web/live/audit_live.ex` matches
    - `grep -n "stream(:events" lib/kiln_web/live/audit_live.ex` matches
    - `grep -n "No events match these filters" lib/kiln_web/live/audit_live.ex` matches
    - `mix test test/kiln_web/live/audit_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/audit_live.ex, test/kiln_web/live/audit_live_test.exs</files>
  <action>
**`live "/audit", AuditLive, :index`**.

**`mount` + `handle_params`:** Parse UUIDs with **`Ecto.UUID.cast`**, build keyword list for **`Audit.replay/1`**, **`stream(socket, :events, events, reset: true)`**.

**Form:** **`to_form/2`** assign; **`<.input field={@form[:run_id]}`** etc.; **`phx-submit="search"`** triggers **`push_patch`** to URL-encoded params.

**`handle_event`:** first line **`unless allow?(socket), do: {:noreply, socket}`** pattern with **`allow?`**.

**Row template:** **`Jason.encode!`** payload in **`<pre phx-no-curly-interpolation>`** OR pretty-print with line cap.

Tests: empty → **"No events match these filters"** when filters applied with no rows; unfiltered shows stream count.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/audit_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Audit ledger read path complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → audit_events | Read-only Ecto selects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | T | mass export of payloads | mitigate | default `limit: 500` on replay + "narrow filters" copy |
</threat_model>

<verification>
mix test test/kiln/audit_test.exs test/kiln_web/live/audit_live_test.exs
</verification>

<success_criteria>
Replay filters + AuditLive streams + stable form id; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-06-SUMMARY.md`
</output>
