---
phase: 07-core-run-ui-liveview
plan: "05"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - lib/kiln/cost_rollups.ex
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/cost_live.ex
  - test/kiln/cost_rollups_test.exs
  - test/kiln_web/live/cost_live_test.exs
autonomous: true
requirements:
  - UI-04
must_haves:
  truths:
    - "Rollups sum `stage_runs.cost_usd` grouped by run, workflow_id, agent_role, provider (`actual_model_used`)"
    - "Today / This week strip uses operator-local midnight boundary with UTC storage (document in moduledoc)"
    - "Projection row is labeled separately from actuals (D-721)"
  artifacts:
    - path: "lib/kiln/cost_rollups.ex"
      provides: "SQL rollups"
      contains: "CostRollups"
    - path: "lib/kiln_web/live/cost_live.ex"
      provides: "Cost dashboard"
      contains: "CostLive"
  key_links:
    - from: "lib/kiln/stages/stage_run.ex"
      to: "lib/kiln/cost_rollups.ex"
      via: "cost_usd, actual_model_used"
      pattern: "Financial attribution"
---

<objective>
Ship **UI-04 cost dashboard** at **`/costs`**: new context **`Kiln.CostRollups`** (or **`Kiln.Telemetry.Cost`** — **use `Kiln.CostRollups`** module name) with **four** query functions **`by_run/1`**, **`by_workflow/1`**, **`by_agent_role/1`**, **`by_provider/1`** where **`opts`** include **`%{from: DateTime, to: DateTime}`** defaulting to **today** and **current_week** per CONTEXT D-720 (document timezone: **DateTime.utc_now** truncated vs **Calendar** week).

**`CostLive`**: summary strip (**Today actual**, **This week actual**, **Week projection** placeholder with **"Not enough calls to project"** when **`count(*) < 5`** for window). **Tabs:** Run | Workflow | Agent role | Provider — each tab shows table with **`Decimal`** USD to **cents** display (**`:erlang.float_to_binary`** or **`Decimal.round/2`** + format). **`requested_model ≠ actual_model_used`** row highlights per D-722 (muted + "Routed" link to **`~p"/audit?..."`** preset optional stub).

Output: pure-query module + LiveView + tests on sandbox data.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
@lib/kiln/stages/stage_run.ex
@lib/kiln/runs/run.ex
@lib/kiln/agents/telemetry_handler.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CostRollups queries</name>
  <read_first>
    - lib/kiln/stages/stage_run.ex
    - lib/kiln/runs/run.ex
    - lib/kiln/stages.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.CostRollups" lib/kiln/cost_rollups.ex` matches
    - `grep -n "sum(sr.cost_usd)" lib/kiln/cost_rollups.ex` matches OR `grep -n "sum(cost_usd)" lib/kiln/cost_rollups.ex` matches
    - `grep -n "actual_model_used" lib/kiln/cost_rollups.ex` matches
    - `mix test test/kiln/cost_rollups_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/cost_rollups.ex, test/kiln/cost_rollups_test.exs</files>
  <action>
Create **`lib/kiln/cost_rollups.ex`** with **`import Ecto.Query`** and **`alias Kiln.Repo`**, **`Kiln.Stages.StageRun`**, **`Kiln.Runs.Run`**.

Implement **`by_run(opts)`**, **`by_workflow(opts)`**, **`by_agent_role(opts)`**, **`by_provider(opts)`** each returning **`[%{key: ..., usd: %Decimal{}, calls: integer}]`**.

Join **`stage_runs` → `runs`** for **`workflow_id`** grouping.

**`by_provider`:** group by **`coalesce(sr.actual_model_used, 'unpriced')`**.

Use **parameterized** `where` on **`sr.updated_at`** or **`sr.inserted_at`** in range — document which timestamp is SSOT for spend.

Tests: insert run + 3 stage_runs with **`cost_usd`**, assert sums per grouping.
  </action>
  <verify>
    <automated>mix test test/kiln/cost_rollups_test.exs --max-failures=1</automated>
  </verify>
  <done>Aggregator matches telemetry SSOT columns.</done>
</task>

<task type="auto">
  <name>Task 2: CostLive UI</name>
  <read_first>
    - lib/kiln_web/router.ex
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
    - lib/kiln/cost_rollups.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "CostLive" lib/kiln_web/router.ex` matches `"/costs"`
    - `grep -n "No spend recorded yet" lib/kiln_web/live/cost_live.ex` matches
    - `grep -n "Last updated" lib/kiln_web/live/cost_live.ex` matches (D-723 footer)
    - `grep -n "defp allow?" lib/kiln_web/live/cost_live.ex` matches
    - `mix test test/kiln_web/live/cost_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/cost_live.ex, test/kiln_web/live/cost_live_test.exs</files>
  <action>
**`live "/costs", CostLive, :index`**.

Mount: load **default tab `:run`**, call **`CostRollups.by_run/1`**, assign **`last_updated_at: DateTime.utc_now()`**.

Template: **summary strip** (three cells), **tab buttons** (`push_patch` to **`?tab=workflow`** etc.), **`<table class="font-mono">`** for rows.

**`handle_params`:** parse **`tab`**.

Empty: **"No spend recorded yet"** + UI-SPEC body.

Tests: empty DB → empty copy; seeded → row count **`has_element?`**.

  </action>
  <verify>
    <automated>mix test test/kiln_web/live/cost_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Cost dashboard MVP for UI-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB aggregates | Read-only selects — no user-controlled SQL fragments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | E | SQL injection | mitigate | Ecto query API only — no string interpolation in `where` |
</threat_model>

<verification>
mix test test/kiln/cost_rollups_test.exs test/kiln_web/live/cost_live_test.exs
</verification>

<success_criteria>
Rollups + CostLive render; cents formatting; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-05-SUMMARY.md`
</output>
