---
phase: 07-core-run-ui-liveview
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/app.css
  - lib/kiln_web/components/layouts.ex
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/run_board_live.ex
  - test/kiln_web/live/run_board_live_test.exs
autonomous: true
requirements:
  - UI-06
must_haves:
  truths:
    - "Inter + IBM Plex Mono loaded; Coal #121212 canvas; Ember #E07A3F reserved per UI-SPEC accent list"
    - "`GET /` renders `RunBoardLive` (not `PageController.redirect_to_ops`)"
    - "`id=\"run-board\"` exists in RunBoard HEEx for LazyHTML tests"
  artifacts:
    - path: "lib/kiln_web/live/run_board_live.ex"
      provides: "Run board LiveView shell"
      contains: "RunBoardLive"
    - path: "lib/kiln_web/router.ex"
      provides: "Domain routes wiring"
      contains: "RunBoardLive"
  key_links:
    - from: "lib/kiln_web/live/run_board_live.ex"
      to: "lib/kiln_web/components/layouts.ex"
      via: "<Layouts.app flash={@flash} current_scope={@current_scope}>"
      pattern: "Phoenix 1.8 authenticated layout"
---

<objective>
Establish **brand-aligned operator chrome** (UI-06) and replace the **`/` → `/ops/dashboard` redirect** with a real **`RunBoardLive`** first paint: empty state copy from `07-UI-SPEC.md`, stable DOM IDs, minimal nav links (`~p"/workflows"`, `~p"/costs"`, `~p"/audit"`, `~p"/ops/dashboard"`, `~p"/ops/oban"`). No PubSub in this plan — realtime ships in Plan 02.

Output: CSS font tokens, trimmed `Layouts.app` marketing links (operator nav), `RunBoardLive`, router change, `mix test test/kiln_web/live/run_board_live_test.exs` green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-core-run-ui-liveview/07-CONTEXT.md
@.planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
@prompts/kiln-brand-book.md
@lib/kiln_web/live/spec_editor_live.ex
@lib/kiln_web/router.ex
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fonts + semantic color utilities in app.css</name>
  <read_first>
    - assets/css/app.css
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
    - prompts/kiln-brand-book.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "Inter" assets/css/app.css` OR `grep -n "fonts.googleapis" lib/kiln_web/components/layouts/root.html.heex` matches at least one line
    - `grep -n "IBM Plex Mono" assets/css/app.css` OR `grep -n "IBM+Plex+Mono" lib/kiln_web/components/layouts/root.html.heex` matches
    - `grep -n "#121212\\|coal" assets/css/app.css` matches (Coal background utility or CSS variable)
  </acceptance_criteria>
  <files>assets/css/app.css, lib/kiln_web/components/layouts/root.html.heex</files>
  <action>
Add **Inter** (400,600) and **IBM Plex Mono** (400) via `@import url(...)` in `app.css` **or** `<link rel="preconnect">` + font stylesheet in `root.html.heex` — pick one approach and document in `@moduledoc` comment in `layouts.ex` if needed.

Declare CSS custom properties: `--color-coal: #121212; --color-char: #1B1D21; --color-iron: #262B31; --color-bone: #F5EFE6; --color-ash: #C7BFB5; --color-smoke: #8C857D; --color-clay: #9A5634; --color-ember: #E07A3F; --color-paper: #FAF6F0; --color-ink: #161514;` and Tailwind `@theme` mappings **or** utility classes `bg-coal`, `text-bone`, `border-ash`, `text-ember` consistent with UI-SPEC hex values.
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Fonts + palette variables available to all LiveViews.</done>
</task>

<task type="auto">
  <name>Task 2: Operator nav + RunBoardLive at /</name>
  <read_first>
    - lib/kiln_web/components/layouts.ex
    - lib/kiln_web/router.ex
    - lib/kiln_web/live/spec_editor_live.ex
    - .planning/phases/07-core-run-ui-liveview/07-UI-SPEC.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "RunBoardLive" lib/kiln_web/router.ex` matches a `live` route for `"/"`
    - `grep -n "redirect_to_ops" lib/kiln_web/router.ex` returns no match OR is commented with Phase 7 supersession note
    - `grep -n 'id="run-board"' lib/kiln_web/live/run_board_live.ex` matches
    - `grep -n "No runs in flight" lib/kiln_web/live/run_board_live.ex` matches (exact UI-SPEC empty heading)
    - `mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/run_board_live.ex, lib/kiln_web/components/layouts.ex, test/kiln_web/live/run_board_live_test.exs</files>
  <action>
1. Create **`KilnWeb.RunBoardLive`** with `mount/3` assigning `page_title: "Runs"`, empty `streams` map placeholder (no DB query yet), render inside **`<Layouts.app flash={@flash} current_scope={@current_scope}>`** per `AGENTS.md`.

2. Outer container **`id="run-board"`**; empty state: heading **"No runs in flight"** and body **"Start a run from the workflow registry when you are ready. New activity appears here in real time."** (verbatim from UI-SPEC).

3. Replace router line `get "/", PageController, :redirect_to_ops` with **`live "/", RunBoardLive, :index`**.

4. Update **`Layouts.app`** header: remove Phoenix marketing `phoenixframework.org` / `Get Started` links; add operator links using **`~p` helpers**: `/workflows`, `/costs`, `/audit`, `/ops/dashboard`, `/ops/oban` (labels: "Workflows", "Costs", "Audit", "LiveDashboard", "Oban").

5. **`handle_event/3`**: add stub event `noop` or future `"start_run"` with first line **`unless allow?(socket), do: raise "forbidden"`** and **`defp allow?(_socket), do: true`** (D-729).

6. Test: `Phoenix.LiveViewTest.live_conn()` GET `/`, assert `has_element?(view, "#run-board")` and empty-state text.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_board_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Home is RunBoardLive with brand empty state and operator nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView | CSRF via `protect_from_forgery`; solo v1 `allow?` stub |
| layout → external fonts | Google Fonts or self-hosted — CSP-friendly URLs only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | font CDN | mitigate | `preconnect` + trusted provider; no inline script |
| T-07-02 | I | operator nav | mitigate | Path helpers only — no raw `href` from user input |
</threat_model>

<verification>
mix test test/kiln_web/live/run_board_live_test.exs
</verification>

<success_criteria>
`/` serves RunBoardLive; UI-SPEC empty copy + `#run-board`; palette/fonts compile; tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-run-ui-liveview/07-01-SUMMARY.md`
</output>
