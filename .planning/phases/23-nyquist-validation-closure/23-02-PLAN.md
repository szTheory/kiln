---
phase: 23-nyquist-validation-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md
  - .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - NYQ-01
milestone: v0.4.0
must_haves:
  truths:
    - "`23-VERIFICATION.md` exists with a passed evidence audit showing all four target artifacts are now either compliant or explicitly waived."
    - "`23-VALIDATION.md` flips to `status: complete` and `nyquist_compliant: true` only after the verification artifact exists."
    - "`.planning/REQUIREMENTS.md` marks NYQ-01 complete and its traceability row says `| NYQ-01 | Phase 23 | Complete |` only after `23-VERIFICATION.md` passes."
    - "`.planning/ROADMAP.md` marks Phase 23 complete with the 2026-04-23 date only after Phase 23 verification is recorded."
  artifacts:
    - path: ".planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md"
      provides: "Phase 23 evidence audit and commands"
    - path: ".planning/phases/23-nyquist-validation-closure/23-VALIDATION.md"
      provides: "Completed phase validation contract"
    - path: ".planning/REQUIREMENTS.md"
      provides: "NYQ-01 requirement completion"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 23 completion state and plan inventory"
  key_links:
    - from: ".planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md"
      to: ".planning/phases/14-fair-parallel-runs/14-VALIDATION.md"
      via: "Automated posture loop"
      pattern: "14-fair-parallel-runs/14-VALIDATION\\.md"
    - from: ".planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md"
      to: ".planning/phases/16-read-only-run-replay/16-VALIDATION.md"
      via: "Waiver-shape grep"
      pattern: "Review-by: 2026-05-23"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md"
      via: "Completion citation"
      pattern: "23-VERIFICATION\\.md"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 23 checkoff follows NYQ-01 completion"
      pattern: "NYQ-01"
---

<objective>
Finish Phase 23 with a verification-first closure flow: create the phase verification artifact, mark the phase validation contract complete, then update milestone SSOT only after the verification artifact records a passed audit.

Purpose: implement D-2317 and prevent false-positive compliance claims or SSOT drift.
Output: `23-VERIFICATION.md`, completed `23-VALIDATION.md`, and NYQ-01 marked complete in `REQUIREMENTS.md` and `ROADMAP.md`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-nyquist-validation-closure/23-CONTEXT.md
@.planning/phases/23-nyquist-validation-closure/23-RESEARCH.md
@.planning/phases/23-nyquist-validation-closure/23-PATTERNS.md
@.planning/phases/23-nyquist-validation-closure/23-VALIDATION.md
@.planning/phases/22-merge-authority-operator-docs/22-VERIFICATION.md
@.planning/phases/20-phase-19-verification-planning-ssot/20-VERIFICATION.md
@.planning/phases/14-fair-parallel-runs/14-VALIDATION.md
@.planning/phases/16-read-only-run-replay/16-VALIDATION.md
@.planning/phases/17-template-library-onboarding-specs/17-VALIDATION.md
@.planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 23 verification and complete the phase validation artifact</name>
  <read_first>
    - .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md
    - .planning/phases/23-nyquist-validation-closure/23-CONTEXT.md
    - .planning/phases/23-nyquist-validation-closure/23-PATTERNS.md
    - .planning/phases/22-merge-authority-operator-docs/22-VERIFICATION.md
    - .planning/phases/20-phase-19-verification-planning-ssot/20-VERIFICATION.md
    - .planning/phases/14-fair-parallel-runs/14-VALIDATION.md
    - .planning/phases/16-read-only-run-replay/16-VALIDATION.md
    - .planning/phases/17-template-library-onboarding-specs/17-VALIDATION.md
    - .planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `test -f .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0
    - `grep -q '^status: passed$' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0 and `grep -q '^phase: 23-nyquist-validation-closure$' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0
    - `grep -q '14-VALIDATION.md' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0 and the same grep exits 0 for `16-VALIDATION.md`, `17-VALIDATION.md`, and `19-VALIDATION.md`
    - `grep -q 'Review-by: 2026-05-23' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0
    - `grep -q 'mix compile --warnings-as-errors' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` exits 0
    - `grep -q '^status: complete$' .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md` exits 0 and `grep -q '^nyquist_compliant: true$' .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md` exits 0
    - `grep -q '^\\*\\*Approval:\\*\\* signed off 2026-04-23 (`23-VERIFICATION.md` passed)' .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md` exits 0
  </acceptance_criteria>
  <files>.planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md, .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md</files>
  <action>
Implement the verification-first closure flow required by D-2304 to D-2308 and D-2317.

Part A — create `.planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md`:
1. Frontmatter:
   - Create the file as `status: draft` while authoring the command block
   - `phase: 23-nyquist-validation-closure`
   - `verified: 2026-04-23`
   - `requirements:` list containing `NYQ-01`
2. Title: `# Phase 23 verification — Nyquist / VALIDATION closure`
3. `## Automated` table with these checks and PASS wording:
   - Four-file posture loop over 14/16/17/19 validations
   - Phase 16 waiver-shape grep for `## Nyquist waiver`, `Owner: @jon`, `Review-by: 2026-05-23`
   - `mix compile --warnings-as-errors` PASS (docs-only compile sanity)
4. Commands block from repo root containing:

```bash
for f in .planning/phases/14-fair-parallel-runs/14-VALIDATION.md .planning/phases/16-read-only-run-replay/16-VALIDATION.md .planning/phases/17-template-library-onboarding-specs/17-VALIDATION.md .planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md; do grep -q '^nyquist_compliant: true$' "$f" || grep -q '^## Nyquist waiver$' "$f"; done
grep -q '^## Nyquist waiver$' .planning/phases/16-read-only-run-replay/16-VALIDATION.md
grep -q 'Owner: @jon' .planning/phases/16-read-only-run-replay/16-VALIDATION.md
grep -q 'Review-by: 2026-05-23' .planning/phases/16-read-only-run-replay/16-VALIDATION.md
mix compile --warnings-as-errors
```

5. `## Must-haves` table with concrete criteria:
   - all four target validations resolved
   - Phase 16 uses the exact waiver block shape
   - no SSOT flips happen before this verification file exists
6. `## Human verification` line: `None required (artifact audit only).`
7. Run the exact audit commands from the command block:
   - four-file posture loop over 14/16/17/19 validations
   - Phase 16 waiver-shape greps for `## Nyquist waiver`, `Owner: @jon`, `Review-by: 2026-05-23`
   - `mix compile --warnings-as-errors`
8. Only after those commands exit 0, change `.planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md` to `status: passed`.

Part B — update `23-VALIDATION.md` only after the verification commands have passed and `23-VERIFICATION.md` is `status: passed`:
1. Set frontmatter to `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, and `updated: 2026-04-23`.
2. Flip all validation sign-off checkboxes to `[x]`.
3. Set approval line exactly to `**Approval:** signed off 2026-04-23 (`23-VERIFICATION.md` passed)`.
4. Do not change the task map semantics or add new runtime testing requirements; this phase is an artifact-audit closure.
  </action>
  <verify>
    <automated>for f in .planning/phases/14-fair-parallel-runs/14-VALIDATION.md .planning/phases/16-read-only-run-replay/16-VALIDATION.md .planning/phases/17-template-library-onboarding-specs/17-VALIDATION.md .planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md; do grep -q '^nyquist_compliant: true$' "$f" || grep -q '^## Nyquist waiver$' "$f"; done && grep -q '^## Nyquist waiver$' .planning/phases/16-read-only-run-replay/16-VALIDATION.md && grep -q 'Owner: @jon' .planning/phases/16-read-only-run-replay/16-VALIDATION.md && grep -q 'Review-by: 2026-05-23' .planning/phases/16-read-only-run-replay/16-VALIDATION.md && mix compile --warnings-as-errors && test -f .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '^status: passed$' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '^phase: 23-nyquist-validation-closure$' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '14-VALIDATION.md' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '16-VALIDATION.md' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '17-VALIDATION.md' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '19-VALIDATION.md' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q 'mix compile --warnings-as-errors' .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md && grep -q '^status: complete$' .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md && grep -q '^nyquist_compliant: true$' .planning/phases/23-nyquist-validation-closure/23-VALIDATION.md</automated>
  </verify>
  <done>Phase 23 has a passed verification artifact and its own validation contract is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Flip milestone SSOT after Phase 23 verification passes</name>
  <read_first>
    - .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/23-nyquist-validation-closure/23-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -q '\\[x\\] \\*\\*NYQ-01\\*\\*' .planning/REQUIREMENTS.md` exits 0
    - `grep -q 'Complete 2026-04-23' .planning/REQUIREMENTS.md` exits 0 and `grep -q '23-VERIFICATION.md' .planning/REQUIREMENTS.md` exits 0
    - `grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md` exits 0
    - `grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md` exits 0
    - `grep -q 'completed 2026-04-23' .planning/ROADMAP.md` exits 0
    - `grep -q '\\*\\*Plans:\\*\\* 2 plans' .planning/ROADMAP.md` exits 0
    - `grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md && grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md && grep -q '23-VERIFICATION.md' .planning/REQUIREMENTS.md` exits 0 as the fast SSOT verification step before final closeout
  </acceptance_criteria>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Apply the minimum SSOT updates required by D-2317, and only after `23-VERIFICATION.md` exists with `status: passed`.

Update `.planning/REQUIREMENTS.md`:
1. Mark the NYQ-01 requirement checkbox complete.
2. Append completion text in the same house style as DOCS-08: `— **Complete 2026-04-23** (`23-VERIFICATION.md`).`
3. Change the traceability row to `| NYQ-01 | Phase 23 | Complete |`.
4. Update the coverage summary counts if needed so the totals remain accurate.

Update `.planning/ROADMAP.md`:
1. In the phase checklist near the top, change Phase 23 to `[x]` and append `— completed 2026-04-23.` using the same style as Phase 22.
2. Preserve the Phase 23 goal and success criteria; do not rewrite milestone intent.
3. Keep the planning inventory visible by leaving or adding `**Plans:** 2 plans` under the Phase 23 detail section if it is missing after the completion edit.
4. Do not mark Phase 24 complete or change unrelated backlog rows.

After the SSOT edits are in place, run a fast SSOT precheck before the AGENTS-required final repo gate:
1. `grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md`
2. `grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md`
3. `grep -q '23-VERIFICATION.md' .planning/REQUIREMENTS.md`

Task 3 owns the final repo gate after these SSOT edits are in place. Do not run `bash script/precommit.sh` until this task's fast SSOT verify passes.
  </action>
  <verify>
    <automated>grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md && grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md && grep -q '23-VERIFICATION.md' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>NYQ-01 and the Phase 23 roadmap row are complete, with citations pointing back to the passed Phase 23 verification artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final repo gate and record any unrelated failures honestly</name>
  <read_first>
    - .planning/phases/23-nyquist-validation-closure/23-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - AGENTS.md
  </read_first>
  <acceptance_criteria>
    - `grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md` exits 0 before running the final repo gate
    - `grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md` exits 0 before running the final repo gate
    - `.planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md` records the final `bash script/precommit.sh` result as either `Precommit: PASS` or `Precommit: PARTIAL — unrelated pre-existing failure`
  </acceptance_criteria>
  <files>.planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md</files>
  <action>
Run the AGENTS-required final repo gate after Task 2 has already established the fast SSOT proof:
1. Confirm the Task 2 SSOT greps still pass.
2. Run `bash script/precommit.sh`.
3. If it passes, record PASS in `.planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md`.
4. If it fails because of unrelated pre-existing repo issues, keep the phase edits intact and record `Precommit: PARTIAL — unrelated pre-existing failure` plus the exact failing checks and file references in `.planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md`.

Do not weaken or skip the final repo gate; this task exists to keep the closeout gate machine-checkable without slowing Task 2's iterative verification loop.
  </action>
  <verify>
    <automated>grep -q '| NYQ-01 | Phase 23 | Complete |' .planning/REQUIREMENTS.md && grep -q '\\[x\\] \\*\\*Phase 23: Nyquist / VALIDATION closure\\*\\*' .planning/ROADMAP.md && grep -Eq 'Precommit: PASS|Precommit: PARTIAL — unrelated pre-existing failure' .planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md</automated>
  </verify>
  <done>The final repo gate has run and its result is represented honestly in the summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 23 artifacts → milestone SSOT | Requirements and roadmap must not outrun what the verification artifact proves |
| Verification prose → future operators | The verification file must show concrete commands, not implied compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | T | `23-VERIFICATION.md` | mitigate | Require explicit posture loop, waiver-shape grep, and compile sanity in the commands block before writing `status: passed` |
| T-23-06 | T | `.planning/REQUIREMENTS.md` | mitigate | Update NYQ-01 only after `23-VERIFICATION.md` exists and is cited by name |
| T-23-07 | T | `.planning/ROADMAP.md` | mitigate | Phase 23 checkoff follows the passed verification artifact and preserves the exact phase intent text |
| T-23-08 | R | milestone closure claim | accept | Git history plus the new verification file provide an audit trail; no external system of record is involved |
</threat_model>

<verification>
Run the commands embedded in `23-VERIFICATION.md`, confirm the SSOT lines with the Task 2 grep precheck, then run `bash script/precommit.sh` in Task 3 as the final repo gate. Do not update `REQUIREMENTS.md` or `ROADMAP.md` first.
</verification>

<success_criteria>
- [ ] `23-VERIFICATION.md` exists with a passed automated evidence audit and concrete command block
- [ ] `23-VALIDATION.md` is complete and `nyquist_compliant: true`
- [ ] `REQUIREMENTS.md` and `ROADMAP.md` both show NYQ-01 / Phase 23 complete and cite the phase verification artifact
- [ ] `bash script/precommit.sh` passes or any unrelated pre-existing failure is captured explicitly in the summary
</success_criteria>

<output>
After completion, create `.planning/phases/23-nyquist-validation-closure/23-02-SUMMARY.md`.
</output>
