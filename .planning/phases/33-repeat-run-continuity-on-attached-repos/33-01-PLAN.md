---
phase: 33-repeat-run-continuity-on-attached-repos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_add_attached_repo_continuity_metadata.exs
  - lib/kiln/attach/attached_repo.ex
  - lib/kiln/attach/continuity.ex
  - lib/kiln/attach.ex
  - lib/kiln/specs.ex
  - lib/kiln/runs.ex
  - test/kiln/attach/continuity_test.exs
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - "Kiln can list recent attached repos from durable repo identity instead of forcing source rediscovery."
    - "One same-repo continuity target is chosen with explicit precedence across open draft, promoted request, and linked run."
    - "Recent attached repo ordering uses explicit continuity metadata rather than overloaded `updated_at`."
  artifacts:
    - path: lib/kiln/attach/continuity.ex
      provides: "Repo-centric continuity read model and precedence rules"
    - path: lib/kiln/attach.ex
      provides: "Public continuity entry points for recent repos and selected repo continuity"
    - path: lib/kiln/attach/attached_repo.ex
      provides: "Durable continuity usage metadata on attached repos"
    - path: test/kiln/attach/continuity_test.exs
      provides: "Continuity query and precedence proof"
  key_links:
    - from: lib/kiln/attach/continuity.ex
      to: lib/kiln/attach/attached_repo.ex
      via: "recent repo ordering and usage metadata remain anchored on durable attached repo identity"
      pattern: "last_selected_at|last_run_started_at|attached_repo_id"
    - from: lib/kiln/attach/continuity.ex
      to: lib/kiln/specs.ex
      via: "carry-forward precedence reads open drafts and promoted attached requests from existing Specs lifetimes"
      pattern: "spec_drafts|spec_revisions|attached_repo_id"
    - from: lib/kiln/attach/continuity.ex
      to: lib/kiln/runs.ex
      via: "continuity detail includes recent linked run facts without parsing snapshots for primary identity"
      pattern: "runs|attached_repo_id|spec_id|spec_revision_id"
---

<objective>
Create the continuity read model that lets Kiln treat one attached repo as a durable repeat-work object instead of a one-shot attach source.

Purpose: satisfy the backend selection half of `CONT-01` so repeat-run continuity is grounded in explicit repo/request/run identity and coherent precedence.
Output: continuity metadata on attached repos, one continuity read model, and domain tests for recent ordering plus carry-forward selection.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-repeat-run-continuity-on-attached-repos/33-CONTEXT.md
@.planning/phases/33-repeat-run-continuity-on-attached-repos/33-RESEARCH.md
@.planning/phases/33-repeat-run-continuity-on-attached-repos/33-PATTERNS.md
@.planning/phases/33-repeat-run-continuity-on-attached-repos/33-VALIDATION.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-01-SUMMARY.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-02-SUMMARY.md
@lib/kiln/attach.ex
@lib/kiln/attach/attached_repo.ex
@lib/kiln/specs.ex
@lib/kiln/runs.ex

<notes>
- Phase 33 continuity is same-repo only.
- Do not add UI work in this plan.
- Do not add a generic continuity-state table.
</notes>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Reuse known repo/workspace/trust context on the second and third run | Task 1, Task 2 |
| REQ | `CONT-01` requires repeat runs on one attached repo to reuse known repo/workspace context | Task 1, Task 2 |
| CONTEXT D-3306-D-3310 | Continuity anchor and carry-forward precedence across draft, promoted request, and run | Task 1 |
| CONTEXT D-3322-D-3324 | Reuse existing tables and use narrow join-based read models | Task 1 |
| CONTEXT D-3323 | Add explicit continuity usage metadata on attached repos | Task 2 |
| DEFERRED | No route UX, broader brownfield guardrails, or PR handoff tightening | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the repo-centric continuity read model and precedence rules</name>
  <files>lib/kiln/attach/continuity.ex, lib/kiln/attach.ex, lib/kiln/specs.ex, lib/kiln/runs.ex, test/kiln/attach/continuity_test.exs</files>
  <behavior>
    - Test 1: for one attached repo, continuity picks an explicit draft first, otherwise the most recent open attached draft, otherwise the most recent promoted attached request, otherwise the most recent linked run, otherwise blank.
    - Test 2: continuity detail returns compact repo, workspace, base-branch, last-run, and carry-forward facts without crossing repos.
    - Test 3: continuity lists use join-based read models for recent repos and continuity targets rather than preload-everything list fetches.
  </behavior>
  <action>Create a dedicated continuity boundary under `Kiln.Attach` that can list recent attached repos and load one selected repo’s continuity detail by attached repo id plus optional explicit work-object ids. Keep precedence exactly aligned with the Phase 33 context: explicit same-repo draft if requested, otherwise most recent open draft on that repo, otherwise most recent promoted attached request on that repo, otherwise most recent linked run, otherwise blank. Reuse `spec_drafts`, `spec_revisions`, and `runs` as the sources of truth for request and run identity; snapshots remain derived context only. Return shaped server-owned data for the UI rather than exposing raw multi-query assembly to LiveView.</action>
  <acceptance_criteria>
    - `rg -n "defmodule Kiln\\.Attach\\.Continuity|list_recent_attached_repos|get_repo_continuity" lib/kiln/attach/continuity.ex lib/kiln/attach.ex` finds the continuity boundary.
    - `rg -n "explicit.*draft|open draft|promoted attached request|linked run|blank" test/kiln/attach/continuity_test.exs` finds precedence proof coverage.
    - `rg -n "attached_repo_id" lib/kiln/attach/continuity.ex lib/kiln/specs.ex lib/kiln/runs.ex` shows continuity stays same-repo and FK-driven.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/kiln/attach/continuity_test.exs</automated>
  </verify>
  <done>Kiln can now answer the continuity question for one known attached repo without forcing the UI to re-discover source identity or re-implement precedence rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit recent-usage metadata to attached repos</name>
  <files>priv/repo/migrations/*_add_attached_repo_continuity_metadata.exs, lib/kiln/attach/attached_repo.ex, lib/kiln/attach/continuity.ex, test/kiln/attach/continuity_test.exs</files>
  <behavior>
    - Test 1: selecting a continuity target can persist `last_selected_at` on the attached repo.
    - Test 2: starting the next run can persist `last_run_started_at` separately from generic row updates.
    - Test 3: recent attached repo ordering prefers explicit continuity metadata over `updated_at`.
  </behavior>
  <action>Add a focused migration and schema fields for `last_selected_at` and `last_run_started_at` on `attached_repos`, then expose narrow update helpers through the continuity boundary. Use those fields for recent ordering and continuity recency displays; do not overload `updated_at` and do not persist generic browser-owned “resume state.” Keep writes small and intentional so future `/attach` route-backed continuity can mark usage without mutating unrelated repo facts.</action>
  <acceptance_criteria>
    - `rg -n "last_selected_at|last_run_started_at" priv/repo/migrations/*_add_attached_repo_continuity_metadata.exs lib/kiln/attach/attached_repo.ex lib/kiln/attach/continuity.ex` finds the continuity metadata path.
    - `rg -n "recent.*ordering|last_selected_at|last_run_started_at" test/kiln/attach/continuity_test.exs` finds ordering coverage.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/kiln/attach/continuity_test.exs</automated>
  </verify>
  <done>Attached repos now carry explicit continuity usage timestamps, making repeat-run recency deterministic and queryable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| continuity selection -> attached repo identity | Continuity must never cross repo boundaries or infer identity from browser-local state. |
| continuity read model -> request/run history | The carry-forward target must be selected from durable same-repo records with explicit precedence. |
| recent ordering -> operator expectations | “Recent repo” ordering must reflect actual continuity usage rather than incidental row churn. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | T | continuity selection | mitigate | Require attached repo ids and same-repo FK joins for continuity target selection. |
| T-33-02 | I | carry-forward selection | mitigate | Implement one precedence boundary in `Kiln.Attach.Continuity` and cover it with unit tests. |
| T-33-03 | R | recent repo ordering | mitigate | Add explicit `last_selected_at` and `last_run_started_at` instead of relying on `updated_at`. |

</threat_model>

<verification>
- `mix test test/kiln/attach/continuity_test.exs`
- `rg -n "last_selected_at|last_run_started_at|list_recent_attached_repos|get_repo_continuity" lib/kiln/attach/attached_repo.ex lib/kiln/attach/continuity.ex lib/kiln/attach.ex test/kiln/attach/continuity_test.exs`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- Kiln can list recent attached repos from explicit continuity metadata.
- One same-repo continuity target is chosen with explicit precedence and without cross-repo leakage.
- `/attach` can later consume one shaped continuity payload instead of rebuilding joins in LiveView.
</success_criteria>

<output>
After completion, create `.planning/phases/33-repeat-run-continuity-on-attached-repos/33-01-SUMMARY.md`
</output>
