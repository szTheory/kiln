---
phase: 29-attach-entry-surfaces
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/attach_entry_live.ex
  - lib/kiln_web/live/onboarding_live.ex
  - lib/kiln_web/live/templates_live.ex
  - test/kiln_web/live/attach_entry_live_test.exs
  - test/kiln_web/live/onboarding_live_test.exs
  - test/kiln_web/live/templates_live_test.exs
  - test/kiln_web/live/route_smoke_test.exs
autonomous: true
requirements:
  - ATTACH-01
must_haves:
  truths:
    - "An operator can see `Attach existing repo` as an explicit first-use branch from both `/onboarding` and `/templates`."
    - "`hello-kiln` remains the single recommended first proof path while attach stays visible as the real-project branch."
    - "Visiting `/attach` renders a dedicated orientation surface instead of overloading templates, scenarios, or template resume state."
  artifacts:
    - path: lib/kiln_web/live/attach_entry_live.ex
      provides: "Dedicated route-backed attach orientation LiveView for Phase 29"
    - path: lib/kiln_web/router.ex
      provides: "Live route entry for `/attach` inside the existing default session"
    - path: lib/kiln_web/live/onboarding_live.ex
      provides: "Secondary attach CTA added to the demo-first onboarding next-step cluster"
    - path: lib/kiln_web/live/templates_live.ex
      provides: "Primary attach discovery module beside the existing template-first start surface"
    - path: test/kiln_web/live/attach_entry_live_test.exs
      provides: "Focused route and content regression for `/attach`"
    - path: test/kiln_web/live/route_smoke_test.exs
      provides: "Route-level proof that `/attach` mounts cleanly with the operator shell"
  key_links:
    - from: lib/kiln_web/router.ex
      to: lib/kiln_web/live/attach_entry_live.ex
      via: "new `live \"/attach\", AttachEntryLive, :index` entry in `live_session :default`"
      pattern: "live \"/attach\", AttachEntryLive, :index"
    - from: lib/kiln_web/live/onboarding_live.ex
      to: /attach
      via: "`onboarding-attach-existing-repo` route-based navigation"
      pattern: "id=\"onboarding-attach-existing-repo\"|navigate=\\{~p\"/attach\"\\}"
    - from: lib/kiln_web/live/templates_live.ex
      to: /attach
      via: "`templates-attach-existing-repo` CTA inside the start-choice module"
      pattern: "id=\"templates-attach-existing-repo\"|id=\"templates-attach-module\""
    - from: test/kiln_web/live/route_smoke_test.exs
      to: lib/kiln_web/router.ex
      via: "index-route smoke coverage now includes `/attach`"
      pattern: "\"/attach\""
---

<objective>
Add the structural Phase 29 attach branch: a dedicated `/attach` LiveView plus additive attach-vs-template entry points on onboarding and templates, while preserving the current template-first proof path.

Purpose: satisfy `ATTACH-01` at the information-architecture layer without pulling repo validation, workspace hydration, or git/PR behavior forward from Phases 30-31.
Output: one route-backed attach entry surface, one additive onboarding attach branch, one first-class templates attach module, and the LiveView regressions that lock those seams in place.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-attach-entry-surfaces/29-CONTEXT.md
@.planning/phases/29-attach-entry-surfaces/29-UI-SPEC.md
@.planning/phases/29-attach-entry-surfaces/29-RESEARCH.md
@.planning/phases/29-attach-entry-surfaces/29-PATTERNS.md
@.planning/phases/26-first-live-template-run/26-CONTEXT.md
@.planning/phases/27-local-first-run-proof/27-CONTEXT.md
@lib/kiln_web/router.ex
@lib/kiln_web/live/onboarding_live.ex
@lib/kiln_web/live/templates_live.ex
@lib/kiln_web/live/settings_live.ex
@test/kiln_web/live/onboarding_live_test.exs
@test/kiln_web/live/templates_live_test.exs
@test/kiln_web/live/route_smoke_test.exs

<notes>
- `/templates` remains the global Start destination and the primary attach discovery surface in this phase; do not turn `/onboarding` or `/` into the new universal entry point.
- Attach is additive and route-backed in Phase 29. Do not model it as a scenario, a template variation, a `template_id`, or an extension of template `return_to` plumbing.
- `hello-kiln` stays the single recommended first proof path even after attach becomes first-class for real-project work.
</notes>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Make attach-to-existing visible as a first-use path without displacing the greenfield/template journey | Task 1, Task 2 |
| REQ | `ATTACH-01` operator can choose attach from onboarding and template/start surfaces with clear framing | Task 1, Task 2 |
| CONTEXT | D-2901, D-2902, D-2903 keep onboarding demo-first and scenario-led while adding attach as a secondary explicit branch | Task 2 |
| CONTEXT | D-2904, D-2905 keep `/templates` as Start and primary attach discovery without replacing the `hello-kiln` hero | Task 2 |
| CONTEXT | D-2908, D-2916, D-2917, D-2918 preserve `hello-kiln` as the recommended first proof path while making attach first-class for real-project work | Task 2 |
| CONTEXT | D-2912, D-2913, D-2914, D-2920 create a dedicated route-backed attach surface with its own ids/state | Task 1 |
| CONTEXT | D-2915 do not reuse template `return_to` / `template_id` plumbing for attach | Task 1, Task 2 |
| CONTEXT | D-2919, D-2921, D-2922 keep attach work additive, explicit, and explorable instead of blocked or hidden | Task 1, Task 2 |
| UI-SPEC | Required ids for `/onboarding`, `/templates`, and `/attach` are present and route-based | Task 1, Task 2 |
| RESEARCH | Reuse the existing default live session, current shell patterns, and stable LiveView/test id conventions | Task 1, Task 2 |
| DEFERRED | No repo validation, hydration, dirty-worktree logic, branch orchestration, or draft PR behavior appears in Phase 29 | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated `/attach` route and orientation-only LiveView</name>
  <files>lib/kiln_web/router.ex, lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs, test/kiln_web/live/route_smoke_test.exs</files>
  <behavior>
    - Test 1: `live(conn, "/attach")` mounts successfully, renders `#attach-entry-root`, `#attach-entry-hero`, `#attach-supported-sources`, `#attach-next-step`, and `#attach-back-to-templates`.
    - Test 2: `/attach` is part of the route smoke matrix and renders the shared operator shell without introducing retired tokens or missing-header regressions.
    - Test 3: The page explains supported sources and Phase 30 next-step guidance without exposing template resume state, scenario controls, repo validation, or git mutation actions.
  </behavior>
  <action>Per D-2912, D-2913, D-2914, D-2920, and D-2921, add `live "/attach", AttachEntryLive, :index` to the existing `live_session :default` and create `KilnWeb.AttachEntryLive` as a lightweight, route-backed Phase 29 surface. Follow the shell, section rhythm, and id conventions from `29-PATTERNS.md`: use `<Layouts.app ...>` with stable attach-specific ids, route-based `<.link navigate={...}>` controls, and calm orientation copy that explicitly says `Attach existing repo`, supported source types (local path, existing clone, GitHub URL), one-repo scope, and that validation/workspace safety happen in the next phase. Do not add form submission handlers, hidden modal state, scenario params, template ids, `return_to` plumbing, repo probing, hydration, dirty-worktree checks, branch orchestration, or draft PR logic. Update route smoke and add focused LiveView coverage for the new route.</action>
  <verify>
    <automated>mix test test/kiln_web/live/attach_entry_live_test.exs test/kiln_web/live/route_smoke_test.exs</automated>
  </verify>
  <done>The repository has a new `/attach` route-backed LiveView that is useful on its own in Phase 29, stays honest about future attach mechanics, and is locked by direct LiveView plus route-smoke tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add attach-vs-template branching to onboarding and templates without disturbing the proof-first template path</name>
  <files>lib/kiln_web/live/onboarding_live.ex, lib/kiln_web/live/templates_live.ex, test/kiln_web/live/onboarding_live_test.exs, test/kiln_web/live/templates_live_test.exs</files>
  <behavior>
    - Test 1: `/onboarding` keeps `#onboarding-start-from-template` as the primary CTA, adds `#onboarding-attach-existing-repo`, and never treats attach as a scenario or `?scenario=` meaning.
    - Test 2: `/templates` keeps `#templates-first-run-hero` and the `hello-kiln` recommendation while adding `#templates-start-choice`, `#templates-attach-module`, and `#templates-attach-existing-repo`.
    - Test 3: The new attach branch navigates to `/attach`, and existing template apply/start behavior still works unchanged for `hello-kiln`.
  </behavior>
  <action>Per D-2901 through D-2905, D-2908, D-2915, D-2916, D-2917, D-2918, D-2919, and D-2922, update `/onboarding` and `/templates` so attach is a first-class additive branch rather than hidden advanced behavior. On `/onboarding`, keep the scenario-led wizard and the existing primary template CTA intact, then add a secondary bordered `Attach existing repo` CTA plus a short note clarifying that attach is for operators who already have code and want real-project work. On `/templates`, keep the existing `hello-kiln` hero largest and recommended, then add a peer start-choice/attach module above the broader catalog with the required ids and explicit one-sentence framing for `Built-in templates` versus `Attach existing repo`. Route all attach CTAs directly to `/attach`; do not reuse scenario ids, template forms, template `return_to` state, `template_id`, or template apply/start handlers. Preserve all current template-first seams, including the scenario banner, template detail flow, and `hello-kiln` happy-path tests.</action>
  <verify>
    <automated>mix test test/kiln_web/live/onboarding_live_test.exs test/kiln_web/live/templates_live_test.exs</automated>
  </verify>
  <done>`/onboarding` and `/templates` both present an explicit attach branch, `hello-kiln` remains the top proof path, and the branch is enforced by targeted LiveView tests instead of copy-only assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| onboarding/templates -> attach route | Operators must be routed into a clearly separate attach path, not a hidden variation of template or scenario state. |
| Phase 29 copy -> future attach mechanics | The new branch must not over-promise validation, hydration, or git safety behavior that is owned by later phases. |
| route surface -> regression suite | Stable ids and route smoke are the defense against silently regressing attach discoverability while preserving existing template seams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | S | Onboarding/templates branch semantics | mitigate | Keep attach on its own `/attach` route with attach-specific ids and route helpers so the UI cannot spoof attach as a scenario or template state. |
| T-29-02 | T | Template flow contracts | mitigate | Do not reuse `template_id`, `return_to`, or template handlers for attach; preserve the current `hello-kiln` seams and lock them with existing tests. |
| T-29-03 | I | Operator-facing attach expectations | mitigate | State supported sources and next-phase validation honestly on `/attach`; exclude repo probing, hydration, dirty-worktree checks, branch orchestration, and PR actions from this plan. |
| T-29-04 | D | Route discoverability | mitigate | Add direct LiveView and route-smoke coverage for `/attach`, plus onboarding/templates tests for the new CTAs and ids. |
| T-29-05 | R | Phase scope drift | accept | Minor copy changes may still need polish after the structural slice; Plan 29-02 owns that dependent alignment rather than overloading this plan. |

</threat_model>

<verification>
- `mix test test/kiln_web/live/attach_entry_live_test.exs test/kiln_web/live/onboarding_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/route_smoke_test.exs`
- `rg -n "Attach existing repo|templates-attach-module|templates-start-choice|onboarding-attach-existing-repo|attach-entry-root|live \"/attach\"" lib/kiln_web/router.ex lib/kiln_web/live/attach_entry_live.ex lib/kiln_web/live/onboarding_live.ex lib/kiln_web/live/templates_live.ex test/kiln_web/live/attach_entry_live_test.exs test/kiln_web/live/onboarding_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/route_smoke_test.exs`
</verification>

<success_criteria>
- `/attach` exists as a dedicated LiveView under the current default operator shell and explains attach honestly without pretending Phase 30/31 already shipped.
- `/onboarding` stays demo-first and template-led while exposing attach as a clear secondary explicit branch.
- `/templates` remains the Start surface, preserves the `hello-kiln` hero as the single recommended first proof path, and adds a first-class attach discovery module above the broader catalog.
- No new attach behavior reuses scenario state, template ids, template start plumbing, or template resume context.
- The structural attach slice is covered by focused LiveView and route-smoke tests.
</success_criteria>
