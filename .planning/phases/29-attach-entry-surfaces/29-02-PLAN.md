---
phase: 29-attach-entry-surfaces
plan: "02"
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - lib/kiln_web/live/attach_entry_live.ex
  - lib/kiln_web/live/onboarding_live.ex
  - lib/kiln_web/live/templates_live.ex
  - test/e2e/tests/onboarding.spec.ts
  - test/e2e/tests/routes.spec.ts
autonomous: true
requirements:
  - ATTACH-01
must_haves:
  truths:
    - "Operator-facing copy consistently distinguishes `Built-in templates` from `Attach existing repo` across the first-use surfaces."
    - "The attach branch stays honest about Phase 29 scope: visible and credible now, but validation and brownfield safety still happen next."
    - "Browser-level proofs cover `/attach` and the onboarding/templates branch without regressing the current template-first journey."
  artifacts:
    - path: lib/kiln_web/live/attach_entry_live.ex
      provides: "Final attach orientation copy and next-step guidance aligned with the rest of the entry surfaces"
    - path: test/e2e/tests/onboarding.spec.ts
      provides: "End-to-end proof that template remains primary and attach navigates explicitly to `/attach`"
    - path: test/e2e/tests/routes.spec.ts
      provides: "Route matrix coverage that includes the new `/attach` surface"
  key_links:
    - from: lib/kiln_web/live/onboarding_live.ex
      to: lib/kiln_web/live/attach_entry_live.ex
      via: "attach CTA copy and navigation stay consistent between the wizard and `/attach`"
      pattern: "Attach existing repo|/attach"
    - from: lib/kiln_web/live/templates_live.ex
      to: lib/kiln_web/live/attach_entry_live.ex
      via: "templates attach module hands off to the attach route with matching scope language"
      pattern: "Supports a local path|GitHub URL|validation"
    - from: test/e2e/tests/routes.spec.ts
      to: lib/kiln_web/router.ex
      via: "browser route matrix includes `/attach` alongside the existing LiveView routes"
      pattern: "\"/attach\""
---

<objective>
Align the operator guidance around the new attach branch so the entry surfaces tell one coherent story: templates for the fastest proof, attach for bounded work on existing code, and no fake promise that Phase 30/31 mechanics already exist.

Purpose: turn the structural branch from Plan 29-01 into an honest, regression-proof operator narrative across copy, secondary guidance surfaces, and browser-level verification.
Output: harmonized entry-surface copy plus E2E proofs that cover `/attach` without weakening the template-first path.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-attach-entry-surfaces/29-CONTEXT.md
@.planning/phases/29-attach-entry-surfaces/29-UI-SPEC.md
@.planning/phases/29-attach-entry-surfaces/29-RESEARCH.md
@.planning/phases/29-attach-entry-surfaces/29-PATTERNS.md
@.planning/phases/26-first-live-template-run/26-CONTEXT.md
@lib/kiln_web/live/attach_entry_live.ex
@lib/kiln_web/live/onboarding_live.ex
@lib/kiln_web/live/templates_live.ex
@test/e2e/tests/onboarding.spec.ts
@test/e2e/tests/routes.spec.ts

<notes>
- This plan depends on Plan 29-01 being present first; treat its route, ids, and baseline attach modules as the fixed substrate.
- Keep `/templates` as the primary attach discovery surface and `hello-kiln` as the recommended first proof path. Copy alignment must not flatten those priorities.
- The optional `/` attach shortcut is intentionally deferred out of Phase 29. Keep this plan centered on `/onboarding`, `/templates`, `/attach`, and the browser proofs that cover them.
</notes>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Make attach visible and honest across first-use surfaces instead of reading like an implied advanced path | Task 1, Task 2 |
| REQ | `ATTACH-01` clear framing for when to use attach versus built-in templates | Task 1, Task 2 |
| CONTEXT | D-2908, D-2909 use the explicit phrase `Attach existing repo` and contrast it with built-in templates | Task 1 |
| CONTEXT | D-2910, D-2911 explain one-repo/local path-existing clone-GitHub URL scope and defer validation/safety mechanics to later phases | Task 1 |
| CONTEXT | D-2916, D-2917, D-2918 preserve `hello-kiln` recommendation while making attach credible for real-project work | Task 1, Task 2 |
| CONTEXT | D-2919 keep prior tested seams additive, not replaced | Task 1, Task 2 |
| CONTEXT | D-2922 keep disconnected/readiness guidance explorable rather than disabled-only | Task 1 |
| UI-SPEC | Copy contract for `Built-in templates`, `Attach existing repo`, source types, and next-step honesty is present across the relevant surfaces | Task 1 |
| UI-SPEC | Browser-visible ids and routes continue to support route-based navigation and attach discovery | Task 2 |
| PATTERNS | `attach_entry_live.ex`, `onboarding_live.ex`, and `templates_live.ex` should follow the mapped route-backed shell, CTA cluster, and start-module analogs in `29-PATTERNS.md` | Task 1 |
| DEFERRED | No Phase 30/31 repo validation, hydration, dirty-worktree refusal, branch orchestration, or draft PR logic is introduced in the alignment pass | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Harmonize attach-vs-template copy across the first-use surfaces</name>
  <files>lib/kiln_web/live/attach_entry_live.ex, lib/kiln_web/live/onboarding_live.ex, lib/kiln_web/live/templates_live.ex</files>
  <action>Starting from the structures shipped in Plan 29-01, tighten the operator-facing narrative per D-2908, D-2909, D-2910, D-2911, D-2916, D-2917, D-2918, D-2919, and D-2922. Use the analogs in `29-PATTERNS.md` explicitly: the full-shell route-backed attach page pattern for `attach_entry_live.ex`, the secondary-CTA cluster pattern for `onboarding_live.ex`, and the hero-plus-peer-module pattern for `templates_live.ex`. Make the same distinction legible on `/onboarding`, `/templates`, and `/attach`: built-in templates are the fastest way to learn or prove Kiln, while `Attach existing repo` is the real-project path for bounded work on one codebase the operator already owns. Use the exact operator-facing phrase `Attach existing repo`; keep source-type copy explicit (local path, existing clone, GitHub URL); and keep next-step language honest that validation and workspace safety checks land in the next phase. Do not add the optional `/` shortcut in this phase, and do not reframe `/settings` or `/` as attach choosers.</action>
  <verify>
    <automated>mix test test/kiln_web/live/onboarding_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/attach_entry_live_test.exs</automated>
  </verify>
  <done>The first-use surfaces all tell the same honest story about templates versus attach, while `hello-kiln` remains the top proof path and the phase stays centered on the required entry surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Extend browser-level proof coverage for the attach branch and preserved template journey</name>
  <files>test/e2e/tests/onboarding.spec.ts, test/e2e/tests/routes.spec.ts</files>
  <action>Update the browser tests so Phase 29 owns proof that the new attach branch is real without breaking the current template-first flow. In `onboarding.spec.ts`, keep the existing scenario/template assertions, then add coverage that the new onboarding attach CTA is visible, uses the explicit `Attach existing repo` branch, and lands on `/attach` without mutating scenario state. In `routes.spec.ts`, add `/attach` to the LiveView route matrix so the browser smoke covers the new surface alongside the existing routes. Do not widen this suite into repo attach mechanics, browser form submission for source validation, or any Phase 30/31 behaviors; the browser proof here is route truth and honest entry-point choreography only. Finish with the repo-standard final gate from the project instructions.</action>
  <verify>
    <automated>npx playwright test test/e2e/tests/onboarding.spec.ts test/e2e/tests/routes.spec.ts && bash script/precommit.sh</automated>
  </verify>
  <done>Playwright proves that attach is discoverable from onboarding, `/attach` renders as part of the route matrix, the template-first journey still works, and the final repo gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy contract -> operator trust | Misaligned wording can make attach feel either hidden, over-promised, or equivalent to mechanics that do not exist yet. |
| browser route proof -> shipped UX | The attach branch is not first-class unless the end-to-end route and CTA coverage proves it in a real browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06 | S | Operator-facing branch framing | mitigate | Use the exact `Attach existing repo` label everywhere and keep templates vs attach responsibilities consistent across `/onboarding`, `/templates`, and `/attach`. |
| T-29-07 | I | Attach scope messaging | mitigate | Repeat the one-repo/source-type/next-phase-validation guidance on attach-facing surfaces so the UI does not imply repo validation or git safety exists already. |
| T-29-08 | D | Browser-level regression | mitigate | Extend Playwright coverage to include the new attach CTA path and `/attach` route matrix entry, then run the repo precommit gate. |

</threat_model>

<verification>
- `mix test test/kiln_web/live/onboarding_live_test.exs test/kiln_web/live/templates_live_test.exs test/kiln_web/live/attach_entry_live_test.exs`
- `npx playwright test test/e2e/tests/onboarding.spec.ts test/e2e/tests/routes.spec.ts`
- `rg -n "Attach existing repo|Built-in templates|local path|existing clone|GitHub URL|validation" lib/kiln_web/live/attach_entry_live.ex lib/kiln_web/live/onboarding_live.ex lib/kiln_web/live/templates_live.ex test/e2e/tests/onboarding.spec.ts test/e2e/tests/routes.spec.ts`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- `/onboarding`, `/templates`, and `/attach` use one coherent template-vs-attach story with the exact `Attach existing repo` wording and honest next-step framing.
- `hello-kiln` still reads as the fastest recommended first proof path; attach reads as the explicit real-project branch, not the new default for first evaluation.
- Playwright covers the attach branch and `/attach` route while preserving the existing onboarding/template journey assertions.
- Final verification includes the repo-standard `bash script/precommit.sh` gate.
</success_criteria>
