---
phase: 999.1-docs-landing-site
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/pnpm-lock.yaml
  - site/astro.config.mjs
  - site/tsconfig.json
  - site/src/content/config.ts
  - site/src/content/docs/index.md
  - site/README.md
autonomous: true
requirements:
  - DOCS-07
must_haves:
  truths:
    - "`site/` exists with committed `pnpm-lock.yaml` and successful `pnpm exec astro build`"
    - "`astro.config.mjs` sets `site: 'https://szTheory.github.io'` and `base: '/kiln'` for GitHub Project Pages"
  artifacts:
    - path: "site/package.json"
      provides: "Astro + Starlight dependencies"
      contains: "\"astro\""
    - path: "site/astro.config.mjs"
      provides: "Pages base path"
      contains: "base:"
---

<objective>
Bootstrap **Astro + Starlight** under **`site/`** as the single meta-project (CONTEXT D-9991-01, DOCS-07): committed lockfile, TypeScript config, Starlight docs root at **`/docs/`** on the built site, and **`astro build`** green with GitHub Pages **`base`** preconfigured for **`szTheory/kiln`** publishing.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
@.planning/phases/999.1-docs-landing-site/999.1-RESEARCH.md
@prompts/kiln-brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm workspace — package.json + Starlight deps</name>
  <read_first>
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
    - prompts/kiln-brand-book.md
  </read_first>
  <acceptance_criteria>
    - `test -f site/package.json` exits 0
    - `grep -E "\"@astrojs/starlight\"|\"astro\"" site/package.json` matches at least one line
    - `grep -q '"packageManager"' site/package.json` OR root documents pnpm in `site/README.md` with exact `pnpm@` version line
  </acceptance_criteria>
  <files>site/package.json</files>
  <action>
Create **`site/package.json`** with **`"private": true`**, scripts: **`"dev": "astro dev"`**, **`"build": "astro build"`**, **`"preview": "astro preview"`**. Add runtime deps **`astro`** `^5.0.0` (or latest 5.x) and **`@astrojs/starlight`** compatible range per Starlight docs. Set **`"packageManager": "pnpm@9.15.0"`** (or current repo-standard 9.x) in `site/package.json` **or** the repository root `package.json` if you introduce a minimal root workspace — prefer **self-contained `site/package.json`** for CONTEXT isolation.
  </action>
  <verify>
    <automated>test -f site/package.json</automated>
  </verify>
  <done>Dependencies declared.</done>
</task>

<task type="auto">
  <name>Task 2: astro.config.mjs — Starlight + GitHub Pages base</name>
  <read_first>
    - site/package.json
  </read_first>
  <acceptance_criteria>
    - `test -f site/astro.config.mjs` exits 0
    - `grep -n "defineConfig" site/astro.config.mjs` matches
    - `grep -n "starlight" site/astro.config.mjs` matches
    - `grep -n "base:" site/astro.config.mjs` contains `'/kiln'` (with or without trailing slash, consistent with Astro docs)
    - `grep -n "site:" site/astro.config.mjs` contains `https://szTheory.github.io`
  </acceptance_criteria>
  <files>site/astro.config.mjs</files>
  <action>
Add **`site/astro.config.mjs`** using **`import { defineConfig } from 'astro/config'`** and **`import starlight from '@astrojs/starlight'`**. Call **`defineConfig({ site: 'https://szTheory.github.io', base: '/kiln', integrations: [starlight({ title: 'Kiln', sidebar: [{ label: 'Overview', link: '/docs/' }] })] })`** (adjust sidebar to match first MD slug). Ensure **`srcDir`** default so content lives under **`site/src/`**.
  </action>
  <verify>
    <automated>node --check site/astro.config.mjs 2>/dev/null || true</automated>
  </verify>
  <done>Config present.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal Starlight content + lockfile</name>
  <read_first>
    - site/astro.config.mjs
  </read_first>
  <acceptance_criteria>
    - `test -f site/src/content/docs/index.md` exits 0
    - `test -f site/pnpm-lock.yaml` exits 0
    - `head -5 site/src/content/docs/index.md` contains frontmatter delimiter `---`
  </acceptance_criteria>
  <files>site/src/content/docs/index.md</files>
  <action>
Create **`site/src/content/docs/index.md`** with Starlight frontmatter: **`title: Overview`**, **`description:`** one sentence on Kiln operator docs. Run **`pnpm install`** inside **`site/`** and commit **`pnpm-lock.yaml`**. Add **`site/tsconfig.json`** extending **`astro/tsconfigs/strict`** if the scaffold does not already.
  </action>
  <verify>
    <automated>test -f site/pnpm-lock.yaml</automated>
  </verify>
  <done>Install reproducible.</done>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] astro build green</name>
  <read_first>
    - site/astro.config.mjs
    - site/src/content/docs/index.md
  </read_first>
  <acceptance_criteria>
    - `cd site && pnpm install && pnpm exec astro build` exits 0 (document in `site/README.md` if CI differs)
    - `test -d site/dist` OR `test -d dist` from within `site` after build (Astro default **`dist/`** under `site/`)
  </acceptance_criteria>
  <files>site/README.md</files>
  <action>
Add **`site/README.md`** with exact commands: **`cd site`**, **`pnpm install`**, **`pnpm exec astro build`**, **`pnpm exec astro preview`**. Fix any Starlight onboarding gaps until build passes.
  </action>
  <verify>
    <automated>cd site && pnpm exec astro build</automated>
  </verify>
  <done>Scaffold verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ build | Dependencies are public packages only; lockfile pins versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-01 | S | dependency chain | mitigate | Commit `pnpm-lock.yaml`; periodic `pnpm audit` in CI optional follow-up |
| T-9991-02 | T | typosquat package names | mitigate | Use official `@astrojs/*` scopes and pinned major ranges |
</threat_model>

<verification>
cd site && pnpm install && pnpm exec astro build
</verification>

<success_criteria>
Starlight docs build to `site/dist` with `base`/`site` correct for `github.io/kiln`; lockfile committed.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-docs-landing-site/999.1-01-SUMMARY.md`
</output>
