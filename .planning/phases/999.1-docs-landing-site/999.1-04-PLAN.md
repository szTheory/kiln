---
phase: 999.1-docs-landing-site
plan: "04"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - .github/workflows/docs.yml
  - site/package.json
  - site/.htmltest.yml
  - site/lychee.toml
  - typos.toml
autonomous: true
requirements:
  - DOCS-06
must_haves:
  truths:
    - "Path-filtered workflow runs on changes under `site/` and workflow file itself"
    - "`main` branch deploy uses `actions/upload-pages-artifact` + `deploy-pages` with concurrency group"
  artifacts:
    - path: ".github/workflows/docs.yml"
      provides: "Docs CI + Pages"
      contains: "upload-pages-artifact"
---

<objective>
Add **path-filtered GitHub Actions** for the docs site (CONTEXT D-9991-21–24, DOCS-06): **`pnpm install` + `astro build`**, **`htmltest`** on output, **`lychee`** with timeouts/cache config, **`typos`**, and **GitHub Pages deploy on push to `main`** using **OIDC / `GITHUB_TOKEN`** permissions documented inline. Do **not** add Node tooling to default **`mix check`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs.yml — triggers + pnpm + build</name>
  <read_first>
    - .github/workflows/ci.yml
    - .planning/phases/999.1-docs-landing-site/999.1-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test -f .github/workflows/docs.yml` exits 0
    - `grep -E "paths:|paths-ignore:" .github/workflows/docs.yml` matches
    - `grep -E "site/|pnpm|astro build" .github/workflows/docs.yml` matches at least 3 lines
  </acceptance_criteria>
  <files>.github/workflows/docs.yml</files>
  <action>
Create **`.github/workflows/docs.yml`** with **`on.push` and `on.pull_request`** limited by **`paths:`** to **`site/**`**, **`pnpm-lock.yaml`**, **`.github/workflows/docs.yml`**, **`typos.toml`**, and **`site/.htmltest.yml` / `site/lychee.toml`** as applicable. Jobs on **`ubuntu-24.04`**: **`actions/checkout@v4`**, **`pnpm/action-setup@v4`** with **`version: 9`**, **`actions/setup-node@v4`** with **`node-version: '22'`** and **`cache: 'pnpm'`** + **`cache-dependency-path: site/pnpm-lock.yaml`**. Default working directory **`site`**. Steps: **`pnpm install --frozen-lockfile`**, **`pnpm exec astro build`**, artifact **`site/dist`** for downstream steps (path adjust if dist location differs with `base`).
  </action>
  <verify>
    <automated>grep -q "astro build" .github/workflows/docs.yml</automated>
  </verify>
  <done>Core workflow present.</done>
</task>

<task type="auto">
  <name>Task 2: htmltest + lychee + typos steps</name>
  <read_first>
    - .github/workflows/docs.yml
  </read_first>
  <acceptance_criteria>
    - `grep -i htmltest .github/workflows/docs.yml` matches
    - `grep -i lychee .github/workflows/docs.yml` matches
    - `grep -i typos .github/workflows/docs.yml` matches
    - `test -f site/.htmltest.yml` OR htmltest config path passed via CLI flags in workflow
  </acceptance_criteria>
  <files>site/.htmltest.yml</files>
  <action>
Add post-build steps: install/run **`htmltest`** against **`site/dist`** (config **`site/.htmltest.yml`** ignoring external URLs if needed). Add **`lychee`** using repo-root **`site/lychee.toml`** with **`timeout`**, **`max_concurrency`**, and **`exclude`** for known-flaky hosts (placeholder list OK with comment). Add **`crate-ci/typos@master`** or **`typos`** action against repo with **`typos.toml`** excluding code dirs if necessary. Ensure steps **run after** successful **`astro build`** in same job or dependent job.
  </action>
  <verify>
    <automated>grep -E "htmltest|lychee|typos" .github/workflows/docs.yml | wc -l | grep -qv '^0$'</automated>
  </verify>
  <done>Quality gates wired.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Pages deploy job on main</name>
  <read_first>
    - .github/workflows/docs.yml
  </read_first>
  <acceptance_criteria>
    - `grep -n "deploy-pages" .github/workflows/docs.yml` matches OR `id: deployment` with `deploy-pages` action
    - `grep -n "concurrency:" .github/workflows/docs.yml` matches
    - `grep -E "branches:.*main|refs/heads/main" .github/workflows/docs.yml` restricts deploy job
    - `grep -E "permissions:|pages: write|id-token: write" .github/workflows/docs.yml` matches (Pages OIDC)
  </acceptance_criteria>
  <files>.github/workflows/docs.yml</files>
  <action>
Add job **`deploy`** (or equivalent) **`if: github.ref == 'refs/heads/main' && github.event_name == 'push'`** depending on workflow structure. Use **`permissions: contents: read, pages: write, id-token: write`**. Steps: **`actions/configure-pages@v5`**, **`actions/upload-pages-artifact@v3`** with **`path: site/dist`**, **`deploy-pages@v4`**. Set **`concurrency: group: pages-${{ github.ref }} cancel-in-progress: true`** at workflow or job level per CONTEXT.
  </action>
  <verify>
    <automated>grep -q upload-pages-artifact .github/workflows/docs.yml</automated>
  </verify>
  <done>Deploy path defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions ↔ Pages | Deploy job runs only on `main` with token scoped to Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9991-04 | E | workflow token | mitigate | Minimal **`permissions`**; no long-lived PATs in YAML |
| T-9991-05 | T | artifact substitution | mitigate | **`upload-pages-artifact`** path restricted to **`site/dist`** output of trusted build steps |
</threat_model>

<verification>
actionlint .github/workflows/docs.yml 2>/dev/null || yamllint .github/workflows/docs.yml 2>/dev/null || true
</verification>

<success_criteria>
PRs touching `site/` get docs checks; merges to `main` publish static site to GitHub Pages.
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-docs-landing-site/999.1-04-SUMMARY.md`
</output>
