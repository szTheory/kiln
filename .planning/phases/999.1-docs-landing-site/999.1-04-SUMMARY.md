---
phase: 999.1-docs-landing-site
plan: "04"
subsystem: infra
requirements-completed: [DOCS-06]
completed: 2026-04-22
---

# Phase 999.1 Plan 04 Summary

**Path-filtered `.github/workflows/docs.yml` builds the site, runs `htmltest` (wrapper for `/kiln` absolute URLs), `lychee`, and `typos`, and publishes `site/dist` to GitHub Pages on `main` pushes.**

## Accomplishments

- Added `site/.htmltest.yml`, `site/.htmltest.ci.yml`, `site/lychee.toml`, root `typos.toml`, and `site/scripts/htmltest-ci.sh`.
- Workflow uses pnpm cache, concurrency group, minimal `permissions` on deploy, and `upload-pages-artifact` + `deploy-pages`.

## Self-Check: PASSED

- `htmltest-ci.sh` passes locally against `site/dist` when `htmltest` is installed.
