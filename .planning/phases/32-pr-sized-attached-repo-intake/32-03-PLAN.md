---
phase: 32-pr-sized-attached-repo-intake
plan: "03"
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - lib/kiln_web/live/attach_entry_live.ex
  - test/kiln_web/live/attach_entry_live_test.exs
  - .planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md
autonomous: true
requirements:
  - WORK-01
must_haves:
  truths:
    - "A ready attached repo exposes one bounded feature-or-bugfix form with explicit done-when framing."
    - "Submitting a valid bounded request from `/attach` starts an attached run instead of stopping at generic readiness."
    - "Invalid or vague requests stay on the form with concrete validation feedback and stable DOM ids for proof."
  artifacts:
    - path: lib/kiln_web/live/attach_entry_live.ex
      provides: "Ready-state UI that collects bounded attached-work requests and launches runs"
    - path: test/kiln_web/live/attach_entry_live_test.exs
      provides: "LiveView proof for bounded-form rendering, validation, and successful launch"
    - path: .planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md
      provides: "Updated phase validation map aligned with the shipped test files"
  key_links:
    - from: lib/kiln_web/live/attach_entry_live.ex
      to: lib/kiln/attach/intake.ex
      via: "ready-state submit uses the backend intake boundary for draft creation"
      pattern: "Kiln.Attach.Intake"
    - from: lib/kiln_web/live/attach_entry_live.ex
      to: lib/kiln/runs.ex
      via: "successful submit launches the attached run through the new run seam"
      pattern: "start_for_attached_request"
    - from: test/kiln_web/live/attach_entry_live_test.exs
      to: lib/kiln_web/live/attach_entry_live.ex
      via: "stable ids and submit outcomes are locked for ready, invalid, and success paths"
      pattern: "attach-request-form|attach-request-kind|attach-request-submit|attach-run-started"
---

<objective>
Turn `/attach` from a readiness checkpoint into the operator-facing bounded-request intake and launch surface.

Purpose: close `WORK-01` at the UI layer so a ready attached repo can become one started PR-sized run with explicit acceptance framing.
Output: updated `/attach` LiveView form and interactions, LiveView proof for invalid and success paths, and refreshed phase validation metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-RESEARCH.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-PATTERNS.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-01-PLAN.md
@.planning/phases/32-pr-sized-attached-repo-intake/32-02-PLAN.md
@lib/kiln_web/live/attach_entry_live.ex
@test/kiln_web/live/attach_entry_live_test.exs

<interfaces>
From `lib/kiln_web/live/attach_entry_live.ex`:
```elixir
def handle_event("validate_source", %{"attach_source" => params}, socket) do
def handle_event("resolve_source", %{"attach_source" => params}, socket) do
```

Ready-state DOM already present:
```heex
<div id="attach-ready">
<div id="attach-ready-summary">
```

From Plan `32-01` intake contract:
```elixir
%Kiln.Attach.IntakeRequest{
  request_kind: :feature | :bugfix,
  title: String.t(),
  change_summary: String.t(),
  acceptance_criteria: [String.t()],
  out_of_scope: [String.t()]
}
```
</interfaces>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Reframe attached work as one bounded feature or bugfix request instead of open-ended continuation | Task 1 |
| REQ | `WORK-01` requires the operator to start an attached-repo run from that bounded request | Task 1, Task 2 |
| RESEARCH | Reuse `/attach` ready state as the entry point for the bounded request form | Task 1 |
| PATTERNS | Keep one `to_form/2` assign, explicit DOM ids, and thin event handlers in `AttachEntryLive` | Task 1 |
| VALIDATION | Expand `test/kiln_web/live/attach_entry_live_test.exs` and refresh `32-VALIDATION.md` to match shipped proof | Task 1, Task 2 |
| DEFERRED | Do not add repeat-run continuity, wider brownfield preflight, or draft-PR handoff details here | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the bounded attached-request form and successful launch path to `/attach`</name>
  <files>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs</files>
  <read_first>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs, lib/kiln/attach/intake.ex, lib/kiln/runs.ex, .planning/phases/32-pr-sized-attached-repo-intake/32-PATTERNS.md</read_first>
  <behavior>
    - Test 1: after a repo reaches ready state, the LiveView renders a bounded-request form with exact ids for request kind, title, change summary, three repeated acceptance rows, three repeated out-of-scope rows, and submit.
    - Test 2: submitting a vague request keeps the form visible and surfaces changeset-backed validation errors instead of starting a run.
    - Test 3: submitting a valid request sends repeated inputs named `attach_request[acceptance_criteria][]` and `attach_request[out_of_scope][]`, relies on backend normalization from Plan 32-01 to trim/drop blank rows, promotes the draft, starts the run through `Kiln.Runs.start_for_attached_request/3`, and renders a success state with the new run id or route target.
  </behavior>
  <action>Extend `KilnWeb.AttachEntryLive` so the `:ready` state includes a second `to_form/2`-backed form for the bounded request contract created in Plan 32-01. Encode `acceptance_criteria` and `out_of_scope` as repeated inputs, not newline-parsed textareas: render exactly three rows for each list on initial display with ids `#attach-request-acceptance-1` through `#attach-request-acceptance-3` and `#attach-request-out-of-scope-1` through `#attach-request-out-of-scope-3`, and submit them with the exact names `attach_request[acceptance_criteria][]` and `attach_request[out_of_scope][]`. Pass those params unchanged into the backend intake changeset so Plan 32-01 owns normalization (trim whitespace and drop blank entries); do not split strings in LiveView, join arrays back into prose, or invent CSV/newline parsing. Add explicit event handlers for request validation and submit; reuse the existing ready attached repo from assigns instead of reposting repo identity through hidden text fields. On submit, call the backend sequence `Kiln.Attach.Intake -> Kiln.Specs.promote_draft -> Kiln.Runs.start_for_attached_request`. Add stable ids at minimum for `#attach-request-form`, `#attach-request-kind`, `#attach-request-title`, `#attach-request-summary`, `#attach-request-acceptance-1`, `#attach-request-acceptance-2`, `#attach-request-acceptance-3`, `#attach-request-out-of-scope-1`, `#attach-request-out-of-scope-2`, `#attach-request-out-of-scope-3`, `#attach-request-submit`, and one success container such as `#attach-run-started`. Keep the existing `Layouts.app` wrapper and Phase 30 ready/blocked truth surfaces intact.</action>
  <acceptance_criteria>
    - `rg -n "attach-request-form|attach-request-kind|attach-request-title|attach-request-acceptance-1|attach-request-acceptance-2|attach-request-acceptance-3|attach-request-out-of-scope-1|attach-request-out-of-scope-2|attach-request-out-of-scope-3|attach-request-submit|attach-run-started" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs` finds the new stable ids and proof.
    - `rg -n "attach_request\\[acceptance_criteria\\]\\[\\]|attach_request\\[out_of_scope\\]\\[\\]" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs` shows the UI encodes both list fields as repeated array params.
    - `rg -n "Kiln\\.Attach\\.Intake|promote_draft|start_for_attached_request" lib/kiln_web/live/attach_entry_live.ex` shows the submit path uses backend boundaries rather than inline persistence.
    - `mix test test/kiln_web/live/attach_entry_live_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/kiln_web/live/attach_entry_live_test.exs</automated>
  </verify>
  <done>The `/attach` LiveView now turns a ready attached repo into one started bounded run with explicit request framing and deterministic DOM ids for proof.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the phase validation map with the shipped test surface</name>
  <files>.planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md</files>
  <read_first>.planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md, .planning/phases/32-pr-sized-attached-repo-intake/32-01-PLAN.md, .planning/phases/32-pr-sized-attached-repo-intake/32-02-PLAN.md, test/kiln_web/live/attach_entry_live_test.exs</read_first>
  <action>Update `32-VALIDATION.md` after the code and tests land so the Quick run command, task map, Wave 0 status, and test-file references all match the real Phase 32 proof surface. Replace the current draft placeholders with the exact test files created by Plans 32-01 through 32-03, mark Wave 0 complete if all planned files exist, and keep the phase gate anchored on `bash script/precommit.sh`.</action>
  <acceptance_criteria>
    - `rg -n "test/kiln/attach/intake_test\\.exs|test/kiln/specs/attach_request_draft_test\\.exs|test/kiln/runs/attached_request_start_test\\.exs|test/integration/attached_repo_intake_test\\.exs|test/kiln_web/live/attach_entry_live_test\\.exs" .planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md` finds the shipped proof files.
    - `rg -n "wave_0_complete: true|bash script/precommit\\.sh" .planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md` reflects the updated validation posture.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "test/kiln/attach/intake_test\\.exs|test/kiln/specs/attach_request_draft_test\\.exs|test/kiln/runs/attached_request_start_test\\.exs|test/integration/attached_repo_intake_test\\.exs|test/kiln_web/live/attach_entry_live_test\\.exs|wave_0_complete: true|bash script/precommit\\.sh" .planning/phases/32-pr-sized-attached-repo-intake/32-VALIDATION.md</automated>
  </verify>
  <done>The phase validation artifact matches the actual proof surface that closes `WORK-01`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/attach` form -> backend intake | The operator-facing form turns untrusted input into a durable request and run launch. |
| ready attach state -> launch submit | Only server-held ready-state repo identity may be used to start the run. |
| LiveView submit -> navigation/success state | Success UI must only render after the bounded request is persisted, promoted, and launched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-08 | T | `AttachEntryLive` request submit | mitigate | Keep request validation changeset-backed via `to_form/2` and refuse vague or incomplete requests in-place. |
| T-32-09 | T | ready-state repo identity | mitigate | Reuse the server-held ready attached repo assign and backend attach lookup instead of hidden fields for workspace path, branch, or remote URL. |
| T-32-10 | S | LiveView success state | mitigate | Only render `#attach-run-started` after `Attach.Intake`, `Specs.promote_draft/2`, and `Runs.start_for_attached_request/3` succeed; keep blocked/invalid paths on the form. |

</threat_model>

<verification>
- `mix test test/kiln_web/live/attach_entry_live_test.exs`
- `rg -n "attach-request-form|attach-request-kind|attach-request-title|attach-request-submit|attach-run-started|start_for_attached_request" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- `/attach` shows a bounded-request form after ready state and refuses vague submissions with in-place validation.
- Valid submissions create and start one attached run through backend boundaries.
- `32-VALIDATION.md` cites the actual Phase 32 proof files and no longer reports Wave 0 gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/32-pr-sized-attached-repo-intake/32-03-SUMMARY.md`
</output>
