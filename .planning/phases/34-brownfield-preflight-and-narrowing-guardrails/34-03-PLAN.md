---
phase: 34-brownfield-preflight-and-narrowing-guardrails
plan: "03"
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - lib/kiln_web/live/attach_entry_live.ex
  - test/kiln_web/live/attach_entry_live_test.exs
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
must_haves:
  truths:
    - "The `/attach` surface distinguishes blocked safety failures from warning-only brownfield narrowing guidance."
    - "A warning report shows evidence and a default narrower request without pretending the repo is unsafe to touch."
    - "Operator action on warnings still routes through the existing backend start path and preserves deterministic start authority."
  artifacts:
    - path: lib/kiln_web/live/attach_entry_live.ex
      provides: "Distinct warning and narrowing UX for brownfield preflight on `/attach`"
    - path: test/kiln_web/live/attach_entry_live_test.exs
      provides: "LiveView proof for blocked vs warning semantics, narrowing acceptance, and successful launch behavior"
  key_links:
    - from: lib/kiln_web/live/attach_entry_live.ex
      to: lib/kiln/attach.ex
      via: "the LiveView requests a server-side preflight report and renders it rather than computing overlap in the browser"
      pattern: "evaluate_brownfield_preflight"
    - from: lib/kiln_web/live/attach_entry_live.ex
      to: lib/kiln/runs.ex
      via: "warning acceptance or manual proceed still lands in the existing attach launch path"
      pattern: "start_for_attached_request"
    - from: test/kiln_web/live/attach_entry_live_test.exs
      to: lib/kiln_web/live/attach_entry_live.ex
      via: "stable DOM ids prove blocked, warning, inspect, narrow, and success states"
      pattern: "attach-blocked|attach-warning|attach-narrowing-accept|attach-run-started"
---

<objective>
Add the `/attach` warning and narrowing UX, then prove blocked and warning states stay behaviorally distinct.

Purpose: satisfy the operator-facing half of `SAFE-01` and `SAFE-02` so brownfield preflight findings are visible, actionable, and safely scoped without bleeding into Phase 35 review handoff work.
Output: a dedicated attach warning/narrowing state, stable DOM ids and flows for suggestion acceptance and manual editing, and LiveView proof that warning-only findings do not become hidden hard refusals.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-CONTEXT.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-RESEARCH.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-PATTERNS.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-01-PLAN.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-02-PLAN.md
@lib/kiln_web/live/attach_entry_live.ex
@test/kiln_web/live/attach_entry_live_test.exs

<interfaces>
From `lib/kiln_web/live/attach_entry_live.ex`:
```elixir
def handle_event("validate_request", %{"attach_request" => params}, socket) do
def handle_event("submit_request", %{"attach_request" => params}, socket) do
```

Existing state ids:
```heex
<div id="attach-ready">
<div id="attach-continuity">
<div id="attach-blocked">
```

From Plan `34-01`/`34-02`:
```elixir
Attach.evaluate_brownfield_preflight(attached_repo, params, opts)
# => report with findings, suggested narrower request, and launchability helpers
```
</interfaces>
</context>

<source_audit>
## Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL | Give the operator concrete remediation or narrowing guidance before coding starts | Task 1, Task 2 |
| REQ | `SAFE-01` needs brownfield conflicts surfaced visibly on `/attach` before launch | Task 1, Task 2 |
| REQ | `SAFE-02` needs explicit remediation and narrowing actions for non-fatal issues | Task 1, Task 2 |
| RESEARCH | Render a dedicated narrowing state rather than hiding warnings inside blocked or flash-only UI | Task 1 |
| RESEARCH | Keep `/attach` server-authoritative and route warning acceptance through existing backend seams | Task 1, Task 2 |
| CONTEXT | D-3418, D-3419, D-3420, D-3421 require a dedicated narrowing state with primary accept-suggestion CTA and distinct blocked semantics | Task 1 |
| CONTEXT | D-3422 and D-3423 require evidence-rich rendering near repo/request facts with a thin LiveView | Task 1 |
| CONTEXT | D-3410 and D-3412 require warning-only findings to avoid autonomous hard refusals | Task 2 |
| DEFERRED | No approval-gate UX, no cross-repo comparison UI, no Phase 35 draft-PR proof packaging | Task 1, Task 2 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the dedicated warning and narrowing state to `/attach`</name>
  <files>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs</files>
  <read_first>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs, .planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-CONTEXT.md</read_first>
  <behavior>
    - Test 1: a fatal preflight outcome still renders the blocked state, while a warning-only report renders a distinct warning/narrowing state.
    - Test 2: the warning state shows evidence for repo, base branch, current branch when present, prior draft/run/PR identity, and concrete next actions.
    - Test 3: the primary CTA applies Kiln's suggested narrower request into the form, the secondary path keeps manual editing available, and an inspect action exposes the triggering prior object reference.
  </behavior>
  <action>Extend `KilnWeb.AttachEntryLive` per D-3418, D-3419, D-3420, D-3421, D-3422, and D-3423 so `/attach` has a dedicated warning/narrowing branch that is visually and behaviorally separate from the existing blocked path. Keep the LiveView wrapped in `<Layouts.app ...>` and use server-owned report data only; do not compute heuristics in the browser. Add stable ids for the new state and controls, including one root such as `#attach-warning`, one evidence container such as `#attach-warning-findings`, one primary accept-suggestion button such as `#attach-narrowing-accept`, one manual-edit path, and one inspect action per finding. The warning copy must state that the repo is attachable, the current request is collision-prone or too wide as written, and Kiln recommends a narrower default before coding starts. Preserve the existing blocked path for deterministic hard refusals and keep the continuity and ready states intact.</action>
  <acceptance_criteria>
    - `rg -n "attach-warning|attach-warning-findings|attach-narrowing-accept|attach-warning-edit|attach-warning-inspect" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs` finds the new stable ids and proof.
    - `rg -n "evaluate_brownfield_preflight" lib/kiln_web/live/attach_entry_live.ex` shows the LiveView consumes the server report instead of recomputing overlap logic.
    - `mix test test/kiln_web/live/attach_entry_live_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/kiln_web/live/attach_entry_live_test.exs</automated>
  </verify>
  <done>`/attach` now has a dedicated warning/narrowing state with visible evidence and a default-forward suggested narrowing path that does not masquerade as a blocked repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove warning-only findings narrow the request without stealing start authority</name>
  <files>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs</files>
  <read_first>lib/kiln_web/live/attach_entry_live.ex, test/kiln_web/live/attach_entry_live_test.exs, lib/kiln/runs.ex, .planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-02-PLAN.md</read_first>
  <behavior>
    - Test 1: warning-only findings keep the operator on `/attach` until they accept the narrower suggestion or manually edit, rather than auto-starting or auto-refusing.
    - Test 2: after accepting the suggested narrower request, the existing attach path still promotes the draft and calls `Runs.start_for_attached_request/3`.
    - Test 3: fatal preflight findings still prevent the launch path from reaching `Runs.start_for_attached_request/3`.
  </behavior>
  <action>Finish the LiveView flow so warning-only findings remain advisory per D-3410 and D-3412: submitting a request with warnings should render the narrowing state, accepting the suggestion should repopulate the request form or resume the submit path with the narrower request, and only the normal backend launch path should call `Runs.start_for_attached_request/3`. Add test doubles in `AttachEntryLiveTest` to prove that fatal reports stop before run start, warning reports allow the operator to continue, and the final success path still ends in `#attach-run-started`. Do not add an approval gate, do not push Phase 35 proof or PR copy into this surface, and do not let warning findings create a new autonomous refusal branch.</action>
  <acceptance_criteria>
    - `rg -n "warning-only findings|accepting the suggested narrower request|fatal findings still prevent" test/kiln_web/live/attach_entry_live_test.exs` finds the proof cases.
    - `rg -n "start_for_attached_request" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs` shows the final launch path still uses the existing Runs seam.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/kiln_web/live/attach_entry_live_test.exs</automated>
  </verify>
  <done>Warning findings now narrow or reshape the attach UX without creating hidden refusals, and final run start still routes through the existing deterministic backend seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisory report -> LiveView rendering | The UI must faithfully distinguish blocked vs warning semantics and avoid inventing client-side preflight logic. |
| warning acceptance -> backend launch path | The operator can continue from a warning, but launch must still pass through the existing server-owned start path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | S | `/attach` warning UX | mitigate | Use explicit state branches and stable ids so blocked and warning semantics cannot collapse into one ambiguous UI. |
| T-34-08 | T | warning acceptance flow | mitigate | Accept the suggested narrower request through server-owned form/state updates and only call the existing backend launch seam afterward. |
| T-34-09 | R | evidence rendering | mitigate | Render repo slug, base branch, prior object ids, and next actions directly from the report so warnings stay auditable and factual. |

</threat_model>

<verification>
- `mix test test/kiln_web/live/attach_entry_live_test.exs`
- `rg -n "attach-warning|attach-narrowing-accept|attach-warning-edit|attach-warning-inspect|attach-run-started|start_for_attached_request" lib/kiln_web/live/attach_entry_live.ex test/kiln_web/live/attach_entry_live_test.exs`
- `bash script/precommit.sh`
</verification>

<success_criteria>
- `/attach` clearly separates blocked safety failures from warning-only narrowing guidance.
- Warning states show concrete evidence and default-forward narrowing actions without auto-refusing the run.
- After the operator narrows or edits, final run start still flows through `Runs.start_for_attached_request/3`.
</success_criteria>

<output>
After completion, create `.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-03-SUMMARY.md`
</output>
