# Deferred items — Phase 26 first live template run

- `bash script/precommit.sh` still reports an existing repo-level `check_no_signature_block` violation on `priv/workflows/_test_bogus_signature.yaml`. This file is outside Plan `26-03` scope and was not modified here.
