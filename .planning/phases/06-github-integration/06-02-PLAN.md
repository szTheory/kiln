---
phase: 06-github-integration
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/github.ex
  - lib/kiln/github/cli.ex
  - lib/kiln/github/checks.ex
  - test/kiln/github/cli_test.exs
  - test/kiln/github/checks_test.exs
autonomous: true
requirements:
  - GIT-02
  - GIT-03
must_haves:
  truths:
    - "`gh` invocations use argv list + `--json` for machine-parseable PR creation output"
    - "Check suite summarization produces required-vs-optional conclusions per D-G01–D-G03"
    - "Auth/permission failures map to `:gh_auth_expired` / `:gh_permissions_insufficient`"
  artifacts:
    - path: "lib/kiln/github/cli.ex"
      provides: "gh pr create wrapper"
      contains: "pr_create"
    - path: "lib/kiln/github/checks.ex"
      provides: "merge predicate inputs"
      contains: "required"
  key_links:
    - from: "lib/kiln/github.ex"
      to: "lib/kiln/github/cli.ex"
      via: "replace empty module"
      pattern: "defmodule Kiln.GitHub"
---

<objective>
Replace the **placeholder** `Kiln.GitHub` module with a **`gh` CLI boundary**: **PR create** (artifact-frozen args) and **checks summarization** (parse `gh api` JSON) suitable for Plan 03 workers and Plan 04 merge predicate. No LiveView.

Purpose: GIT-02 (`gh pr create` idempotency inputs) + GIT-03 (checks data shape) with BLOCK-01 typed errors for auth.
Output: `lib/kiln/github/cli.ex`, `lib/kiln/github/checks.ex`, refactor `lib/kiln/github.ex` as delegating API + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-github-integration/06-CONTEXT.md
@.planning/phases/06-github-integration/06-RESEARCH.md
@lib/kiln/github.ex
@priv/playbooks/v1/gh_auth_expired.md
@priv/playbooks/v1/gh_permissions_insufficient.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gh CLI runner + classify auth errors</name>
  <read_first>
    - lib/kiln/git/cmd.ex
    - lib/kiln/github.ex
  </read_first>
  <acceptance_criteria>
    - `lib/kiln/github/cli.ex` exists and defines `pr_create/2` or `create_pr/2`
    - `grep -c ":gh_auth_expired" lib/kiln/github/cli.ex` is `>= 1` OR same atom appears in `lib/kiln/github.ex`
    - `mix test test/kiln/github/cli_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/github/cli.ex, lib/kiln/github.ex, test/kiln/github/cli_test.exs</files>
  <action>
Implement **`Kiln.GitHub.Cli`** with injectable runner (mirror `Kiln.Git` pattern).

**`create_pr/2`** accepts map with **string keys** exactly: `"title"`, `"body"`, `"base"`, `"head"`, `"draft"` (boolean), `"reviewers"` (list string, may be empty). Invokes:

`gh pr create --title ... --body-file <tmp> OR heredoc avoided` — **prefer `--body` with escaped content** only if size < argv limits; otherwise write temp file under `System.tmp_dir!/0` with `0600` perms and pass `--body-file`.

Require **`--json number,url,headRefName,baseRefName,isDraft`** on success parse.

**`classify_gh_error/2`**: map stderr + exit → `:gh_auth_expired` | `:gh_permissions_insufficient` | `:gh_cli_failed` (fallback).

Expand **`lib/kiln/github.ex`** with `@moduledoc` and thin delegates to `Cli` / `Checks`.
  </action>
  <verify>
    <automated>mix test test/kiln/github/cli_test.exs --max-failures=1</automated>
  </verify>
  <done>PR create + auth classification testable without network.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checks summarization + merge predicate inputs</name>
  <read_first>
    - .planning/phases/06-github-integration/06-CONTEXT.md
    - lib/kiln/audit/event_kind.ex
  </read_first>
  <acceptance_criteria>
    - `lib/kiln/github/checks.ex` defines pure `summarize/2` or `merge_inputs/3`
    - Fixture JSON under `test/fixtures/github/check_runs.json` exists (committed file)
    - `mix test test/kiln/github/checks_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/github/checks.ex, test/fixtures/github/check_runs.json, test/kiln/github/checks_test.exs</files>
  <action>
Add **fixture** mimicking `check-runs` list response (2 required + 1 optional). Implement summarizer returning **struct or map** with:

- `:head_sha` (string)
- `:required` → list of `%{id: integer(), name: string(), conclusion: string() | nil, status: string()}`
- `:predicate_pass` → boolean per D-G01–D-G03 (required all `success`; `skipped`/`neutral` on optional ignored)

**Draft rule D-G04:** if `is_draft: true` then `predicate_pass == false` regardless of checks.

Document **legacy commit status** gap (D-G20): if input lacks check-runs, return `{:error, :checks_transport_unsupported}` — no silent pass.
  </action>
  <verify>
    <automated>mix test test/kiln/github/checks_test.exs --max-failures=1</automated>
  </verify>
  <done>Pure merge-input builder ready for CheckPoller + Promoter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifacts → gh | only materialized title/body; no env interpolation |
| gh stderr → logs | treat as credential-bearing; redact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | I | PR body | mitigate | no secrets; artifact refs only |
| T-06-11 | E | gh token | mitigate | never pass token on argv; rely on gh auth |
| T-06-12 | T | JSON decode | mitigate | Jason.decode! guarded; invalid → error tuple |
</threat_model>

<verification>
mix test test/kiln/github/cli_test.exs test/kiln/github/checks_test.exs
</verification>

<success_criteria>
GitHub CLI + checks summarization covered by fixtures; auth errors typed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-integration/06-02-SUMMARY.md`
</output>
