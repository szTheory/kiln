---
phase: 06-github-integration
plan: "04"
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - priv/repo/migrations/*_runs_github_delivery_snapshot.exs
  - lib/kiln/runs/run.ex
  - lib/kiln/github/promoter.ex
  - lib/kiln/audit.ex
  - priv/audit_schemas/v1/ci_status_observed.json
  - test/kiln/github/promoter_test.exs
  - test/integration/github_delivery_test.exs
autonomous: true
requirements:
  - GIT-01
  - GIT-02
  - GIT-03
must_haves:
  truths:
    - "`runs.github_delivery_snapshot` jsonb stores last checks summary + pr refs for Phase 7 GIT-03 surfacing"
    - "`Kiln.GitHub.Promoter` (or equivalent) calls `Kiln.Runs.Transitions.transition/3` for `verifying → merged` when predicate passes, and `verifying → planning` with diagnostic map when required checks fail"
    - "Mid-flight kill + Oban retry does not create duplicate `external_operations` completions for same idempotency key (ORCH-07)"
  artifacts:
    - path: "lib/kiln/github/promoter.ex"
      provides: "run state promotion from CI"
      contains: "Transitions.transition"
  key_links:
    - from: "lib/kiln/runs/transitions.ex"
      to: "lib/kiln/github/promoter.ex"
      via: "transition verifying"
      pattern: ":merged"
---

<objective>
Wire **delivery outcomes** into **durable run state**: add **`github_delivery_snapshot`** jsonb, implement **`Kiln.GitHub.Promoter`** to consume completed **`gh_check_observe`** / **`gh_pr_create`** results, update snapshot + emit **`:ci_status_observed`** / **`:git_op_completed`** / **`:pr_created`** audits where missing, and drive **`Transitions`** edges per ROADMAP SC3–4.

Purpose: close verifier loop for GIT-03 + preserve GIT-01 idempotency across worker crashes (integration proof).
Output: migration, promoter module, JSV schema for `ci_status_observed` if not present, tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-github-integration/06-CONTEXT.md
@lib/kiln/runs/transitions.ex
@lib/kiln/runs/run.ex
@lib/kiln/audit.ex
@priv/audit_schemas/v1
@lib/kiln/agents/roles/qa_verifier.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration — runs.github_delivery_snapshot</name>
  <read_first>
    - priv/repo/migrations/20260419000002_create_runs.exs
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - New migration file name matches `*_runs_github_delivery_snapshot.exs`
    - `grep "github_delivery_snapshot" lib/kiln/runs/run.ex` matches
    - `mix test test/kiln/runs/run_test.exs --max-failures=1` OR dedicated migration test exits 0
  </acceptance_criteria>
  <files>priv/repo/migrations/*_runs_github_delivery_snapshot.exs, lib/kiln/runs/run.ex</files>
  <action>
Run `mix ecto.gen.migration runs_github_delivery_snapshot`.

Add nullable **`:map`** column `github_delivery_snapshot` with default `%{}` in migration comment: stores `%{ "pr" => %{...}, "checks" => %{...}, "predicate_pass" => bool, "updated_at" => iso8601 }` — exact keys documented in `@moduledoc` on `Run`.

Update **`Run`** schema + any `cast`/`change` functions that must allow internal promoter updates (prefer dedicated **`Kiln.Runs.promote_github_snapshot/2`** in `lib/kiln/runs.ex` rather than public cast from web).
  </action>
  <verify>
    <automated>mix test test/kiln/runs/run_test.exs --max-failures=1 || mix test test/kiln/runs/ --max-failures=1</automated>
  </verify>
  <done>Run row persists GitHub delivery snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promoter — transitions + audits</name>
  <read_first>
    - lib/kiln/runs/transitions.ex
    - lib/kiln/audit.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "Transitions.transition" lib/kiln/github/promoter.ex` matches at least two lines (merged + planning paths) OR two distinct function heads calling transition
    - `grep -n ":ci_status_observed" lib/kiln/audit.ex priv/audit_schemas/v1/ci_status_observed.json` matches in at least one file
    - `mix test test/kiln/github/promoter_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/github/promoter.ex, lib/kiln/audit.ex, priv/audit_schemas/v1/ci_status_observed.json, test/kiln/github/promoter_test.exs</files>
  <action>
Implement **`Kiln.GitHub.Promoter.apply_check_result/2`** `(run_id, summary_map)`:

- Loads run; if state != `:verifying`, no-op return `{:ok, :ignored}`.
- Updates `github_delivery_snapshot` via Runs context in same transaction as needed OR before transition (choose one; if split, document ordering to avoid orphan audits).

- If **`predicate_pass`** and not draft: `Kiln.Runs.Transitions.transition(run_id, :merged, %{reason: :github_checks_passed})` — if matrix rejects, use actual meta reason atom allowed by transitions tests (adjust to **`%{}`** payload only if illegal — follow existing verifier transition tests).

- If **failed required check**: `Transitions.transition(run_id, :planning, %{reason: :github_checks_failed, diagnostic: %{...}})` — diagnostic must include **check names + conclusions** (plain strings).

Append **`:ci_status_observed`** audit with JSV-validated payload (add schema file if missing).

**Note:** If `transition/3` does not yet accept arbitrary diagnostic meta, extend **`Transitions`** in this plan with **backward-compatible** meta keys only after reading existing tests — do not break D-87 matrix.
  </action>
  <verify>
    <automated>mix test test/kiln/github/promoter_test.exs --max-failures=1</automated>
  </verify>
  <done>Verifier-equivalent promotion path exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration — idempotent replay after crash</name>
  <read_first>
    - test/integration/rehydration_test.exs
    - lib/kiln/external_operations.ex
  </read_first>
  <acceptance_criteria>
    - File `test/integration/github_delivery_test.exs` exists
    - Test inserts one `external_operations` row with `state: :completed` and re-invokes worker callback path asserting **single** audit count delta OR `{:ok, :already_done}` (document assertion)
    - `mix test test/integration/github_delivery_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>test/integration/github_delivery_test.exs</files>
  <action>
Sandbox integration: create minimal **run** in `:verifying`, insert **completed** `git_push` op with idempotency key, call **`PushWorker.perform/1`** twice — second call must **not** insert duplicate `external_op_completed` (assert audit count via `Kiln.Audit` query or refute duplicate `idempotency_key`).

Use **`Kiln.DataCase`** or existing integration case template.
  </action>
  <verify>
    <automated>mix test test/integration/github_delivery_test.exs --max-failures=1</automated>
  </verify>
  <done>ORCH-07 replay safety demonstrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| promoter → runs | only internal system caller |
| audits → operator | diagnostics contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-30 | E | illegal transition | mitigate | matrix guards; tests for illegal calls |
| T-06-31 | T | forged summary_map | mitigate | only accept promoter calls from workers after op verified |
| T-06-32 | I | audit payload | mitigate | JSV schema rejects oversized strings |
</threat_model>

<verification>
mix test test/kiln/github/promoter_test.exs test/integration/github_delivery_test.exs
</verification>

<success_criteria>
Run snapshot persists; promoter drives allowed transitions; integration proves idempotent replay.
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-integration/06-04-SUMMARY.md`
</output>
