---
phase: 09-dogfood-release-v0-1-0
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - mix.lock
  - config/config.exs
  - config/dev.exs
  - config/runtime.exs
  - lib/kiln/application.ex
  - lib/kiln/telemetry/otel.ex
  - compose.yaml
  - test/kiln/telemetry/otel_smoke_test.exs
autonomous: true
requirements:
  - OBS-02
must_haves:
  truths:
    - "Traces export via OTLP; Phoenix, Bandit, Ecto, Oban instrumented per CONTEXT D-912"
    - "Manual span names `kiln.run.stage`, `kiln.agent.call`, `kiln.docker.op`, `kiln.llm.request` exist as stubs or first real call sites"
  artifacts:
    - path: "compose.yaml"
      provides: "Collector + Jaeger for local proof"
      contains: "4317"
---

<objective>
Deliver **OBS-02**: OpenTelemetry **traces** for stage, agent, Docker, and LLM boundaries with **`opentelemetry_process_propagator`** through **Oban** (and other async boundaries per CONTEXT D-912–D-915). **Defer metrics** (D-910). Add **local compose** services for **OTLP collector → Jaeger** (D-911) without hard-coding vendor in app code.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
@.planning/phases/09-dogfood-release-v0-1-0/09-RESEARCH.md
@lib/kiln/application.ex
@mix.exs
@compose.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hex deps for instrumentation</name>
  <read_first>
    - mix.exs
    - mix.lock
  </read_first>
  <acceptance_criteria>
    - `grep -n "opentelemetry_phoenix" mix.exs` matches
    - `grep -n "opentelemetry_bandit" mix.exs` matches
    - `grep -n "opentelemetry_ecto" mix.exs` matches
    - `grep -n "opentelemetry_oban" mix.exs` matches
    - `grep -n "opentelemetry_process_propagator" mix.exs` matches
  </acceptance_criteria>
  <files>mix.exs, mix.lock</files>
  <action>
Add to **`deps/0`**: `{:opentelemetry_phoenix, "~> 2.0"}`, `{:opentelemetry_bandit, "~> 0.2"}`, `{:opentelemetry_ecto, "~> 0.2"}`, `{:opentelemetry_oban, "~> 1.0"}`, `{:opentelemetry_process_propagator, "~> 0.3"}` (or **current compatible** versions per `mix hex.info` — adjust tilde). Run **`mix deps.get`** and resolve any version conflicts with existing `opentelemetry` / `opentelemetry_exporter`.
  </action>
  <verify>
    <automated>mix deps.compile</automated>
  </verify>
  <done>Lockfile records new instrumenters.</done>
</task>

<task type="auto">
  <name>Task 2: SDK + exporter + instrumenter setup</name>
  <read_first>
    - lib/kiln/application.ex
    - config/runtime.exs
    - config/dev.exs
    - config/config.exs
  </read_first>
  <acceptance_criteria>
    - `grep -n "OpentelemetryPhoenix" lib/kiln/application.ex` OR setup in `Kiln.Telemetry` matches project pattern
    - `grep -n ":opentelemetry" config/dev.exs` OR `config/runtime.exs` matches
    - `grep -n "OpentelemetryEcto" lib/` matches for `Kiln.Repo` telemetry id (often `:kiln_repo` or actual OTP name)
  </acceptance_criteria>
  <files>lib/kiln/application.ex, config/dev.exs, config/runtime.exs, lib/kiln/telemetry/otel.ex</files>
  <action>
Create **`lib/kiln/telemetry/otel.ex`** (or extend **`Kiln.Telemetry`**) with a **`setup/0`** that: starts **OTLP exporter** when `OTEL_EXPORTER_OTLP_ENDPOINT` set (no-op or console in test per `MIX_ENV`); calls **`OpentelemetryBandit.setup()`**; **`OpentelemetryPhoenix.setup(adapter: :bandit)`**; **`OpentelemetryEcto.setup([:kiln, :repo])`** — **use actual telemetry prefix** from `Kiln.Repo` config; **`OpentelemetryOban.setup()`** (or documented equivalent for current oban_otel version). Add **`OpentelemetryProcessPropagator`** fetch/attach per Hex README for **Oban perform**. Invoke **`setup`** from **`Application.start/2`** **after** core supervision but **before** accepting traffic if order matters.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Booting dev with OTLP endpoint does not crash.</done>
</task>

<task type="auto">
  <name>Task 3: Semantic span helpers (stubs wired)</name>
  <read_first>
    - lib/kiln/application.ex
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "kiln.run.stage" lib/` matches at least one module
    - `grep -n "kiln.docker.op" lib/` matches
    - `grep -n "OpenTelemetry.Tracer" lib/kiln/telemetry/otel.ex` OR dedicated `lib/kiln/telemetry/spans.ex` matches
  </acceptance_criteria>
  <files>lib/kiln/telemetry/spans.ex, lib/kiln/sandboxes/*.ex (or executor — planner discretion)</files>
  <action>
Add **`Kiln.Telemetry.Spans`** (module name adjustable) exposing **`with_run_stage/2`**, **`with_agent_call/2`**, **`with_docker_op/2`**, **`with_llm_request/2`** that delegate to **`OpenTelemetry.Tracer.with_span/3`** with literal names **`kiln.run.stage`**, **`kiln.agent.call`**, **`kiln.docker.op`**, **`kiln.llm.request`** and **bounded** attributes only (`run_id` binary copy limits, **no** prompt bodies). Wire **at least one real call site** each: e.g. **sandbox `System.cmd`**, **Req LLM adapter**, **stage transition** command — if full wiring is blocked by missing Phase 4/5 hooks, **wrap nearest existing** long-running IO with TODO linking to future phase (**document in SUMMARY**).
  </action>
  <verify>
    <automated>mix test test/kiln/telemetry/otel_smoke_test.exs --max-failures=1</automated>
  </verify>
  <done>Smoke test proves a span can be started in test (OTel Elixir testing exporter pattern).</done>
</task>

<task type="auto">
  <name>Task 4: compose — collector + Jaeger</name>
  <read_first>
    - compose.yaml
    - .planning/phases/09-dogfood-release-v0-1-0/09-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "4317" compose.yaml` matches (OTLP gRPC default) OR documents OTLP HTTP port
    - `grep -n "jaeger" compose.yaml` matches case-insensitive
    - `grep -n "otel-collector" compose.yaml` OR `collector` service name documented in README cross-link (plan 03 may complete README — here add **comment** in compose if needed)
  </acceptance_criteria>
  <files>compose.yaml, deploy/otel-collector.yaml (optional)</files>
  <action>
Extend **`compose.yaml`** with **`otel-collector`** (official image `otel/opentelemetry-collector-contrib` pinned digest or minor tag) receiving **OTLP** on **4317**, exporting to **`jaeger`** (`jaegertracing/all-in-one` pinned). Wire **environment** snippet for **`kiln`** service: **`OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4317`** (adjust host to service name). **Do not** commit secrets.
  </action>
  <verify>
    <automated>docker compose config</automated>
  </verify>
  <done>`docker compose config` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OTel ↔ operators | Exporters receive metadata only — **no** secrets, prompts, or API keys in attributes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | I | span attrs | mitigate | Allowlist attributes; redact `DATABASE_URL` host if ever attached |
| T-09-04 | E | OTLP endpoint | mitigate | Default localhost/collector only; prod via env |
</threat_model>

<verification>
mix test test/kiln/telemetry/otel_smoke_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
Dev stack can export traces to local Jaeger; Oban + HTTP + DB spans appear in a manual Jaeger run (record evidence path in SUMMARY).
</success_criteria>

<output>
After completion, create `.planning/phases/09-dogfood-release-v0-1-0/09-02-SUMMARY.md`
</output>
