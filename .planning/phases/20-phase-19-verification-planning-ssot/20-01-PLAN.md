---
phase: 20-phase-19-verification-planning-ssot
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md
autonomous: true
requirements:
  - SELF-01
  - FEEDBACK-01
milestone: v0.3.0
must_haves:
  truths:
    - "`19-VERIFICATION.md` exists beside Phase 19 artifacts with `status: passed` only after listed Mix commands exit 0"
    - "Must-haves table names SELF-01 and FEEDBACK-01 with results traceable to Phase 19 plan scope"
  artifacts:
    - path: ".planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md"
      provides: "Formal three-source verification record for Phase 19"
      contains: "SELF-01"
  key_links:
    - from: ".planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md"
      to: ".planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md"
      via: "command list"
      pattern: "scoped mix test"
---

<objective>
Author **`.planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md`** mirroring **Phase 18** verification shape: YAML frontmatter (`status: draft` until commands green, then **`status: passed`**), **Automated** section with **`mix compile --warnings-as-errors`** plus the **scoped test invocation** below (single line or broken with `\` — keep copy-paste friendly), **Must-haves** table for **SELF-01** and **FEEDBACK-01**, **Human verification: none**. After file content is correct, **run** every cited command from repo root; on success set frontmatter **`status: passed`** and **`verified: 2026-04-22`** (or actual date).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/20-phase-19-verification-planning-ssot/20-CONTEXT.md
@.planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md
@.planning/phases/18-cost-hints-budget-alerts/18-VERIFICATION.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 19-VERIFICATION.md shell</name>
  <read_first>
    - .planning/phases/18-cost-hints-budget-alerts/18-VERIFICATION.md
    - .planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - `test -f .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
    - `grep -q 'SELF-01' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
    - `grep -q 'FEEDBACK-01' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
    - `grep -q 'mix compile --warnings-as-errors' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
    - `grep -q 'test/kiln/runs/post_mortem_test.exs' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
    - `grep -q 'operator_nudges_test.exs' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0
  </acceptance_criteria>
  <files>.planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md</files>
  <action>
Create **`.planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md`** with YAML frontmatter keys **`status`**, **`phase`**, **`verified`** (use `status: draft` until final grep in Task 2). Body sections:

1. **`# Phase 19 verification`**
2. **`## Automated`** — include **exactly** this command block (single ``` fence) so operators can copy-paste:

```
mix compile --warnings-as-errors
mix test test/kiln/audit/event_kind_test.exs test/kiln/audit/append_test.exs test/kiln/runs/post_mortem_test.exs test/kiln/oban/post_mortem_materialize_worker_test.exs test/kiln/operator_nudges_test.exs test/kiln_web/live/run_detail_live_test.exs --max-failures 5
```

3. **`## Must-haves (from plans)`** — markdown table with rows:
   - **SELF-01** — structured post-mortem persistence + materialize worker + run detail surface — **Covered** by `post_mortem_test.exs`, `post_mortem_materialize_worker_test.exs`, `run_detail_live_test.exs`
   - **FEEDBACK-01** — `operator_feedback_received` taxonomy + nudge path — **Covered** by `event_kind_test.exs`, `append_test.exs`, `operator_nudges_test.exs`, `run_detail_live_test.exs`

4. **`## Human verification`** — `None required (LiveView + unit tests cover operator paths).`

Do **not** set `status: passed` until Task 2 succeeds.
  </action>
  <verify>
    <automated>grep -q 'Phase 19 verification' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md</automated>
  </verify>
  <done>Verification file exists with required sections and command block.</done>
</task>

<task type="auto">
  <name>Task 2: Run automated block and flip status</name>
  <read_first>
    - .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md
  </read_first>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 from repo root
    - `mix test test/kiln/audit/event_kind_test.exs test/kiln/audit/append_test.exs test/kiln/runs/post_mortem_test.exs test/kiln/oban/post_mortem_materialize_worker_test.exs test/kiln/operator_nudges_test.exs test/kiln_web/live/run_detail_live_test.exs --max-failures 5` exits 0
    - `grep -q '^status: passed' .planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md` exits 0 after edits
  </acceptance_criteria>
  <files>.planning/phases/19-post-mortems-soft-feedback/19-VERIFICATION.md</files>
  <action>
From repository root (`/Users/jon/projects/kiln`), run the **two commands** exactly as printed in `19-VERIFICATION.md` **Automated** section. If either fails, **leave** `status: draft` and append a **`## Execution log`** subsection with exit codes — **do not** claim pass. If both succeed, set frontmatter **`status: passed`**, **`verified:`** ISO date, and optional **`phase: 19-post-mortems-soft-feedback`** string matching sibling artifacts.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Verification status reflects actual command results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs ↔ operator | Verification doc must not invent passing status without running commands |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | `19-VERIFICATION.md` | Mitigate | `status: passed` only after successful local command run recorded in repo |
| T-20-02 | Repudiation | SSOT | Accept | Git history is audit trail for doc edits |

## Residual Risks

- CI environment differs from local Mix — mitigation: same commands as CI `mix test` subsets used elsewhere in project.

</threat_model>
