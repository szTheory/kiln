---
phase: 36-remote-access-and-operator-auth
plan: 03
type: execute
wave: 2
depends_on: [36-01, 36-02]
files_modified:
  - test/kiln_web/route_gate_test.exs
  - docs/remote-access-smoke.md
autonomous: true
requirements: [REMOTE-01, REMOTE-02, REMOTE-03]
must_haves:
  truths:
    - "The public health probe stays open while dashboard routes stay locked"
    - "Authenticated access still reaches the dashboard after auth is added"
    - "The operator has a written remote smoke checklist for tailnet access"
  artifacts:
    - path: "test/kiln_web/route_gate_test.exs"
      provides: "route-level auth posture regression"
    - path: "docs/remote-access-smoke.md"
      provides: "manual tailnet smoke runbook"
  key_links:
    - from: "test/kiln_web/route_gate_test.exs"
      to: "lib/kiln_web/router.ex"
      via: "unauthenticated redirect and authenticated pass-through assertions"
      pattern: "live\(|redirected_to|/users/log_in"
    - from: "docs/remote-access-smoke.md"
      to: "compose.yaml"
      via: "runbook tells the operator which profile to start"
      pattern: "docker compose --profile remote"
---

<objective>
Prove the remote dashboard posture from the operator's point of view and hand the operator a smoke test they can run from another device.

Purpose: Phase 36 is only done when the auth gate and the private tunnel are both verifiable, not just implemented.
Output: route-gate regression and a tailnet smoke runbook.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/36-remote-access-and-operator-auth/36-CONTEXT.md
@.planning/phases/36-remote-access-and-operator-auth/36-RESEARCH.md
@lib/kiln_web/router.ex
@lib/kiln_web/endpoint.ex
@lib/kiln_web/plugs/health.ex
@test/kiln_web/session_controller_test.exs
@test/kiln_web/router_auth_test.exs

<interfaces>
Use the existing LiveView and ConnTest style already used in Kiln tests:
```elixir
{:ok, view, _html} = live(conn, ~p"/templates")
assert has_element?(view, "#templates-first-run-hero")

assert {:error, {:live_redirect, %{to: to}}} = live(conn, ~p"/some-route")
```

The new route-gate test should use those same helpers and the stable route IDs already present in the dashboard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the route-gate regression</name>
  <files>test/kiln_web/route_gate_test.exs</files>
  <action>Write a focused router test that proves `/health` remains public, the dashboard LiveViews redirect unauthenticated visitors to `/users/log_in`, and an authenticated conn can still reach the dashboard after the Sigra wiring lands.</action>
  <verify>
    <automated>mix test test/kiln_web/route_gate_test.exs</automated>
  </verify>
  <done>The auth gate and health probe posture are both proven by route-level tests.</done>
</task>

<task type="auto">
  <name>Task 2: Write the remote smoke runbook</name>
  <files>docs/remote-access-smoke.md</files>
  <action>Document the exact operator smoke path: start the `remote` compose profile with `TS_AUTHKEY`, open the tailnet MagicDNS URL from another device, confirm the login page appears before the dashboard, and note the expected success/failure signals for the operator.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('docs/remote-access-smoke.md').read_text()
assert 'docker compose --profile remote' in text
assert 'TS_AUTHKEY' in text
assert 'MagicDNS' in text or 'tailnet' in text.lower()
PY</automated>
  </verify>
  <done>The operator has a short, concrete remote smoke checklist that matches the implemented tunnel and auth boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| another device → tailnet URL | the remote smoke path crosses outside the local host |
| smoke checklist → operator action | manual verification must not become ambiguous or open-ended |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-07 | S/T | route-gate test | mitigate | Keep the test focused on real route IDs and `live/2` redirects so stale assumptions do not mask an auth hole. |
| T-36-08 | I | smoke runbook | mitigate | State the tailnet-only URL and the expected login gate so the doc does not accidentally describe a public exposure path. |
| T-36-09 | R | manual verification | mitigate | Make the runbook explicit about what was checked and what a failure looks like so the operator can trust the result later. |
</threat_model>

<verification>
The route test must fail before the auth boundary exists and pass after it is wired. The smoke doc must read as an operator checklist, not a design note.
</verification>

<success_criteria>
- Dashboard routes are proven to redirect unauthenticated visitors.
- `/health` remains public.
- The operator can follow the smoke checklist from another device.
</success_criteria>

<output>
After completion, create `.planning/phases/36-remote-access-and-operator-auth/36-03-SUMMARY.md`
</output>
