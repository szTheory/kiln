---
phase: 19-post-mortems-soft-feedback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/audit/event_kind.ex
  - priv/audit_schemas/v1/operator_feedback_received.json
  - priv/audit_schemas/v1/post_mortem_snapshot_stored.json
  - priv/repo/migrations/*_extend_audit_event_kinds_p19_operator_feedback_post_mortem.exs
  - test/kiln/audit/event_kind_test.exs
autonomous: true
requirements:
  - FEEDBACK-01
  - SELF-01
milestone: v0.3.0
must_haves:
  truths:
    - "`Kiln.Audit.EventKind` includes `:operator_feedback_received` and `:post_mortem_snapshot_stored` appended after existing kinds"
    - "DB CHECK constraint on `audit_events.event_kind` accepts both new atoms"
    - "JSV schemas exist for both kinds under `priv/audit_schemas/v1/`"
  artifacts:
    - path: "priv/audit_schemas/v1/operator_feedback_received.json"
      provides: "payload contract for nudge audit rows"
      contains: "body_preview"
    - path: "priv/audit_schemas/v1/post_mortem_snapshot_stored.json"
      provides: "optional ledger echo after materialize"
      contains: "schema_version"
  key_links:
    - from: "lib/kiln/audit/event_kind.ex"
      to: "priv/repo/migrations/*_extend_audit_event_kinds_p19_operator_feedback_post_mortem.exs"
      via: "values_as_strings/0"
      pattern: "Phase 18 append-only migration"
---

<objective>
Extend the **append-only audit taxonomy** for Phase 19: add **`:operator_feedback_received`** (FEEDBACK-01) and **`:post_mortem_snapshot_stored`** (optional ledger echo per D-1917 / D-1940), ship **JSON Schema Draft 2020-12** files under **`priv/audit_schemas/v1/`**, and an **Ecto migration** that replaces `audit_events_event_kind_check` using the same **generated CHECK list** pattern as Phase 8/18 migrations.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-mortems-soft-feedback/19-CONTEXT.md
@.planning/phases/19-post-mortems-soft-feedback/19-RESEARCH.md
@.planning/phases/19-post-mortems-soft-feedback/19-VALIDATION.md
@lib/kiln/audit/event_kind.ex
@priv/repo/migrations/20260421224335_extend_audit_event_kinds_p8_follow_up_drafted.exs
@priv/audit_schemas/v1/budget_threshold_crossed.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: EventKind atoms + JSON schemas + migration</name>
  <read_first>
    - lib/kiln/audit/event_kind.ex
    - priv/repo/migrations/20260421224335_extend_audit_event_kinds_p8_follow_up_drafted.exs
    - priv/audit_schemas/v1/budget_threshold_crossed.json
  </read_first>
  <acceptance_criteria>
    - `grep -q ':operator_feedback_received' lib/kiln/audit/event_kind.ex` exits 0
    - `grep -q ':post_mortem_snapshot_stored' lib/kiln/audit/event_kind.ex` exits 0
    - `test -f priv/audit_schemas/v1/operator_feedback_received.json` exits 0
    - `test -f priv/audit_schemas/v1/post_mortem_snapshot_stored.json` exits 0
    - `grep -q 'operator_feedback_received' priv/audit_schemas/v1/operator_feedback_received.json` exits 0
    - New migration filename contains `extend_audit_event_kinds_p19` OR `p19_operator` as substring
  </acceptance_criteria>
  <files>lib/kiln/audit/event_kind.ex, priv/audit_schemas/v1/operator_feedback_received.json, priv/audit_schemas/v1/post_mortem_snapshot_stored.json, priv/repo/migrations/*_extend_audit_event_kinds_p19*.exs</files>
  <action>
Append **`:operator_feedback_received`** then **`:post_mortem_snapshot_stored`** to the end of **`@kinds`** in **`Kiln.Audit.EventKind`** (never reorder existing atoms). Update the module doc **kind count** narrative to include **+2** for Phase 19.

Add **`priv/audit_schemas/v1/operator_feedback_received.json`** with **required** string-key fields: **`body_preview`** (operator-visible truncated text, max length enforced later in app — schema uses **`maxLength`** 256), **`grapheme_count`** (integer or string per existing project JSON style — **match `budget_threshold_crossed` style: strings for numeric fields if that is dominant** in adjacent audit schemas; pick one style and document in a one-line comment in schema `title`). Include **`client_message_id`** as optional **UUID string** for idempotency.

Add **`priv/audit_schemas/v1/post_mortem_snapshot_stored.json`** with required **`schema_version`** (string), **`source_watermark`** (string), **`run_id`** (string UUID).

Run **`mix ecto.gen.migration extend_audit_event_kinds_p19_operator_feedback_post_mortem`** (or equivalent slug). Implement **`up`**/**`down`** by copying the **DROP CONSTRAINT + ADD CONSTRAINT** pattern from the Phase 8 migration in **`read_first`**, sourcing the IN-list **only** from **`Kiln.Audit.EventKind.values_as_strings/0`** in **`up`** (no hand-maintained divergent lists).
  </action>
  <verify>
    <automated>mix test test/kiln/audit/event_kind_test.exs test/kiln/audit/event_kind_p3_test.exs --max-failures 1</automated>
  </verify>
  <done>Taxonomy + schemas + migration land.</done>
</task>

<task type="auto">
  <name>Task 2: Register schemas in Audit validation (if required)</name>
  <read_first>
    - lib/kiln/audit.ex
    - lib/kiln/audit/event_kind.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'operator_feedback_received' lib/kiln/audit.ex` exits 0 OR `grep -q 'priv/audit_schemas/v1/operator_feedback_received.json' lib/kiln/audit.ex` exits 0
    - `mix test test/kiln/audit/event_kind_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/audit.ex (or whichever module maps kinds to schema paths)</files>
  <action>
If **`Kiln.Audit.append/1`** requires an explicit registry entry for new kinds, add mappings for **`operator_feedback_received`** and **`post_mortem_snapshot_stored`** to the same mechanism used for **`budget_threshold_crossed`**. If kinds auto-resolve by atom-to-filename convention and **no registry change** is needed, skip code changes and **document “no registry — convention only”** in **`19-01-SUMMARY.md`** only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Audit append path recognizes new kinds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → LiveView | Nudge text is untrusted input (later plans); audit payload is operator-visible |
| Migration → production DB | CHECK must match Ecto enum domain exactly |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | T | audit payload | mitigate | `body_preview` truncated; no raw secrets fields in schema |
| T-19-02 | E | CHECK drift vs Elixir enum | mitigate | Generate CHECK only from `EventKind.values_as_strings/0` |
| T-19-03 | I | forged event_kind string | mitigate | Ecto.Enum + DB CHECK reject unknown kinds |
</threat_model>

<verification>
mix test test/kiln/audit/event_kind_test.exs test/kiln/audit/event_kind_p3_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
New kinds are legal in Elixir and Postgres; JSON schemas exist; migration applies cleanly in test env.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-mortems-soft-feedback/19-01-SUMMARY.md`
</output>

## PLANNING COMPLETE
