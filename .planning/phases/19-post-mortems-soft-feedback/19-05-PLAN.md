---
phase: 19-post-mortems-soft-feedback
plan: "05"
type: execute
wave: 3
depends_on:
  - "19-03-PLAN.md"
  - "19-04-PLAN.md"
files_modified:
  - lib/kiln/stages/stage_worker.ex
  - lib/kiln/operator_nudges.ex
  - lib/kiln_web/live/run_detail_live.ex
  - test/kiln_web/live/run_detail_live_test.exs
  - test/kiln/stages/stage_worker_test.exs
autonomous: true
requirements:
  - FEEDBACK-01
  - SELF-01
milestone: v0.3.0
must_haves:
  truths:
    - "Planning `StageWorker` dispatch advances `runs.operator_nudge_last_audit_id` inside `Repo.transact` before stub_dispatch when pending nudges exist"
    - "`RunDetailLive` shows post-mortem summary panel when `run.state == :merged` with generating vs ready states"
    - "`RunDetailLive` exposes nudge composer with labeled input and server-driven disabled state"
  artifacts:
    - path: "lib/kiln_web/live/run_detail_live.ex"
      provides: "operator post-mortem + nudge UX"
      contains: "post_mortem"
  key_links:
    - from: "lib/kiln/stages/stage_worker.ex"
      to: "lib/kiln/operator_nudges.ex"
      via: "consume_before_planning"
      pattern: "stage boundary DB consumption"
---

<objective>
Wire **FEEDBACK-01 consumption** at the **planning stage boundary** (D-1921–D-1924): extend **`Kiln.OperatorNudges`** with **`consume_pending_for_planning/2`** (exact name planner’s choice) that runs inside **`Kiln.Stages.StageWorker`** **before** **`stub_dispatch/3`** when **`stage_kind == :planning`**, advancing **`runs.operator_nudge_last_audit_id`** transactionally. Ship **`RunDetailLive`** UI: **post-mortem** panel (merged runs; partial vs materialized) and **nudge composer** calling **`OperatorNudges.submit/3`** with **`phx-disable-with`**, **cooldown messaging**, and **PubSub-driven refresh** on **`{:run_state, _}`** or dedicated messages.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-post-mortems-soft-feedback/19-CONTEXT.md
@.planning/phases/19-post-mortems-soft-feedback/19-RESEARCH.md
@lib/kiln_web/live/run_detail_live.ex
@lib/kiln/stages/stage_worker.ex
@lib/kiln/runs/post_mortems.ex
@lib/kiln/operator_nudges.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperatorNudges.consume + StageWorker hook</name>
  <read_first>
    - lib/kiln/stages/stage_worker.ex
    - lib/kiln/operator_nudges.ex
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - `grep -q 'consume' lib/kiln/operator_nudges.ex` exits 0
    - `grep -q ':planning' lib/kiln/stages/stage_worker.ex` matches new hook path (e.g. `stage_kind == :planning` within perform)
    - `mix test test/kiln/stages/stage_worker_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/operator_nudges.ex, lib/kiln/stages/stage_worker.ex, test/kiln/stages/stage_worker_test.exs</files>
  <action>
Add **`Kiln.OperatorNudges.consume_pending_for_planning(run_id)`** ( arity per design ) that:

1. Opens **`Repo.transact`**.
2. **`FOR UPDATE`** lock **`runs`** row.
3. Selects **`audit_events`** for **`run_id`** with **`event_kind == :operator_feedback_received`**, **`where`** if **`run.operator_nudge_last_audit_id` is `nil`** match all rows else **`audit_events.id > ^run.operator_nudge_last_audit_id`**, ordered by **`id` asc**, **LIMIT 50**.
4. If non-empty: updates **`runs.operator_nudge_last_audit_id`** to **`max(id)`** among selected.
5. Returns **`{:ok, [%{...}]}`** list of structured maps (**no** raw string beyond what is needed for planner stub — may return **`body_preview`** only).

**`StageWorker.perform/1`**: after **`update_stage_run(..., :running)`** and before **`stub_dispatch`**, when **`stage_kind == :planning`**, call consume function; if **`{:error, _}`**, return **`{:error, reason}`** path that fails stage (should not happen — prefer **`{:ok, _}`** always and log).

**Stub-era visibility:** When list non-empty, append to **`stub_dispatch`** planning artifact body a line **`operator_nudges_consumed: N`** where **N** is integer count (grep-verifiable in tests).

Extend **`stage_worker_test`** to insert run + two audit rows + call worker path OR unit-test **`OperatorNudges.consume`** in isolation with Sandbox.
  </action>
  <verify>
    <automated>mix test test/kiln/stages/stage_worker_test.exs --max-failures 1</automated>
  </verify>
  <done>Consumption advances cursor at planning boundary.</done>
</task>

<task type="auto">
  <name>Task 2: RunDetailLive post-mortem panel + nudge composer</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - test/kiln_web/live/run_detail_live_test.exs
    - prompts/kiln-brand-book.md
  </read_first>
  <acceptance_criteria>
    - `grep -q 'post_mortem' lib/kiln_web/live/run_detail_live.ex` OR `grep -q 'Post-mortem' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -q 'Operator note' lib/kiln_web/live/run_detail_live.ex` OR `grep -q 'operator note' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `grep -q 'phx-disable-with' lib/kiln_web/live/run_detail_live.ex` exits 0
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures 1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
In **`mount/3`**, **`preload`** **`[:post_mortem]`** when assoc exists (use **`Repo.preload`** via **`Runs.get/1`** wrapper if needed — follow existing **`RunDetailLive`** data loading patterns).

Assigns:

- **`@post_mortem`** — **`nil`**, **`%PostMortem{status: :pending}`**, or complete struct
- **`@nudge_form`** — **`to_form(%{"body" => ""}, as: :nudge)`** per Phoenix 1.8 rules

Template region near **header / status**:

1. **Post-mortem panel** when **`@run.state == :merged`**: if **no row** → show **“Summary generating…”**; if row **`status: :complete`** → render **`schema_version`** + compact summary (e.g. **stage count** from **`snapshot["stages"]` length** if map).
2. **Nudge composer** when run **not terminal** OR when **`blocked`** — follow **D-1934** separation from block UI (use distinct **`id="operator-nudge-form"`**).

**`handle_event("submit_nudge", ...)`** calls **`OperatorNudges.submit/3`**; on **`{:ok, _}`** put_flash **:info** `"Note recorded"` (exact string optional); on **`{:error, :rate_limited}`** show cooldown copy containing substring **`Try again`** (grep-stable).

**PubSub:** extend **`handle_info`** for run topic to reload post-mortem assign when **`PostMortems.get_by_run_id/1`** changes (planner may use **`{:post_mortem_updated, run_id}`** tuple emitted from worker — if so, broadcast from worker end — **optional**; minimum is **full run reload** on **`{:run_state, run}`** already received).

Tests: **`has_element?(view, "#operator-nudge-form")`** for active run fixture; merged run shows post-mortem placeholder element **`id="post-mortem-panel"`**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures 1</automated>
  </verify>
  <done>LiveView surfaces ship with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → LiveView | CSRF via Phoenix forms; nudge text untrusted |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-40 | X | XSS from nudge | mitigate | HEEx escapes text; no `raw/1` on body |
| T-19-41 | I | CSRF on submit | mitigate | LiveView CSRF token via `.form` |
</threat_model>

<verification>
mix test test/kiln/stages/stage_worker_test.exs test/kiln_web/live/run_detail_live_test.exs && mix compile --warnings-as-errors
</verification>

<success_criteria>
Planning consumes nudges durably; RunDetailLive exposes post-mortem + composer with passing LiveView tests.
</success_criteria>

<output>
After completion, create `.planning/phases/19-post-mortems-soft-feedback/19-05-SUMMARY.md`
</output>

## PLANNING COMPLETE
