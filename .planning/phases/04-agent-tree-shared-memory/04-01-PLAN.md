---
phase: 04-agent-tree-shared-memory
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/20260421000001_create_work_units.exs
  - priv/repo/migrations/20260421000002_create_work_unit_events.exs
  - priv/repo/migrations/20260421000003_work_unit_events_immutability.exs
  - lib/kiln/work_units/work_unit.ex
  - lib/kiln/work_units/dependency.ex
  - lib/kiln/work_units/work_unit_event.ex
  - test/kiln/work_units/work_unit_test.exs
  - test/kiln/repo/migrations/work_unit_events_immutability_test.exs
autonomous: true
requirements:
  - AGENT-04
must_haves:
  truths:
    - "Work-unit coordination state lives in Postgres rows, not OTP process memory"
    - "Ready-queue reads can filter by `blockers_open_count == 0` without replaying event history"
    - "`work_unit_events` is append-only at the database layer"
    - "Every work unit and dependency row is scoped to a run and survives BEAM restarts"
  artifacts:
    - path: "priv/repo/migrations/20260421000001_create_work_units.exs"
      provides: "mutable work-unit read model plus blocker-edge table"
      contains: "create table(:work_units"
    - path: "priv/repo/migrations/20260421000002_create_work_unit_events.exs"
      provides: "immutable work-unit event ledger table"
      contains: "create table(:work_unit_events"
    - path: "lib/kiln/work_units/work_unit.ex"
      provides: "current-state work-unit schema"
      contains: "schema \"work_units\""
    - path: "lib/kiln/work_units/work_unit_event.ex"
      provides: "append-only event schema"
      contains: "schema \"work_unit_events\""
  key_links:
    - from: "lib/kiln/work_units/dependency.ex"
      to: "lib/kiln/work_units/work_unit.ex"
      via: "blocked/blocker work-unit foreign keys"
      pattern: "field\\(:blocked_work_unit_id|field\\(:blocker_work_unit_id"
    - from: "priv/repo/migrations/20260421000003_work_unit_events_immutability.exs"
      to: "test/kiln/repo/migrations/work_unit_events_immutability_test.exs"
      via: "trigger/rule enforcement verification"
      pattern: "append-only"
---

<objective>
Create the durable storage floor for Phase 4: `work_units` as the mutable coordination table, `work_unit_dependencies` as blocker edges, and `work_unit_events` as the append-only ledger. This is the source-of-truth layer for AGENT-04 and must follow the repo's existing Ecto/Postgres durability patterns exactly.

Purpose: Postgres remains the source of truth for agent coordination. OTP processes in later plans consume this store; they do not own it.
Output: migrations, schemas, and immutability tests for the work-unit storage layer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-agent-tree-shared-memory/04-RESEARCH.md
@.planning/phases/04-agent-tree-shared-memory/04-PATTERNS.md
@priv/repo/migrations/20260419000002_create_runs.exs
@priv/repo/migrations/20260419000003_create_stage_runs.exs
@priv/repo/migrations/20260418000004_audit_events_immutability.exs
@lib/kiln/runs/run.ex
@lib/kiln/stages/stage_run.ex
@lib/kiln/audit/event.ex

<interfaces>
From `lib/kiln/stages/stage_run.ex`:

```elixir
@primary_key {:id, :binary_id, autogenerate: false, read_after_writes: true}
@foreign_key_type :binary_id
field(:agent_role, Ecto.Enum, values: @agent_roles)
field(:state, Ecto.Enum, values: @states, default: :pending)
```

From `lib/kiln/audit/event.ex`:

```elixir
schema "audit_events" do
  field(:event_kind, Ecto.Enum, values: EventKind.values())
  field(:payload, :map, default: %{})
  field(:occurred_at, :utc_datetime_usec)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the `work_units` and blocker-edge schema floor</name>
  <files>priv/repo/migrations/20260421000001_create_work_units.exs, lib/kiln/work_units/work_unit.ex, lib/kiln/work_units/dependency.ex, test/kiln/work_units/work_unit_test.exs</files>
  <behavior>
    - Test 1: a valid work unit inserts with uuidv7 id, `state: :open`, and `blockers_open_count: 0`
    - Test 2: invalid `state`, invalid `agent_role`, and negative `priority` fail with changeset errors tied to named DB constraints
    - Test 3: dependency rows reject self-links and duplicate `{blocked_work_unit_id, blocker_work_unit_id}` pairs
    - Test 4: deleting a run with referenced work units or dependencies is rejected by FK restrictions
    - Test 5: the ready-queue partial index exists for `state IN ('open','blocked','in_progress') AND blockers_open_count = 0`
  </behavior>
  <action>
Generate `20260421000001_create_work_units.exs` with `mix ecto.gen.migration create_work_units`, then edit it to create both `work_units` and `work_unit_dependencies`.

For `work_units`, follow the local `runs`/`stage_runs` migration pattern: uuidv7 primary key, `run_id` FK with `on_delete: :restrict`, `agent_role` and `state` as text columns backed by CHECK constraints, `priority` integer default `100`, `blockers_open_count` integer default `0`, `claimed_by_role`, `claimed_at`, `closed_at`, `input_payload`, `result_payload`, and `external_ref` fields. Add partial indexes for ready-queue reads and run-scoped lookups. Transfer ownership to `kiln_owner`; grant `INSERT, SELECT, UPDATE` to `kiln_app`; do not grant `DELETE`.

Create `lib/kiln/work_units/work_unit.ex` with the same schema conventions as `Run` and `StageRun`: binary_id PK with `read_after_writes: true`, `Ecto.Enum` for bounded fields, explicit changeset constraint wiring, and no programmatic fields in `cast/3` beyond what later context APIs will own safely.

Create `work_unit_dependencies` in the same migration and map it in `lib/kiln/work_units/dependency.ex` as a first-class schema. Use `blocked_work_unit_id` and `blocker_work_unit_id` FKs back to `work_units`, a uniqueness index on the pair, a CHECK preventing self-links, and run-scoped indexes for blocker fan-out queries. Keep the dependency table purely structural; no process-state shortcuts.

Add `test/kiln/work_units/work_unit_test.exs` to lock the schema and changeset behavior before any runtime logic lands.
  </action>
  <verify>
    <automated>mix test test/kiln/work_units/work_unit_test.exs --max-failures=1</automated>
  </verify>
  <done>`work_units` and `work_unit_dependencies` exist with the expected indexes, enums, FK policies, and schema tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the append-only `work_unit_events` ledger and immutability enforcement</name>
  <files>priv/repo/migrations/20260421000002_create_work_unit_events.exs, priv/repo/migrations/20260421000003_work_unit_events_immutability.exs, lib/kiln/work_units/work_unit_event.ex, test/kiln/repo/migrations/work_unit_events_immutability_test.exs</files>
  <behavior>
    - Test 1: inserting a `work_unit_event` succeeds for the runtime role
    - Test 2: UPDATE as `kiln_app` raises insufficient privilege
    - Test 3: UPDATE/DELETE as `kiln_owner` raises the append-only trigger error
    - Test 4: enabling the RULE with the trigger disabled turns mutation into a silent no-op
    - Test 5: the event schema exposes bounded event kinds and `occurred_at` timestamps
  </behavior>
  <action>
Generate `20260421000002_create_work_unit_events.exs` and `20260421000003_work_unit_events_immutability.exs`. Mirror the `audit_events` posture rather than inventing a new one: binary_id PK, `work_unit_id` FK with `on_delete: :restrict`, enum-backed `event_kind`, `actor_role`, `payload`, and `occurred_at`, ownership transferred to `kiln_owner`, `kiln_app` granted only `INSERT` and `SELECT`, and no `UPDATE` or `DELETE`.

In the immutability migration, clone the proven `audit_events` pattern: `BEFORE UPDATE/DELETE/TRUNCATE` trigger plus disabled-by-default `DO INSTEAD NOTHING` rules. Use work-unit-specific names and error text so the tests assert the right relation.

Create `lib/kiln/work_units/work_unit_event.ex` following the `Kiln.Audit.Event` schema shape. Keep the event ledger separate from `work_units`; do not collapse current state and history into one table.

Add `test/kiln/repo/migrations/work_unit_events_immutability_test.exs` using the same style as `audit_events_immutability_test.exs`, proving each defence-in-depth layer independently.
  </action>
  <verify>
    <automated>mix test test/kiln/repo/migrations/work_unit_events_immutability_test.exs --max-failures=1</automated>
  </verify>
  <done>`work_unit_events` exists as an append-only ledger with DB-enforced immutability and schema coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent runtime -> `Kiln.WorkUnits` storage | transient processes request durable coordination changes |
| app role -> Postgres ledger tables | runtime role must not be able to rewrite history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `work_unit_events` | mitigate | REVOKE mutation privileges, append-only triggers, disabled-by-default RULE safety net |
| T-04-02 | D | ready queue query path | mitigate | denormalize `blockers_open_count` and add partial indexes instead of replaying history on every claim |
| T-04-03 | E | migration ownership/grants | mitigate | transfer ownership to `kiln_owner`; grant only `INSERT, SELECT, UPDATE` on mutable tables and no delete on forensic tables |
</threat_model>

<verification>
Run the targeted schema and migration tests above, then run `mix test test/kiln/work_units/work_unit_test.exs test/kiln/repo/migrations/work_unit_events_immutability_test.exs`.
</verification>

<success_criteria>
The repo has a durable work-unit read model, a separate append-only event ledger, and DB-enforced immutability aligned with the existing audit posture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-tree-shared-memory/04-01-SUMMARY.md`
</output>
