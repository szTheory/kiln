---
phase: 04-agent-tree-shared-memory
plan: "04"
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - lib/kiln/application.ex
  - lib/kiln/runs/run_director.ex
  - test/kiln/application_test.exs
  - test/kiln/runs/run_director_test.exs
  - test/integration/run_subtree_crash_test.exs
  - test/integration/agent_role_crash_test.exs
  - test/integration/rehydration_test.exs
  - test/integration/agent_tree_shared_memory_test.exs
  - test/support/rehydration_case.ex
autonomous: true
requirements:
  - AGENT-03
  - AGENT-04
must_haves:
  truths:
    - "The global `Kiln.Agents.SessionSupervisor` scaffold is removed from the app tree in favor of per-run ownership"
    - "RunDirector and rehydration tests prove runs rebuild their agent subtree from Postgres-backed run state"
    - "A role crash is contained to the affected run subtree; other runs continue untouched"
    - "A blocked work unit stays out of the ready queue until its blockers clear, then another role can claim it after broadcast"
    - "The explicit validation-matrix runtime proof exists: agent role crash containment is covered by a dedicated integration test"
  artifacts:
    - path: "lib/kiln/application.ex"
      provides: "application tree without the global session supervisor scaffold"
      contains: "infra_children"
    - path: "test/integration/run_subtree_crash_test.exs"
      provides: "containment proof using the real Phase 4 agent subtree"
      contains: "RunSubtree"
    - path: "test/integration/agent_tree_shared_memory_test.exs"
      provides: "end-to-end role handoff and ready-queue proof"
      contains: "claim_next_ready"
    - path: "test/integration/agent_role_crash_test.exs"
      provides: "dedicated role-crash containment proof"
      contains: "Process.exit"
  key_links:
    - from: "lib/kiln/runs/run_director.ex"
      to: "lib/kiln/runs/run_subtree.ex"
      via: "boot-scan rehydration"
      pattern: "DynamicSupervisor.start_child"
    - from: "test/integration/agent_tree_shared_memory_test.exs"
      to: "lib/kiln/work_units/pubsub.ex"
      via: "after-commit role handoff assertions"
      pattern: "work_units:run:"
---

<objective>
Finish the Phase 4 runtime by removing the remaining global scaffold, wiring the application and rehydration surface to the per-run tree, and proving the role/work-unit interaction end to end.

Purpose: This closes the gap between the new storage/runtime layers and the live app. Phase 4 is only complete when crash containment, rehydration, and role handoff are all exercised against the real per-run tree.
Output: application/runtime cleanup plus integration coverage for containment, rehydration, and shared-memory handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-tree-shared-memory/04-RESEARCH.md
@lib/kiln/application.ex
@lib/kiln/runs/run_director.ex
@lib/kiln/runs/run_subtree.ex
@test/kiln/application_test.exs
@test/integration/run_subtree_crash_test.exs
@test/integration/rehydration_test.exs
@test/support/rehydration_case.ex

<interfaces>
From `lib/kiln/application.ex` today:

```elixir
infra_children do
  ...
  Kiln.Agents.SessionSupervisor,
  ...
end
```

From `test/integration/run_subtree_crash_test.exs`:

```elixir
RunSubtree.lived_child_pid(run.id)
Process.exit(lived_child_a, :kill)
```

This plan updates those tests/helpers to target the real per-run role subtree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the backward-compat app-tree scaffold and refresh runtime ownership tests</name>
  <files>lib/kiln/application.ex, lib/kiln/runs/run_director.ex, test/kiln/application_test.exs, test/kiln/runs/run_director_test.exs, test/support/rehydration_case.ex</files>
  <behavior>
    - Test 1: the application tree no longer boots a global `Kiln.Agents.SessionSupervisor`
    - Test 2: `RunDirector` still boot-scans active runs and spawns per-run subtrees without global session state
    - Test 3: rehydration helpers wait on the real per-run subtree/role pids instead of a placeholder child
    - Test 4: app-tree child-count assertions are updated to the new Phase 4 count and composition
  </behavior>
  <action>
Update `lib/kiln/application.ex` to remove the Phase 3 global `Kiln.Agents.SessionSupervisor` scaffold from `infra_children/0`. Preserve the rest of the Phase 3 boot order and do not move per-run ownership outside `RunSupervisor -> RunSubtree`. This task is the planned removal point so 04-03 can remain independently runnable.

Adjust `lib/kiln/runs/run_director.ex` only where needed so boot-scan/restart logic works with the real per-run subtree helpers introduced in 04-03. Keep the rehydration contract DB-driven; do not add a second in-memory ownership table for role state.

Refresh `test/kiln/application_test.exs`, `test/kiln/runs/run_director_test.exs`, and `test/support/rehydration_case.ex` to target the real per-run subtree and the new child-count/app-tree shape. Replace any reliance on the Phase 2/3 placeholder lived child with helpers that discover the session supervisor or role pids under a given run.
  </action>
  <verify>
    <automated>mix test test/kiln/application_test.exs test/kiln/runs/run_director_test.exs --max-failures=1</automated>
  </verify>
  <done>The live app tree no longer relies on the global session scaffold, and rehydration helpers target the real per-run runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove crash containment, rehydration, and role handoff end to end</name>
  <files>test/integration/run_subtree_crash_test.exs, test/integration/agent_role_crash_test.exs, test/integration/rehydration_test.exs, test/integration/agent_tree_shared_memory_test.exs</files>
  <behavior>
    - Test 1: killing a role process under one run restarts that run's role subtree or leaves it awaiting rehydration, while another run keeps running
    - Test 2: `agent_role_crash_test.exs` proves dedicated role-crash containment against the real Phase 4 role tree
    - Test 3: after a simulated restart, a run in an active state rebuilds its per-run agent subtree from Postgres-backed run state
    - Test 4: a blocked work unit is absent from the ready queue until blocker edges clear
    - Test 5: once blockers clear and the transaction commits, the next eligible role claims the work after PubSub broadcast
    - Test 6: handoff never creates duplicate work-unit claims under concurrent roles
  </behavior>
  <action>
Replace the placeholder-child assumptions in `test/integration/run_subtree_crash_test.exs` and `test/integration/rehydration_test.exs` with assertions against the real Phase 4 tree: session supervisor and role pids. Keep the central guarantees the same: run-local containment, director survival, and DB-backed rehydration.

Add `test/integration/agent_role_crash_test.exs` as the dedicated validation-matrix crash proof. Use a real role pid under a run-owned session supervisor, kill it abnormally, and prove sibling restart or subtree recovery stays local to that run.

Add `test/integration/agent_tree_shared_memory_test.exs` as the signature Phase 4 integration test. Seed a run plus role-tagged work units and blocker edges, start the run subtree, and prove the sequence: Mayor or Planner claims a ready unit, a dependent unit stays out of the ready queue while blocked, blocker clearance commits, PubSub fires after commit, and the next eligible role claims the dependent work without duplicates.

Use real `Kiln.WorkUnits` APIs and the per-run role tree from 04-03. Do not shortcut the test through direct Repo writes once the setup phase is done.
  </action>
  <verify>
    <automated>mix test test/integration/run_subtree_crash_test.exs test/integration/agent_role_crash_test.exs test/integration/rehydration_test.exs test/integration/agent_tree_shared_memory_test.exs --max-failures=1</automated>
  </verify>
  <done>Phase 4 runtime guarantees are proven end to end: crash containment, DB-backed rehydration, and shared-memory role handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application tree -> per-run subtree | app-wide singleton boot must not own per-run agent state |
| run crash/restart -> durable coordination store | restart must rebuild from Postgres instead of stale process memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | D | app-tree/global scaffold | mitigate | remove the global session supervisor so only per-run ownership exists |
| T-04-12 | D | run crash isolation | mitigate | integration tests kill real role processes and prove other runs stay alive |
| T-04-13 | R | rehydration correctness | mitigate | boot-scan tests assert the subtree is rebuilt from DB-backed active runs |
| T-04-14 | T | handoff race/double claim | mitigate | end-to-end test exercises blocker release + after-commit PubSub + duplicate-claim prevention |
</threat_model>

<verification>
Run the targeted unit and integration tests above, then run `mix test test/kiln/application_test.exs test/integration/run_subtree_crash_test.exs test/integration/agent_role_crash_test.exs test/integration/rehydration_test.exs test/integration/agent_tree_shared_memory_test.exs`.
</verification>

<success_criteria>
Phase 4’s live runtime owns agent sessions per run, rebuilds from Postgres after restart, and proves work-unit handoff without duplicate claims or cross-run crash bleed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-tree-shared-memory/04-04-SUMMARY.md`
</output>
