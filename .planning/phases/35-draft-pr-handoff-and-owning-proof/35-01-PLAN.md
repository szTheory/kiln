---
phase: 35-draft-pr-handoff-and-owning-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/kiln/attach/delivery.ex
  - test/kiln/attach/delivery_test.exs
  - test/integration/github_delivery_test.exs
autonomous: true
requirements:
  - TRUST-04
must_haves:
  truths:
    - "A reviewer-facing attached draft PR shows scoped request framing instead of a generic attached-repo placeholder."
    - "The PR body includes a reviewer-visible `Verification` section wired to the owning proof command, with exact delegated proof-layer citations finalized in the proof-contract plan so the body cannot drift from command truth."
    - "The visible PR body includes branch and base-branch facts plus one `kiln-run:` footer, without exposing raw `attached_repo_id` or replaying Phase 34 warning UI."
  artifacts:
    - path: "lib/kiln/attach/delivery.ex"
      provides: "Frozen attached-repo PR title/body assembly from durable request fields and exact verification citations"
      min_lines: 120
    - path: "test/kiln/attach/delivery_test.exs"
      provides: "Focused seam proof for compact PR body sections, conditional out-of-scope, and omission of internal ids"
      min_lines: 80
    - path: "test/integration/github_delivery_test.exs"
      provides: "Hermetic end-to-end lock on frozen draft PR payload contents"
      min_lines: 120
  key_links:
    - from: "lib/kiln/attach/delivery.ex"
      to: "lib/kiln/specs/spec_revision.ex"
      via: "load promoted attached-request fields for PR body rendering"
      pattern: "change_summary|acceptance_criteria|out_of_scope"
    - from: "lib/kiln/attach/delivery.ex"
      to: "lib/mix/tasks/kiln.attach.prove.ex"
      via: "verification section must stay wired to the owning proof command, with final exact delegated-layer literals landing only after the command contract is updated in Plan 35-02"
      pattern: "mix kiln\\.attach\\.prove"
    - from: "lib/kiln/attach/delivery.ex"
      to: "test/integration/github_delivery_test.exs"
      via: "frozen snapshot body/title asserted through delivery happy path"
      pattern: "github_delivery_snapshot"
---

<objective>
Lock the attached draft PR handoff into a compact reviewer-facing contract built from durable request fields and exact proof citations.

Purpose: Close `TRUST-04` by making the frozen PR body look like a normal bounded feature or bugfix handoff instead of a generic attach placeholder.
Output: Updated delivery formatter plus focused seam and integration coverage for the frozen title/body contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-draft-pr-handoff-and-owning-proof/35-CONTEXT.md
@.planning/phases/35-draft-pr-handoff-and-owning-proof/35-RESEARCH.md
@.planning/phases/35-draft-pr-handoff-and-owning-proof/35-PATTERNS.md
@.planning/phases/31-draft-pr-trust-ramp-and-attach-proof/31-01-SUMMARY.md
@.planning/phases/31-draft-pr-trust-ramp-and-attach-proof/31-02-SUMMARY.md
@.planning/phases/33-repeat-run-continuity-on-attached-repos/33-03-SUMMARY.md
@.planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-CONTEXT.md
@lib/kiln/attach/delivery.ex
@lib/kiln/attach/intake.ex
@lib/kiln/specs/spec_revision.ex
@lib/mix/tasks/kiln.attach.prove.ex
@test/kiln/attach/delivery_test.exs
@test/integration/github_delivery_test.exs

<interfaces>
From lib/kiln/attach/delivery.ex:
```elixir
@spec prepare(Run.t() | Ecto.UUID.t(), Ecto.UUID.t(), Ecto.UUID.t(), keyword()) ::
        {:ok, prepared()} | {:error, term()}

defp frozen_snapshot_fragment(frozen) do
  %{
    "attach" => %{
      "attached_repo_id" => frozen["attached_repo_id"],
      "base_branch" => frozen["base_branch"],
      "branch" => frozen["branch"]
    },
    "pr" => %{
      "title" => frozen["title"],
      "body" => frozen["body"],
      "base" => frozen["base_branch"],
      "head" => frozen["branch"],
      "draft" => true
    }
  }
end
```

From lib/kiln/specs/spec_revision.ex:
```elixir
schema "spec_revisions" do
  field(:request_kind, Ecto.Enum, values: [:feature, :bugfix])
  field(:change_summary, :string)
  field(:acceptance_criteria, {:array, :string}, default: [])
  field(:out_of_scope, {:array, :string}, default: [])
end
```

From lib/mix/tasks/kiln.attach.prove.ex:
```elixir
@proof_layers [
  ["env", "MIX_ENV=test", "mix", "test", "test/integration/github_delivery_test.exs"],
  ["env", "MIX_ENV=test", "mix", "test", "test/kiln/attach/safety_gate_test.exs"],
  ["env", "MIX_ENV=test", "mix", "test", "test/kiln_web/live/attach_entry_live_test.exs"]
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild frozen PR body/title assembly from durable attached-request facts</name>
  <files>lib/kiln/attach/delivery.ex</files>
  <read_first>
    .planning/phases/35-draft-pr-handoff-and-owning-proof/35-CONTEXT.md
    lib/kiln/attach/delivery.ex
    lib/kiln/attach/intake.ex
    lib/kiln/specs/spec_revision.ex
    lib/mix/tasks/kiln.attach.prove.ex
    .planning/phases/34-brownfield-preflight-and-narrowing-guardrails/34-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: per D-3509 and D-3510, the frozen PR body renders `## Summary` and `## Acceptance criteria` from promoted attached-request fields instead of the current generic attached-repo placeholder text.
    - Test 2: per D-3511 and D-3513, `## Out of scope` is omitted when the stored list is empty and rendered only when the promoted request contains clarifying items.
    - Test 3: per D-3504 through D-3507, the `## Verification` section visibly cites `MIX_ENV=test mix kiln.attach.prove` and is structured so Plan 35-02 can replace temporary command-only wording with the exact delegated proof-layer literals from the owning command in one drift-free follow-up.
    - Test 4: per D-3515 through D-3519, the body still includes branch/base facts plus exactly one `kiln-run:` footer, while omitting visible `Attached repo:` or `attached_repo_id` copy and any dedicated warning/preflight section.
  </behavior>
  <action>Update `Kiln.Attach.Delivery` so `create_snapshot/3` builds the frozen PR title/body from durable promoted request fields on the run rather than from attach-only repo facts. Keep the seam inside `Kiln.Attach.Delivery` per phase guidance: load or preload the run's `spec_revision`, derive a compact human-first title from `request_kind` plus `change_summary`, and render the body in this exact order per D-3509 through D-3519: `Summary`, `Acceptance criteria`, conditional `Out of scope`, `Verification`, compact branch/base facts, one `kiln-run: <run_id>` footer. In this plan, wire `Verification` to the owning command `MIX_ENV=test mix kiln.attach.prove` and leave the exact delegated proof-layer literals for Plan 35-02, where the command contract itself is updated. Do not dump `spec_draft.body`, raw metadata, JSON/YAML blobs, naked `attached_repo_id`, or a dedicated Phase 34 warning section. Preserve the existing frozen snapshot contract (`runs.github_delivery_snapshot`) and replay behavior so retries reuse the exact same title/body once frozen.</action>
  <acceptance_criteria>
    - `rg -n "## Summary|## Acceptance criteria|## Verification|kiln-run:" lib/kiln/attach/delivery.ex` finds the rendered section headings and footer code path.
    - `rg -n "MIX_ENV=test mix kiln\\.attach\\.prove" lib/kiln/attach/delivery.ex` finds the owning proof-command citation, and no exact delegated file list is hardcoded before Plan 35-02 updates the command contract.
    - `rg -n "Attached repo:|attached_repo_id" lib/kiln/attach/delivery.ex` returns no matches inside the visible PR body formatter.
    - `rg -n "warning|preflight" lib/kiln/attach/delivery.ex` shows no dedicated warning/preflight body section was introduced.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/kiln/attach/delivery_test.exs test/integration/github_delivery_test.exs</automated>
  </verify>
  <done>The frozen delivery seam emits a compact reviewer-facing title/body derived from durable attached-request fields, preserves a visible `Verification` section wired to `MIX_ENV=test mix kiln.attach.prove`, and keeps replay-safe snapshot freezing intact without pre-locking a delegated file list ahead of Plan 35-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the PR handoff contract with focused seam and integration tests</name>
  <files>test/kiln/attach/delivery_test.exs, test/integration/github_delivery_test.exs</files>
  <read_first>
    test/kiln/attach/delivery_test.exs
    test/integration/github_delivery_test.exs
    lib/kiln/attach/delivery.ex
    .planning/phases/35-draft-pr-handoff-and-owning-proof/35-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the seam test proves the frozen body contains summary, acceptance criteria, a visible `MIX_ENV=test mix kiln.attach.prove` verification citation, branch/base facts, and one `kiln-run:` footer.
    - Test 2: the seam or integration test proves `Out of scope` disappears when the stored list is empty and appears only when present.
    - Test 3: the seam or integration test proves the visible PR body does not contain `Attached repo:` or raw `attached_repo_id`.
    - Test 4: the integration test proves the persisted `github_delivery_snapshot["pr"]["body"]` matches the new reviewer-facing contract through the happy path.
  </behavior>
  <action>Update `test/kiln/attach/delivery_test.exs` and `test/integration/github_delivery_test.exs` first in RED, then GREEN, so they assert the Phase 35 handoff contract from Task 1. Add concrete assertions for the new section headings, the explicit `MIX_ENV=test mix kiln.attach.prove` citation, branch/base facts, the conditional `Out of scope` section, and the absence of visible internal-id leakage. Do not lock the final delegated proof-layer filenames in this plan; Plan 35-02 will add those assertions in the same change that updates `mix kiln.attach.prove`. Reuse the existing frozen-snapshot and happy-path delivery setup instead of adding another transport seam or manual GitHub stub path.</action>
  <acceptance_criteria>
    - `rg -n "## Summary|## Acceptance criteria|MIX_ENV=test mix kiln\\.attach\\.prove|kiln-run:" test/kiln/attach/delivery_test.exs test/integration/github_delivery_test.exs` finds assertions for the new body contract.
    - `rg -n "Out of scope|refute .*Attached repo:|refute .*attached_repo_id" test/kiln/attach/delivery_test.exs test/integration/github_delivery_test.exs` finds the conditional-section and omission assertions.
    - `rg -n "github_delivery_snapshot|stored\\[\"pr\"\\]\\[\"body\"\\]" test/integration/github_delivery_test.exs` finds a persisted-snapshot assertion rather than a formatter-only unit check.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/kiln/attach/delivery_test.exs test/integration/github_delivery_test.exs</automated>
  </verify>
  <done>Focused tests fail before the formatter change, pass afterward, and mechanically lock the reviewer-facing PR handoff contract inside the frozen delivery seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attached request -> `Kiln.Attach.Delivery` | Durable request fields become reviewer-visible PR prose. |
| `Kiln.Attach.Delivery` -> `runs.github_delivery_snapshot` | Once frozen, the PR title/body is replayed on retries and worker re-entry. |
| frozen PR body -> GitHub reviewer | Reviewer trust depends on the body being factual, compact, and free of internal-only identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | I | `lib/kiln/attach/delivery.ex` | mitigate | Render body text only from allowed request/repo facts per D-3509 through D-3519; omit raw `attached_repo_id`, metadata blobs, and warning dumps; lock omission with `test/kiln/attach/delivery_test.exs`. |
| T-35-02 | T | `runs.github_delivery_snapshot` body/title freeze | mitigate | Keep snapshot freezing in `Kiln.Attach.Delivery` and assert persisted `stored["pr"]["body"]`/`stored["pr"]["title"]` in `test/integration/github_delivery_test.exs` so retries cannot silently drift from the reviewed contract. |
| T-35-03 | R | reviewer-visible verification claims | mitigate | Hardcode the exact owning command plus delegated file paths in the body and lock them in tests so cited proof remains rerunnable and auditable. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/kiln/attach/delivery_test.exs test/integration/github_delivery_test.exs`.

Confirm the frozen happy path persists the updated body in `github_delivery_snapshot` and the body text cites the exact proof command and delegated layers.
</verification>

<success_criteria>
`TRUST-04` is satisfied for the delivery seam when attached draft PRs render durable request framing, exact proof citations, repo-fitting branch/base facts, and no raw internal-id or warning-section noise.
</success_criteria>

<output>
After completion, create `.planning/phases/35-draft-pr-handoff-and-owning-proof/35-01-SUMMARY.md`
</output>
