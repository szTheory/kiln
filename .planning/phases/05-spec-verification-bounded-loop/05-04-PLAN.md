---
phase: 05-spec-verification-bounded-loop
plan: "04"
type: execute
wave: 3
depends_on: ["01"]
files_modified:
  - priv/repo/migrations/*_holdout_privileges.exs
  - lib/kiln/repo/verifier_read_repo.ex
  - config/runtime.exs
  - lib/kiln/stages/next_stage_dispatcher.ex
  - test/kiln/specs/holdout_priv_test.exs
  - test/kiln/specs/holdout_manifest_test.exs
autonomous: true
requirements:
  - SPEC-04
must_haves:
  truths:
    - "Integration test proves `kiln_app` role cannot SELECT `holdout_scenarios`"
    - "Verifier code path can read holdouts via dedicated Repo or `sandbox SET ROLE` pattern documented in module doc"
    - "CAS / stage input builder excludes holdout digests for non-verifier kinds (extend `NextStageDispatcher` or documented successor)"
  artifacts:
    - path: "test/kiln/specs/holdout_priv_test.exs"
      provides: "privilege negative test"
      contains: "holdout_scenarios"
  key_links:
    - from: "lib/kiln/stages/next_stage_dispatcher.ex"
      to: "test/kiln/specs/holdout_manifest_test.exs"
      via: "allowlist"
      pattern: "holdout_excluded"
---

<objective>
Implement **SPEC-04** defense in depth: Postgres **`REVOKE SELECT`** on `holdout_scenarios` for `kiln_app`, introduce **`Kiln.Repo.VerifierReadRepo`** (or equivalent second Repo) using credentials with **narrow SELECT** only where migrations allow, integration tests for role separation, and **manifest closure** tests ensuring non-verifier stage input maps never contain holdout body substrings or digests from dispatcher/builder code.

Purpose: holdouts never enter Planner/Coder/Reviewer paths at compile or hydration time.
Output: migration(s), Repo module, tests; minimal production wiring behind feature flag if needed (default off until verifier calls it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
@lib/kiln/stages/next_stage_dispatcher.ex
@priv/repo/migrations/*_create_holdout_scenarios.exs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration — grants for holdout table</name>
  <read_first>
    - priv/repo/migrations/*_create_holdout_scenarios.exs
    - priv/repo/migrations/20260419000002_create_runs.exs
  </read_first>
  <acceptance_criteria>
    - Migration contains `REVOKE ALL ON TABLE holdout_scenarios FROM kiln_app` or equivalent + `GRANT SELECT` to new role `kiln_verifier` (name exact as implemented)
    - `grep "kiln_verifier" priv/repo/migrations/*.exs` non-empty
  </acceptance_criteria>
  <files>priv/repo/migrations/*_holdout_privileges.exs</files>
  <action>
Add role `kiln_verifier` if not present; grant minimal privileges. Ensure `kiln_owner` retains DDL. Document connection URL env var `DATABASE_VERIFIER_URL` in `config/runtime.exs` with safe default falling back to primary URL in **dev only** (test uses sandbox DB config).

Follow project’s existing migration grant style from audit/work_unit migrations.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/holdout_priv_test.exs --max-failures=1</automated>
  </verify>
  <done>DB privileges express SPEC-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `VerifierReadRepo` + privilege tests</name>
  <read_first>
    - lib/kiln/repo.ex
    - config/test.exs
  </read_first>
  <acceptance_criteria>
    - `lib/kiln/repo/verifier_read_repo.ex` exists and `use Ecto.Repo`
    - `test/kiln/specs/holdout_priv_test.exs` runs `Repo` as app role and asserts `Postgrex.Error` or `Ecto.QueryError` with **42501** SQLSTATE on forbidden select OR equivalent project pattern
    - Second assertion: `VerifierReadRepo` can `SELECT` inserted holdout row in test when role configured
  </acceptance_criteria>
  <files>lib/kiln/repo/verifier_read_repo.ex, config/runtime.exs, config/test.exs, test/kiln/specs/holdout_priv_test.exs, lib/kiln/application.ex</files>
  <action>
Supervise second Repo child only when config present (avoid duplicate in tests via `start_supervised`).

Insert fixture holdout in test using `kiln_owner` connection pattern mirroring `audit_events_immutability_test.exs`.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/holdout_priv_test.exs --max-failures=1</automated>
  </verify>
  <done>Role separation proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hydration allowlist / dispatcher audit</name>
  <read_first>
    - lib/kiln/stages/next_stage_dispatcher.ex
    - .planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `test/kiln/specs/holdout_manifest_test.exs` builds a fake artifact manifest map and asserts **no** key contains a holdout digest substring when `holdout_excluded: true` contract path is exercised
    - `grep -n "holdout" lib/kiln/stages/next_stage_dispatcher.ex` shows comment or guard documenting exclusion
  </acceptance_criteria>
  <files>lib/kiln/stages/next_stage_dispatcher.ex, test/kiln/specs/holdout_manifest_test.exs</files>
  <action>
If dispatcher currently only sets booleans, add pure function `artifact_allowlist/2` used by future sandbox hydration (may live in `Kiln.Sandboxes` in stub form) — **callable from tests** without Docker. Document linkage to D-S02c.

Test constructs list of artifact refs including a synthetic holdout digest and asserts filter removes it for `holdout_excluded: true` contexts.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/holdout_manifest_test.exs --max-failures=1</automated>
  </verify>
  <done>Manifest layer documented and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `kiln_app` -> holdout rows | blocked at SQL privilege layer |
| verifier worker -> holdout | narrow read path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | E | Postgres | mitigate | REVOKE + separate role + tests |
| T-05-04b | I | logs/Oban | mitigate | ids+hashes only in job args (assert grep in test if args serialized) |
</threat_model>

<verification>
`mix test test/kiln/specs/holdout_priv_test.exs test/kiln/specs/holdout_manifest_test.exs`
</verification>

<success_criteria>
Privilege and manifest tests green; migrations apply.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-04-SUMMARY.md`
</output>
