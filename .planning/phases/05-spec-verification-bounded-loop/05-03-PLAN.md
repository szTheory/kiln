---
phase: 05-spec-verification-bounded-loop
plan: "03"
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - lib/kiln/specs/verifier_result.ex
  - priv/jsv/verifier_result_v1.json
  - lib/kiln/agents/roles/qa_verifier.ex
  - lib/kiln/agents/adapter.ex
  - test/kiln/specs/verifier_result_test.exs
  - test/kiln/agents/roles/qa_verifier_test.exs
autonomous: true
requirements:
  - SPEC-03
  - ORCH-05
must_haves:
  truths:
    - "`VerifierResult` forbids machine verdict override by LLM fields (property test or schema)"
    - "Disagreement path: machine fail + structured LLM pass → `verdict` stays `:fail`, `llm_disagreement` true"
    - "QAVerifier role module orchestrates Phase A (machine) before Phase B (LLM)"
  artifacts:
    - path: "priv/jsv/verifier_result_v1.json"
      provides: "JSON schema for persisted result"
      contains: "verdict"
  key_links:
    - from: "lib/kiln/agents/roles/qa_verifier.ex"
      to: "lib/kiln/specs/verifier_result.ex"
      via: "build result"
      pattern: "VerifierResult"
---

<objective>
Deliver **SPEC-03** and **ORCH-05** core: a typed `%VerifierResult{}` (or validated map) with JSV schema **`verifier_result_v1`**, **Phase A** machine capture (exit code + refs), **Phase B** optional LLM JSON narrative with **`allow_override: false`** enforced in code, and expand **`Kiln.Agents.Roles.QAVerifier`** to return structured diagnostics suitable for looping Planner input (work-unit / audit payload shape defined here and used in plan 05/06).

Purpose: deterministic verdict is the only branch input; LLM is explain-only.
Output: modules, JSV, ExUnit including **stub adapter** disagreement case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
@lib/kiln/agents/role.ex
@lib/kiln/agents/roles/planner.ex
@lib/kiln/sandboxes/sandbox.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `VerifierResult` struct + JSV `verifier_result_v1.json`</name>
  <read_first>
    - lib/kiln/audit/event.ex
    - .planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `priv/jsv/verifier_result_v1.json` contains top-level key `"verdict"`
    - `lib/kiln/specs/verifier_result.ex` defines `defstruct` or schema with `verdict`, `machine`, `llm`, `llm_disagreement`
    - `mix test test/kiln/specs/verifier_result_test.exs` asserts JSV rejects map with `verdict: "pass"` and `machine.verdict: "fail"` if schema encodes invariant OR code enforces after validate
  </acceptance_criteria>
  <files>lib/kiln/specs/verifier_result.ex, priv/jsv/verifier_result_v1.json, test/kiln/specs/verifier_result_test.exs</files>
  <action>
Define `VerifierResult.build/2` that always sets `allow_override: false` in output map. Encode D-S03a–d: machine map includes `exit_code`; LLM map includes `structured`, `narrative`, optional flags; `llm_disagreement` computed when structured implies pass on machine fail.

JSV schema must not include any field named `override` or `final_verdict` that could be misread by executors.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/verifier_result_test.exs --max-failures=1</automated>
  </verify>
  <done>VerifierResult invariant encoded and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `QAVerifier` — machine-first, stub LLM disagreement</name>
  <read_first>
    - lib/kiln/agents/roles/qa_verifier.ex
    - lib/kiln/agents/adapter.ex
    - test/support/mocks.ex
  </read_first>
  <acceptance_criteria>
    - `test/kiln/agents/roles/qa_verifier_test.exs` uses `Mox` or stub to return LLM JSON implying pass when machine failed
    - Final `VerifierResult.verdict` is `:fail` (or `"fail"` in map — be consistent with `Ecto.Enum`)
    - `grep "llm_disagreement" test/kiln/agents/roles/qa_verifier_test.exs` matches
  </acceptance_criteria>
  <files>lib/kiln/agents/roles/qa_verifier.ex, test/kiln/agents/roles/qa_verifier_test.exs</files>
  <action>
Implement role `perform/1` (or existing behaviour callback) skeleton: **Phase A** accept injected `machine_exit_code` or call a behaviour `Kiln.Specs.VerificationRunner` stub (defined in test) returning non-zero; persist machine checkpoint via **`Repo`** transaction or dedicated function on **`Kiln.Specs`** — do not commit partial state without tests.

**Phase B**: if machine fail, call `Kiln.Agents.Adapter` mock with `temperature: 0` params; parse JSON; feed `VerifierResult.build/2`.

Export function `diagnostic_for_planner/1` returning map including failing test ids for **ORCH-05** loop-back payload contract (document keys in `@moduledoc`).
  </action>
  <verify>
    <automated>mix test test/kiln/agents/roles/qa_verifier_test.exs --max-failures=1</automated>
  </verify>
  <done>Disagreement test proves SPEC-03 non-override.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> run transition | only `VerifierResult` validated map crosses boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T | LLM JSON | mitigate | JSV + `allow_override: false` + disagreement flag; no branching on narrative |
| T-05-03b | E | adapter | mitigate | JSON mode only; redact secrets in prompts |
</threat_model>

<verification>
`mix test test/kiln/specs/verifier_result_test.exs test/kiln/agents/roles/qa_verifier_test.exs`
</verification>

<success_criteria>
VerifierResult + QAVerifier tests green; disagreement case is deterministic via stub.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-03-SUMMARY.md`
</output>
