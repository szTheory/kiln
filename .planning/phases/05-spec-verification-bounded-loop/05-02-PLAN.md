---
phase: 05-spec-verification-bounded-loop
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/kiln/specs/scenario_parser.ex
  - lib/kiln/specs/scenario_compiler.ex
  - priv/jsv/scenario_ir_v1.json
  - test/kiln/specs/scenario_parser_test.exs
  - test/kiln/specs/scenario_compiler_test.exs
  - test/fixtures/specs/minimal_spec.md
autonomous: true
requirements:
  - SPEC-02
must_haves:
  truths:
    - "Fenced blocks tagged `kiln-scenario` parse to IR validated by JSV"
    - "Compiler emits deterministic ExUnit module(s) under a path keyed by revision id"
  artifacts:
    - path: "lib/kiln/specs/scenario_compiler.ex"
      provides: "codegen entrypoint"
      contains: "compile"
  key_links:
    - from: "lib/kiln/specs/scenario_parser.ex"
      to: "priv/jsv/scenario_ir_v1.json"
      via: "JSV.validate"
      pattern: "JSV"
---

<objective>
Implement **SPEC-02**: parse markdown for fenced **`kiln-scenario`** blocks, validate inner YAML as **scenario IR v1** with JSV, compute `scenario_manifest_sha256` over canonical bytes, and **compile** to ExUnit test module(s) written to a deterministic path (e.g. under `test/generated/kiln_scenarios/<revision_id>/`) suitable for `mix test` invocation inside the Phase 3 sandbox.

Purpose: one grammar for editor preview and verifier (D-S01d).
Output: pure modules + fixtures + tests (no Docker in this plan unless reuse of existing test helpers is trivial).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-spec-verification-bounded-loop/05-CONTEXT.md
@lib/kiln/workflows/loader.ex
@lib/kiln/audit/event.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSV schema `scenario_ir_v1.json` + parser</name>
  <read_first>
    - lib/kiln/specs/spec_revision.ex
    - mix.exs
  </read_first>
  <acceptance_criteria>
    - File `priv/jsv/scenario_ir_v1.json` exists and is valid JSON
    - `lib/kiln/specs/scenario_parser.ex` exports `parse_document/1` or equivalent
    - `mix test test/kiln/specs/scenario_parser_test.exs` includes a test that invalid YAML returns `{:error, _}` with line approximate
    - `grep "kiln-scenario" test/fixtures/specs/minimal_spec.md` returns a match
  </acceptance_criteria>
  <files>priv/jsv/scenario_ir_v1.json, lib/kiln/specs/scenario_parser.ex, test/kiln/specs/scenario_parser_test.exs, test/fixtures/specs/minimal_spec.md</files>
  <action>
Define minimal IR: `scenarios` array with `id`, `description`, `steps` (ordered checks). Add fixture `test/fixtures/specs/minimal_spec.md` with one valid and one intentionally invalid block for error-path tests.

Parser: extract fenced blocks only when info string is exactly `kiln-scenario`; use `:yaml_elixir` to decode; pipe through `JSV.validate/2` using project’s existing JSV pattern from workflows.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/scenario_parser_test.exs --max-failures=1</automated>
  </verify>
  <done>Parser + JSV boundary locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compiler → ExUnit + manifest hash</name>
  <read_first>
    - lib/kiln/specs/scenario_parser.ex
    - lib/kiln/specs/spec_revision.ex
  </read_first>
  <acceptance_criteria>
    - `ScenarioCompiler.compile(revision)` writes file path containing `revision.id` string
    - Generated file contains `defmodule` and `use ExUnit.Case`
    - `mix test` on generated file path exits 0 for passing scenario and non-zero for failing scenario (assert via `Mix.Tasks.Test.run` in test OR shell-out in integration test — pick one, document)
    - `Kiln.Specs` updates `scenario_manifest_sha256` on revision when compile succeeds
  </acceptance_criteria>
  <files>lib/kiln/specs/scenario_compiler.ex, test/kiln/specs/scenario_compiler_test.exs, lib/kiln/specs.ex</files>
  <action>
Implement deterministic codegen: given `%SpecRevision{}`, emit tests that assert trivial predicates from IR (e.g. `assert 1 == 1` for smoke). Include `@moduletag :kiln_scenario` on generated module. Compute manifest SHA256 over sorted scenario ids + canonical IR JSON.

Wire `Kiln.Specs.compile_revision!/1` that persists hash via `Repo.update` on success.
  </action>
  <verify>
    <automated>mix test test/kiln/specs/scenario_compiler_test.exs --max-failures=1</automated>
  </verify>
  <done>SPEC-02 compile path proven in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown -> IR | attacker-controlled spec must not escape codegen into arbitrary Elixir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | I | codegen | mitigate | emit only from fixed templates + escaped string literals from IR |
| T-05-02b | E | JSV | mitigate | reject unknown keys; max string lengths in schema |
</threat_model>

<verification>
`mix test test/kiln/specs/scenario_parser_test.exs test/kiln/specs/scenario_compiler_test.exs`
</verification>

<success_criteria>
Parser and compiler tests green; fixture drives both happy and sad paths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-verification-bounded-loop/05-02-SUMMARY.md`
</output>
