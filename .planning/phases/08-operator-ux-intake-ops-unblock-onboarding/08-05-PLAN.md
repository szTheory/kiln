---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "05"
type: execute
wave: 2
depends_on: []
files_modified:
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/provider_health_live.ex
  - lib/kiln/model_registry.ex
  - test/kiln_web/live/provider_health_live_test.exs
autonomous: true
requirements:
  - OPS-01
must_haves:
  truths:
    - "`GET /providers` (not under `/ops`) renders provider cards"
    - "Poll assigns refresh on `Process.send_after` tick — card can flip red on simulated error between polls"
---

<objective>
**OPS-01:** **`KilnWeb.ProviderHealthLive`** at **`/providers`** showing per-provider cards: API key present/valid (boolean from registry), last success timestamp, rate-limit headroom (from last response metadata if available), recent error rate, token budget remaining today. RAG styling per `08-UI-SPEC.md`. Poll interval **5s** default (assign `@poll_ms`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln/model_registry.ex
@lib/kiln_web/live/cost_live.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ModelRegistry read API for health cards</name>
  <read_first>
    - lib/kiln/model_registry.ex
    - .planning/REQUIREMENTS.md
  </read_first>
  <acceptance_criteria>
    - `grep -n "provider_health\|health_snapshot\|adapter_status" lib/kiln/model_registry.ex` matches at least one **new** public function name chosen in implementation (document in plan summary)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <files>lib/kiln/model_registry.ex</files>
  <action>
Expose a **pure struct or map** listing configured providers with: `:key_configured?`, `:last_ok_at`, `:recent_error_rate`, `:rate_limit_remaining`, `:token_budget_remaining_today`. If telemetry tables absent, stub with **`persistent_term` / in-memory** counters **clearly marked TODO** only if tests require — prefer reading existing Phase 3/7 storage; if none, return zeros with `last_ok_at: nil` and document follow-up in SUMMARY.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Registry exposes card data.</done>
</task>

<task type="auto">
  <name>Task 2: ProviderHealthLive + tests</name>
  <read_first>
    - lib/kiln_web/router.ex
    - lib/kiln_web/live/cost_live.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n 'live "/providers"' lib/kiln_web/router.ex` matches
    - `grep -n 'id="provider-health"' lib/kiln_web/live/provider_health_live.ex` matches
    - `mix test test/kiln_web/live/provider_health_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/provider_health_live.ex, test/kiln_web/live/provider_health_live_test.exs, lib/kiln_web/router.ex, lib/kiln_web/components/layouts.ex</files>
  <action>
LiveView: **`mount`** assigns snapshot; **`handle_info(:tick, …)`** refreshes assigns. Template: grid of cards with **RAG** border classes. Navbar link **Providers** → `~p"/providers"`. Test: HTML contains **`id="provider-health"`** and at least one **`Operational`** or **`API key missing`** string from UI-SPEC.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/provider_health_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Provider health panel shipped.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | I | health UI | mitigate | Never render raw API keys; booleans only |
</threat_model>

<verification>
mix test test/kiln_web/live/provider_health_live_test.exs
</verification>

<success_criteria>
/providers live with cards + poll + tests.
</success_criteria>

<output>
After completion, create `08-05-SUMMARY.md` in this phase directory.
</output>
