---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "08"
type: execute
wave: 4
depends_on: []
files_modified:
  - lib/kiln_web/components/unblock_panel.ex
  - lib/kiln_web/live/run_detail_live.ex
  - test/kiln_web/live/run_detail_live_test.exs
autonomous: true
requirements:
  - BLOCK-02
must_haves:
  truths:
    - "When `run.state == :blocked`, run detail renders **`id=\"unblock-panel\"`**"
    - "**I fixed it — retry** calls `Kiln.Runs.Transitions.transition(run_id, next_state, %{reason: ...})` with legal edge from `:blocked` per D-87 matrix"
---

<objective>
**BLOCK-02:** Add **`KilnWeb.Components.UnblockPanel`** (function component) showing typed **`Blockers.Reason`**, rendered playbook lines via **`Kiln.Blockers.render/2`**, and **"I fixed it — retry"** button that invokes **`Kiln.Runs.Transitions.transition/3`** to the **last non-terminal workflow state** (derive from audit tail or persisted `blocked_resume` field — **choose minimal**: transition to **`:planning`** as safe default resume unless run row already stores resume target).

Output: component + `RunDetailLive` integration + LiveView tests with blocked fixture.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln/blockers.ex
@lib/kiln/runs/transitions.ex
@lib/kiln_web/live/run_detail_live.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: UnblockPanel component</name>
  <read_first>
    - lib/kiln/blockers.ex
    - lib/kiln_web/components/core_components.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "def unblock_panel" lib/kiln_web/components/unblock_panel.ex` OR `def unblock_panel(assigns)` matches
    - `grep -n "Blockers.render" lib/kiln_web/components/unblock_panel.ex` matches
    - `grep -n 'id="unblock-panel"' lib/kiln_web/components/unblock_panel.ex` matches
  </acceptance_criteria>
  <files>lib/kiln_web/components/unblock_panel.ex</files>
  <action>
Attrs: **`run`**, **`block_reason`** (atom). Title copy **Run blocked** from UI-SPEC. Body: **`Blockers.render(reason, run_context_map)`** inside `<pre>` or monospace div with **`phx-no-curly-interpolation`** if showing raw markdown with braces. Primary button **`phx-click="unblock_retry"`** with **`phx-value-to="planning"`** (or computed value).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>Component compiles.</done>
</task>

<task type="auto">
  <name>Task 2: RunDetailLive + transition + tests</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - lib/kiln/runs/transitions.ex
    - lib/kiln/runs/run.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "unblock_retry" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "Transitions.transition" lib/kiln_web/live/run_detail_live.ex` matches
    - `mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex, test/kiln_web/live/run_detail_live_test.exs</files>
  <action>
When **`run.state == :blocked`**, render `<.unblock_panel …>`. **`handle_event("unblock_retry", %{"to" => to}, socket)`** maps **`to`** only through an explicit whitelist **`%{"planning" => :planning, "coding" => :coding, "testing" => :testing, "verifying" => :verifying}`** — if missing, return `{:noreply, put_flash(:error, "Invalid resume target")}`. On match, call **`Kiln.Runs.Transitions.transition(run.id, target, %{reason: :operator_unblock})`**. First line of handler: **`unless allow?(socket), do: {:noreply, socket}`**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Unblock path tested.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | E | illegal resume | mitigate | Whitelist target states; `Transitions` enforces matrix |
</threat_model>

<verification>
mix test test/kiln_web/live/run_detail_live_test.exs
</verification>

<success_criteria>
Blocked runs show typed unblock + retry into Transitions with tests.
</success_criteria>

<output>
After completion, create `08-08-SUMMARY.md` in this phase directory.
</output>
