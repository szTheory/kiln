---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "09"
type: execute
wave: 5
depends_on: []
files_modified:
  - lib/kiln_web/plugs/onboarding_gate.ex
  - lib/kiln_web/endpoint.ex
  - lib/kiln_web/router.ex
  - lib/kiln_web/live/onboarding_live.ex
  - lib/kiln/runs/run_director.ex
  - lib/kiln/operator_readiness.ex
  - test/kiln_web/live/onboarding_live_test.exs
  - test/kiln/operator_readiness_test.exs
autonomous: true
requirements:
  - BLOCK-04
must_haves:
  truths:
    - "Router plug redirects to **`/onboarding`** when readiness false; allowlist includes `/onboarding`, `/health`, static, `/ops/*` per D-807"
    - "`RunDirector` (or `Runs.enqueue`) rejects new runs when readiness false — **authoritative** gate"
---

<objective>
**BLOCK-04:** First-run **wizard** at **`/onboarding`**: steps for Anthropic key (reference only), optional providers, **`gh auth status`** probe, Docker probe. Persist **probe results** in Postgres (new `operator_environment_checks` table or JSON on `settings` row — plan executor picks minimal). **Router plug** + **domain** double gate per D-807. Copy from **`08-UI-SPEC.md`**.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln/boot_checks.ex
@lib/kiln/runs/run_director.ex
@lib/kiln_web/router.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OperatorReadiness + persistence</name>
  <read_first>
    - lib/kiln/boot_checks.ex
    - lib/kiln/runs/run_director.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.OperatorReadiness" lib/kiln/operator_readiness.ex` matches
    - `grep -n "ready?\\|factory_ready" lib/kiln/operator_readiness.ex` matches
    - `mix test test/kiln/operator_readiness_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/operator_readiness.ex, priv/repo/migrations/*operator*.exs, test/kiln/operator_readiness_test.exs</files>
  <action>
Create context **`Kiln.OperatorReadiness`** with **`ready?/0`** aggregating persisted probe rows: **anthropic_configured?**, **github_cli_ok?**, **docker_ok?** (exact keys in schema). **`mark_step/2`** updates after wizard **Verify** buttons. Probes: Anthropic = **`Application.get_env(:kiln, :anthropic_api_key_ref)`** present OR persistent_term lookup pattern used elsewhere — **match existing SEC-01 pattern** from Phase 3 code. GitHub = **`System.cmd("gh", ["auth", "status"], stderr_to_stdout: true)`** exit 0. Docker = call existing sandbox ping if available else `System.cmd("docker", ["info"], …)` with timeout.
  </action>
  <verify>
    <automated>mix test test/kiln/operator_readiness_test.exs --max-failures=1</automated>
  </verify>
  <done>Readiness API + tests (stub commands in test via Mox or a small `SystemCmd` port if introduced).</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingGate plug + OnboardingLive</name>
  <read_first>
    - lib/kiln_web/endpoint.ex
    - lib/kiln_web/router.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "OnboardingGate" lib/kiln_web/endpoint.ex` OR router pipeline includes plug
    - `grep -n 'live "/onboarding"' lib/kiln_web/router.ex` matches
    - `grep -n "Set up Kiln" lib/kiln_web/live/onboarding_live.ex` matches
    - `mix test test/kiln_web/live/onboarding_live_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln_web/plugs/onboarding_gate.ex, lib/kiln_web/live/onboarding_live.ex, lib/kiln_web/router.ex, test/kiln_web/live/onboarding_live_test.exs</files>
  <action>
Plug: if **`not OperatorReadiness.ready?()`** and path **not** in allowlist `{"/onboarding", "/health", ... static prefixes ..., starts_with?("/ops")}` then **`conn |> redirect(to: "/onboarding") |> halt()`**. **`OnboardingLive`**: multi-step form; **`id="onboarding-wizard"`**. Query **`?review=1`** shows checklist without blocking others if readiness true (D-812 sketch).
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/onboarding_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Wizard + plug tested.</done>
</task>

<task type="auto">
  <name>Task 3: RunDirector enqueue guard</name>
  <read_first>
    - lib/kiln/runs/run_director.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "OperatorReadiness" lib/kiln/runs/run_director.ex` matches
    - `test -f test/kiln/runs/run_director_readiness_test.exs` exits 0
    - `mix test test/kiln/runs/run_director_readiness_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/runs/run_director.ex, test/kiln/runs/run_director_readiness_test.exs</files>
  <action>
At start of enqueue/start path, **`unless OperatorReadiness.ready?(), do: {:error, :factory_not_ready}`** (tuple shape consistent with director APIs). Add **`test/kiln/runs/run_director_readiness_test.exs`** asserting enqueue returns **`:factory_not_ready`** when readiness forced false in setup.
  </action>
  <verify>
    <automated>mix test test/kiln/runs/run_director_readiness_test.exs --max-failures=1</automated>
  </verify>
  <done>Authoritative gate enforced.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | E | plug bypass | mitigate | Domain guard duplicates UI; allowlist audited |
| T-08-09b | T | gh command injection | mitigate | Fixed argv list only — no user strings in `System.cmd` |
</threat_model>

<verification>
mix test test/kiln/operator_readiness_test.exs test/kiln_web/live/onboarding_live_test.exs
</verification>

<success_criteria>
Onboarding wizard + plug + RunDirector readiness gate + tests green.
</success_criteria>

<output>
After completion, create `08-09-SUMMARY.md` in this phase directory.
</output>
