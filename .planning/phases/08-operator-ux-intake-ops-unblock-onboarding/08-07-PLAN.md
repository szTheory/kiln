---
phase: 08-operator-ux-intake-ops-unblock-onboarding
plan: "07"
type: execute
wave: 3
depends_on: []
files_modified:
  - lib/kiln/diagnostics/snapshot.ex
  - lib/kiln_web/live/run_detail_live.ex
  - lib/kiln_web/router.ex
  - test/kiln/diagnostics/snapshot_test.exs
autonomous: true
requirements:
  - OPS-05
must_haves:
  truths:
    - "**Bundle last 60 minutes** triggers server zip build"
    - "Zip bytes grep clean for placeholder **`REDACTED_SECRET`** or equivalent after redaction pass"
---

<objective>
**OPS-05:** Implement **`Kiln.Diagnostics.Snapshot.build_zip_last_60m/1`** assembling runs, configs, logs for last 60 minutes; **redaction** pipeline; expose **download** from **`RunDetailLive`** (and optionally settings — minimum run detail). Button label **exact** `Bundle last 60 minutes` per ROADMAP.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/kiln_web/live/run_detail_live.ex
@.planning/phases/08-operator-ux-intake-ops-unblock-onboarding/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot module + redaction</name>
  <read_first>
    - lib/kiln/runs/run.ex
    - lib/kiln/audit.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "defmodule Kiln.Diagnostics.Snapshot" lib/kiln/diagnostics/snapshot.ex` matches
    - `grep -n "redact" lib/kiln/diagnostics/snapshot.ex` matches
    - `mix test test/kiln/diagnostics/snapshot_test.exs --max-failures=1` exits 0
  </acceptance_criteria>
  <files>lib/kiln/diagnostics/snapshot.ex, test/kiln/diagnostics/snapshot_test.exs</files>
  <action>
Create **`lib/kiln/diagnostics/snapshot.ex`**. Public API: **`build_zip!(opts)`** returning `{:ok, path}` to temp **`System.tmp_dir!`** file with **TTL cleanup** (`File.rm` after send OR `on_send` callback). Include JSON manifest listing run ids + config paths. **Redaction:** replace lines matching **`sk-ant-api`**, **`sk-`**, **`ghp_`**, **`xoxb-`** with **`[REDACTED]`** (literal string must appear in output fixture test).
  </action>
  <verify>
    <automated>mix test test/kiln/diagnostics/snapshot_test.exs --max-failures=1</automated>
  </verify>
  <done>Zip builder + redaction unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: RunDetailLive download event</name>
  <read_first>
    - lib/kiln_web/live/run_detail_live.ex
    - lib/kiln_web/endpoint.ex
  </read_first>
  <acceptance_criteria>
    - `grep -n "Bundle last 60 minutes" lib/kiln_web/live/run_detail_live.ex` matches
    - `grep -n "allow?" lib/kiln_web/live/run_detail_live.ex` matches in new `handle_event` for bundle
  </acceptance_criteria>
  <files>lib/kiln_web/live/run_detail_live.ex</files>
  <action>
Add button **`phx-click="bundle_diagnostics"`**. On event, call **`Snapshot.build_zip!(run_id: socket.assigns.run.id)`**, then **`{:noreply, push_event(socket, "download", %{url: ...})}`** OR **`redirect`** to controller route **`get "/downloads/diagnostic/:token"`** — pick one pattern used elsewhere in app; if none, use **`send_download`** via **`Plug.Conn`** through **`live_redirect` forbidden** — use **`push_event` + colocated hook** per `AGENTS.md` **only if** no simpler `Controller` exists. Simplest: **`Phoenix.LiveView.send_download/3`** if on Phoenix 1.8 (verify docs) else dedicated **`DiagnosticsController`**.
  </action>
  <verify>
    <automated>mix test test/kiln_web/live/run_detail_live_test.exs --max-failures=1</automated>
  </verify>
  <done>Operator can download bundle.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | I | zip leak | mitigate | Redaction tests + short-lived random filename |
</threat_model>

<verification>
mix test test/kiln/diagnostics/snapshot_test.exs test/kiln_web/live/run_detail_live_test.exs
</verification>

<success_criteria>
Diagnostic zip builds, redacts, downloadable from UI.
</success_criteria>

<output>
After completion, create `08-07-SUMMARY.md` in this phase directory.
</output>
