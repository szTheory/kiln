---
phase: 13-docs-requirements-reconciliation
plan: "01"
type: execute
wave: 1
depends_on:
  - phase: 11-gameboy-dogfood-vertical-slice
    plan: "01"
requirements:
  - DOCS-ALIGN-01
milestone: v0.2.0
---

# Phase 13 Plan 01 — Requirements & roadmap hygiene

## Objective

Eliminate ambiguity between **shipped** work and **§ v1 Requirements** checkboxes: align `REQUIREMENTS.md` with `PROJECT.md` **Validated** and ROADMAP Phases **1–9**, and remove the “do not trust REQUIREMENTS alone” banner once reconciliation is done (or replace with a CI check).

## Preconditions

- Phase **11** complete so v0.2 “what we proved in the wild” is stable narrative-wise (dogfood may surface REQ wording gaps).

## Falsifiable UAT

1. Every **shipped** v1 REQ-ID in `REQUIREMENTS.md` § v1 shows `[x]` with a pointer (phase or “Validated bundle”) where appropriate.
2. `PROJECT.md` **Active** / **Validated** does not contradict `REQUIREMENTS.md` for the same ID.
3. Optional: a `mix` task or script fails CI if a known-shipped REQ regresses to `[ ]` without a matching `<!-- deferred -->` note.

## Tasks (outline)

1. Machine or scripted pass: diff `REQUIREMENTS.md` checklist against `PROJECT.md` Validated list.
2. Batch-edit checkboxes + add a one-line “shipped Phase N” suffix where useful.
3. Update or remove the sync warning banner at top of `REQUIREMENTS.md`.
4. Add `13-01-SUMMARY.md` at execution time.

## Context

@.planning/PROJECT.md  
@.planning/REQUIREMENTS.md  
@.planning/ROADMAP.md
