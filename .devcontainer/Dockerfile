# Phase 21 — matches `.tool-versions` Elixir/OTP pins (image tag documents patch level).
FROM hexpm/elixir:1.19.5-erlang-28.1.2-alpine-3.20.3

RUN apk add --no-cache \
  build-base \
  git \
  nodejs \
  npm

WORKDIR /workspaces/kiln
