{
  "name": "kiln-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end verification for Kiln's operator LiveViews. Runs every route in light/dark x mobile/desktop, a11y-scans with axe-core, and guards the Phase reskin against visual regression. Not published.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "install-browsers": "playwright install --with-deps chromium webkit",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.2",
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0"
  }
}
